---
phase: 10-open-seo-frontend-absorption
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(shell)/clients/[clientId]/seo/page.tsx
  - apps/web/src/app/(shell)/layout.tsx
autonomous: true
requirements: [ABSORB-02, ABSORB-06]

must_haves:
  truths:
    - "The iframe is removed from /clients/[clientId]/seo/page.tsx"
    - "The seo/page.tsx now redirects to the audit sub-route or renders a nav landing"
    - "SEO nav items (Audit, Keywords, Backlinks, Domain Overview, Saved, AI) are registered in the AppShell sidebar"
    - "grep -r 'iframe' apps/web/src/ returns zero matches"
  artifacts:
    - path: "apps/web/src/app/(shell)/clients/[clientId]/seo/page.tsx"
      provides: "SEO landing page — no iframe, redirects to /audit"
      contains: "redirect"
    - path: "apps/web/src/app/(shell)/layout.tsx"
      provides: "AppShell with SEO nav section registered"
      contains: "seo"
  key_links:
    - from: "apps/web/src/app/(shell)/clients/[clientId]/seo/page.tsx"
      to: "/clients/[clientId]/seo/[projectId]/audit"
      via: "Next.js redirect or client redirect"
      pattern: "redirect|useRouter"
---

<objective>
Remove the iframe from `/clients/[clientId]/seo/page.tsx` and register SEO nav items in the AppShell sidebar. This wave-1 plan runs in parallel with plan 01 (server actions). It has zero file overlap with plan 01.

Purpose: The iframe is the current Phase 7 temporary bridge. Before porting pages in Wave 2, we must remove the iframe (per success criterion 2) and wire the nav so that users can navigate to the new routes once they exist.

Output: Clean seo/page.tsx with no iframe. AppShell sidebar with SEO nav section. TanStack Query provider added to the SEO layout if not already present (the ported pages use `useQuery`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-open-seo-frontend-absorption/10-CONTEXT.md
</context>

<interfaces>
<!-- apps/web/src/app/(shell)/clients/[clientId]/seo/page.tsx — current content (iframe stub) -->
<!-- Replace entirely. The new page should redirect to the audit route. -->
<!-- Since projectId is unknown at this level, redirect to a "pick project" intermediate or -->
<!-- use the default project. For Phase 10, redirect to the default project ID -->
<!-- which the user can obtain from the open-seo API. For simplicity, show a -->
<!-- "Select a project" landing or redirect to /audit with the first project. -->
<!-- Per CONTEXT.md: route is /clients/[clientId]/seo/[projectId]/audit -->
<!-- The seo/page.tsx cannot know projectId without an API call. -->
<!-- Best approach: render a server component that fetches the default project ID -->
<!-- from open-seo and redirects. Or render a simple client landing with a CTA. -->
<!-- Decision: Use a server component redirect — fetch default projectId from open-seo API. -->

<!-- apps/web/src/app/(shell)/layout.tsx — AppShell layout -->
<!-- Find where nav items are defined and add a SEO section with sub-items. -->
<!-- Check the actual file path and structure before editing. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove iframe from seo/page.tsx</name>
  <files>apps/web/src/app/(shell)/clients/[clientId]/seo/page.tsx</files>
  <action>
Replace the entire content of `apps/web/src/app/(shell)/clients/[clientId]/seo/page.tsx`.

First, read the current file to understand what's there. Then replace it with a server component that:
1. Is NOT marked `"use client"` (it becomes a server component)
2. Imports `redirect` from `next/navigation` and `getOpenSeo` from `@/lib/server-fetch`
3. Calls `getOpenSeo('/api/projects?client_id={clientId}')` to fetch the client's project list
4. If projects exist, redirects to `/clients/{clientId}/seo/{firstProjectId}/audit`
5. If the API call fails or returns no projects, renders a simple "No SEO project found" message with a "Set up SEO" button linking to the open-seo project creation (or just showing an informational card)

The `clientId` is available from Next.js route params (`params: { clientId: string }`).

**Important:** Remove all references to:
- `useAuth` from `@clerk/nextjs`
- `NEXT_PUBLIC_OPEN_SEO_URL`
- `<iframe>`
- `postMessage`
- The old `SeoAuditPage` component name

After this task, `grep -r "iframe" apps/web/src/` must return zero matches.

Read the actual open-seo project listing endpoint before calling it — grep open-seo-main/src/server/ for `/projects` route to confirm the path.
  </action>
  <verify>
    <automated>grep -r "iframe" /home/dominic/Documents/TeveroSEO/apps/web/src/ 2>/dev/null | wc -l</automated>
  </verify>
  <done>File has no iframe. grep -r "iframe" apps/web/src/ returns 0 matches. pnpm --filter web tsc --noEmit passes for this file.</done>
</task>

<task type="auto">
  <name>Task 2: Register SEO nav section in AppShell sidebar</name>
  <files>apps/web/src/app/(shell)/layout.tsx</files>
  <action>
Read `apps/web/src/app/(shell)/layout.tsx` first to understand the current nav structure.

Then locate where client-specific nav items are defined (look for "calendar", "articles", "intelligence" etc. — they'll be in a nav config array or inline JSX).

Add a SEO section with these nav items (shown only when inside a `/clients/[clientId]/*` context):
```
SEO
├── Audit           → /clients/[clientId]/seo/[projectId]/audit
├── Keywords        → /clients/[clientId]/seo/[projectId]/keywords
├── Backlinks       → /clients/[clientId]/seo/[projectId]/backlinks
├── Domain Overview → /clients/[clientId]/seo/[projectId]/domain
├── Saved Keywords  → /clients/[clientId]/seo/[projectId]/saved
└── AI              → /clients/[clientId]/seo/[projectId]/ai
```

The `[projectId]` in nav links must be dynamic. The sidebar needs the projectId to build these links. For Phase 10, the approach is:
- Read `projectId` from the current URL params (if already on a seo route) OR
- Store the last-used projectId in a cookie/store, OR
- Link to `/clients/[clientId]/seo` (the landing page) which will redirect to the correct projectId

**Simplest correct approach for Phase 10:** The nav links point to `/clients/[clientId]/seo` and let the landing page redirect. This avoids requiring projectId in the nav. The active state detection still works via `usePathname().startsWith('/clients/{clientId}/seo')`.

Alternatively, if the AppShell sidebar is a client component that already reads `useParams`, extract `projectId` from params when available and use it in nav links.

Read the actual layout file structure before making changes. Follow the existing nav item pattern exactly (same component, same prop shape). Do not introduce new nav components — use whatever pattern already exists.

Cast all route strings with `as Parameters<typeof router.push>[0]` per the typedRoutes convention established in Phase 8 (STATE.md decision).
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && pnpm --filter web tsc --noEmit 2>&1 | grep -E "\(shell\)/layout" | head -10 || echo "NO ERRORS IN LAYOUT"</automated>
  </verify>
  <done>AppShell sidebar shows SEO nav section with 6 items. TypeScript compiles cleanly. Nav items link to /clients/[clientId]/seo/* routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js server component | Clerk session validated by middleware before render |
| Next.js server → open-seo-api | Internal Docker network for project lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | Server component renders open-seo error in HTML | mitigate | Wrap getOpenSeo call in try/catch; render generic error UI, not upstream error details |
| T-10-05 | Tampering | Nav links constructed from clientId in URL | accept | clientId is from authenticated Clerk session; middleware validates access |
</threat_model>

<verification>
```bash
# No iframes remain
grep -r "iframe" apps/web/src/ | wc -l   # must be 0

# TypeScript clean
pnpm --filter web tsc --noEmit 2>&1 | grep -E "\(shell\)" | head -20

# SEO nav items present in layout
grep -n "seo\|SEO\|Audit\|Keywords\|Backlinks" apps/web/src/app/\(shell\)/layout.tsx | head -20
```
</verification>

<success_criteria>
- `grep -r "iframe" apps/web/src/` returns zero lines
- `apps/web/src/app/(shell)/clients/[clientId]/seo/page.tsx` is a server component with no iframe or useAuth
- AppShell sidebar has SEO section with 6 nav items
- `pnpm --filter web tsc --noEmit` passes with no errors in layout or seo/page.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/10-open-seo-frontend-absorption/10-02-SUMMARY.md`
</output>
