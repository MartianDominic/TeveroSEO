---
context: phase
phase: 100-world-class-scraping
task: planning_revised_v3
total_tasks: 4_implementation_weeks
status: planning_complete
last_updated: 2026-05-11 20:15:00 EEST
---

<current_state>
Phase 100 REVISED v3 plan complete. 5 Opus agents found the REAL bottleneck: `concurrency=10` in TieredFetcher.
Architecture decision: Config fix + parser swap, NO Scrapling rewrite needed.
Ready to begin implementation.
</current_state>

<key_finding>
THE BOTTLENECK IS CONFIGURATION, NOT FRAMEWORK.

TieredFetcher line 637: `concurrency ?? 10`
Should be: `concurrency ?? 200`

That's it. That's the fix. No Python microservice. No gRPC. No dual-language stack.
</key_finding>

<completed_work>

Research Phase:
- 10-agent DARPA-level deep dive (Phase 95)
- Scrapling evaluation (found: not needed for speed)
- 5 Opus agent investigation (found: concurrency=10 is the bottleneck)
- Cost analysis (found: $32/month "hidden costs" was BS)

Phase 100 Planning (Complete):
- Root cause identified: concurrency=10
- 4-week implementation plan (vs 8-week Scrapling plan)
- Two-stage flow documented (prospect fast → client full)
- Streaming architecture designed
</completed_work>

<remaining_work>

Implementation Weeks (4 weeks):
1. Week 1: Concurrency Optimization - 10→200, per-domain rate limiting
2. Week 2: Parser Migration - Cheerio→node-html-parser for 10 critical files
3. Week 3: Streaming Architecture - Redis Streams, real-time RAG ingestion
4. Week 4: Two-Stage Flow + Worker Threads - Prospect vs Client separation
</remaining_work>

<decisions_made>

| Decision | Rationale |
|----------|-----------|
| **NO Scrapling rewrite** | Config fix achieves same speed (2-3 min for 5000 pages) |
| **Concurrency 10→200** | THE actual bottleneck, line 637 of TieredFetcher.ts |
| **node-html-parser** | 6x faster than Cheerio (2ms vs 12ms) |
| **Streaming batches** | 2.5GB peak → <500MB, real-time RAG updates |
| **Two-stage flow** | Fast prospect (content) → Deferred client (109 checks) |
| **Keep Camoufox** | 88% success, best for JS challenges |
</decisions_made>

<what_we_learned>

1. Read the code first: `concurrency ?? 10` was the problem all along
2. $32/month "hidden costs" was BS: Python on same VPS costs $0 extra
3. Framework doesn't matter at this scale: Config matters more
4. Phase 98 uses DataForSEO APIs, not our scraping
5. 76% of SEO checks are simple HTML parsing
</what_we_learned>

<blockers>
None - ready to implement
</blockers>

## Required Reading
1. `PHASE-100-REVISED-PLAN.md` — REVISED v3 implementation plan (4 weeks)
2. `UI-PROGRESS-SPEC.md` — Progress UI with security through abstraction

## Key Targets

| Metric | Current | Target |
|--------|---------|--------|
| 5000-page scrape | 15 min | <3 min |
| Cost per prospect | $2.60 | $0.80 |
| Parse time/page | 12ms | 2ms |
| Memory peak | 2.5GB | <500MB |

## The Fix (Week 1)

```typescript
// TieredFetcher.ts line 637
// BEFORE:
const concurrency = opts.concurrency ?? 10;

// AFTER:
const concurrency = opts.concurrency ?? 200;
```

<next_action>
Begin Week 1: Change `concurrency ?? 10` to `concurrency ?? 200` in TieredFetcher.ts line 637.
Add per-domain rate limiting to prevent IP bans.
</next_action>
