---
phase: 80-cascade-selection
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - open-seo-main/src/server/features/keywords/selection/types.ts
  - open-seo-main/src/server/features/keywords/selection/presets.ts
  - open-seo-main/src/server/features/keywords/selection/CascadeSelector.ts
  - open-seo-main/src/server/features/keywords/selection/CascadeSelector.test.ts
  - open-seo-main/src/server/features/keywords/selection/index.ts
autonomous: true
requirements:
  - KW-80-01
  - KW-80-02
  - KW-80-03
  - KW-80-04
  - KW-80-05
  - KW-80-06

must_haves:
  truths:
    - "BOFU keywords are selected first before MOFU/TOFU"
    - "Target count is reached via cascade fallback"
    - "Minimum guarantees are enforced per stage"
    - "Maximum caps are respected per stage"
    - "Breakdown accurately reflects stage distribution"
    - "Warnings generated when minimums not met"
    - "Excluded keywords tracked with cascade position"
  artifacts:
    - path: "open-seo-main/src/server/features/keywords/selection/types.ts"
      provides: "CascadeConfig, SelectionResult, SelectedKeyword, ExcludedKeyword, SelectionBreakdown types"
      exports: ["CascadeConfig", "SelectionResult", "SelectedKeyword", "ExcludedKeyword", "SelectionBreakdown", "StageConfig", "FunnelStage"]
    - path: "open-seo-main/src/server/features/keywords/selection/presets.ts"
      provides: "DEFAULT_CASCADE and SERVICE_CASCADE preset configurations"
      exports: ["DEFAULT_CASCADE", "SERVICE_CASCADE", "ECOMMERCE_CASCADE", "CONTENT_CASCADE"]
    - path: "open-seo-main/src/server/features/keywords/selection/CascadeSelector.ts"
      provides: "Core cascade selection algorithm"
      exports: ["CascadeSelector", "cascadeSelector"]
    - path: "open-seo-main/src/server/features/keywords/selection/CascadeSelector.test.ts"
      provides: "Comprehensive test coverage for cascade selection"
      min_lines: 200
  key_links:
    - from: "CascadeSelector.ts"
      to: "types.ts"
      via: "import { CascadeConfig, SelectionResult }"
      pattern: "import.*CascadeConfig.*from.*types"
    - from: "CascadeSelector.ts"
      to: "presets.ts"
      via: "import { DEFAULT_CASCADE }"
      pattern: "import.*DEFAULT_CASCADE.*from.*presets"
---

<objective>
Implement BOFU-first cascade selection algorithm with configurable fallback.

Purpose: Replace naive `slice(0, N)` keyword selection with intelligent cascade that prioritizes BOFU keywords, respects min/max constraints per funnel stage, and falls back to MOFU/TOFU when BOFU is exhausted.

Output: CascadeSelector service with full test coverage, type definitions, and preset configurations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-cascade-selection/80-CONTEXT.md
@open-seo-main/src/server/features/keywords/types/index.ts
@open-seo-main/src/server/features/keywords/services/PrioritizationService.ts

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From PrioritizationService.ts - scoring model we'll consume:
```typescript
export interface ScoreWeights {
  volume: number;
  competition: number;
  relevance: number;
  focus: number;
  position: number;
}

export type KeywordTier = "must_do" | "should_do" | "nice_to_have" | "ignore";
```

From classification/types.ts - keyword classification we'll consume:
```typescript
export type KeywordType = "product" | "long_tail" | "question" | "local" | "comparison";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define cascade types and presets</name>
  <files>
    open-seo-main/src/server/features/keywords/selection/types.ts,
    open-seo-main/src/server/features/keywords/selection/presets.ts
  </files>
  <behavior>
    - FunnelStage enum: 'bofu' | 'mofu' | 'tofu'
    - StageConfig has min, max, priority fields
    - CascadeConfig has targetCount, stages, allowOverflow, strictMax
    - SelectedKeyword has keyword, funnelStage, compositeScore, cascadePosition, metrics
    - ExcludedKeyword has exclusionReason: 'cascade_overflow' | 'stage_max_reached' | 'target_reached'
    - SelectionBreakdown has total, per-stage counts/percentages, meetsTarget, meetsMinimums, warnings[]
    - DEFAULT_CASCADE: target=100, bofu min=20 max=60, mofu min=15 max=40, tofu min=5 max=30
    - SERVICE_CASCADE: target=100, bofu min=40 max=80, mofu min=10 max=30, tofu min=5 max=15
    - ECOMMERCE_CASCADE: target=150, balanced distribution
    - CONTENT_CASCADE: target=100, tofu-heavy for awareness content
  </behavior>
  <action>
Create `selection/` directory under `open-seo-main/src/server/features/keywords/`.

Create `types.ts` with:
- FunnelStage type as literal union 'bofu' | 'mofu' | 'tofu'
- StageConfig interface with min, max (numbers), priority (1|2|3)
- CascadeConfig interface per CONTEXT.md specification
- SelectedKeyword interface with cascadePosition (1-based), metrics object
- ExcludedKeyword interface with exclusionReason enum
- SelectionBreakdown interface with per-stage poolSize tracking
- SelectionResult interface combining selected, excluded, breakdown, config, metadata

Create `presets.ts` with:
- DEFAULT_CASCADE constant matching CONTEXT.md values
- SERVICE_CASCADE constant for BOFU-heavy businesses
- ECOMMERCE_CASCADE constant for product catalogs (target=150)
- CONTENT_CASCADE constant for blog/awareness (tofu-heavy)
- Type assertion ensuring presets satisfy CascadeConfig
  </action>
  <verify>
    <automated>cd open-seo-main && npx tsc --noEmit src/server/features/keywords/selection/types.ts src/server/features/keywords/selection/presets.ts</automated>
  </verify>
  <done>
    - types.ts exports all 7 type definitions
    - presets.ts exports 4 preset configurations
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CascadeSelector with TDD</name>
  <files>
    open-seo-main/src/server/features/keywords/selection/CascadeSelector.ts,
    open-seo-main/src/server/features/keywords/selection/CascadeSelector.test.ts
  </files>
  <behavior>
    - Test: 80 BOFU, 120 MOFU, 100 TOFU -> selects 60 BOFU (max), 40 MOFU, 0 TOFU = 100 total
    - Test: 25 BOFU only -> selects 25 BOFU, fills MOFU to 40, TOFU to 30 = 95 (under target)
    - Test: 12 BOFU (under min=20) -> warns "BOFU minimum (20) not met: only 12 available"
    - Test: SERVICE_CASCADE with 50 BOFU -> selects 50, 30 MOFU, 15 TOFU = 95
    - Test: Empty input -> returns empty selection with warning
    - Test: All keywords same stage -> respects max, warns if target not met
    - Test: cascadePosition numbering is 1-based and sequential
    - Test: excludedKeywords have correct exclusionReason
    - Test: compositeScore sorting within each pool
  </behavior>
  <action>
Write tests FIRST in CascadeSelector.test.ts covering:
1. Basic cascade (sufficient keywords per CONTEXT.md test case)
2. Insufficient BOFU fallback (per CONTEXT.md test case)
3. BOFU minimum not met warning (per CONTEXT.md test case)
4. SERVICE_CASCADE preset behavior
5. Edge case: empty input
6. Edge case: single stage only
7. cascadePosition sequential numbering
8. exclusionReason correctness
9. compositeScore ordering within pools

Implement CascadeSelector class with:
- `select(keywords: FilteredKeyword[], config?: CascadeConfig): SelectionResult`
- Private methods: groupByStage, sortByScore, selectFromStage
- Default to DEFAULT_CASCADE if no config provided
- Track poolSize in breakdown for visibility
- Generate warnings array for: unmet minimums, unmet target
- Immutable selection (no mutation of input array)

Create singleton export `cascadeSelector`.
  </action>
  <verify>
    <automated>cd open-seo-main && npx vitest run src/server/features/keywords/selection/CascadeSelector.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - All 9+ test cases pass
    - CascadeSelector correctly implements BOFU-first cascade
    - Warnings generated for unmet minimums/targets
    - Excluded keywords tracked with correct reasons
    - cascadePosition is 1-based sequential
  </done>
</task>

<task type="auto">
  <name>Task 3: Create module index and integrate exports</name>
  <files>
    open-seo-main/src/server/features/keywords/selection/index.ts,
    open-seo-main/src/server/features/keywords/index.ts
  </files>
  <action>
Create `selection/index.ts` that re-exports:
- All types from types.ts
- All presets from presets.ts
- CascadeSelector class and cascadeSelector singleton from CascadeSelector.ts

Update `keywords/index.ts` to add selection module exports:
```typescript
// Cascade Selection - BOFU-first keyword selection
export {
  CascadeSelector,
  cascadeSelector,
  DEFAULT_CASCADE,
  SERVICE_CASCADE,
  ECOMMERCE_CASCADE,
  CONTENT_CASCADE,
  type CascadeConfig,
  type SelectionResult,
  type SelectedKeyword,
  type ExcludedKeyword,
  type SelectionBreakdown,
  type StageConfig,
  type FunnelStage,
} from './selection';
```
  </action>
  <verify>
    <automated>cd open-seo-main && npx tsc --noEmit src/server/features/keywords/index.ts && npx vitest run src/server/features/keywords/selection/ --reporter=verbose</automated>
  </verify>
  <done>
    - selection/index.ts exports all types, presets, and CascadeSelector
    - keywords/index.ts re-exports selection module
    - All tests still pass
    - No TypeScript errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyword input | Keywords from Phase 79 filter - trusted internal data |
| config input | CascadeConfig from API or preset - validated at entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Denial of Service | CascadeSelector.select() | mitigate | Cap input array at 10,000 keywords, return early with warning |
| T-80-02 | Tampering | config.stages | mitigate | Validate min <= max, priority 1-3, targetCount > 0 at select() entry |
</threat_model>

<verification>
```bash
# All tests pass
cd open-seo-main && npx vitest run src/server/features/keywords/selection/ --reporter=verbose

# TypeScript compiles
cd open-seo-main && npx tsc --noEmit

# Exports work correctly
cd open-seo-main && node -e "const k = require('./src/server/features/keywords'); console.log(Object.keys(k).filter(k => k.includes('Cascade') || k.includes('CASCADE')))"
```
</verification>

<success_criteria>
1. CascadeSelector.select() returns correct SelectionResult
2. BOFU keywords selected first, then MOFU, then TOFU
3. Min/max constraints respected per stage
4. Target count reached when possible via cascade
5. Warnings generated for unmet minimums and targets
6. Excluded keywords tracked with cascadePosition and exclusionReason
7. All 9+ test cases pass with >80% coverage
8. Types exported from keywords/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/80-cascade-selection/80-01-SUMMARY.md`
</output>
