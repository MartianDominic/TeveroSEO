---
phase: 44-component-library-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/globals.css
  - apps/web/src/app/layout.tsx
  - packages/ui/src/lib/tokens.ts
  - packages/ui/src/index.ts
  - packages/ui/vitest.config.ts
  - packages/ui/vitest.setup.ts
autonomous: true
requirements:
  - SC-01
  - SC-02
  - SC-03

must_haves:
  truths:
    - "Tailwind utility classes like bg-canvas, text-text-1, shadow-card work in apps/web"
    - "Geist, Geist Mono, and Newsreader fonts render correctly"
    - "Font variables --font-sans, --font-mono, --font-display are applied to html element"
    - "TypeScript can import token constants from @tevero/ui"
  artifacts:
    - path: "apps/web/src/app/globals.css"
      provides: "Tailwind @theme inline mapping v6 tokens to utilities"
      contains: "@theme inline"
    - path: "apps/web/src/app/layout.tsx"
      provides: "Font loading via next/font with CSS variables"
      contains: "Geist"
    - path: "packages/ui/src/lib/tokens.ts"
      provides: "TypeScript token exports"
      exports: ["colors", "spacing", "radii", "shadows"]
    - path: "packages/ui/vitest.config.ts"
      provides: "Test configuration for UI package"
      contains: "vitest"
  key_links:
    - from: "apps/web/src/app/globals.css"
      to: "packages/ui/src/lib/tokens.css"
      via: "@import"
      pattern: "@import.*@tevero/ui"
    - from: "apps/web/src/app/layout.tsx"
      to: "next/font/google"
      via: "font variables on html"
      pattern: "className=.*font-sans.*font-mono.*font-display"
---

<objective>
Establish the design token foundation for Phase 44 by extending Tailwind with v6 tokens and configuring font loading.

Purpose: All subsequent component plans depend on Tailwind utilities (bg-canvas, shadow-card, etc.) and fonts being available. This plan completes the token-to-utility mapping.

Output: Working Tailwind integration with v6 tokens, font loading via next/font, TypeScript token exports, and test infrastructure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/design-system-v6.md
@.planning/phases/44-component-library-foundation/44-RESEARCH.md

<interfaces>
<!-- tokens.css already exists with all v6 CSS variables -->
From packages/ui/src/lib/tokens.css:
```css
:root {
  --canvas: #FAFAF7;
  --surface: #FFFFFF;
  --text-1: #14141A;
  --accent: #0F4F3D;
  --shadow-card: 0 1px 2px rgba(20, 20, 26, 0.04), ...;
  --radius-card: 12px;
  --space-5: clamp(16px, 1.4vw, 22px);
  /* ... all tokens defined */
}
```

From packages/ui/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend globals.css with v6 @theme inline mapping</name>
  <files>apps/web/src/app/globals.css</files>
  <read_first>
    - packages/ui/src/lib/tokens.css (source tokens)
    - apps/web/src/app/globals.css (current file)
    - .planning/phases/44-component-library-foundation/44-RESEARCH.md (Pattern 1)
  </read_first>
  <action>
Replace the entire globals.css with Tailwind v4 configuration that:

1. Imports tailwindcss
2. Imports @tevero/ui/src/lib/tokens.css
3. Uses @theme inline {} to map v6 CSS variables to Tailwind utilities

The @theme inline block must include:
- Colors: canvas, canvas-dim, surface, surface-2, surface-3, text-1, text-2, text-3, text-4, accent, accent-soft, accent-ink, success, success-soft, error, error-soft, warning, warning-soft, info, info-soft, hairline, hairline-2, hairline-3
- Shadows: shadow-card, shadow-lift, shadow-pop, shadow-cta, shadow-cta-hover
- Radii: radius-input, radius-button, radius-card, radius-modal, radius-pill
- Spacing: space-1 through space-9

Keep the @layer base block for body styling but update to use v6 tokens (bg-canvas, text-text-2, font-sans).

Remove the old OKLCH-based shadcn colors - they are replaced by the v6 token system.
  </action>
  <acceptance_criteria>
    - File contains `@import "tailwindcss";`
    - File contains `@import "@tevero/ui/src/lib/tokens.css";`
    - File contains `@theme inline {` block
    - @theme block includes `--color-canvas: var(--canvas);`
    - @theme block includes `--shadow-card: var(--shadow-card);`
    - @theme block includes `--radius-card: var(--radius-card);`
    - @layer base body uses `background-color: var(--canvas);`
  </acceptance_criteria>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -q "@theme inline" apps/web/src/app/globals.css && grep -q "@import.*@tevero/ui" apps/web/src/app/globals.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>globals.css updated with @theme inline mapping all v6 tokens to Tailwind utilities</done>
</task>

<task type="auto">
  <name>Task 2: Configure font loading via next/font</name>
  <files>apps/web/src/app/layout.tsx</files>
  <read_first>
    - apps/web/src/app/layout.tsx (current implementation)
    - .planning/phases/44-component-library-foundation/44-RESEARCH.md (Pattern 2)
  </read_first>
  <action>
Update layout.tsx to load Geist, Geist_Mono, and Newsreader fonts via next/font/google:

1. Import fonts from 'next/font/google':
   - Geist with variable: '--font-sans', subsets: ['latin'], display: 'swap'
   - Geist_Mono with variable: '--font-mono', subsets: ['latin'], display: 'swap'
   - Newsreader with variable: '--font-display', subsets: ['latin'], display: 'swap', axes: ['opsz']

2. Apply font variables to html element:
   ```tsx
   <html lang="en" className={`${geist.variable} ${geistMono.variable} ${newsreader.variable}`}>
   ```

3. Update body className to use v6 token classes:
   - Remove: "min-h-screen bg-background text-foreground antialiased"
   - Add: "min-h-screen bg-canvas text-text-2 antialiased font-sans"

Keep ClerkProvider wrapping and existing metadata.
  </action>
  <acceptance_criteria>
    - File imports `Geist` from 'next/font/google'
    - File imports `Geist_Mono` from 'next/font/google'
    - File imports `Newsreader` from 'next/font/google'
    - geist configured with `variable: '--font-sans'`
    - newsreader configured with `axes: ['opsz']`
    - html element has className with all three font variables
    - body has className including "bg-canvas" and "text-text-2"
  </acceptance_criteria>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -q "Geist_Mono" apps/web/src/app/layout.tsx && grep -q "Newsreader" apps/web/src/app/layout.tsx && grep -q "font-sans" apps/web/src/app/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Fonts loading via next/font with CSS variables applied to html element</done>
</task>

<task type="auto">
  <name>Task 3: Create TypeScript token exports and test infrastructure</name>
  <files>packages/ui/src/lib/tokens.ts, packages/ui/src/index.ts, packages/ui/vitest.config.ts, packages/ui/vitest.setup.ts</files>
  <read_first>
    - packages/ui/src/lib/utils.ts (pattern for utility exports)
    - packages/ui/src/lib/tokens.css (token values to mirror)
    - packages/ui/package.json (dependencies)
    - apps/web/vitest.config.ts (if exists, for reference)
  </read_first>
  <action>
1. Create packages/ui/src/lib/tokens.ts exporting TypeScript constants that mirror tokens.css:
```typescript
export const colors = {
  canvas: '#FAFAF7',
  canvasDim: '#F5F4EE',
  surface: '#FFFFFF',
  surface2: '#F8F8F3',
  surface3: '#F2F1EB',
  text1: '#14141A',
  text2: '#54545A',
  text3: '#93939A',
  text4: '#C4C3BB',
  accent: '#0F4F3D',
  accent2: '#1A6E55',
  accentSoft: '#EAF1ED',
  accentInk: '#093528',
  success: '#1B6E45',
  successSoft: '#EAF2EE',
  error: '#9B2C2C',
  errorSoft: '#F4E6E6',
  warning: '#A87F1A',
  warningSoft: '#F4EDDA',
  info: '#2D5A87',
  infoSoft: '#EFF4F9',
} as const;

export const spacing = {
  space1: '4px',
  space2: '8px',
  // ... all spacing values
} as const;

export const radii = {
  input: '6px',
  button: '8px',
  card: '12px',
  modal: '14px',
  pill: '999px',
} as const;

// Export type helpers
export type ColorToken = keyof typeof colors;
export type SpacingToken = keyof typeof spacing;
```

2. Add tokens.ts export to packages/ui/src/index.ts:
```typescript
export * from './lib/tokens';
```

3. Create packages/ui/vitest.config.ts:
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./vitest.setup.ts'],
    include: ['src/**/*.test.{ts,tsx}'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      include: ['src/**/*.{ts,tsx}'],
      exclude: ['src/**/*.test.{ts,tsx}', 'src/**/*.stories.{ts,tsx}'],
    },
  },
});
```

4. Create packages/ui/vitest.setup.ts:
```typescript
import '@testing-library/jest-dom/vitest';
```

5. Add test dependencies to package.json (note in summary for manual add):
   - vitest, @vitejs/plugin-react, @testing-library/react, @testing-library/jest-dom, @testing-library/user-event, jsdom
  </action>
  <acceptance_criteria>
    - packages/ui/src/lib/tokens.ts exports `colors` object with accent: '#0F4F3D'
    - packages/ui/src/lib/tokens.ts exports `radii` object with card: '12px'
    - packages/ui/src/index.ts includes `export * from './lib/tokens'`
    - packages/ui/vitest.config.ts configures jsdom environment
    - packages/ui/vitest.setup.ts imports jest-dom matchers
  </acceptance_criteria>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -q "export const colors" packages/ui/src/lib/tokens.ts && grep -q "./lib/tokens" packages/ui/src/index.ts && test -f packages/ui/vitest.config.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>TypeScript tokens exported, test infrastructure configured, ready for component development</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS tokens | Static file, no user input |
| Font loading | Google Fonts CDN, trusted source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | CSS variables | accept | Static CSS, no runtime manipulation |
| T-44-02 | Information Disclosure | Font loading | accept | Public fonts, no sensitive data |

No security-critical code in this plan. All files are static configuration.
</threat_model>

<verification>
After all tasks complete:
1. Run `pnpm --filter @tevero/web dev` and verify app starts without errors
2. Inspect browser DevTools > Elements > html to confirm font variables present
3. Add a test div with `className="bg-canvas text-accent shadow-card"` and verify styles apply
4. Import `{ colors }` from '@tevero/ui' in a test file to verify TS exports work
</verification>

<success_criteria>
- Tailwind utilities bg-canvas, text-text-1, shadow-card, etc. work in apps/web
- Fonts render: body text in Geist, code in Geist Mono, headlines in Newsreader
- TypeScript `import { colors } from '@tevero/ui'` compiles without errors
- Test infrastructure ready for component tests
</success_criteria>

<output>
After completion, create `.planning/phases/44-component-library-foundation/44-01-SUMMARY.md`
</output>
