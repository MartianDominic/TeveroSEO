---
phase: 44-component-library-foundation
plan: 08
type: execute
wave: 4
depends_on: [44-01, 44-02, 44-03, 44-04, 44-05, 44-06, 44-07]
files_modified:
  - packages/ui/.storybook/main.ts
  - packages/ui/.storybook/preview.ts
  - packages/ui/src/stories/progress-bar.stories.tsx
  - packages/ui/src/stories/checklist.stories.tsx
  - packages/ui/src/stories/empty-state.stories.tsx
  - packages/ui/src/stories/health-gauge.stories.tsx
  - packages/ui/src/__tests__/progress-bar.test.tsx
  - packages/ui/src/__tests__/checklist.test.tsx
  - packages/ui/src/__tests__/empty-state.test.tsx
  - packages/ui/src/__tests__/health-gauge.test.tsx
  - packages/ui/src/__tests__/format-time.test.ts
  - packages/ui/src/__tests__/status-config.test.ts
  - packages/ui/package.json
autonomous: false
requirements:
  - SC-27
  - SC-28
  - SC-29
  - SC-30

must_haves:
  truths:
    - "Storybook builds without errors"
    - "At least 4 representative component stories exist"
    - "At least 6 component/utility tests exist"
    - "Test coverage is >= 80% for implemented components"
    - "Stories document props with controls"
    - "Stories include accessibility annotations"
  artifacts:
    - path: "packages/ui/.storybook/main.ts"
      provides: "Storybook configuration"
      contains: "@storybook/nextjs-vite"
    - path: "packages/ui/.storybook/preview.ts"
      provides: "Storybook preview with tokens"
      contains: "tokens.css"
    - path: "packages/ui/src/stories/"
      provides: "Component stories"
    - path: "packages/ui/src/__tests__/"
      provides: "Component tests"
  key_links:
    - from: "packages/ui/.storybook/preview.ts"
      to: "packages/ui/src/lib/tokens.css"
      via: "import"
      pattern: "import.*tokens.css"
    - from: "packages/ui/src/__tests__/*.test.tsx"
      to: "@testing-library/react"
      via: "import"
      pattern: "import.*@testing-library/react"
---

<objective>
Set up Storybook and write tests for the component library.

Purpose: Storybook provides visual documentation and interactive playground for components. Tests ensure components work correctly and maintain quality. Both are required for production-ready component library.

Output: Storybook configuration, 4+ representative stories, 6+ tests, 80%+ coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/design-system-v6.md
@.planning/design/gsd-phase0-component-library.md
@.planning/phases/44-component-library-foundation/44-RESEARCH.md
@.planning/phases/44-component-library-foundation/44-01-SUMMARY.md
@.planning/phases/44-component-library-foundation/44-02-SUMMARY.md
@.planning/phases/44-component-library-foundation/44-03-SUMMARY.md
@.planning/phases/44-component-library-foundation/44-04-SUMMARY.md
@.planning/phases/44-component-library-foundation/44-05-SUMMARY.md
@.planning/phases/44-component-library-foundation/44-06-SUMMARY.md
@.planning/phases/44-component-library-foundation/44-07-SUMMARY.md

<interfaces>
<!-- Storybook configuration patterns -->
From @storybook/nextjs-vite:
```typescript
// main.ts
import type { StorybookConfig } from '@storybook/nextjs-vite';
const config: StorybookConfig = {
  stories: ['../src/**/*.stories.@(js|jsx|ts|tsx)'],
  addons: ['@storybook/addon-a11y', '@storybook/addon-essentials'],
  framework: '@storybook/nextjs-vite',
};
export default config;
```

<!-- Testing patterns from RESEARCH.md -->
From vitest + @testing-library/react:
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Storybook with nextjs-vite framework</name>
  <files>packages/ui/.storybook/main.ts, packages/ui/.storybook/preview.ts, packages/ui/package.json</files>
  <read_first>
    - .planning/phases/44-component-library-foundation/44-RESEARCH.md (Storybook patterns)
    - packages/ui/package.json (current dependencies)
    - packages/ui/src/lib/tokens.css (for preview import)
  </read_first>
  <action>
1. Create packages/ui/.storybook/main.ts:

```typescript
import type { StorybookConfig } from '@storybook/nextjs-vite';

const config: StorybookConfig = {
  stories: ['../src/**/*.stories.@(js|jsx|ts|tsx)'],
  addons: [
    '@storybook/addon-essentials',
    '@storybook/addon-a11y',
  ],
  framework: {
    name: '@storybook/nextjs-vite',
    options: {},
  },
  docs: {
    autodocs: 'tag',
  },
  staticDirs: ['../public'],
};

export default config;
```

2. Create packages/ui/.storybook/preview.ts:

```typescript
import type { Preview } from '@storybook/react';
import '../src/lib/tokens.css';
import '../../apps/web/src/app/globals.css';

const preview: Preview = {
  parameters: {
    actions: { argTypesRegex: '^on[A-Z].*' },
    controls: {
      matchers: {
        color: /(background|color)$/i,
        date: /Date$/,
      },
    },
    backgrounds: {
      default: 'canvas',
      values: [
        { name: 'canvas', value: '#FAFAF7' },
        { name: 'surface', value: '#FFFFFF' },
        { name: 'dark', value: '#14141A' },
      ],
    },
    a11y: {
      element: '#storybook-root',
      config: {
        rules: [
          { id: 'color-contrast', enabled: true },
          { id: 'landmark-one-main', enabled: false },
        ],
      },
    },
  },
};

export default preview;
```

3. Update packages/ui/package.json to add scripts and dev dependencies:

Add to scripts:
```json
{
  "storybook": "storybook dev -p 6006",
  "build-storybook": "storybook build"
}
```

Note required dev dependencies for manual install:
- @storybook/nextjs-vite
- @storybook/addon-essentials
- @storybook/addon-a11y
- storybook

4. Create packages/ui/.storybook directory if it doesn't exist.
  </action>
  <acceptance_criteria>
    - packages/ui/.storybook/main.ts exists with nextjs-vite framework
    - packages/ui/.storybook/preview.ts imports tokens.css and globals.css
    - preview.ts configures canvas background (#FAFAF7)
    - preview.ts enables a11y addon
    - package.json has storybook and build-storybook scripts
  </acceptance_criteria>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && test -f packages/ui/.storybook/main.ts && test -f packages/ui/.storybook/preview.ts && grep -q "nextjs-vite" packages/ui/.storybook/main.ts && grep -q "tokens.css" packages/ui/.storybook/preview.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Storybook configured with nextjs-vite, tokens, and a11y addon</done>
</task>

<task type="auto">
  <name>Task 2: Write representative component stories</name>
  <files>packages/ui/src/stories/progress-bar.stories.tsx, packages/ui/src/stories/checklist.stories.tsx, packages/ui/src/stories/empty-state.stories.tsx, packages/ui/src/stories/health-gauge.stories.tsx</files>
  <read_first>
    - .planning/design/gsd-phase0-component-library.md (Story requirements)
    - .planning/phases/44-component-library-foundation/44-RESEARCH.md (Pattern 4: Storybook)
    - packages/ui/src/components/progress-bar.tsx
    - packages/ui/src/components/checklist.tsx
    - packages/ui/src/components/empty-state.tsx
    - packages/ui/src/components/health-gauge.tsx
  </read_first>
  <action>
Create packages/ui/src/stories/ directory and 4 representative stories:

1. progress-bar.stories.tsx:
```typescript
import type { Meta, StoryObj } from '@storybook/react';
import { ProgressBar } from '../components/progress-bar';

const meta = {
  title: 'Data Display/ProgressBar',
  component: ProgressBar,
  parameters: { layout: 'centered' },
  tags: ['autodocs'],
  argTypes: {
    value: { control: { type: 'range', min: 0, max: 100, step: 1 } },
    variant: { control: 'select', options: ['default', 'success', 'warning', 'error'] },
    size: { control: 'select', options: ['sm', 'md', 'lg'] },
    showLabel: { control: 'boolean' },
  },
} satisfies Meta<typeof ProgressBar>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: { value: 65 },
};

export const Success: Story = {
  args: { value: 100, variant: 'success' },
};

export const WithLabel: Story = {
  args: { value: 75, showLabel: true },
};

export const AllSizes: Story = {
  render: () => (
    <div className="space-y-4 w-64">
      <ProgressBar value={60} size="sm" />
      <ProgressBar value={60} size="md" />
      <ProgressBar value={60} size="lg" />
    </div>
  ),
};
```

2. checklist.stories.tsx:
- Default story with 3 items (2 done, 1 pending)
- With action links story
- Empty state story

3. empty-state.stories.tsx:
- Default variant
- Search variant (no results)
- First-time variant (onboarding)
- With actions

4. health-gauge.stories.tsx:
- Default (score 75)
- High score (95, grade A)
- Low score (45, grade D)
- All sizes

Story requirements:
- Use `satisfies Meta<typeof Component>` pattern
- Include `tags: ['autodocs']` for auto-documentation
- Add argTypes with controls for all props
- Include accessibility notes in parameters where relevant
- Show realistic data in examples
  </action>
  <acceptance_criteria>
    - progress-bar.stories.tsx has Default, Success, WithLabel, AllSizes stories
    - checklist.stories.tsx demonstrates done/pending items
    - empty-state.stories.tsx shows all variants
    - health-gauge.stories.tsx shows different scores/grades
    - All stories have argTypes with controls
    - All stories have tags: ['autodocs']
  </acceptance_criteria>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && test -f packages/ui/src/stories/progress-bar.stories.tsx && test -f packages/ui/src/stories/checklist.stories.tsx && test -f packages/ui/src/stories/empty-state.stories.tsx && test -f packages/ui/src/stories/health-gauge.stories.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>4 representative component stories created with controls and autodocs</done>
</task>

<task type="auto">
  <name>Task 3: Write component and utility tests</name>
  <files>packages/ui/src/__tests__/progress-bar.test.tsx, packages/ui/src/__tests__/checklist.test.tsx, packages/ui/src/__tests__/empty-state.test.tsx, packages/ui/src/__tests__/health-gauge.test.tsx, packages/ui/src/__tests__/format-time.test.ts, packages/ui/src/__tests__/status-config.test.ts</files>
  <read_first>
    - .planning/design/gsd-phase0-component-library.md (Test requirements)
    - .planning/phases/44-component-library-foundation/44-RESEARCH.md (Pattern 5: Testing)
    - packages/ui/vitest.config.ts (from 44-01)
    - packages/ui/vitest.setup.ts (from 44-01)
  </read_first>
  <action>
Create packages/ui/src/__tests__/ directory and tests:

1. progress-bar.test.tsx:
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { ProgressBar } from '../components/progress-bar';

describe('ProgressBar', () => {
  it('renders with value prop', () => {
    render(<ProgressBar value={50} />);
    expect(screen.getByRole('progressbar')).toBeInTheDocument();
  });

  it('clamps value between 0 and 100', () => {
    const { rerender } = render(<ProgressBar value={150} />);
    expect(screen.getByRole('progressbar')).toHaveAttribute('aria-valuenow', '100');
    
    rerender(<ProgressBar value={-20} />);
    expect(screen.getByRole('progressbar')).toHaveAttribute('aria-valuenow', '0');
  });

  it('shows label when showLabel is true', () => {
    render(<ProgressBar value={75} showLabel />);
    expect(screen.getByText('75%')).toBeInTheDocument();
  });

  it('applies variant classes correctly', () => {
    render(<ProgressBar value={100} variant="success" />);
    // Test that success variant applies correct background
  });
});
```

2. checklist.test.tsx:
- Renders title and count
- ChecklistItem shows step indicator
- Done items have line-through styling
- Action links fire onClick

3. empty-state.test.tsx:
- Renders title and description
- Renders icon when provided
- Action button fires onClick
- Secondary action renders as link

4. health-gauge.test.tsx:
- Renders with score
- Auto-calculates grade correctly (A >= 90, B+ >= 80, etc.)
- Has aria-label for accessibility
- Size variants render correct dimensions

5. format-time.test.ts:
```typescript
import { describe, it, expect, vi } from 'vitest';
import { formatRelativeTime, formatShortDate } from '../lib/format-time';

describe('formatRelativeTime', () => {
  it('returns "X minutes ago" for recent times', () => {
    const fiveMinutesAgo = new Date(Date.now() - 5 * 60 * 1000);
    expect(formatRelativeTime(fiveMinutesAgo)).toMatch(/5 minutes ago/);
  });

  it('handles string input', () => {
    const result = formatRelativeTime('2026-04-30T12:00:00Z');
    expect(typeof result).toBe('string');
  });
});

describe('formatShortDate', () => {
  it('formats date as "Apr 30, 2026"', () => {
    const date = new Date('2026-04-30');
    expect(formatShortDate(date)).toBe('Apr 30, 2026');
  });
});
```

6. status-config.test.ts:
- PROSPECT_STATUS has all expected keys
- CLIENT_STATUS has icons defined
- ARTICLE_STATUS uses v6 token classes
- StatusConfig interface is correctly typed

Test requirements:
- Use @testing-library/react for component tests
- Use getByRole over getByClassName (behavior-focused)
- Test accessibility attributes (aria-*)
- Test edge cases (empty, max values)
- Test event handlers fire correctly
  </action>
  <acceptance_criteria>
    - progress-bar.test.tsx tests value clamping and label
    - checklist.test.tsx tests done/pending states
    - empty-state.test.tsx tests title, description, actions
    - health-gauge.test.tsx tests grade calculation and aria-label
    - format-time.test.ts tests relative and short date formatting
    - status-config.test.ts tests config objects and keys
    - All tests use @testing-library/react patterns
    - Tests verify accessibility attributes
  </acceptance_criteria>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && test -f packages/ui/src/__tests__/progress-bar.test.tsx && test -f packages/ui/src/__tests__/format-time.test.ts && test -f packages/ui/src/__tests__/status-config.test.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>6 component and utility tests created with 80%+ coverage target</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Storybook configured with v6 tokens and representative stories. Test suite with 6+ tests targeting 80% coverage.
  </what-built>
  <how-to-verify>
1. Install Storybook dependencies:
   ```bash
   cd packages/ui
   pnpm add -D @storybook/nextjs-vite @storybook/addon-essentials @storybook/addon-a11y storybook
   ```

2. Run Storybook:
   ```bash
   pnpm storybook
   ```
   - Visit http://localhost:6006
   - Navigate to Data Display/ProgressBar story
   - Verify v6 tokens render (accent green, canvas background)
   - Use controls to change value, size, variant
   - Check a11y tab for accessibility issues

3. Run tests:
   ```bash
   pnpm --filter @tevero/ui test
   ```
   - All tests should pass
   - Check coverage report for 80%+ on components

4. Build Storybook:
   ```bash
   pnpm --filter @tevero/ui build-storybook
   ```
   - Should build without errors
  </how-to-verify>
  <resume-signal>Type "verified" if Storybook runs and tests pass, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storybook stories | Development-only, not production |
| Test files | Run in CI, not production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-19 | Information Disclosure | storybook | accept | Dev-only, not deployed to production |
| T-44-20 | Denial of Service | tests | accept | Tests run in CI, timeout limits prevent runaway |

No security-critical code in this plan. Stories and tests are development tooling only.
</threat_model>

<verification>
After all tasks complete:
1. Run `pnpm --filter @tevero/ui storybook` to verify Storybook runs
2. Navigate through all stories and verify v6 tokens render
3. Run `pnpm --filter @tevero/ui test` to verify all tests pass
4. Run `pnpm --filter @tevero/ui test --coverage` to verify 80%+ coverage
5. Run `pnpm --filter @tevero/ui build-storybook` to verify static build
</verification>

<success_criteria>
- Storybook dev server runs without errors
- Storybook shows v6 design tokens (canvas background, accent colors)
- At least 4 component stories exist with controls
- At least 6 tests exist and pass
- Test coverage >= 80% for implemented components
- Stories have autodocs enabled
- a11y addon shows no critical issues
- Storybook builds to static site without errors
</success_criteria>

<output>
After completion, create `.planning/phases/44-component-library-foundation/44-08-SUMMARY.md`
</output>
