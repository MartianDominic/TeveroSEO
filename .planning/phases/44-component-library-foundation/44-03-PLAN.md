---
phase: 44-component-library-foundation
plan: 03
type: execute
wave: 2
depends_on: [44-01, 44-02]
files_modified:
  - packages/ui/src/components/checklist.tsx
  - packages/ui/src/components/pipeline-stage-card.tsx
  - packages/ui/src/components/kanban.tsx
  - packages/ui/src/components/today-feed-item.tsx
  - packages/ui/src/index.ts
autonomous: true
requirements:
  - SC-07
  - SC-08
  - SC-09
  - SC-10

must_haves:
  truths:
    - "Checklist renders with completed/total count and ChecklistItem children"
    - "PipelineStageCard displays stage name, count, and percentage bar"
    - "KanbanColumn renders with header status dot and accepts KanbanCard children"
    - "TodayFeedItem shows mono timestamp, title, and semantic tag"
  artifacts:
    - path: "packages/ui/src/components/checklist.tsx"
      provides: "Checklist and ChecklistItem compound components"
      exports: ["Checklist", "ChecklistItem", "ChecklistProps", "ChecklistItemProps"]
    - path: "packages/ui/src/components/pipeline-stage-card.tsx"
      provides: "Pipeline funnel stage visualization"
      exports: ["PipelineStageCard", "PipelineStageCardProps"]
    - path: "packages/ui/src/components/kanban.tsx"
      provides: "Kanban board column and card components"
      exports: ["KanbanColumn", "KanbanCard", "KanbanColumnProps", "KanbanCardProps"]
    - path: "packages/ui/src/components/today-feed-item.tsx"
      provides: "Activity feed item with timestamp and tag"
      exports: ["TodayFeedItem", "TodayFeedItemProps"]
  key_links:
    - from: "packages/ui/src/components/checklist.tsx"
      to: "packages/ui/src/components/step-indicator.tsx"
      via: "import"
      pattern: "import.*StepIndicator"
    - from: "packages/ui/src/components/pipeline-stage-card.tsx"
      to: "packages/ui/src/lib/status-config.ts"
      via: "import"
      pattern: "import.*PIPELINE_STAGE"
    - from: "packages/ui/src/components/kanban.tsx"
      to: "packages/ui/src/lib/status-config.ts"
      via: "import"
      pattern: "import.*StatusConfig"
---

<objective>
Implement New Primitives Part 1: four new component patterns that don't exist in the codebase yet.

Purpose: These components (Checklist, PipelineStageCard, KanbanColumn, TodayFeedItem) are specified in design-system-v6.md and gsd-phase0-component-library.md but not yet implemented. They enable onboarding flows, pipeline visualizations, and activity feeds.

Output: 4 new component files with full v6 token integration, exported from packages/ui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/design-system-v6.md
@.planning/design/gsd-phase0-component-library.md
@.planning/phases/44-component-library-foundation/44-RESEARCH.md
@.planning/phases/44-component-library-foundation/44-01-SUMMARY.md
@.planning/phases/44-component-library-foundation/44-02-SUMMARY.md

<interfaces>
<!-- From 44-02 outputs -->
From packages/ui/src/components/step-indicator.tsx:
```typescript
export interface StepIndicatorProps {
  step: number | 'done';
  current?: boolean;
  size?: 'sm' | 'md';
}
export function StepIndicator({ step, current, size }: StepIndicatorProps): JSX.Element;
```

From packages/ui/src/lib/status-config.ts:
```typescript
export interface StatusConfig {
  label: string;
  color: string;
  bgColor: string;
  textColor: string;
  icon?: React.ComponentType<{ className?: string }>;
  pulse?: boolean;
}

export const PROSPECT_STATUS: Record<string, StatusConfig>;
export const CLIENT_STATUS: Record<string, StatusConfig>;
export const ARTICLE_STATUS: Record<string, StatusConfig>;
```

From packages/ui/src/lib/format-time.ts:
```typescript
export function formatRelativeTime(date: Date | string | number): string;
```

From packages/ui/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Checklist and ChecklistItem components</name>
  <files>packages/ui/src/components/checklist.tsx, packages/ui/src/index.ts</files>
  <read_first>
    - .planning/design/gsd-phase0-component-library.md (Section 3.1)
    - packages/ui/src/components/step-indicator.tsx (from 44-02)
    - apps/web/src/components/onboarding/GettingStartedCard.tsx (usage pattern)
  </read_first>
  <action>
Create packages/ui/src/components/checklist.tsx:

1. Define ChecklistProps interface:
```typescript
interface ChecklistProps {
  title?: string;
  description?: string;
  completedCount?: number;
  totalCount?: number;
  children: React.ReactNode;
  className?: string;
}
```

2. Define ChecklistItemProps interface:
```typescript
interface ChecklistItemProps {
  done: boolean;
  title: string;
  description?: string;
  action?: {
    label: string;
    onClick: () => void;
  };
  children?: React.ReactNode;
}
```

3. Implement Checklist component:
   - Card container with shadow-card, rounded-radius-card
   - Header with title and "X/Y complete" badge
   - Children rendered in a space-y-3 container

4. Implement ChecklistItem component:
   - Uses StepIndicator from 44-02 for done/pending states
   - Title with conditional line-through opacity-60 when done
   - Description in text-text-3 text-type-small
   - Action link in text-accent hover:underline when not done
   - Children slot for custom content

5. Apply v6 tokens:
   - bg-surface for card background
   - text-text-1 for title, text-text-2 for description
   - Action link uses text-accent

6. Export both components and types from index.ts.
  </action>
  <acceptance_criteria>
    - Checklist renders with title and completedCount/totalCount badge
    - ChecklistItem uses StepIndicator for completion state
    - Done items have line-through opacity-60 styling
    - Action links render with text-accent color
    - Both components exported from packages/ui/src/index.ts
  </acceptance_criteria>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -q "export.*Checklist" packages/ui/src/index.ts && grep -q "ChecklistItem" packages/ui/src/components/checklist.tsx && grep -q "StepIndicator" packages/ui/src/components/checklist.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Checklist and ChecklistItem components created with v6 tokens and StepIndicator integration</done>
</task>

<task type="auto">
  <name>Task 2: Create PipelineStageCard and KanbanColumn/KanbanCard</name>
  <files>packages/ui/src/components/pipeline-stage-card.tsx, packages/ui/src/components/kanban.tsx, packages/ui/src/index.ts</files>
  <read_first>
    - .planning/design/gsd-phase0-component-library.md (Sections 3.2 and 3.3)
    - .planning/design/design-system-v6.md (Section 14.5 for pipeline visual spec)
    - packages/ui/src/lib/status-config.ts (for PIPELINE_STAGE)
  </read_first>
  <action>
1. Create packages/ui/src/components/pipeline-stage-card.tsx:

PipelineStageCardProps:
```typescript
interface PipelineStageCardProps {
  stage: string;
  label: string;
  count: number;
  percentage: number;
  value?: number;
  isActive?: boolean;
  onClick?: () => void;
  className?: string;
}
```

Visual structure (from design-system-v6.md Section 14.5):
- Small-caps label at top (font-variant-caps: all-small-caps, 12px)
- Newsreader serif count numeral (--num-row size)
- 3px relative-volume bar showing percentage
- Active stage: accent gradient fill, accent bar color
- Inactive: surface-3 bar, hairline-2 left border
- 2px left border in stage color

2. Create packages/ui/src/components/kanban.tsx:

KanbanColumnProps:
```typescript
interface KanbanColumnProps {
  title: string;
  count: number;
  status: string;
  statusConfig?: StatusConfig;
  children: React.ReactNode;
  onDrop?: (itemId: string) => void;
  className?: string;
}
```

KanbanCardProps:
```typescript
interface KanbanCardProps {
  id: string;
  title: string;
  subtitle?: string;
  meta?: React.ReactNode;
  status?: string;
  draggable?: boolean;
  onClick?: () => void;
  className?: string;
}
```

KanbanColumn implementation:
- Header with status dot (colored circle from statusConfig)
- Title and count badge
- role="listbox" for accessibility
- Children container with space-y-2

KanbanCard implementation:
- shadow-card at rest, shadow-lift on hover
- Lift animation with translateY(-1px) on hover
- role="option" for accessibility
- Optional drag handle icon on hover

3. Export all components and types from index.ts.
  </action>
  <acceptance_criteria>
    - PipelineStageCard renders small-caps label, Newsreader count, percentage bar
    - isActive prop applies accent styling to PipelineStageCard
    - KanbanColumn renders status dot, title, and count
    - KanbanColumn has role="listbox" attribute
    - KanbanCard has shadow-card and hover:shadow-lift
    - KanbanCard has role="option" attribute
    - All exported from packages/ui/src/index.ts
  </acceptance_criteria>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -q "PipelineStageCard" packages/ui/src/components/pipeline-stage-card.tsx && grep -q "KanbanColumn" packages/ui/src/components/kanban.tsx && grep -q 'role="listbox"' packages/ui/src/components/kanban.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>PipelineStageCard and Kanban components created with v6 tokens and accessibility attributes</done>
</task>

<task type="auto">
  <name>Task 3: Create TodayFeedItem component</name>
  <files>packages/ui/src/components/today-feed-item.tsx, packages/ui/src/index.ts</files>
  <read_first>
    - .planning/design/gsd-phase0-component-library.md (Section 3.4)
    - .planning/design/design-system-v6.md (Section 10.2)
    - packages/ui/src/lib/format-time.ts (formatRelativeTime)
  </read_first>
  <action>
Create packages/ui/src/components/today-feed-item.tsx:

1. Define TodayFeedItemProps:
```typescript
interface TodayFeedItemProps {
  timestamp: string | Date;
  title: string;
  description?: string;
  tag?: {
    label: string;
    variant: 'ranking' | 'audit' | 'alert' | 'report' | 'connection';
  };
  onClick?: () => void;
  className?: string;
}
```

2. Visual structure (from design-system-v6.md Section 10.2):
- Two-column layout: mono timestamp (44px fixed width) + body content
- Timestamp in font-mono, text-type-tiny, text-text-3
- Title in text-type-small, text-text-1
- Description in text-type-small, text-text-2
- Tag at bottom with semantic dot + small-caps label

3. Tag variant colors:
- ranking: accent colors
- audit: info colors
- alert: error colors
- report: success colors
- connection: warning colors

4. Tag dot uses 6px circle with variant color.

5. Apply v6 tokens:
- font-mono for timestamp
- font-variant-caps: all-small-caps for tag label
- Hover state: bg-surface-2, cursor-pointer

6. Export component and types from index.ts.
  </action>
  <acceptance_criteria>
    - TodayFeedItem renders two-column layout (timestamp + body)
    - Timestamp uses font-mono and text-type-tiny
    - Tag renders with colored dot and small-caps label
    - Hover state applies bg-surface-2
    - Component exported from packages/ui/src/index.ts
  </acceptance_criteria>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -q "TodayFeedItem" packages/ui/src/components/today-feed-item.tsx && grep -q "font-mono" packages/ui/src/components/today-feed-item.tsx && grep -q "export.*TodayFeedItem" packages/ui/src/index.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>TodayFeedItem component created with two-column layout, semantic tags, and v6 tokens</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onClick handlers | Callbacks passed from parent components |
| timestamp input | Date/string passed to TodayFeedItem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Tampering | checklist.tsx | accept | Props are typed; no dangerous HTML rendering |
| T-44-07 | Spoofing | kanban.tsx | accept | onDrop itemId is passed up, not executed |
| T-44-08 | Information Disclosure | today-feed-item.tsx | accept | Only displays data passed via props |

No security-critical code in this plan. All components are presentational.
</threat_model>

<verification>
After all tasks complete:
1. Run `pnpm --filter @tevero/ui build` to verify TypeScript compilation
2. Import each new component in a test file to verify exports work
3. Verify small-caps renders correctly (font-variant-caps)
4. Check role attributes are present on Kanban components
</verification>

<success_criteria>
- Checklist and ChecklistItem render onboarding-style checklists
- PipelineStageCard displays funnel stage with percentage bar
- KanbanColumn and KanbanCard render accessible kanban boards
- TodayFeedItem shows activity feed items with timestamps and tags
- All components use v6 design tokens
- All components exported from '@tevero/ui'
</success_criteria>

<output>
After completion, create `.planning/phases/44-component-library-foundation/44-03-SUMMARY.md`
</output>
