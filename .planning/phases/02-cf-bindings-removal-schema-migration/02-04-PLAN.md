---
phase: 02-cf-bindings-removal-schema-migration
plan: 04
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - open-seo-main/src/lib/auth.ts
  - open-seo-main/src/server/email/loops.ts
  - open-seo-main/src/server/lib/posthog.ts
  - open-seo-main/src/server/lib/dataforseo.ts
  - open-seo-main/src/server/lib/dataforseoLighthouse.ts
  - open-seo-main/src/serverFunctions/config.ts
  - open-seo-main/src/routes/api/auth/$.ts
  - open-seo-main/src/routes/api/autumn/$.ts
  - open-seo-main/src/middleware/ensureUser.ts
  - open-seo-main/src/middleware/errorHandling.ts
  - open-seo-main/src/middleware/ensure-user/cloudflareAccess.ts
  - open-seo-main/src/server/lib/dataforseoClient.test.ts
autonomous: true
requirements:
  - CF-01
  - CF-04
  - DB-05
must_haves:
  truths:
    - "Zero `cloudflare:workers` imports remain in any app-lib, routes, middleware, or email/posthog/dataforseo file"
    - "better-auth drizzleAdapter uses provider: \"pg\""
    - "Every env.* read is replaced with process.env.* or a runtime-env helper"
    - "`waitUntil` is replaced with a Node-compatible fire-and-forget pattern"
  artifacts:
    - path: "open-seo-main/src/lib/auth.ts"
      provides: "better-auth configured with drizzleAdapter provider: pg; BETTER_AUTH_URL / BETTER_AUTH_SECRET from process.env"
      contains: "provider: \"pg\""
    - path: "open-seo-main/src/server/lib/posthog.ts"
      provides: "PostHog client with Node.js-friendly flush (flushAt: 20, flushInterval: 10_000)"
      contains: "process.env.POSTHOG_PUBLIC_KEY"
    - path: "open-seo-main/src/server/email/loops.ts"
      provides: "Loops transactional email helpers reading env via getRequiredEnvValue"
      contains: "getRequiredEnvValue"
  key_links:
    - from: "src/lib/auth.ts"
      to: "better-auth drizzleAdapter"
      via: "provider: \"pg\""
      pattern: "provider:\\s*\"pg\""
    - from: "src/middleware/errorHandling.ts"
      to: "captureServerError"
      via: "queueMicrotask-based fire-and-forget (Node replacement for waitUntil)"
      pattern: "queueMicrotask|void\\s+captureServerError"
---

<objective>
Remove every `cloudflare:workers` import from the application libraries, middleware, route handlers, and tests. Replace `env.*` reads with `process.env.*` or the `runtime-env` helpers introduced in Plan 03. Switch better-auth to the `pg` provider. Replace the `waitUntil` Worker API with a Node.js-compatible fire-and-forget helper.

Purpose: These are the direct consumers of CF Worker bindings. Without cleaning these up, TypeScript fails (the `cloudflare:workers` module no longer exists) and routes crash at runtime.
Output: 12 files with zero CF imports; same external behavior preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cf-bindings-removal-schema-migration/02-CONTEXT.md
@open-seo-main/CLAUDE.md

<interfaces>
<!-- runtime-env helpers available from Plan 03 (src/server/lib/runtime-env.ts) -->
export async function getRequiredEnvValue(name: string): Promise<string>
export async function getOptionalEnvValue(name: string): Promise<string | undefined>
export async function isHostedServerAuthMode(): Promise<boolean>
export function validateEnv(required: readonly string[]): void
export const REQUIRED_ENV_HOSTED: readonly string[]

<!-- better-auth drizzleAdapter provider change (CLAUDE.md §2 better-auth adapter) -->
drizzleAdapter(db, { provider: "pg" })   // was "sqlite"

<!-- Node.js replacement for CF waitUntil (fire-and-forget promise) -->
// Pattern used below for errorHandling.ts and serverFunctions/audit.ts:
function runDetached(promise: Promise<unknown>): void {
  promise.catch((err) => {
    console.error("detached task failed:", err);
  });
}
// or simply: `void captureServerError(...)` — both discard the promise.

<!-- Current CF env references to migrate (sample) -->
env.BETTER_AUTH_URL           → process.env.BETTER_AUTH_URL
env.BETTER_AUTH_SECRET        → process.env.BETTER_AUTH_SECRET
env.DATAFORSEO_API_KEY        → process.env.DATAFORSEO_API_KEY
env.POSTHOG_PUBLIC_KEY        → process.env.POSTHOG_PUBLIC_KEY
env.POSTHOG_HOST              → process.env.POSTHOG_HOST
env.AUTH_MODE                 → process.env.AUTH_MODE
env.TEAM_DOMAIN               → process.env.TEAM_DOMAIN
env.POLICY_AUD                → process.env.POLICY_AUD
env.LOOPS_API_KEY             → await getRequiredEnvValue("LOOPS_API_KEY")
env.LOOPS_TRANSACTIONAL_*     → await getRequiredEnvValue("LOOPS_TRANSACTIONAL_*")
Reflect.get(env, name)        → process.env[name]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove cloudflare:workers from auth, email, posthog, dataforseo, config — swap better-auth adapter to pg</name>
  <files>open-seo-main/src/lib/auth.ts, open-seo-main/src/server/email/loops.ts, open-seo-main/src/server/lib/posthog.ts, open-seo-main/src/server/lib/dataforseo.ts, open-seo-main/src/server/lib/dataforseoLighthouse.ts, open-seo-main/src/serverFunctions/config.ts</files>
  <read_first>
    - open-seo-main/src/lib/auth.ts (imports env from cloudflare:workers, uses env.BETTER_AUTH_URL / BETTER_AUTH_SECRET / LOOPS_* via Reflect.get, sets drizzleAdapter(db, { provider: "sqlite" }))
    - open-seo-main/src/server/email/loops.ts (imports env from cloudflare:workers, uses Reflect.get(env, name))
    - open-seo-main/src/server/lib/posthog.ts (imports env from cloudflare:workers, reads POSTHOG_PUBLIC_KEY / POSTHOG_HOST; flushAt: 1 was tuned for Workers and hurts Node throughput)
    - open-seo-main/src/server/lib/dataforseo.ts (imports env, uses env.DATAFORSEO_API_KEY)
    - open-seo-main/src/server/lib/dataforseoLighthouse.ts (imports env, uses env.DATAFORSEO_API_KEY)
    - open-seo-main/src/serverFunctions/config.ts (imports env, uses env.DATAFORSEO_API_KEY)
    - open-seo-main/src/server/lib/runtime-env.ts (AFTER Plan 03 — source of getRequiredEnvValue)
    - open-seo-main/CLAUDE.md (§2 better-auth adapter change)
  </read_first>
  <action>
    Make the following changes across 6 files:

    **1a. `src/lib/auth.ts`:**
    - Delete `import { env } from "cloudflare:workers";`.
    - Replace `env.BETTER_AUTH_URL?.trim()` → `process.env.BETTER_AUTH_URL?.trim()`.
    - Replace `env.BETTER_AUTH_SECRET?.trim()` → `process.env.BETTER_AUTH_SECRET?.trim()`.
    - Replace the `hasHostedAuthEmailConfig` body that does `Reflect.get(env, name)` with `process.env[name]` read:
      ```ts
      return loopsVars.every((name) => {
        const value = process.env[name];
        return typeof value === "string" && value.trim() !== "";
      });
      ```
    - Change `drizzleAdapter(db, { provider: "sqlite" })` → `drizzleAdapter(db, { provider: "pg" })`.
    - Every other line stays identical.

    **1b. `src/server/email/loops.ts`:**
    - Delete `import { env } from "cloudflare:workers";`.
    - Replace the `getRequiredEnv(name)` helper with a call into runtime-env:
      ```ts
      import { getRequiredEnvValue } from "@/server/lib/runtime-env";

      async function getHostedAuthEmailConfig() {
        return {
          apiKey: await getRequiredEnvValue("LOOPS_API_KEY"),
          verificationTemplateId: await getRequiredEnvValue("LOOPS_TRANSACTIONAL_VERIFY_EMAIL_ID"),
          passwordResetTemplateId: await getRequiredEnvValue("LOOPS_TRANSACTIONAL_RESET_PASSWORD_ID"),
        };
      }
      ```
    - `getHostedAuthEmailConfig()` becomes `async`. Update its two callers in this file (`sendHostedVerificationEmail`, `sendHostedPasswordResetEmail`) to `await` the call: `const config = await getHostedAuthEmailConfig();`.
    - Remove the local `getRequiredEnv` function and the `env` import.

    **1c. `src/server/lib/posthog.ts`:**
    - Delete `import { env } from "cloudflare:workers";`.
    - Replace `env.POSTHOG_PUBLIC_KEY?.trim()` → `process.env.POSTHOG_PUBLIC_KEY?.trim()`.
    - Replace `env.POSTHOG_HOST?.trim()` → `process.env.POSTHOG_HOST?.trim()`.
    - Retune for Node.js server lifetime: change the `new PostHog(apiKey, { ... })` options from `{ host, flushAt: 1, flushInterval: 0 }` to `{ host, flushAt: 20, flushInterval: 10_000 }`. Rationale: on Workers the process ends after every request, so flushAt:1 is needed; on Node the process is long-lived, so we batch. Update the doc comment to "Returns a one-shot PostHog client, or null if the key is missing. Caller must shut down after use." — keep comment but drop the "runs on Cloudflare Workers" line.

    **1d. `src/server/lib/dataforseo.ts`:**
    - Delete `import { env } from "cloudflare:workers";`.
    - Replace `env.DATAFORSEO_API_KEY` (single use inside `createAuthenticatedFetch`) with `process.env.DATAFORSEO_API_KEY ?? ""`.

    **1e. `src/server/lib/dataforseoLighthouse.ts`:**
    - Delete `import { env } from "cloudflare:workers";`.
    - Replace ``Basic ${env.DATAFORSEO_API_KEY?.trim() ?? ""}`` → ``Basic ${process.env.DATAFORSEO_API_KEY?.trim() ?? ""}``.

    **1f. `src/serverFunctions/config.ts`:**
    - Delete `import { env } from "cloudflare:workers";`.
    - Replace `Boolean(env.DATAFORSEO_API_KEY?.trim())` → `Boolean(process.env.DATAFORSEO_API_KEY?.trim())`.
  </action>
  <verify>
    <automated>! grep -q 'cloudflare:workers' open-seo-main/src/lib/auth.ts open-seo-main/src/server/email/loops.ts open-seo-main/src/server/lib/posthog.ts open-seo-main/src/server/lib/dataforseo.ts open-seo-main/src/server/lib/dataforseoLighthouse.ts open-seo-main/src/serverFunctions/config.ts && grep -q 'provider: "pg"' open-seo-main/src/lib/auth.ts && ! grep -q 'provider: "sqlite"' open-seo-main/src/lib/auth.ts && grep -q 'process.env.BETTER_AUTH_URL' open-seo-main/src/lib/auth.ts && grep -q 'process.env.BETTER_AUTH_SECRET' open-seo-main/src/lib/auth.ts && grep -q 'getRequiredEnvValue' open-seo-main/src/server/email/loops.ts && grep -q 'process.env.POSTHOG_PUBLIC_KEY' open-seo-main/src/server/lib/posthog.ts && grep -q 'flushAt: 20' open-seo-main/src/server/lib/posthog.ts && grep -q 'process.env.DATAFORSEO_API_KEY' open-seo-main/src/server/lib/dataforseo.ts && grep -q 'process.env.DATAFORSEO_API_KEY' open-seo-main/src/server/lib/dataforseoLighthouse.ts && grep -q 'process.env.DATAFORSEO_API_KEY' open-seo-main/src/serverFunctions/config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cloudflare:workers' open-seo-main/src/lib/auth.ts` returns 0
    - `grep -c 'cloudflare:workers' open-seo-main/src/server/email/loops.ts` returns 0
    - `grep -c 'cloudflare:workers' open-seo-main/src/server/lib/posthog.ts` returns 0
    - `grep -c 'cloudflare:workers' open-seo-main/src/server/lib/dataforseo.ts` returns 0
    - `grep -c 'cloudflare:workers' open-seo-main/src/server/lib/dataforseoLighthouse.ts` returns 0
    - `grep -c 'cloudflare:workers' open-seo-main/src/serverFunctions/config.ts` returns 0
    - `grep -c 'provider: "pg"' open-seo-main/src/lib/auth.ts` returns 1
    - `grep -c 'provider: "sqlite"' open-seo-main/src/lib/auth.ts` returns 0
    - `grep -c 'process.env.BETTER_AUTH_URL' open-seo-main/src/lib/auth.ts` returns 1
    - `grep -c 'process.env.BETTER_AUTH_SECRET' open-seo-main/src/lib/auth.ts` returns 1
    - `grep -c 'Reflect.get' open-seo-main/src/lib/auth.ts` returns 0 (replaced with process.env[name])
    - `grep -c 'getRequiredEnvValue' open-seo-main/src/server/email/loops.ts` returns at least 1 (import) + 3 usages → 4 total occurrences
    - `grep -c 'Reflect.get' open-seo-main/src/server/email/loops.ts` returns 0
    - `grep -c 'flushAt: 20' open-seo-main/src/server/lib/posthog.ts` returns 1
    - `grep -c 'flushInterval: 10_000\|flushInterval: 10000' open-seo-main/src/server/lib/posthog.ts` returns 1
    - `grep -c 'process.env.POSTHOG_PUBLIC_KEY\|process.env.POSTHOG_HOST' open-seo-main/src/server/lib/posthog.ts` returns 2
    - `grep -c 'process.env.DATAFORSEO_API_KEY' open-seo-main/src/server/lib/dataforseo.ts` returns 1
    - `grep -c 'process.env.DATAFORSEO_API_KEY' open-seo-main/src/server/lib/dataforseoLighthouse.ts` returns 1
    - `grep -c 'process.env.DATAFORSEO_API_KEY' open-seo-main/src/serverFunctions/config.ts` returns 1
  </acceptance_criteria>
  <done>All 6 files free of `cloudflare:workers`; better-auth on `provider: "pg"`; PostHog flush retuned for Node; Loops uses runtime-env helper.</done>
</task>

<task type="auto">
  <name>Task 2: Remove cloudflare:workers from routes, middleware, and the vitest mock</name>
  <files>open-seo-main/src/routes/api/auth/$.ts, open-seo-main/src/routes/api/autumn/$.ts, open-seo-main/src/middleware/ensureUser.ts, open-seo-main/src/middleware/errorHandling.ts, open-seo-main/src/middleware/ensure-user/cloudflareAccess.ts, open-seo-main/src/server/lib/dataforseoClient.test.ts</files>
  <read_first>
    - open-seo-main/src/routes/api/auth/$.ts (uses env.AUTH_MODE from cloudflare:workers)
    - open-seo-main/src/routes/api/autumn/$.ts (uses env.AUTH_MODE)
    - open-seo-main/src/middleware/ensureUser.ts (imports env from cloudflare:workers, reads env.AUTH_MODE)
    - open-seo-main/src/middleware/errorHandling.ts (imports waitUntil from cloudflare:workers)
    - open-seo-main/src/middleware/ensure-user/cloudflareAccess.ts (imports env from cloudflare:workers, reads env.TEAM_DOMAIN + env.POLICY_AUD)
    - open-seo-main/src/server/lib/dataforseoClient.test.ts (has `vi.mock("cloudflare:workers", ...)` shim — this must be removed since the module no longer exists)
  </read_first>
  <action>
    **2a. `src/routes/api/auth/$.ts`:**
    - Delete `import { env } from "cloudflare:workers";`.
    - Replace `isHostedAuthMode(env.AUTH_MODE)` with `isHostedAuthMode(process.env.AUTH_MODE)`.

    **2b. `src/routes/api/autumn/$.ts`:**
    - Delete `import { env } from "cloudflare:workers";`.
    - Replace `isHostedAuthMode(env.AUTH_MODE)` with `isHostedAuthMode(process.env.AUTH_MODE)`.

    **2c. `src/middleware/ensureUser.ts`:**
    - Delete `import { env } from "cloudflare:workers";`.
    - Replace `getAuthMode(env.AUTH_MODE)` with `getAuthMode(process.env.AUTH_MODE)`.

    **2d. `src/middleware/errorHandling.ts`:**
    - Delete `import { waitUntil } from "cloudflare:workers";`.
    - Replace the `waitUntil(captureServerError(error, {...}))` call with a Node-compatible fire-and-forget. Use:
      ```ts
      void captureServerError(error, {
        errorCode: appError?.code ?? "INTERNAL_ERROR",
        method: request.method,
        path: url.pathname,
      }).catch((err) => {
        console.error("posthog captureServerError failed:", err);
      });
      ```
      (`void` discards the promise; `.catch` keeps unhandled-rejection noise out of stderr.)

    **2e. `src/middleware/ensure-user/cloudflareAccess.ts`:**
    - Delete `import { env } from "cloudflare:workers";`.
    - Replace `env.TEAM_DOMAIN` → `process.env.TEAM_DOMAIN` (two occurrences — one in the conditional, one in the argument to `getValidatedTeamDomain`).
    - Replace `env.POLICY_AUD?.trim()` → `process.env.POLICY_AUD?.trim()`.
    - Do NOT rename the file or change any of the JWKS/jose verification logic — it's already runtime-agnostic.

    **2f. `src/server/lib/dataforseoClient.test.ts`:**
    - Remove the `vi.mock("cloudflare:workers", () => ({ waitUntil: vi.fn() }));` block. The cloudflare:workers module does not exist in Node.js — attempting to `vi.mock` a missing module errors out. If any assertion in this test relies on the mocked `waitUntil`, replace the expectation with a `queueMicrotask`/`setImmediate` flush or a direct spy on the real function. (Grep the file for `waitUntil` — if there are no other references beyond the `vi.mock` call, simply delete the `vi.mock(...)` line.)
  </action>
  <verify>
    <automated>! grep -q 'cloudflare:workers' open-seo-main/src/routes/api/auth/$.ts open-seo-main/src/routes/api/autumn/$.ts open-seo-main/src/middleware/ensureUser.ts open-seo-main/src/middleware/errorHandling.ts open-seo-main/src/middleware/ensure-user/cloudflareAccess.ts open-seo-main/src/server/lib/dataforseoClient.test.ts && grep -q 'process.env.AUTH_MODE' open-seo-main/src/routes/api/auth/$.ts && grep -q 'process.env.AUTH_MODE' open-seo-main/src/routes/api/autumn/$.ts && grep -q 'process.env.AUTH_MODE' open-seo-main/src/middleware/ensureUser.ts && grep -q 'process.env.TEAM_DOMAIN' open-seo-main/src/middleware/ensure-user/cloudflareAccess.ts && grep -q 'process.env.POLICY_AUD' open-seo-main/src/middleware/ensure-user/cloudflareAccess.ts && ! grep -q 'waitUntil' open-seo-main/src/middleware/errorHandling.ts && grep -q 'void captureServerError' open-seo-main/src/middleware/errorHandling.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'cloudflare:workers' open-seo-main/src/routes/api/auth/$.ts` returns 0
    - `grep -rc 'cloudflare:workers' open-seo-main/src/routes/api/autumn/$.ts` returns 0
    - `grep -rc 'cloudflare:workers' open-seo-main/src/middleware/ensureUser.ts` returns 0
    - `grep -rc 'cloudflare:workers' open-seo-main/src/middleware/errorHandling.ts` returns 0
    - `grep -rc 'cloudflare:workers' open-seo-main/src/middleware/ensure-user/cloudflareAccess.ts` returns 0
    - `grep -rc 'cloudflare:workers' open-seo-main/src/server/lib/dataforseoClient.test.ts` returns 0
    - `grep -c 'process.env.AUTH_MODE' open-seo-main/src/routes/api/auth/$.ts` returns 1
    - `grep -c 'process.env.AUTH_MODE' open-seo-main/src/routes/api/autumn/$.ts` returns 1
    - `grep -c 'process.env.AUTH_MODE' open-seo-main/src/middleware/ensureUser.ts` returns 1
    - `grep -c 'process.env.TEAM_DOMAIN' open-seo-main/src/middleware/ensure-user/cloudflareAccess.ts` returns 2 (used in condition + passed to helper)
    - `grep -c 'process.env.POLICY_AUD' open-seo-main/src/middleware/ensure-user/cloudflareAccess.ts` returns 1
    - `grep -c 'waitUntil' open-seo-main/src/middleware/errorHandling.ts` returns 0
    - `grep -c 'void captureServerError' open-seo-main/src/middleware/errorHandling.ts` returns 1
    - `grep -c 'vi.mock("cloudflare:workers"' open-seo-main/src/server/lib/dataforseoClient.test.ts` returns 0
  </acceptance_criteria>
  <done>All 6 route/middleware/test files have zero CF Worker imports; auth-mode, team domain, policy aud all come from process.env; errorHandling uses a Node-friendly fire-and-forget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (/api/auth, /api/autumn) | Untrusted HTTP requests hit route handlers; AUTH_MODE gates hosted-mode behavior |
| process env → app | Env is the source of secrets (DATABASE_URL, BETTER_AUTH_SECRET, DATAFORSEO_API_KEY, LOOPS_*) |
| DataForSEO API → app | External HTTP responses parsed by zod before use (already present) |
| Loops API → app | Bearer-token auth outbound; responses not deserialized into DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Info Disclosure) | runtime-env error messages | mitigate | `getRequiredEnvValue` throws with the env VAR NAME but never its value; error string is `"Missing required environment variable: <NAME>"` — no secret leak. |
| T-02-02 | T (Tampering) | BETTER_AUTH_SECRET | mitigate | `getHostedSecret()` in src/lib/auth.ts validates `length >= 32` — existing check retained; additionally `validateEnv(REQUIRED_ENV_HOSTED)` at startup fails fast. |
| T-02-03 | S (Spoofing) | api/auth/$.ts, api/autumn/$.ts | accept | `isHostedAuthMode(process.env.AUTH_MODE)` still returns 404 when not hosted — behavior identical to CF binding read. |
| T-02-04 | R (Repudiation) | errorHandling.ts PostHog capture | mitigate | `void captureServerError(...).catch(console.error)` — failed telemetry never silently swallowed; logs to stderr for Docker log capture in Phase 4. |
| T-02-05 | E (Elevation) | drizzleAdapter provider change | accept | Swap from `sqlite` → `pg` is a driver/dialect change only; auth logic (`baseAuthConfig`, session hooks) is unchanged. |
| T-02-06 | I | posthog.ts key read | mitigate | Keys remain in `process.env.POSTHOG_*`; never logged; `flushAt: 20` does not buffer across requests to different users. |
</threat_model>

<verification>
- `grep -rn "cloudflare:workers" open-seo-main/src/` returns zero matches across the 12 files above (any remaining hits live in Plan 05 scope — workflows/r2/progress-kv/AuditService/audit.ts/server.ts)
- No `Reflect.get(env` patterns remain in Plan-04 files
</verification>

<success_criteria>
1. 12 files are fully cleaned of `cloudflare:workers` imports and `env.*` reads.
2. better-auth adapter uses `provider: "pg"`.
3. `waitUntil` replaced by `void ...catch` in `errorHandling.ts`.
4. PostHog client retuned for Node.js (`flushAt: 20`, `flushInterval: 10_000`).
5. Vitest test no longer mocks the non-existent `cloudflare:workers` module.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cf-bindings-removal-schema-migration/02-04-SUMMARY.md`
</output>
