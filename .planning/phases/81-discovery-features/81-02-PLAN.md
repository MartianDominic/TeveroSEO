---
phase: 81-discovery-features
plan: 02
type: execute
wave: 2
depends_on: [81-01]
files_modified:
  - open-seo-main/src/server/features/keywords/discovery/SideKeywordExpander.ts
  - open-seo-main/src/server/features/keywords/discovery/SideKeywordExpander.test.ts
  - open-seo-main/src/server/features/keywords/discovery/types.ts
autonomous: true
requirements:
  - DISCOVERY-04
  - DISCOVERY-05
status: ready

must_haves:
  truths:
    - "Problem terms from conversation expand to related keywords via DataForSEO"
    - "Discovered keywords are filtered by relevance scoring (Phase 78)"
    - "Only new keywords not in original list are returned"
  artifacts:
    - path: "open-seo-main/src/server/features/keywords/discovery/SideKeywordExpander.ts"
      provides: "Side keyword discovery via DataForSEO keyword ideas"
      exports: ["SideKeywordExpander", "discoverSideKeywords"]
    - path: "open-seo-main/src/server/features/keywords/discovery/SideKeywordExpander.test.ts"
      provides: "Unit tests for side keyword expansion"
      min_lines: 80
  key_links:
    - from: "SideKeywordExpander.ts"
      to: "dataforseo.ts"
      via: "fetchKeywordIdeasRaw import"
      pattern: "fetchKeywordIdeasRaw\\("
---

<objective>
Implement side keyword discovery via problem-to-solution expansion.

Purpose: Extract problem terms from client conversation (e.g., "plaukų slinkimas", "sausa oda") and use DataForSEO keyword ideas API to find related keywords the client didn't think to include.

Output: SideKeywordExpander service that takes conversation constraints, calls DataForSEO for each problem term, filters results by relevance, and returns new keyword opportunities.
</objective>

<context>
@.planning/phases/81-discovery-features/81-CONTEXT.md
@open-seo-main/src/server/lib/dataforseo.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add side keyword types to discovery types</name>
  <files>open-seo-main/src/server/features/keywords/discovery/types.ts</files>
  <behavior>
    - SideKeywordExpansion type has source, seedTerm, discoveredKeywords, expansionMethod
    - SideKeyword type has keyword, volume, difficulty, relevanceScore, passesFilters, discoverySource
  </behavior>
  <action>
Add to types.ts per CONTEXT.md:
- SideKeywordExpansion interface (source: 'problem'|'solution'|'related', seedTerm, discoveredKeywords[], expansionMethod)
- SideKeyword interface (keyword, volume, difficulty, relevanceScore, passesFilters, discoverySource)
- SideKeywordExpanderConfig (locationCode, languageCode, limit, relevanceThreshold)
  </action>
  <verify><automated>cd open-seo-main && npx tsc --noEmit src/server/features/keywords/discovery/types.ts</automated></verify>
  <done>Types exported and compile without errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SideKeywordExpander with DataForSEO integration</name>
  <files>
    open-seo-main/src/server/features/keywords/discovery/SideKeywordExpander.ts,
    open-seo-main/src/server/features/keywords/discovery/SideKeywordExpander.test.ts
  </files>
  <behavior>
    - Test 1: discoverSideKeywords with ["sausa oda"] returns keywords from DataForSEO
    - Test 2: Keywords already in existingKeywords set are excluded
    - Test 3: Keywords below relevanceThreshold (0.4) are filtered out
    - Test 4: Each expansion has correct source='problem' and seedTerm
    - Test 5: expansionMethod is 'dataforseo_keyword_ideas'
  </behavior>
  <action>
Implement per CONTEXT.md algorithm:

1. **discoverSideKeywords(constraints, existingKeywords)** - Main function:
   - Extract problems from constraints.businessContext.problemsSolved
   - For each problem, call fetchKeywordIdeasRaw(problem, locationCode, languageCode, limit)
   - Filter results to exclude existingKeywords
   - Score relevance (stub: use volume/difficulty ratio until Phase 78 integrated)
   - Filter by relevanceThreshold (default 0.4)
   - Return SideKeywordExpansion[] grouped by seed term

2. **SideKeywordExpander class** - Stateful expander:
   - Constructor takes SideKeywordExpanderConfig
   - expand(constraints, existingKeywords) returns Promise<SideKeywordExpansion[]>
   - Uses fetchKeywordIdeasRaw from @/server/lib/dataforseo

3. Mock DataForSEO in tests to avoid API calls.
  </action>
  <verify><automated>cd open-seo-main && npm test -- --filter=SideKeywordExpander</automated></verify>
  <done>
    - SideKeywordExpander calls DataForSEO keyword ideas API
    - Results filtered by existing keywords and relevance
    - Expansions grouped by seed term with correct metadata
  </done>
</task>

</tasks>

<verification>
```bash
cd open-seo-main && npm test -- --filter=SideKeywordExpander
cd open-seo-main && npx tsc --noEmit
```
</verification>

<success_criteria>
- Problem terms expand to keyword ideas via DataForSEO
- Existing keywords are excluded from results
- Relevance filtering removes irrelevant expansions
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/81-discovery-features/81-02-SUMMARY.md`
</output>
