---
phase: 81-discovery-features
plan: 03
type: execute
wave: 3
depends_on: [81-01, 81-02]
files_modified:
  - open-seo-main/src/server/features/keywords/discovery/ProductLinker.ts
  - open-seo-main/src/server/features/keywords/discovery/RecommendationEngine.ts
  - open-seo-main/src/server/features/keywords/discovery/RecommendationEngine.test.ts
  - open-seo-main/src/server/features/keywords/discovery/index.ts
autonomous: true
requirements:
  - DISCOVERY-06
  - DISCOVERY-07
status: ready

must_haves:
  truths:
    - "Keywords are linked to products/services with match reasons"
    - "Discovery recommendations are generated with priority and estimated impact"
    - "DiscoveryResult aggregates pSEO, side keywords, linkages, and recommendations"
  artifacts:
    - path: "open-seo-main/src/server/features/keywords/discovery/ProductLinker.ts"
      provides: "Keyword to product/service matching"
      exports: ["ProductLinker", "linkKeywordsToProducts"]
    - path: "open-seo-main/src/server/features/keywords/discovery/RecommendationEngine.ts"
      provides: "Discovery recommendations with priorities"
      exports: ["RecommendationEngine", "generateDiscoveryResult"]
  key_links:
    - from: "RecommendationEngine.ts"
      to: "PSEODetector.ts"
      via: "PSEOCluster import"
      pattern: "import.*PSEOCluster"
---

<objective>
Complete the discovery pipeline with product linkage and actionable recommendations.

Purpose: Link discovered keywords to client products/services, then generate prioritized recommendations (pSEO templates, content gaps, product pages) with estimated impact.

Output: ProductLinker for keyword-product matching, RecommendationEngine for generating DiscoveryResult with all discovery outputs combined.
</objective>

<context>
@.planning/phases/81-discovery-features/81-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ProductLinker for keyword-product matching</name>
  <files>
    open-seo-main/src/server/features/keywords/discovery/ProductLinker.ts,
    open-seo-main/src/server/features/keywords/discovery/ProductLinker.test.ts
  </files>
  <behavior>
    - Test 1: "serumas sausai odai" matches product "Hialurono serumas" (direct_match + solves_problem)
    - Test 2: "kaip sustabdyti plauku slinkima" matches via problem only (solves_problem)
    - Test 3: Keyword with no matches has linkageConfidence 0.3 and generic landing page
    - Test 4: suggestedLandingPage uses product category when matched
  </behavior>
  <action>
Implement per CONTEXT.md:

1. **linkKeywordsToProducts(keywords, products)** - Main function:
   - For each keyword, find matches by:
     - Direct product name match (matchReason: 'direct_match')
     - Problem-solution match (matchReason: 'solves_problem')
     - Category match (matchReason: 'category_match')
   - Return ProductLinkage[] with linkedProducts, linkageConfidence, suggestedLandingPage

2. **Product interface** - Input type:
   - name, category, solvedProblems[]

3. **ProductLinkage interface** - Output type (add to types.ts):
   - keyword, linkedProducts[], linkageConfidence, suggestedLandingPage
  </action>
  <verify><automated>cd open-seo-main && npm test -- --filter=ProductLinker</automated></verify>
  <done>Product linkage works with 80%+ accuracy for direct matches</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RecommendationEngine and DiscoveryResult</name>
  <files>
    open-seo-main/src/server/features/keywords/discovery/RecommendationEngine.ts,
    open-seo-main/src/server/features/keywords/discovery/RecommendationEngine.test.ts,
    open-seo-main/src/server/features/keywords/discovery/types.ts
  </files>
  <behavior>
    - Test 1: PSEOCluster with opportunityScore > 0.7 generates 'high' priority recommendation
    - Test 2: Recommendation type is 'pseo_template' for pSEO clusters
    - Test 3: DiscoveryResult.metadata has correct totals (totalPSEOPages, totalSideKeywords, linkageRate)
    - Test 4: Recommendations sorted by priority (high > medium > low)
  </behavior>
  <action>
Implement per CONTEXT.md:

1. **Add types to types.ts**:
   - DiscoveryResult (pseoOpportunities, sideKeywords, productLinkages, recommendations, metadata)
   - DiscoveryRecommendation (type, priority, title, description, keywords, estimatedImpact)

2. **generateDiscoveryResult(pseo, sideKeywords, linkages)** - Aggregator:
   - Combine all discovery outputs
   - Generate recommendations from pSEO clusters (type: 'pseo_template')
   - Generate recommendations from unlinked keywords (type: 'content_gap')
   - Compute metadata (totalPSEOPages, totalSideKeywords, linkageRate)
   - Sort recommendations by priority

3. **RecommendationEngine class** - Stateful engine:
   - generate(pseo, sideKeywords, linkages) returns DiscoveryResult
  </action>
  <verify><automated>cd open-seo-main && npm test -- --filter=RecommendationEngine</automated></verify>
  <done>DiscoveryResult aggregates all discovery outputs with prioritized recommendations</done>
</task>

<task type="auto">
  <name>Task 3: Update discovery index with all exports</name>
  <files>open-seo-main/src/server/features/keywords/discovery/index.ts</files>
  <action>
Update index.ts to export all discovery components:
- ProductLinker, linkKeywordsToProducts
- RecommendationEngine, generateDiscoveryResult
- DiscoveryResult, DiscoveryRecommendation, ProductLinkage types
  </action>
  <verify><automated>cd open-seo-main && npx tsc --noEmit src/server/features/keywords/discovery/index.ts</automated></verify>
  <done>All discovery exports available from module index</done>
</task>

</tasks>

<verification>
```bash
cd open-seo-main && npm test -- --filter=discovery
cd open-seo-main && npx tsc --noEmit
```
</verification>

<success_criteria>
- ProductLinker matches keywords to products with direct/problem/category reasons
- RecommendationEngine generates prioritized recommendations
- DiscoveryResult contains all discovery outputs with metadata
- All Phase 81 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/81-discovery-features/81-03-SUMMARY.md`
</output>
