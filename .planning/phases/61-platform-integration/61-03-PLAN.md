---
phase: 61-platform-integration
plan: 03
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.ts
  - open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.test.ts
  - open-seo-main/src/server/features/platform-oauth/providers/WixOAuthProvider.ts
  - open-seo-main/src/server/features/platform-oauth/providers/WixOAuthProvider.test.ts
  - open-seo-main/src/server/features/platform-oauth/services/ShopifyService.ts
  - open-seo-main/src/server/features/platform-oauth/services/WixService.ts
  - apps/web/src/app/api/oauth/shopify/authorize/route.ts
  - apps/web/src/app/api/oauth/shopify/callback/route.ts
  - apps/web/src/app/api/oauth/wix/authorize/route.ts
  - apps/web/src/app/api/oauth/wix/callback/route.ts
autonomous: true
requirements:
  - PLAT-06
  - PLAT-07

must_haves:
  truths:
    - "Shopify OAuth uses shop-specific authorization URL"
    - "Shopify access tokens do not expire (stored without expiry)"
    - "Wix OAuth uses standard OAuth 2.0 flow"
    - "Both providers store encrypted tokens in platform_connections"
  artifacts:
    - path: "open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.ts"
      provides: "Shopify OAuth implementation"
      exports: ["ShopifyOAuthProvider", "SHOPIFY_SCOPES"]
    - path: "open-seo-main/src/server/features/platform-oauth/providers/WixOAuthProvider.ts"
      provides: "Wix OAuth implementation"
      exports: ["WixOAuthProvider", "WIX_SCOPES"]
    - path: "apps/web/src/app/api/oauth/shopify/callback/route.ts"
      provides: "Shopify OAuth callback"
      exports: ["GET"]
  key_links:
    - from: "ShopifyOAuthProvider"
      to: "Shopify Admin API"
      via: "https://{shop}/admin/oauth/authorize"
      pattern: "shop.*myshopify.com"
---

<objective>
Implement Shopify and Wix OAuth providers for e-commerce and website builder platforms.

Purpose: Enable OAuth connections for Shopify stores (4.4% market share, 28% e-commerce) and Wix sites (2.6% market share) per D-07 and DESIGN.md Tier 1-2 requirements.

Output:
- ShopifyOAuthProvider with shop-specific URLs and permanent tokens
- WixOAuthProvider with standard OAuth 2.0 flow
- Data fetching services for products, pages, SEO metadata
- Next.js API routes for both platforms
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-platform-integration/DESIGN.md
@.planning/phases/61-platform-integration/61-CONTEXT.md
@.planning/phases/61-platform-integration/61-01-SUMMARY.md

<interfaces>
<!-- OAuthProvider interface from 61-01 -->
```typescript
export interface OAuthProvider {
  readonly name: string;
  readonly platform: OAuthPlatform;
  getAuthorizationUrl(state: string, options?: { scopes?: string[] }): string;
  exchangeCodeForTokens(code: string): Promise<TokenSet>;
  refreshAccessToken(refreshToken: string): Promise<TokenSet>;
  revokeToken(token: string): Promise<void>;
}

export interface TokenSet {
  accessToken: string;
  refreshToken?: string;
  expiresIn: number;
  tokenType: string;
  scope?: string;
}
```

<!-- Shopify OAuth from DESIGN.md -->
```typescript
const SHOPIFY_CONFIG = {
  scopes: [
    'read_products',
    'read_content',
    'read_themes',
    'read_online_store_pages',
    'read_publications',
  ],
};
// Authorization: https://{shop}/admin/oauth/authorize
// Token: https://{shop}/admin/oauth/access_token
// Shopify tokens do NOT expire
```

<!-- Shopify data from DESIGN.md -->
```typescript
interface ShopifyData {
  products: { id: string; title: string; handle: string; seoTitle: string; seoDescription: string; status: string; }[];
  collections: { id: string; title: string; handle: string; }[];
  pages: { id: string; title: string; handle: string; }[];
  redirects: { path: string; target: string; }[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ShopifyOAuthProvider with shop-specific URLs</name>
  <files>open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.ts, open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.test.ts</files>
  <read_first>
    - .planning/phases/61-platform-integration/DESIGN.md (lines 371-410 Shopify OAuth)
    - open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.ts (pattern from 61-02)
  </read_first>
  <behavior>
    - Test 1: getAuthorizationUrl builds https://{shop}/admin/oauth/authorize URL
    - Test 2: getAuthorizationUrl includes client_id, scope, redirect_uri, state params
    - Test 3: exchangeCodeForTokens POSTs to https://{shop}/admin/oauth/access_token
    - Test 4: TokenSet.expiresIn is Infinity for Shopify tokens
    - Test 5: Shop domain is validated (must end in .myshopify.com)
  </behavior>
  <action>
Create ShopifyOAuthProvider per D-07:

```typescript
// open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.ts

import type { OAuthProvider, TokenSet } from '../OAuthProviderBase';
import { getRequiredEnvValueSync } from '@/server/lib/runtime-env';

export const SHOPIFY_SCOPES = [
  'read_products',
  'read_content', 
  'read_themes',
  'read_online_store_pages',
  'read_publications',
] as const;

export class ShopifyOAuthProvider implements OAuthProvider {
  readonly name = 'Shopify';
  readonly platform = 'shopify' as const;
  
  private readonly clientId: string;
  private readonly clientSecret: string;
  private readonly redirectUri: string;
  private readonly shop: string;
  
  constructor(shop: string, redirectUri: string) {
    // Validate shop domain
    if (!shop.endsWith('.myshopify.com') && !shop.includes('.myshopify.com')) {
      // Allow just shop name, append .myshopify.com
      this.shop = `${shop.replace(/\.myshopify\.com$/, '')}.myshopify.com`;
    } else {
      this.shop = shop;
    }
    
    this.clientId = getRequiredEnvValueSync('SHOPIFY_CLIENT_ID');
    this.clientSecret = getRequiredEnvValueSync('SHOPIFY_CLIENT_SECRET');
    this.redirectUri = redirectUri;
  }
  
  getAuthorizationUrl(state: string): string {
    const params = new URLSearchParams({
      client_id: this.clientId,
      scope: SHOPIFY_SCOPES.join(','),
      redirect_uri: this.redirectUri,
      state,
    });
    
    return `https://${this.shop}/admin/oauth/authorize?${params.toString()}`;
  }
  
  async exchangeCodeForTokens(code: string): Promise<TokenSet> {
    const response = await fetch(`https://${this.shop}/admin/oauth/access_token`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        client_id: this.clientId,
        client_secret: this.clientSecret,
        code,
      }),
    });
    
    if (!response.ok) {
      const error = await response.text();
      throw new Error(`Shopify token exchange failed: ${error}`);
    }
    
    const data = await response.json();
    
    // Shopify tokens do NOT expire
    return {
      accessToken: data.access_token,
      expiresIn: Number.MAX_SAFE_INTEGER, // Never expires
      tokenType: 'Bearer',
      scope: data.scope,
    };
  }
  
  async refreshAccessToken(_refreshToken: string): Promise<TokenSet> {
    // Shopify tokens don't expire, so refresh is not needed
    throw new Error('Shopify tokens do not expire and cannot be refreshed');
  }
  
  async revokeToken(_token: string): Promise<void> {
    // Shopify requires uninstall webhook or Admin API to revoke
    // For now, just delete from our database
    throw new Error('Shopify token revocation requires app uninstall');
  }
}
```

Create tests mocking fetch for shop-specific URLs.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm vitest run src/server/features/platform-oauth/providers/ShopifyOAuthProvider.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class ShopifyOAuthProvider" open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.ts
    - grep -q "myshopify.com" open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.ts
    - grep -q "read_products" open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.ts
    - grep -q "MAX_SAFE_INTEGER" open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.ts
    - test -f open-seo-main/src/server/features/platform-oauth/providers/ShopifyOAuthProvider.test.ts
  </acceptance_criteria>
  <done>ShopifyOAuthProvider handles shop-specific URLs and permanent tokens</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement WixOAuthProvider with standard OAuth 2.0</name>
  <files>open-seo-main/src/server/features/platform-oauth/providers/WixOAuthProvider.ts, open-seo-main/src/server/features/platform-oauth/providers/WixOAuthProvider.test.ts</files>
  <read_first>
    - .planning/phases/61-platform-integration/DESIGN.md (Wix in Tier 2)
    - open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.ts (standard OAuth pattern)
  </read_first>
  <behavior>
    - Test 1: getAuthorizationUrl returns Wix OAuth URL with correct params
    - Test 2: exchangeCodeForTokens calls Wix token endpoint
    - Test 3: refreshAccessToken calls Wix refresh endpoint
    - Test 4: Scopes include WIX.SITE.READ and WIX.CONTACTS.READ
  </behavior>
  <action>
Create WixOAuthProvider:

```typescript
// open-seo-main/src/server/features/platform-oauth/providers/WixOAuthProvider.ts

import type { OAuthProvider, TokenSet } from '../OAuthProviderBase';
import { getRequiredEnvValueSync } from '@/server/lib/runtime-env';

export const WIX_SCOPES = [
  'WIX.SITE.READ',
  'WIX.CONTACTS.READ',
  'WIX.BLOG.READ',
] as const;

const WIX_CONFIG = {
  authorizationUrl: 'https://www.wix.com/installer/install',
  tokenUrl: 'https://www.wixapis.com/oauth/access',
  refreshUrl: 'https://www.wixapis.com/oauth/access',
};

export class WixOAuthProvider implements OAuthProvider {
  readonly name = 'Wix';
  readonly platform = 'wix' as const;
  
  private readonly clientId: string;
  private readonly clientSecret: string;
  private readonly redirectUri: string;
  
  constructor(redirectUri: string) {
    this.clientId = getRequiredEnvValueSync('WIX_CLIENT_ID');
    this.clientSecret = getRequiredEnvValueSync('WIX_CLIENT_SECRET');
    this.redirectUri = redirectUri;
  }
  
  getAuthorizationUrl(state: string): string {
    const params = new URLSearchParams({
      appId: this.clientId,
      redirectUrl: this.redirectUri,
      state,
    });
    
    return `${WIX_CONFIG.authorizationUrl}?${params.toString()}`;
  }
  
  async exchangeCodeForTokens(code: string): Promise<TokenSet> {
    const response = await fetch(WIX_CONFIG.tokenUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        grant_type: 'authorization_code',
        client_id: this.clientId,
        client_secret: this.clientSecret,
        code,
      }),
    });
    
    if (!response.ok) {
      const error = await response.text();
      throw new Error(`Wix token exchange failed: ${error}`);
    }
    
    const data = await response.json();
    return {
      accessToken: data.access_token,
      refreshToken: data.refresh_token,
      expiresIn: data.expires_in ?? 3600,
      tokenType: 'Bearer',
    };
  }
  
  async refreshAccessToken(refreshToken: string): Promise<TokenSet> {
    const response = await fetch(WIX_CONFIG.refreshUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        grant_type: 'refresh_token',
        client_id: this.clientId,
        client_secret: this.clientSecret,
        refresh_token: refreshToken,
      }),
    });
    
    if (!response.ok) {
      const error = await response.text();
      throw new Error(`Wix token refresh failed: ${error}`);
    }
    
    const data = await response.json();
    return {
      accessToken: data.access_token,
      refreshToken: data.refresh_token ?? refreshToken,
      expiresIn: data.expires_in ?? 3600,
      tokenType: 'Bearer',
    };
  }
  
  async revokeToken(_token: string): Promise<void> {
    // Wix doesn't have a revoke endpoint; just delete from our database
  }
}
```

Update providers/index.ts to export both new providers.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm vitest run src/server/features/platform-oauth/providers/WixOAuthProvider.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class WixOAuthProvider" open-seo-main/src/server/features/platform-oauth/providers/WixOAuthProvider.ts
    - grep -q "WIX.SITE.READ" open-seo-main/src/server/features/platform-oauth/providers/WixOAuthProvider.ts
    - grep -q "wixapis.com" open-seo-main/src/server/features/platform-oauth/providers/WixOAuthProvider.ts
    - test -f open-seo-main/src/server/features/platform-oauth/providers/WixOAuthProvider.test.ts
  </acceptance_criteria>
  <done>WixOAuthProvider implements standard OAuth 2.0 with refresh support</done>
</task>

<task type="auto">
  <name>Task 3: Create Shopify and Wix data services and API routes</name>
  <files>open-seo-main/src/server/features/platform-oauth/services/ShopifyService.ts, open-seo-main/src/server/features/platform-oauth/services/WixService.ts, apps/web/src/app/api/oauth/shopify/authorize/route.ts, apps/web/src/app/api/oauth/shopify/callback/route.ts, apps/web/src/app/api/oauth/wix/authorize/route.ts, apps/web/src/app/api/oauth/wix/callback/route.ts</files>
  <read_first>
    - apps/web/src/app/api/oauth/google/authorize/route.ts (pattern from 61-02)
    - apps/web/src/app/api/oauth/google/callback/route.ts (pattern from 61-02)
    - .planning/phases/61-platform-integration/DESIGN.md (Shopify data interface lines 149-175)
  </read_first>
  <action>
**ShopifyService.ts:**
```typescript
export interface ShopifyProduct {
  id: string;
  title: string;
  handle: string;
  seoTitle: string;
  seoDescription: string;
  status: string;
}

export class ShopifyService {
  private readonly shop: string;
  private readonly accessToken: string;
  
  constructor(shop: string, accessToken: string) {
    this.shop = shop;
    this.accessToken = accessToken;
  }
  
  private async graphql<T>(query: string): Promise<T> {
    const response = await fetch(`https://${this.shop}/admin/api/2024-01/graphql.json`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-Shopify-Access-Token': this.accessToken,
      },
      body: JSON.stringify({ query }),
    });
    
    if (!response.ok) {
      throw new Error(`Shopify API error: ${response.statusText}`);
    }
    
    const { data, errors } = await response.json();
    if (errors) {
      throw new Error(`Shopify GraphQL error: ${errors[0].message}`);
    }
    return data;
  }
  
  async getProducts(first: number = 50): Promise<ShopifyProduct[]> {
    // GraphQL query for products with SEO fields
  }
  
  async getCollections(): Promise<{ id: string; title: string; handle: string; }[]> {
    // GraphQL query for collections
  }
  
  async getPages(): Promise<{ id: string; title: string; handle: string; }[]> {
    // REST API for pages
  }
  
  async getRedirects(): Promise<{ path: string; target: string; }[]> {
    // REST API for redirects
  }
}
```

**WixService.ts:**
```typescript
export class WixService {
  private readonly accessToken: string;
  
  async getSiteInfo(): Promise<{ siteDisplayName: string; siteUrl: string }> {
    // Wix Sites API
  }
  
  async getPages(): Promise<{ id: string; title: string; url: string }[]> {
    // Wix Pages API
  }
  
  async getBlogPosts(): Promise<{ id: string; title: string; slug: string }[]> {
    // Wix Blog API
  }
}
```

**API routes for Shopify:**
- /api/oauth/shopify/authorize - Accept shop param, validate domain, redirect
- /api/oauth/shopify/callback - Handle Shopify callback with shop param

**API routes for Wix:**
- /api/oauth/wix/authorize - Standard OAuth redirect
- /api/oauth/wix/callback - Standard OAuth callback

All callbacks follow same pattern as Google: validate state, exchange code, encrypt tokens, store in platform_connections.
  </action>
  <verify>
    <automated>ls -la apps/web/src/app/api/oauth/*/route.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class ShopifyService" open-seo-main/src/server/features/platform-oauth/services/ShopifyService.ts
    - grep -q "class WixService" open-seo-main/src/server/features/platform-oauth/services/WixService.ts
    - test -f apps/web/src/app/api/oauth/shopify/authorize/route.ts
    - test -f apps/web/src/app/api/oauth/shopify/callback/route.ts
    - test -f apps/web/src/app/api/oauth/wix/authorize/route.ts
    - test -f apps/web/src/app/api/oauth/wix/callback/route.ts
    - grep -q "X-Shopify-Access-Token" open-seo-main/src/server/features/platform-oauth/services/ShopifyService.ts
  </acceptance_criteria>
  <done>Shopify and Wix data services and API routes created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shop input | Shopify shop domain must be validated |
| Wix callback | State parameter required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07 | Spoofing | Shopify shop | mitigate | Validate .myshopify.com domain format |
| T-61-08 | Information Disclosure | Shopify token | mitigate | Never log tokens, encrypt immediately |
| T-61-09 | Denial of Service | API calls | mitigate | Respect Shopify rate limits (40 req/s) |
</threat_model>

<verification>
- [ ] ShopifyOAuthProvider tests pass
- [ ] WixOAuthProvider tests pass
- [ ] Shopify API routes handle shop parameter
- [ ] Wix API routes follow standard OAuth flow
- [ ] All tokens encrypted before storage
</verification>

<success_criteria>
- ShopifyOAuthProvider handles shop-specific URLs
- Shopify tokens stored without expiry (permanent)
- WixOAuthProvider supports token refresh
- Both platforms have authorize/callback routes
- Data services can fetch products/pages from both platforms
</success_criteria>

<output>
After completion, create `.planning/phases/61-platform-integration/61-03-SUMMARY.md`
</output>
