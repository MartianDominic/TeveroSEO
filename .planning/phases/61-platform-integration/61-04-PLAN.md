---
phase: 61-platform-integration
plan: 04
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.ts
  - open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.test.ts
  - open-seo-main/src/server/features/platform-oauth/services/WordPressService.ts
  - apps/web/src/app/api/connections/wordpress/validate/route.ts
  - apps/web/src/app/api/connections/wordpress/connect/route.ts
  - open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.ts
  - open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.test.ts
autonomous: true
requirements:
  - PLAT-08
  - PLAT-09

must_haves:
  truths:
    - "WordPress Application Passwords validate via /wp-json/wp/v2/users/me"
    - "Credentials stored as credentialType: 'app_password' with encrypted JSON"
    - "PlatformConnectionService provides unified CRUD for all connection types"
    - "WordPress data includes posts, pages, categories from REST API"
  artifacts:
    - path: "open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.ts"
      provides: "WordPress App Password validation"
      exports: ["WordPressAppPasswordProvider"]
    - path: "open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.ts"
      provides: "Unified connection management"
      exports: ["PlatformConnectionService", "platformConnectionService"]
    - path: "apps/web/src/app/api/connections/wordpress/validate/route.ts"
      provides: "WordPress credential validation endpoint"
      exports: ["POST"]
  key_links:
    - from: "WordPressAppPasswordProvider"
      to: "WordPress REST API"
      via: "Basic auth header"
      pattern: "Authorization.*Basic"
---

<objective>
Implement WordPress Application Passwords for self-hosted WordPress sites (38% market share) and create unified PlatformConnectionService.

Purpose: Enable non-OAuth connection for WordPress.org sites per D-13, D-14, D-15 using Application Passwords (WP 5.6+) instead of OAuth.

Output:
- WordPressAppPasswordProvider for credential validation
- WordPressService for fetching posts, pages, SEO data
- API routes for validate and connect flows
- PlatformConnectionService unifying OAuth and non-OAuth connections
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-platform-integration/DESIGN.md
@.planning/phases/61-platform-integration/61-CONTEXT.md
@.planning/phases/61-platform-integration/61-01-SUMMARY.md

<interfaces>
<!-- Existing WordPress adapter from Phase 31 -->
From open-seo-main/src/server/features/connections/adapters/WordPressAdapter.ts:
```typescript
export class WordPressAdapter implements PlatformAdapter {
  constructor(config: { siteUrl: string; username: string; appPassword: string });
  async verifyConnection(): Promise<CapabilityResult>;
  async getPosts(): Promise<any[]>;
  async getPages(): Promise<any[]>;
}
```

<!-- WordPress data from DESIGN.md -->
```typescript
interface WordPressData {
  posts: {
    id: number;
    title: string;
    slug: string;
    status: string;
    seoTitle?: string;
    seoDescription?: string;
    focusKeyword?: string;
  }[];
  pages: {
    id: number;
    title: string;
    slug: string;
    template: string;
  }[];
  categories: { id: number; name: string; count: number }[];
  tags: { id: number; name: string; count: number }[];
}
```

<!-- Platform connections schema from 61-01 -->
```typescript
export const platformConnections = pgTable("platform_connections", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull(),
  prospectId: text("prospect_id"),
  platform: text("platform").notNull(),
  credentialType: text("credential_type"), // 'oauth' | 'app_password' | 'api_key'
  credentialsEncrypted: text("credentials_encrypted"), // For non-OAuth
  status: text("status").notNull().default('pending'),
  // ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement WordPressAppPasswordProvider for credential validation</name>
  <files>open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.ts, open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.test.ts</files>
  <read_first>
    - .planning/phases/61-platform-integration/DESIGN.md (lines 417-439 WordPress App Passwords)
    - open-seo-main/src/server/features/connections/adapters/WordPressAdapter.ts (existing implementation)
  </read_first>
  <behavior>
    - Test 1: validateCredentials returns true for valid username/appPassword
    - Test 2: validateCredentials returns false for invalid credentials (401 response)
    - Test 3: validateCredentials handles network errors gracefully
    - Test 4: buildAuthHeader creates correct Basic auth base64 encoding
    - Test 5: getSiteInfo returns site title and description
  </behavior>
  <action>
Create WordPressAppPasswordProvider per D-13, D-14:

```typescript
// open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.ts

export interface WordPressCredentials {
  username: string;
  appPassword: string;
}

export interface WordPressUserInfo {
  id: number;
  name: string;
  slug: string;
  roles: string[];
}

export interface WordPressSiteInfo {
  name: string;
  description: string;
  url: string;
  home: string;
}

export class WordPressAppPasswordProvider {
  private readonly siteUrl: string;
  
  constructor(siteUrl: string) {
    // Normalize site URL (remove trailing slash)
    this.siteUrl = siteUrl.replace(/\/$/, '');
  }
  
  /**
   * Build Basic auth header from credentials.
   * WordPress Application Passwords use standard HTTP Basic Auth.
   */
  private buildAuthHeader(credentials: WordPressCredentials): string {
    const encoded = Buffer.from(
      `${credentials.username}:${credentials.appPassword}`
    ).toString('base64');
    return `Basic ${encoded}`;
  }
  
  /**
   * Validate credentials by calling /wp-json/wp/v2/users/me
   * per D-14.
   */
  async validateCredentials(credentials: WordPressCredentials): Promise<{
    valid: boolean;
    user?: WordPressUserInfo;
    error?: string;
  }> {
    try {
      const response = await fetch(
        `${this.siteUrl}/wp-json/wp/v2/users/me`,
        {
          headers: {
            Authorization: this.buildAuthHeader(credentials),
          },
        }
      );
      
      if (response.status === 401) {
        return { valid: false, error: 'Invalid username or application password' };
      }
      
      if (response.status === 403) {
        return { valid: false, error: 'Insufficient permissions' };
      }
      
      if (!response.ok) {
        return { valid: false, error: `HTTP ${response.status}: ${response.statusText}` };
      }
      
      const user = await response.json();
      return {
        valid: true,
        user: {
          id: user.id,
          name: user.name,
          slug: user.slug,
          roles: user.roles ?? [],
        },
      };
    } catch (error) {
      // Network error or invalid URL
      return {
        valid: false,
        error: error instanceof Error ? error.message : 'Connection failed',
      };
    }
  }
  
  /**
   * Get site info (no auth required for public endpoint).
   */
  async getSiteInfo(): Promise<WordPressSiteInfo | null> {
    try {
      const response = await fetch(`${this.siteUrl}/wp-json`);
      if (!response.ok) return null;
      
      const data = await response.json();
      return {
        name: data.name,
        description: data.description,
        url: data.url,
        home: data.home,
      };
    } catch {
      return null;
    }
  }
  
  /**
   * Check if site has WordPress REST API available.
   */
  async isWordPressSite(): Promise<boolean> {
    try {
      const response = await fetch(`${this.siteUrl}/wp-json`, {
        method: 'HEAD',
      });
      return response.ok;
    } catch {
      return false;
    }
  }
}
```

Create comprehensive tests with mocked fetch.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm vitest run src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class WordPressAppPasswordProvider" open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.ts
    - grep -q "validateCredentials" open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.ts
    - grep -q "wp-json/wp/v2/users/me" open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.ts
    - grep -q "Basic" open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.ts
    - test -f open-seo-main/src/server/features/platform-oauth/providers/WordPressAppPasswordProvider.test.ts
  </acceptance_criteria>
  <done>WordPressAppPasswordProvider validates credentials via REST API</done>
</task>

<task type="auto">
  <name>Task 2: Create WordPress data service and API routes</name>
  <files>open-seo-main/src/server/features/platform-oauth/services/WordPressService.ts, apps/web/src/app/api/connections/wordpress/validate/route.ts, apps/web/src/app/api/connections/wordpress/connect/route.ts</files>
  <read_first>
    - open-seo-main/src/server/features/connections/adapters/WordPressAdapter.ts (existing data fetching)
    - .planning/phases/61-platform-integration/DESIGN.md (WordPress data interface)
  </read_first>
  <action>
**WordPressService.ts:**
```typescript
export interface WPPost {
  id: number;
  title: string;
  slug: string;
  status: string;
  seoTitle?: string;
  seoDescription?: string;
  focusKeyword?: string;
}

export class WordPressService {
  private readonly siteUrl: string;
  private readonly authHeader: string;
  
  constructor(siteUrl: string, credentials: { username: string; appPassword: string }) {
    this.siteUrl = siteUrl.replace(/\/$/, '');
    this.authHeader = `Basic ${Buffer.from(`${credentials.username}:${credentials.appPassword}`).toString('base64')}`;
  }
  
  private async fetch<T>(endpoint: string): Promise<T> {
    const response = await fetch(`${this.siteUrl}/wp-json${endpoint}`, {
      headers: { Authorization: this.authHeader },
    });
    
    if (!response.ok) {
      throw new Error(`WordPress API error: ${response.statusText}`);
    }
    
    return response.json();
  }
  
  async getPosts(perPage: number = 100): Promise<WPPost[]> {
    const posts = await this.fetch<any[]>(`/wp/v2/posts?per_page=${perPage}&status=any`);
    
    return posts.map(post => ({
      id: post.id,
      title: post.title.rendered,
      slug: post.slug,
      status: post.status,
      // Yoast/RankMath SEO fields if available
      seoTitle: post.yoast_head_json?.title ?? post.rank_math_title,
      seoDescription: post.yoast_head_json?.description ?? post.rank_math_description,
      focusKeyword: post.yoast_head_json?.focuskw ?? post.rank_math_focus_keyword,
    }));
  }
  
  async getPages(perPage: number = 100): Promise<{ id: number; title: string; slug: string; template: string }[]> {
    const pages = await this.fetch<any[]>(`/wp/v2/pages?per_page=${perPage}`);
    
    return pages.map(page => ({
      id: page.id,
      title: page.title.rendered,
      slug: page.slug,
      template: page.template,
    }));
  }
  
  async getCategories(): Promise<{ id: number; name: string; count: number }[]> {
    return this.fetch('/wp/v2/categories?per_page=100');
  }
  
  async getTags(): Promise<{ id: number; name: string; count: number }[]> {
    return this.fetch('/wp/v2/tags?per_page=100');
  }
}
```

**validate/route.ts:**
```typescript
import { NextRequest, NextResponse } from 'next/server';
import { auth } from '@clerk/nextjs/server';
import { WordPressAppPasswordProvider } from '@/server/features/platform-oauth/providers';
import { z } from 'zod';

const validateSchema = z.object({
  siteUrl: z.string().url(),
  username: z.string().min(1),
  appPassword: z.string().min(1),
});

export async function POST(request: NextRequest) {
  const { userId } = auth();
  if (!userId) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  
  const body = await request.json();
  const result = validateSchema.safeParse(body);
  
  if (!result.success) {
    return NextResponse.json({ error: 'Invalid input', details: result.error }, { status: 400 });
  }
  
  const { siteUrl, username, appPassword } = result.data;
  const provider = new WordPressAppPasswordProvider(siteUrl);
  
  const validation = await provider.validateCredentials({ username, appPassword });
  
  if (!validation.valid) {
    return NextResponse.json({ valid: false, error: validation.error }, { status: 200 });
  }
  
  return NextResponse.json({
    valid: true,
    user: validation.user,
  });
}
```

**connect/route.ts:**
```typescript
// Similar to validate but stores encrypted credentials in platform_connections
// with credentialType: 'app_password'
```
  </action>
  <verify>
    <automated>ls -la apps/web/src/app/api/connections/wordpress/*/route.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class WordPressService" open-seo-main/src/server/features/platform-oauth/services/WordPressService.ts
    - grep -q "getPosts" open-seo-main/src/server/features/platform-oauth/services/WordPressService.ts
    - test -f apps/web/src/app/api/connections/wordpress/validate/route.ts
    - test -f apps/web/src/app/api/connections/wordpress/connect/route.ts
    - grep -q "app_password" apps/web/src/app/api/connections/wordpress/connect/route.ts
  </acceptance_criteria>
  <done>WordPress service and API routes created for credential validation and connection</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create unified PlatformConnectionService</name>
  <files>open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.ts, open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.test.ts</files>
  <read_first>
    - open-seo-main/src/server/features/connections/services/ConnectionService.ts (existing service pattern)
    - open-seo-main/src/db/platform-connection-schema.ts (from 61-01)
  </read_first>
  <behavior>
    - Test 1: createOAuthConnection stores encrypted OAuth tokens
    - Test 2: createAppPasswordConnection stores encrypted credential JSON
    - Test 3: getConnection returns connection without decrypted credentials
    - Test 4: getConnectionWithCredentials decrypts and returns credentials (server only)
    - Test 5: updateConnectionStatus updates status and lastError
    - Test 6: deleteConnection removes connection from database
  </behavior>
  <action>
Create PlatformConnectionService unifying OAuth and non-OAuth connections:

```typescript
// open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.ts

import { eq, and } from 'drizzle-orm';
import { nanoid } from 'nanoid';
import { db } from '@/db';
import { platformConnections, type OAuthPlatformType, type OAuthConnectionStatus } from '@/db/platform-connection-schema';
import { encryptToken, decryptToken } from './TokenEncryption';

export interface CreateOAuthConnectionInput {
  workspaceId: string;
  prospectId?: string;
  platform: OAuthPlatformType;
  accessToken: string;
  refreshToken?: string;
  expiresIn: number;
  tokenType?: string;
  scopes?: string[];
  platformAccountId?: string;
  platformAccountName?: string;
  platformSiteUrl?: string;
  connectedBy: string;
}

export interface CreateAppPasswordConnectionInput {
  workspaceId: string;
  prospectId?: string;
  platform: 'wordpress_org';
  siteUrl: string;
  credentials: {
    username: string;
    appPassword: string;
  };
  connectedBy: string;
}

export interface ConnectionWithoutCredentials {
  id: string;
  workspaceId: string;
  prospectId: string | null;
  platform: string;
  platformAccountId: string | null;
  platformAccountName: string | null;
  platformSiteUrl: string | null;
  credentialType: string | null;
  status: string;
  lastSyncAt: Date | null;
  lastSyncStatus: string | null;
  lastError: string | null;
  connectedAt: Date | null;
  hasTokens: boolean;
}

export class PlatformConnectionService {
  /**
   * Create OAuth connection with encrypted tokens.
   */
  async createOAuthConnection(input: CreateOAuthConnectionInput): Promise<string> {
    const id = nanoid();
    const now = new Date();
    const expiresAt = new Date(now.getTime() + input.expiresIn * 1000);
    
    await db.insert(platformConnections).values({
      id,
      workspaceId: input.workspaceId,
      prospectId: input.prospectId ?? null,
      platform: input.platform,
      platformAccountId: input.platformAccountId ?? null,
      platformAccountName: input.platformAccountName ?? null,
      platformSiteUrl: input.platformSiteUrl ?? null,
      accessTokenEncrypted: encryptToken(input.accessToken),
      refreshTokenEncrypted: input.refreshToken ? encryptToken(input.refreshToken) : null,
      tokenExpiresAt: expiresAt,
      tokenType: input.tokenType ?? 'Bearer',
      credentialType: 'oauth',
      status: 'active',
      scopesRequested: input.scopes ?? [],
      scopesGranted: input.scopes ?? [],
      connectedAt: now,
      connectedBy: input.connectedBy,
      createdAt: now,
      updatedAt: now,
    });
    
    return id;
  }
  
  /**
   * Create Application Password connection (WordPress).
   */
  async createAppPasswordConnection(input: CreateAppPasswordConnectionInput): Promise<string> {
    const id = nanoid();
    const now = new Date();
    
    // Store credentials as encrypted JSON per D-15
    const credentialsJson = JSON.stringify(input.credentials);
    
    await db.insert(platformConnections).values({
      id,
      workspaceId: input.workspaceId,
      prospectId: input.prospectId ?? null,
      platform: input.platform,
      platformSiteUrl: input.siteUrl,
      credentialType: 'app_password',
      credentialsEncrypted: encryptToken(credentialsJson),
      status: 'active',
      connectedAt: now,
      connectedBy: input.connectedBy,
      createdAt: now,
      updatedAt: now,
    });
    
    return id;
  }
  
  /**
   * Get connection by ID (without credentials).
   */
  async getConnection(id: string): Promise<ConnectionWithoutCredentials | null> {
    const row = await db.query.platformConnections.findFirst({
      where: eq(platformConnections.id, id),
    });
    
    if (!row) return null;
    
    return {
      id: row.id,
      workspaceId: row.workspaceId,
      prospectId: row.prospectId,
      platform: row.platform,
      platformAccountId: row.platformAccountId,
      platformAccountName: row.platformAccountName,
      platformSiteUrl: row.platformSiteUrl,
      credentialType: row.credentialType,
      status: row.status,
      lastSyncAt: row.lastSyncAt,
      lastSyncStatus: row.lastSyncStatus,
      lastError: row.lastError,
      connectedAt: row.connectedAt,
      hasTokens: !!(row.accessTokenEncrypted || row.credentialsEncrypted),
    };
  }
  
  /**
   * Get connections for workspace.
   */
  async getConnectionsForWorkspace(
    workspaceId: string,
    prospectId?: string
  ): Promise<ConnectionWithoutCredentials[]> {
    // Implementation
  }
  
  /**
   * Get decrypted OAuth tokens (server-side only).
   */
  async getOAuthTokens(id: string): Promise<{
    accessToken: string;
    refreshToken?: string;
    expiresAt: Date | null;
  } | null> {
    const row = await db.query.platformConnections.findFirst({
      where: eq(platformConnections.id, id),
    });
    
    if (!row?.accessTokenEncrypted) return null;
    
    return {
      accessToken: decryptToken(row.accessTokenEncrypted),
      refreshToken: row.refreshTokenEncrypted ? decryptToken(row.refreshTokenEncrypted) : undefined,
      expiresAt: row.tokenExpiresAt,
    };
  }
  
  /**
   * Get decrypted app password credentials (server-side only).
   */
  async getAppPasswordCredentials(id: string): Promise<{
    username: string;
    appPassword: string;
  } | null> {
    const row = await db.query.platformConnections.findFirst({
      where: eq(platformConnections.id, id),
    });
    
    if (!row?.credentialsEncrypted || row.credentialType !== 'app_password') {
      return null;
    }
    
    return JSON.parse(decryptToken(row.credentialsEncrypted));
  }
  
  /**
   * Update connection status.
   */
  async updateStatus(
    id: string,
    status: OAuthConnectionStatus,
    error?: string
  ): Promise<void> {
    await db.update(platformConnections)
      .set({
        status,
        lastError: error ?? null,
        updatedAt: new Date(),
      })
      .where(eq(platformConnections.id, id));
  }
  
  /**
   * Update tokens after refresh.
   */
  async updateTokens(
    id: string,
    accessToken: string,
    refreshToken?: string,
    expiresIn?: number
  ): Promise<void> {
    const now = new Date();
    const expiresAt = expiresIn 
      ? new Date(now.getTime() + expiresIn * 1000)
      : undefined;
    
    await db.update(platformConnections)
      .set({
        accessTokenEncrypted: encryptToken(accessToken),
        refreshTokenEncrypted: refreshToken ? encryptToken(refreshToken) : undefined,
        tokenExpiresAt: expiresAt,
        updatedAt: now,
      })
      .where(eq(platformConnections.id, id));
  }
  
  /**
   * Record successful sync.
   */
  async recordSync(id: string, status: 'success' | 'partial' | 'failed', error?: string): Promise<void> {
    await db.update(platformConnections)
      .set({
        lastSyncAt: new Date(),
        lastSyncStatus: status,
        lastError: status === 'failed' ? error : null,
        updatedAt: new Date(),
      })
      .where(eq(platformConnections.id, id));
  }
  
  /**
   * Delete connection.
   */
  async deleteConnection(id: string): Promise<void> {
    await db.delete(platformConnections).where(eq(platformConnections.id, id));
  }
}

export const platformConnectionService = new PlatformConnectionService();
```

Create comprehensive tests for all CRUD operations.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm vitest run src/server/features/platform-oauth/PlatformConnectionService.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class PlatformConnectionService" open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.ts
    - grep -q "createOAuthConnection" open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.ts
    - grep -q "createAppPasswordConnection" open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.ts
    - grep -q "getOAuthTokens" open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.ts
    - grep -q "platformConnectionService" open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.ts
    - test -f open-seo-main/src/server/features/platform-oauth/PlatformConnectionService.test.ts
  </acceptance_criteria>
  <done>PlatformConnectionService provides unified CRUD for OAuth and app password connections</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input | Site URL and credentials from user |
| WordPress API | External API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-10 | Information Disclosure | App password | mitigate | Encrypt credentials JSON before storage per D-15 |
| T-61-11 | Spoofing | Site URL | mitigate | Validate URL format, check REST API availability |
| T-61-12 | Tampering | Credential storage | mitigate | AES-256-GCM encryption |
</threat_model>

<verification>
- [ ] WordPressAppPasswordProvider tests pass
- [ ] Credential validation calls /wp-json/wp/v2/users/me
- [ ] PlatformConnectionService tests pass
- [ ] App password stored as encrypted JSON
- [ ] API routes validate input with Zod
</verification>

<success_criteria>
- WordPress credentials validate via REST API
- Credentials stored encrypted with credentialType: 'app_password'
- PlatformConnectionService handles both OAuth and app password connections
- API routes for validate and connect WordPress
- Tests pass for all components
</success_criteria>

<output>
After completion, create `.planning/phases/61-platform-integration/61-04-SUMMARY.md`
</output>
