---
phase: 61-platform-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.ts
  - open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.test.ts
  - open-seo-main/src/server/features/platform-oauth/services/GoogleSearchConsoleService.ts
  - open-seo-main/src/server/features/platform-oauth/services/GoogleAnalyticsService.ts
  - open-seo-main/src/server/features/platform-oauth/services/GoogleBusinessProfileService.ts
  - apps/web/src/app/api/oauth/google/authorize/route.ts
  - apps/web/src/app/api/oauth/google/callback/route.ts
  - open-seo-main/src/server/features/platform-oauth/providers/index.ts
autonomous: true
requirements:
  - PLAT-04
  - PLAT-05

must_haves:
  truths:
    - "Google OAuth redirects to Google consent screen with correct scopes"
    - "OAuth callback exchanges code for tokens and stores encrypted"
    - "GSC service fetches search queries, pages, index status"
    - "GA service fetches sessions, users, pageviews, traffic sources"
    - "GBP service fetches reviews and insights"
  artifacts:
    - path: "open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.ts"
      provides: "Google OAuth implementation for GSC + GA + GBP"
      exports: ["GoogleOAuthProvider", "GOOGLE_SCOPES"]
    - path: "open-seo-main/src/server/features/platform-oauth/services/GoogleSearchConsoleService.ts"
      provides: "GSC data fetching"
      exports: ["GoogleSearchConsoleService", "GSCData"]
    - path: "apps/web/src/app/api/oauth/google/authorize/route.ts"
      provides: "OAuth authorization endpoint"
      exports: ["GET"]
    - path: "apps/web/src/app/api/oauth/google/callback/route.ts"
      provides: "OAuth callback handler"
      exports: ["GET"]
  key_links:
    - from: "apps/web/src/app/api/oauth/google/callback/route.ts"
      to: "GoogleOAuthProvider.exchangeCodeForTokens"
      via: "import and method call"
      pattern: "exchangeCodeForTokens"
---

<objective>
Implement Google OAuth 2.0 provider covering Search Console, Analytics, and Business Profile with unified consent flow.

Purpose: Enable one-click Google authorization per D-05, D-06, D-08 that grants access to GSC queries, GA traffic, and GBP insights.

Output:
- GoogleOAuthProvider class implementing OAuthProvider interface
- Data fetching services for GSC, GA, GBP
- Next.js API routes for /api/oauth/google/authorize and /api/oauth/google/callback
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-platform-integration/DESIGN.md
@.planning/phases/61-platform-integration/61-CONTEXT.md

<interfaces>
<!-- From 61-01 (run in parallel, but interface defined in DESIGN.md) -->
```typescript
// OAuthProvider interface from OAuthProviderBase.ts
export interface OAuthProvider {
  readonly name: string;
  readonly platform: OAuthPlatform;
  getAuthorizationUrl(state: string, options?: { scopes?: string[] }): string;
  exchangeCodeForTokens(code: string): Promise<TokenSet>;
  refreshAccessToken(refreshToken: string): Promise<TokenSet>;
  revokeToken(token: string): Promise<void>;
}

export interface TokenSet {
  accessToken: string;
  refreshToken?: string;
  expiresIn: number;
  tokenType: string;
  scope?: string;
}
```

<!-- Google API scopes from DESIGN.md -->
```typescript
const GOOGLE_CONFIG = {
  authorizationUrl: 'https://accounts.google.com/o/oauth2/v2/auth',
  tokenUrl: 'https://oauth2.googleapis.com/token',
  scopes: {
    searchConsole: 'https://www.googleapis.com/auth/webmasters.readonly',
    analytics: 'https://www.googleapis.com/auth/analytics.readonly',
    businessProfile: 'https://www.googleapis.com/auth/business.manage',
  },
};
```

<!-- GSC data types from DESIGN.md -->
```typescript
interface GSCData {
  queries: { query: string; clicks: number; impressions: number; ctr: number; position: number; }[];
  pages: { page: string; clicks: number; impressions: number; }[];
  indexStatus: { indexed: number; notIndexed: number; errors: string[]; };
  coreWebVitals: { lcp: number; fid: number; cls: number; status: 'good' | 'needs_improvement' | 'poor'; };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GoogleOAuthProvider class</name>
  <files>open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.ts, open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.test.ts, open-seo-main/src/server/features/platform-oauth/providers/index.ts</files>
  <read_first>
    - .planning/phases/61-platform-integration/DESIGN.md (lines 309-365 Google OAuth spec)
    - open-seo-main/src/server/lib/runtime-env.ts (env var pattern)
  </read_first>
  <behavior>
    - Test 1: getAuthorizationUrl returns URL with client_id, redirect_uri, scope, state, access_type=offline, prompt=consent
    - Test 2: getAuthorizationUrl with services=['searchConsole'] includes webmasters.readonly scope
    - Test 3: getAuthorizationUrl with services=['searchConsole','analytics'] includes both scopes
    - Test 4: exchangeCodeForTokens calls Google token endpoint with correct params (mock fetch)
    - Test 5: refreshAccessToken calls token endpoint with grant_type=refresh_token
  </behavior>
  <action>
Create GoogleOAuthProvider implementing OAuthProvider interface per D-06:

```typescript
// open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.ts

import type { OAuthProvider, TokenSet } from '../OAuthProviderBase';
import { getRequiredEnvValueSync } from '@/server/lib/runtime-env';

export const GOOGLE_SCOPES = {
  searchConsole: 'https://www.googleapis.com/auth/webmasters.readonly',
  analytics: 'https://www.googleapis.com/auth/analytics.readonly',
  businessProfile: 'https://www.googleapis.com/auth/business.manage',
} as const;

export type GoogleService = keyof typeof GOOGLE_SCOPES;

const GOOGLE_CONFIG = {
  authorizationUrl: 'https://accounts.google.com/o/oauth2/v2/auth',
  tokenUrl: 'https://oauth2.googleapis.com/token',
  revokeUrl: 'https://oauth2.googleapis.com/revoke',
};

export class GoogleOAuthProvider implements OAuthProvider {
  readonly name = 'Google';
  readonly platform = 'google_search_console' as const;
  
  private readonly clientId: string;
  private readonly clientSecret: string;
  private readonly redirectUri: string;
  
  constructor(redirectUri: string) {
    this.clientId = getRequiredEnvValueSync('GOOGLE_CLIENT_ID');
    this.clientSecret = getRequiredEnvValueSync('GOOGLE_CLIENT_SECRET');
    this.redirectUri = redirectUri;
  }
  
  getAuthorizationUrl(state: string, options?: { services?: GoogleService[] }): string {
    const services = options?.services ?? ['searchConsole', 'analytics', 'businessProfile'];
    const scopes = services.map(s => GOOGLE_SCOPES[s]).join(' ');
    
    const params = new URLSearchParams({
      client_id: this.clientId,
      redirect_uri: this.redirectUri,
      response_type: 'code',
      scope: scopes,
      state,
      access_type: 'offline', // D-08: Always get refresh token
      prompt: 'consent',      // D-08: Force consent to ensure refresh token
    });
    
    return `${GOOGLE_CONFIG.authorizationUrl}?${params.toString()}`;
  }
  
  async exchangeCodeForTokens(code: string): Promise<TokenSet> {
    const response = await fetch(GOOGLE_CONFIG.tokenUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: new URLSearchParams({
        client_id: this.clientId,
        client_secret: this.clientSecret,
        code,
        grant_type: 'authorization_code',
        redirect_uri: this.redirectUri,
      }),
    });
    
    if (!response.ok) {
      const error = await response.text();
      throw new Error(`Token exchange failed: ${error}`);
    }
    
    const data = await response.json();
    return {
      accessToken: data.access_token,
      refreshToken: data.refresh_token,
      expiresIn: data.expires_in,
      tokenType: data.token_type,
      scope: data.scope,
    };
  }
  
  async refreshAccessToken(refreshToken: string): Promise<TokenSet> {
    const response = await fetch(GOOGLE_CONFIG.tokenUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: new URLSearchParams({
        client_id: this.clientId,
        client_secret: this.clientSecret,
        refresh_token: refreshToken,
        grant_type: 'refresh_token',
      }),
    });
    
    if (!response.ok) {
      const error = await response.text();
      throw new Error(`Token refresh failed: ${error}`);
    }
    
    const data = await response.json();
    return {
      accessToken: data.access_token,
      refreshToken: data.refresh_token ?? refreshToken, // May not return new refresh token
      expiresIn: data.expires_in,
      tokenType: data.token_type,
      scope: data.scope,
    };
  }
  
  async revokeToken(token: string): Promise<void> {
    const response = await fetch(`${GOOGLE_CONFIG.revokeUrl}?token=${token}`, {
      method: 'POST',
    });
    
    if (!response.ok) {
      const error = await response.text();
      throw new Error(`Token revocation failed: ${error}`);
    }
  }
}
```

Create tests with mocked fetch for all methods.
Create providers/index.ts barrel export.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm vitest run src/server/features/platform-oauth/providers/GoogleOAuthProvider.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class GoogleOAuthProvider" open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.ts
    - grep -q "access_type.*offline" open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.ts
    - grep -q "prompt.*consent" open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.ts
    - grep -q "webmasters.readonly" open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.ts
    - test -f open-seo-main/src/server/features/platform-oauth/providers/GoogleOAuthProvider.test.ts
  </acceptance_criteria>
  <done>GoogleOAuthProvider class implements OAuth flow with GSC/GA/GBP scopes, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create Google data fetching services (GSC, GA, GBP)</name>
  <files>open-seo-main/src/server/features/platform-oauth/services/GoogleSearchConsoleService.ts, open-seo-main/src/server/features/platform-oauth/services/GoogleAnalyticsService.ts, open-seo-main/src/server/features/platform-oauth/services/GoogleBusinessProfileService.ts, open-seo-main/src/server/features/platform-oauth/services/index.ts</files>
  <read_first>
    - .planning/phases/61-platform-integration/DESIGN.md (GSC/GA/GBP data interfaces lines 69-126)
  </read_first>
  <action>
Create three service classes that fetch data from Google APIs using access tokens:

**GoogleSearchConsoleService.ts:**
```typescript
export interface GSCQueryData {
  query: string;
  clicks: number;
  impressions: number;
  ctr: number;
  position: number;
}

export interface GSCPageData {
  page: string;
  clicks: number;
  impressions: number;
}

export interface GSCData {
  queries: GSCQueryData[];
  pages: GSCPageData[];
  indexStatus: {
    indexed: number;
    notIndexed: number;
    errors: string[];
  };
}

export class GoogleSearchConsoleService {
  private readonly accessToken: string;
  private readonly siteUrl: string;
  
  constructor(accessToken: string, siteUrl: string) {
    this.accessToken = accessToken;
    this.siteUrl = siteUrl;
  }
  
  async getSearchQueries(options: { 
    startDate: string; 
    endDate: string; 
    rowLimit?: number 
  }): Promise<GSCQueryData[]> {
    const response = await fetch(
      `https://www.googleapis.com/webmasters/v3/sites/${encodeURIComponent(this.siteUrl)}/searchAnalytics/query`,
      {
        method: 'POST',
        headers: {
          'Authorization': `Bearer ${this.accessToken}`,
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          startDate: options.startDate,
          endDate: options.endDate,
          dimensions: ['query'],
          rowLimit: options.rowLimit ?? 1000,
        }),
      }
    );
    
    if (!response.ok) {
      throw new Error(`GSC API error: ${response.statusText}`);
    }
    
    const data = await response.json();
    return (data.rows ?? []).map((row: any) => ({
      query: row.keys[0],
      clicks: row.clicks,
      impressions: row.impressions,
      ctr: row.ctr,
      position: row.position,
    }));
  }
  
  async getPagePerformance(options: { startDate: string; endDate: string }): Promise<GSCPageData[]> {
    // Similar implementation with dimensions: ['page']
  }
  
  async getIndexStatus(): Promise<{ indexed: number; notIndexed: number; errors: string[] }> {
    // Fetch from URL Inspection API
  }
}
```

**GoogleAnalyticsService.ts:**
- getOverview(): sessions, users, pageviews, bounceRate, avgSessionDuration
- getTopPages(): path, pageviews, avgTimeOnPage
- getTrafficSources(): source, medium, sessions

**GoogleBusinessProfileService.ts:**
- getReviews(): rating, text, author, date
- getInsights(): views, searches, actions

Create services/index.ts barrel export.
  </action>
  <verify>
    <automated>grep -l "class Google.*Service" open-seo-main/src/server/features/platform-oauth/services/*.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class GoogleSearchConsoleService" open-seo-main/src/server/features/platform-oauth/services/GoogleSearchConsoleService.ts
    - grep -q "getSearchQueries" open-seo-main/src/server/features/platform-oauth/services/GoogleSearchConsoleService.ts
    - grep -q "class GoogleAnalyticsService" open-seo-main/src/server/features/platform-oauth/services/GoogleAnalyticsService.ts
    - grep -q "class GoogleBusinessProfileService" open-seo-main/src/server/features/platform-oauth/services/GoogleBusinessProfileService.ts
    - test -f open-seo-main/src/server/features/platform-oauth/services/index.ts
  </acceptance_criteria>
  <done>Three Google services created for GSC, GA, GBP data fetching</done>
</task>

<task type="auto">
  <name>Task 3: Create Next.js OAuth API routes for Google</name>
  <files>apps/web/src/app/api/oauth/google/authorize/route.ts, apps/web/src/app/api/oauth/google/callback/route.ts</files>
  <read_first>
    - apps/web/src/lib/auth/api-auth.ts (auth pattern)
    - apps/web/src/app/api/proposals/[id]/route.ts (existing API route pattern)
    - .planning/phases/61-platform-integration/DESIGN.md (API endpoints lines 798-822)
  </read_first>
  <action>
Create Next.js App Router API routes:

**apps/web/src/app/api/oauth/google/authorize/route.ts:**
```typescript
import { NextRequest, NextResponse } from 'next/server';
import { auth } from '@clerk/nextjs/server';
import { nanoid } from 'nanoid';
import { db } from '@/db'; // Drizzle instance
import { oauthStates } from '@/db/oauth-state-schema';

export async function GET(request: NextRequest) {
  const { userId, orgId } = auth();
  if (!userId || !orgId) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  
  // Parse query params
  const searchParams = request.nextUrl.searchParams;
  const services = searchParams.get('services')?.split(',') ?? ['searchConsole', 'analytics'];
  const prospectId = searchParams.get('prospectId');
  
  // Generate CSRF state
  const state = nanoid(32);
  const expiresAt = new Date(Date.now() + 10 * 60 * 1000); // 10 minutes per D-03
  
  // Store state in DB for validation on callback
  await db.insert(oauthStates).values({
    id: nanoid(),
    state,
    platform: 'google',
    workspaceId: orgId,
    prospectId,
    userId,
    redirectUri: `${process.env.NEXT_PUBLIC_APP_URL}/api/oauth/google/callback`,
    scopes: services,
    expiresAt,
  });
  
  // Build Google authorization URL
  const provider = new GoogleOAuthProvider(
    `${process.env.NEXT_PUBLIC_APP_URL}/api/oauth/google/callback`
  );
  const authUrl = provider.getAuthorizationUrl(state, { services });
  
  return NextResponse.redirect(authUrl);
}
```

**apps/web/src/app/api/oauth/google/callback/route.ts:**
```typescript
import { NextRequest, NextResponse } from 'next/server';
import { eq, and, gt } from 'drizzle-orm';
import { db } from '@/db';
import { oauthStates, platformConnections } from '@/db';
import { GoogleOAuthProvider } from '@/server/features/platform-oauth/providers';
import { encryptToken } from '@/server/features/platform-oauth/TokenEncryption';
import { nanoid } from 'nanoid';

export async function GET(request: NextRequest) {
  const searchParams = request.nextUrl.searchParams;
  const code = searchParams.get('code');
  const state = searchParams.get('state');
  const error = searchParams.get('error');
  
  // Handle OAuth errors
  if (error) {
    return NextResponse.redirect(
      `${process.env.NEXT_PUBLIC_APP_URL}/settings/connections?error=${error}`
    );
  }
  
  if (!code || !state) {
    return NextResponse.redirect(
      `${process.env.NEXT_PUBLIC_APP_URL}/settings/connections?error=invalid_request`
    );
  }
  
  // Validate state (CSRF protection)
  const storedState = await db.query.oauthStates.findFirst({
    where: and(
      eq(oauthStates.state, state),
      gt(oauthStates.expiresAt, new Date()),
    ),
  });
  
  if (!storedState) {
    return NextResponse.redirect(
      `${process.env.NEXT_PUBLIC_APP_URL}/settings/connections?error=invalid_state`
    );
  }
  
  // Mark state as used
  await db.update(oauthStates)
    .set({ usedAt: new Date() })
    .where(eq(oauthStates.id, storedState.id));
  
  // Exchange code for tokens
  const provider = new GoogleOAuthProvider(storedState.redirectUri);
  const tokens = await provider.exchangeCodeForTokens(code);
  
  // Store encrypted tokens
  const now = new Date();
  const expiresAt = new Date(now.getTime() + tokens.expiresIn * 1000);
  
  await db.insert(platformConnections).values({
    id: nanoid(),
    workspaceId: storedState.workspaceId,
    prospectId: storedState.prospectId,
    platform: 'google_search_console', // Primary platform
    accessTokenEncrypted: encryptToken(tokens.accessToken),
    refreshTokenEncrypted: tokens.refreshToken ? encryptToken(tokens.refreshToken) : null,
    tokenExpiresAt: expiresAt,
    tokenType: tokens.tokenType,
    status: 'active',
    scopesRequested: storedState.scopes,
    scopesGranted: tokens.scope?.split(' ') ?? [],
    connectedAt: now,
    connectedBy: storedState.userId,
    createdAt: now,
    updatedAt: now,
  });
  
  return NextResponse.redirect(
    `${process.env.NEXT_PUBLIC_APP_URL}/settings/connections?success=google`
  );
}
```
  </action>
  <verify>
    <automated>ls -la apps/web/src/app/api/oauth/google/*/route.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - test -f apps/web/src/app/api/oauth/google/authorize/route.ts
    - test -f apps/web/src/app/api/oauth/google/callback/route.ts
    - grep -q "GET.*request.*NextRequest" apps/web/src/app/api/oauth/google/authorize/route.ts
    - grep -q "exchangeCodeForTokens" apps/web/src/app/api/oauth/google/callback/route.ts
    - grep -q "encryptToken" apps/web/src/app/api/oauth/google/callback/route.ts
    - grep -q "oauthStates" apps/web/src/app/api/oauth/google/callback/route.ts
  </acceptance_criteria>
  <done>Next.js API routes created for Google OAuth authorize and callback</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> API | Clerk auth required for /authorize |
| Google -> Callback | State parameter validates origin |
| API -> Database | Tokens encrypted before storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04 | Spoofing | OAuth callback | mitigate | Validate state parameter with 10-min expiry |
| T-61-05 | Information Disclosure | Token response | mitigate | Never log tokens, encrypt immediately |
| T-61-06 | Tampering | State table | mitigate | Mark state as used after single use |
</threat_model>

<verification>
- [ ] GoogleOAuthProvider tests pass
- [ ] Authorization URL includes access_type=offline and prompt=consent
- [ ] Callback validates state against database
- [ ] Tokens encrypted before storage
- [ ] API routes use Clerk auth
</verification>

<success_criteria>
- GoogleOAuthProvider implements full OAuth 2.0 flow
- GSC, GA, GBP data services fetch from Google APIs
- /api/oauth/google/authorize redirects to Google consent
- /api/oauth/google/callback stores encrypted tokens
- State validation prevents CSRF attacks
</success_criteria>

<output>
After completion, create `.planning/phases/61-platform-integration/61-02-SUMMARY.md`
</output>
