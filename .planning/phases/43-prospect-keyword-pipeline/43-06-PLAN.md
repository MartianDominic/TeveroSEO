---
phase: 43-prospect-keyword-pipeline
plan: 06
type: execute
wave: 4
depends_on: [43-04, 42-02]
status: deferred
deferred_reason: "UI components require design-system-v6 foundation. Backend services stay here; UI moves to Phase 46-47 (agency pipeline proposal flows)."
deferred_to: "Phase 46/47"
files_modified:
  - open-seo-main/src/server/features/proposals/services/ProposalGeneratorService.ts
  - open-seo-main/src/server/features/proposals/services/ProposalGeneratorService.test.ts
  - open-seo-main/src/server/features/proposals/services/AwarenessClassifier.ts
  - open-seo-main/src/server/features/proposals/services/AwarenessClassifier.test.ts
  - open-seo-main/src/server/features/proposals/services/SectionGenerator.ts
  - open-seo-main/src/server/features/proposals/services/SectionGenerator.test.ts
  - open-seo-main/src/server/features/proposals/prompts/awareness-classifier.xml
  - open-seo-main/src/server/features/proposals/prompts/presale-hook.xml
  - open-seo-main/src/server/features/proposals/prompts/executive-summary.xml
  - open-seo-main/src/server/features/proposals/prompts/investment-section.xml
  - open-seo-main/src/server/features/proposals/prompts/agreement-generator.xml
  - open-seo-main/src/routes/api/proposals/[id]/generate.ts
  - apps/web/src/app/(shell)/prospects/[prospectId]/proposal/builder/page.tsx
  - apps/web/src/app/(shell)/prospects/[prospectId]/proposal/builder/actions.ts
  - apps/web/src/app/(shell)/prospects/[prospectId]/proposal/builder/components/ScenarioSelector.tsx
  - apps/web/src/app/(shell)/prospects/[prospectId]/proposal/builder/components/SectionEditor.tsx
  - apps/web/src/app/(shell)/prospects/[prospectId]/proposal/preview/page.tsx
autonomous: false
requirements:
  - PKP-17
  - PKP-18
  - PKP-19
  - PKP-20

must_haves:
  truths:
    - "Awareness classifier determines prospect's Schwartz awareness level"
    - "Three proposal scenarios generate with appropriate content depth"
    - "AI generates sections using XML prompts with copywriting frameworks"
    - "Agreement generator produces valid Lithuanian sutartis with legal sections"
    - "Sections can be edited after AI generation"
  artifacts:
    - path: "open-seo-main/src/server/features/proposals/services/ProposalGeneratorService.ts"
      provides: "Full proposal generation orchestration"
      exports: ["ProposalGeneratorService", "generateProposal"]
    - path: "open-seo-main/src/server/features/proposals/services/AwarenessClassifier.ts"
      provides: "Schwartz awareness level classification"
      exports: ["AwarenessClassifier", "classifyAwareness", "AwarenessLevel"]
    - path: "open-seo-main/src/server/features/proposals/services/SectionGenerator.ts"
      provides: "AI section generation with XML prompts"
      exports: ["SectionGenerator", "generateSection"]
    - path: "apps/web/src/app/(shell)/prospects/[prospectId]/proposal/builder/page.tsx"
      provides: "Proposal builder UI with drag-reorder"
  key_links:
    - from: "ProposalGeneratorService"
      to: "SectionGenerator"
      via: "Generates each section using appropriate XML prompt"
      pattern: "sectionGenerator.generate"
    - from: "AwarenessClassifier"
      to: "Claude API"
      via: "Analyzes prospect signals to determine awareness"
      pattern: "anthropic.messages.create"
---

<objective>
Build the AI-powered proposal generation system with awareness classification, copywriting-framework-based section generation, and Lithuanian agreement (sutartis) creation.

Purpose: Agencies need to generate professional proposals quickly. The awareness classifier selects the right hook strategy, while XML prompts using Schwartz/Halbert/Kennedy frameworks generate compelling copy. The agreement generator produces legally valid Lithuanian contracts.

Output: AwarenessClassifier, SectionGenerator with 5 XML prompts, ProposalGeneratorService, proposal builder UI with scenario selection and section editing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-prospect-keyword-pipeline/43-CONTEXT.md
@.planning/phases/43-prospect-keyword-pipeline/GSD-PLAN-REVISION.md
@.planning/keyword-intelligence/PROPOSAL-XML-PROMPTS.md

<interfaces>
<!-- From PROPOSAL-XML-PROMPTS.md -->

```typescript
// Awareness levels (Schwartz)
type AwarenessLevel = 
  | "unaware"       // Doesn't know they have a problem
  | "problem-aware" // Knows problem, not solutions
  | "solution-aware"// Knows solutions exist
  | "product-aware" // Knows your offer
  | "most-aware";   // Ready to buy

// Proposal scenarios from 43-CONTEXT.md
type ProposalScenario = "focused" | "full_audit" | "competitor_only";

// Section types
type SectionType = 
  | "executive_summary"
  | "current_state"
  | "keyword_analysis"
  | "competitor_comparison"
  | "page_mapping"
  | "roi_projections"
  | "investment"
  | "appendix";
```

From open-seo-main/src/db/proposal-schema.ts:
```typescript
export interface ProposalContent {
  hero: { headline: string; subheadline: string; trafficValue: number };
  currentState: { traffic: number; keywords: number; value: number; chartData: Array<...> };
  opportunities: Array<{ keyword: string; volume: number; difficulty: string; potential: number }>;
  roi: { projectedTrafficGain: number; trafficValue: number; defaultConversionRate: number; defaultAov: number };
  investment: { setupFee: number; monthlyFee: number; inclusions: string[] };
  nextSteps: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AwarenessClassifier with Schwartz framework</name>
  <files>open-seo-main/src/server/features/proposals/services/AwarenessClassifier.ts, open-seo-main/src/server/features/proposals/services/AwarenessClassifier.test.ts, open-seo-main/src/server/features/proposals/prompts/awareness-classifier.xml</files>
  <read_first>
    - .planning/keyword-intelligence/PROPOSAL-XML-PROMPTS.md (awareness classifier prompt)
  </read_first>
  <behavior>
    - Test 1: Cold lead with no SEO mention -> "unaware"
    - Test 2: Lead mentions traffic problems -> "problem-aware"
    - Test 3: Lead asks about SEO services -> "solution-aware"
    - Test 4: Lead comparing providers -> "product-aware"
    - Test 5: Lead requests proposal -> "most-aware"
    - Test 6: Returns recommended hook strategy for each level
  </behavior>
  <action>
1. Create `open-seo-main/src/server/features/proposals/prompts/awareness-classifier.xml` based on PROPOSAL-XML-PROMPTS.md.

2. Create `open-seo-main/src/server/features/proposals/services/AwarenessClassifier.ts`:

```typescript
import Anthropic from "@anthropic-ai/sdk";
import { readFileSync } from "fs";
import { join } from "path";

const PROMPT_PATH = join(__dirname, "../prompts/awareness-classifier.xml");

export type AwarenessLevel = 
  | "unaware"
  | "problem-aware"
  | "solution-aware"
  | "product-aware"
  | "most-aware";

export interface ClassificationResult {
  awarenessLevel: AwarenessLevel;
  confidence: number;
  signalsDetected: string[];
  hookStrategy: string;
  recommendedApproach: {
    openingAngle: string;
    primaryCialdini: string;
    objectionsToAddress: string[];
  };
  reasoning: string;
}

export interface ClassificationInput {
  domain: string;
  scrapeSummary?: string;
  initialInquiry?: string;
  leadSource?: string;
  conversationHistory?: string;
}

export class AwarenessClassifier {
  private anthropic: Anthropic;

  constructor() {
    this.anthropic = new Anthropic();
  }

  /**
   * Classify prospect's awareness level using Schwartz framework.
   */
  async classify(input: ClassificationInput): Promise<ClassificationResult> {
    const promptTemplate = readFileSync(PROMPT_PATH, "utf-8");
    
    const prompt = promptTemplate
      .replace("{{DOMAIN}}", input.domain)
      .replace("{{SCRAPE_SUMMARY}}", input.scrapeSummary || "N/A")
      .replace("{{INQUIRY_TEXT}}", input.initialInquiry || "N/A")
      .replace("{{LEAD_SOURCE}}", input.leadSource || "unknown")
      .replace("{{CONVERSATION_NOTES}}", input.conversationHistory || "N/A");

    const response = await this.anthropic.messages.create({
      model: "claude-sonnet-4-20250514",
      max_tokens: 1000,
      messages: [
        { role: "user", content: prompt },
      ],
    });

    const content = response.content[0];
    if (content.type !== "text") {
      throw new Error("Unexpected response type");
    }

    // Extract JSON
    const jsonMatch = content.text.match(/\{[\s\S]*\}/);
    if (!jsonMatch) {
      throw new Error("No JSON found in response");
    }

    const result = JSON.parse(jsonMatch[0]);

    return {
      awarenessLevel: result.awareness_level as AwarenessLevel,
      confidence: result.confidence,
      signalsDetected: result.signals_detected,
      hookStrategy: result.hook_strategy,
      recommendedApproach: {
        openingAngle: result.recommended_approach.opening_angle,
        primaryCialdini: result.recommended_approach.primary_cialdini,
        objectionsToAddress: result.recommended_approach.objections_to_address,
      },
      reasoning: result.reasoning,
    };
  }

  /**
   * Quick classification without AI (rule-based).
   * Use for initial screening before full AI classification.
   */
  quickClassify(input: ClassificationInput): AwarenessLevel {
    const inquiry = (input.initialInquiry || "").toLowerCase();
    
    // Most-aware signals
    if (
      inquiry.includes("pasiulymas") || // proposal in Lithuanian
      inquiry.includes("proposal") ||
      inquiry.includes("quote") ||
      inquiry.includes("kaina") // price in Lithuanian
    ) {
      return "most-aware";
    }

    // Product-aware signals
    if (
      inquiry.includes("compare") ||
      inquiry.includes("other agencies") ||
      inquiry.includes("why you") ||
      inquiry.includes("konkurentai") // competitors in Lithuanian
    ) {
      return "product-aware";
    }

    // Solution-aware signals
    if (
      inquiry.includes("seo") ||
      inquiry.includes("optimiz") ||
      inquiry.includes("search engine") ||
      inquiry.includes("google rank")
    ) {
      return "solution-aware";
    }

    // Problem-aware signals
    if (
      inquiry.includes("traffic") ||
      inquiry.includes("visitors") ||
      inquiry.includes("lankytojai") || // visitors in Lithuanian
      inquiry.includes("sales down") ||
      inquiry.includes("not found")
    ) {
      return "problem-aware";
    }

    // Default to unaware
    return "unaware";
  }
}

export const awarenessClassifier = new AwarenessClassifier();
```

3. Write tests with mocked scenarios.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm test -- --grep "AwarenessClassifier"</automated>
  </verify>
  <done>AwarenessClassifier determines Schwartz awareness level and recommends hook strategy</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SectionGenerator with XML prompts</name>
  <files>open-seo-main/src/server/features/proposals/services/SectionGenerator.ts, open-seo-main/src/server/features/proposals/services/SectionGenerator.test.ts, open-seo-main/src/server/features/proposals/prompts/presale-hook.xml, open-seo-main/src/server/features/proposals/prompts/executive-summary.xml, open-seo-main/src/server/features/proposals/prompts/investment-section.xml, open-seo-main/src/server/features/proposals/prompts/agreement-generator.xml</files>
  <read_first>
    - .planning/keyword-intelligence/PROPOSAL-XML-PROMPTS.md (all prompt templates)
  </read_first>
  <behavior>
    - Test 1: generateSection("executive_summary") returns Lithuanian text
    - Test 2: generateSection("presale_hook") uses Halbert fascinations
    - Test 3: generateSection("investment") includes pricing and CTA
    - Test 4: generateSection("agreement") produces valid contract structure
    - Test 5: Awareness level affects tone and approach
  </behavior>
  <action>
1. Create XML prompt files based on PROPOSAL-XML-PROMPTS.md:
   - `presale-hook.xml` - Halbert fascination formulas
   - `executive-summary.xml` - Awareness-adjusted summary
   - `investment-section.xml` - Kennedy direct response CTA
   - `agreement-generator.xml` - Lithuanian legal contract

2. Create `open-seo-main/src/server/features/proposals/services/SectionGenerator.ts`:

```typescript
import Anthropic from "@anthropic-ai/sdk";
import { readFileSync } from "fs";
import { join } from "path";
import type { AwarenessLevel } from "./AwarenessClassifier";

const PROMPTS_DIR = join(__dirname, "../prompts");

export type SectionType =
  | "presale_hook"
  | "executive_summary"
  | "current_state"
  | "keyword_analysis"
  | "competitor_comparison"
  | "page_mapping"
  | "roi_projections"
  | "investment"
  | "agreement";

export interface SectionInput {
  // Prospect data
  companyName: string;
  domain: string;
  awarenessLevel: AwarenessLevel;
  
  // Analysis data
  totalKeywords?: number;
  quickWins?: number;
  trafficOpportunity?: number;
  revenueOpportunity?: number;
  topCompetitor?: string;
  competitorTraffic?: number;
  biggestGap?: string;
  
  // Pricing
  setupFee?: number;
  monthlyFee?: number;
  contractMonths?: number;
  
  // Agency info
  agencyName?: string;
  agencyPositioning?: string;
  differentiators?: string[];
  caseStudies?: Array<{ client: string; result: string }>;
  
  // Lithuanian-specific
  language?: "lt" | "en";
}

export interface GeneratedSection {
  type: SectionType;
  content: string;
  language: string;
  generatedAt: string;
}

export class SectionGenerator {
  private anthropic: Anthropic;

  constructor() {
    this.anthropic = new Anthropic();
  }

  /**
   * Generate a single section using the appropriate XML prompt.
   */
  async generateSection(
    sectionType: SectionType,
    input: SectionInput
  ): Promise<GeneratedSection> {
    const promptPath = this.getPromptPath(sectionType);
    const promptTemplate = readFileSync(promptPath, "utf-8");
    
    // Replace placeholders
    const prompt = this.fillTemplate(promptTemplate, input);

    const response = await this.anthropic.messages.create({
      model: "claude-sonnet-4-20250514",
      max_tokens: 2000,
      messages: [
        { role: "user", content: prompt },
      ],
    });

    const content = response.content[0];
    if (content.type !== "text") {
      throw new Error("Unexpected response type");
    }

    // Extract the generated content (may be wrapped in XML tags)
    let generatedContent = content.text;
    
    // Try to extract from <output> tags if present
    const outputMatch = generatedContent.match(/<output>([\s\S]*?)<\/output>/);
    if (outputMatch) {
      generatedContent = outputMatch[1].trim();
    }

    return {
      type: sectionType,
      content: generatedContent,
      language: input.language || "lt",
      generatedAt: new Date().toISOString(),
    };
  }

  /**
   * Generate multiple sections in parallel.
   */
  async generateSections(
    sectionTypes: SectionType[],
    input: SectionInput
  ): Promise<GeneratedSection[]> {
    const promises = sectionTypes.map(type =>
      this.generateSection(type, input)
    );

    return Promise.all(promises);
  }

  private getPromptPath(sectionType: SectionType): string {
    const promptFiles: Record<SectionType, string> = {
      presale_hook: "presale-hook.xml",
      executive_summary: "executive-summary.xml",
      current_state: "current-state.xml",
      keyword_analysis: "keyword-analysis.xml",
      competitor_comparison: "competitor-comparison.xml",
      page_mapping: "page-mapping.xml",
      roi_projections: "roi-projections.xml",
      investment: "investment-section.xml",
      agreement: "agreement-generator.xml",
    };

    return join(PROMPTS_DIR, promptFiles[sectionType]);
  }

  private fillTemplate(template: string, input: SectionInput): string {
    const replacements: Record<string, string> = {
      "{{COMPANY_NAME}}": input.companyName,
      "{{DOMAIN}}": input.domain,
      "{{AWARENESS_LEVEL}}": input.awarenessLevel,
      "{{TOTAL_KEYWORDS}}": String(input.totalKeywords || 0),
      "{{QUICK_WINS}}": String(input.quickWins || 0),
      "{{TRAFFIC_OPPORTUNITY}}": String(input.trafficOpportunity || 0),
      "{{REVENUE_OPPORTUNITY}}": String(input.revenueOpportunity || 0),
      "{{TOP_COMPETITOR}}": input.topCompetitor || "N/A",
      "{{COMPETITOR_TRAFFIC}}": String(input.competitorTraffic || 0),
      "{{BIGGEST_GAP}}": input.biggestGap || "N/A",
      "{{SETUP_FEE}}": String(input.setupFee || 0),
      "{{MONTHLY_FEE}}": String(input.monthlyFee || 0),
      "{{CONTRACT_MONTHS}}": String(input.contractMonths || 6),
      "{{AGENCY_NAME}}": input.agencyName || "Tevero",
      "{{AGENCY_POSITIONING}}": input.agencyPositioning || "",
      "{{DIFFERENTIATORS}}": (input.differentiators || []).join(", "),
      "{{CASE_STUDIES}}": JSON.stringify(input.caseStudies || []),
      "{{LANGUAGE}}": input.language || "lt",
    };

    let result = template;
    for (const [placeholder, value] of Object.entries(replacements)) {
      result = result.replace(new RegExp(placeholder, "g"), value);
    }

    return result;
  }
}

export const sectionGenerator = new SectionGenerator();
```

3. Write tests with mocked AI responses.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm test -- --grep "SectionGenerator"</automated>
  </verify>
  <done>SectionGenerator creates sections using XML prompts with Schwartz/Halbert/Kennedy frameworks</done>
</task>

<task type="auto">
  <name>Task 3: Create ProposalGeneratorService orchestrating full proposals</name>
  <files>open-seo-main/src/server/features/proposals/services/ProposalGeneratorService.ts, open-seo-main/src/server/features/proposals/services/ProposalGeneratorService.test.ts, open-seo-main/src/routes/api/proposals/[id]/generate.ts</files>
  <read_first>
    - open-seo-main/src/db/proposal-schema.ts (proposal content structure)
    - .planning/phases/43-prospect-keyword-pipeline/43-CONTEXT.md (proposal scenarios)
  </read_first>
  <action>
1. Create `open-seo-main/src/server/features/proposals/services/ProposalGeneratorService.ts`:

```typescript
import { db } from "@/db";
import { proposals, type ProposalContent } from "@/db/proposal-schema";
import { prospects, prospectAnalyses } from "@/db/prospect-schema";
import { prospectKeywords } from "@/db/prospect-keyword-schema";
import { eq, desc } from "drizzle-orm";
import { nanoid } from "nanoid";
import { awarenessClassifier, type AwarenessLevel } from "./AwarenessClassifier";
import { sectionGenerator, type SectionType, type GeneratedSection } from "./SectionGenerator";

export type ProposalScenario = "focused" | "full_audit" | "competitor_only";

// Sections included per scenario (from 43-CONTEXT.md)
const SCENARIO_SECTIONS: Record<ProposalScenario, SectionType[]> = {
  focused: [
    "executive_summary",
    "keyword_analysis",
    "competitor_comparison",
    "page_mapping",
    "roi_projections",
    "investment",
  ],
  full_audit: [
    "executive_summary",
    "current_state",
    "keyword_analysis",
    "competitor_comparison",
    "page_mapping",
    "roi_projections",
    "investment",
  ],
  competitor_only: [
    "executive_summary",
    "competitor_comparison",
    "investment",
  ],
};

export interface GenerateProposalInput {
  prospectId: string;
  scenario: ProposalScenario;
  awarenessLevel?: AwarenessLevel; // Override auto-detection
  pricing: {
    setupFee: number;
    monthlyFee: number;
    contractMonths: number;
  };
  agencyInfo?: {
    name?: string;
    positioning?: string;
    differentiators?: string[];
    caseStudies?: Array<{ client: string; result: string }>;
  };
  language?: "lt" | "en";
}

export interface GenerateProposalResult {
  proposalId: string;
  sections: GeneratedSection[];
  awarenessLevel: AwarenessLevel;
  scenario: ProposalScenario;
}

export class ProposalGeneratorService {
  /**
   * Generate a full proposal for a prospect.
   */
  async generateProposal(input: GenerateProposalInput): Promise<GenerateProposalResult> {
    // Fetch prospect and analysis data
    const [prospect] = await db
      .select()
      .from(prospects)
      .where(eq(prospects.id, input.prospectId))
      .limit(1);

    if (!prospect) {
      throw new Error("Prospect not found");
    }

    // Get latest analysis
    const [analysis] = await db
      .select()
      .from(prospectAnalyses)
      .where(eq(prospectAnalyses.prospectId, input.prospectId))
      .orderBy(desc(prospectAnalyses.createdAt))
      .limit(1);

    // Get keywords summary
    const keywords = await db
      .select()
      .from(prospectKeywords)
      .where(eq(prospectKeywords.prospectId, input.prospectId));

    const quickWins = keywords.filter(k => k.quickWinType !== null).length;
    const totalVolume = keywords.reduce((sum, k) => sum + (k.searchVolume || 0), 0);

    // Determine awareness level
    let awarenessLevel = input.awarenessLevel;
    if (!awarenessLevel) {
      const classification = await awarenessClassifier.classify({
        domain: prospect.domain,
        scrapeSummary: analysis?.scrapedContent?.businessInfo?.summary,
        initialInquiry: prospect.notes || undefined,
        leadSource: prospect.source || undefined,
      });
      awarenessLevel = classification.awarenessLevel;
    }

    // Get sections for this scenario
    const sectionTypes = SCENARIO_SECTIONS[input.scenario];

    // Build section input
    const sectionInput = {
      companyName: prospect.companyName || prospect.domain,
      domain: prospect.domain,
      awarenessLevel,
      totalKeywords: keywords.length,
      quickWins,
      trafficOpportunity: totalVolume,
      revenueOpportunity: Math.round(totalVolume * 0.02 * 50), // Rough estimate
      topCompetitor: analysis?.competitorDomains?.[0] || undefined,
      competitorTraffic: analysis?.domainMetrics?.organicTraffic,
      biggestGap: analysis?.keywordGaps?.[0]?.keyword,
      setupFee: input.pricing.setupFee,
      monthlyFee: input.pricing.monthlyFee,
      contractMonths: input.pricing.contractMonths,
      agencyName: input.agencyInfo?.name,
      agencyPositioning: input.agencyInfo?.positioning,
      differentiators: input.agencyInfo?.differentiators,
      caseStudies: input.agencyInfo?.caseStudies,
      language: input.language || "lt",
    };

    // Generate sections
    const sections = await sectionGenerator.generateSections(sectionTypes, sectionInput);

    // Create proposal record
    const proposalId = `prop_${nanoid(12)}`;
    const token = nanoid(24);

    // Build ProposalContent from sections
    const content: ProposalContent = this.buildContent(sections, sectionInput);

    await db.insert(proposals).values({
      id: proposalId,
      prospectId: input.prospectId,
      workspaceId: prospect.workspaceId,
      template: input.scenario,
      content,
      setupFeeCents: input.pricing.setupFee * 100,
      monthlyFeeCents: input.pricing.monthlyFee * 100,
      status: "draft",
      token,
    });

    return {
      proposalId,
      sections,
      awarenessLevel,
      scenario: input.scenario,
    };
  }

  /**
   * Regenerate a single section for an existing proposal.
   */
  async regenerateSection(
    proposalId: string,
    sectionType: SectionType
  ): Promise<GeneratedSection> {
    // Fetch proposal and related data
    const [proposal] = await db
      .select()
      .from(proposals)
      .where(eq(proposals.id, proposalId))
      .limit(1);

    if (!proposal) {
      throw new Error("Proposal not found");
    }

    // Get prospect
    const [prospect] = await db
      .select()
      .from(prospects)
      .where(eq(prospects.id, proposal.prospectId!))
      .limit(1);

    // Build minimal input from existing proposal
    const sectionInput = {
      companyName: prospect?.companyName || prospect?.domain || "Client",
      domain: prospect?.domain || "",
      awarenessLevel: "solution-aware" as AwarenessLevel, // Default
      setupFee: (proposal.setupFeeCents || 0) / 100,
      monthlyFee: (proposal.monthlyFeeCents || 0) / 100,
      contractMonths: 6,
      language: "lt" as const,
    };

    return sectionGenerator.generateSection(sectionType, sectionInput);
  }

  private buildContent(
    sections: GeneratedSection[],
    input: any
  ): ProposalContent {
    // Map sections to ProposalContent structure
    const execSummary = sections.find(s => s.type === "executive_summary");

    return {
      hero: {
        headline: `SEO Galimybes: ${input.companyName}`,
        subheadline: execSummary?.content.substring(0, 200) || "",
        trafficValue: input.trafficOpportunity || 0,
      },
      currentState: {
        traffic: input.competitorTraffic || 0,
        keywords: input.totalKeywords || 0,
        value: input.revenueOpportunity || 0,
        chartData: [], // Would be populated from actual data
      },
      opportunities: [], // Would be populated from keyword data
      roi: {
        projectedTrafficGain: input.trafficOpportunity || 0,
        trafficValue: input.revenueOpportunity || 0,
        defaultConversionRate: 0.02,
        defaultAov: 50,
      },
      investment: {
        setupFee: input.setupFee || 0,
        monthlyFee: input.monthlyFee || 0,
        inclusions: [
          "Techninis SEO auditas",
          "On-page optimizacija",
          "Turinio strategija",
          "Menesines ataskaitos",
        ],
      },
      nextSteps: [
        "Pasirasyti sutarti",
        "Atlikti pradini audita",
        "Paruosti veiklos plana",
      ],
    };
  }
}

export const proposalGeneratorService = new ProposalGeneratorService();
```

2. Create API endpoint `open-seo-main/src/routes/api/proposals/[id]/generate.ts`.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm test -- --grep "ProposalGeneratorService" && pnpm tsc --noEmit</automated>
  </verify>
  <done>ProposalGeneratorService orchestrates full proposal generation with awareness-based sections</done>
</task>

<task type="auto">
  <name>Task 4: Create proposal builder UI with scenario selection and section editing</name>
  <files>apps/web/src/app/(shell)/prospects/[prospectId]/proposal/builder/page.tsx, apps/web/src/app/(shell)/prospects/[prospectId]/proposal/builder/actions.ts, apps/web/src/app/(shell)/prospects/[prospectId]/proposal/builder/components/ScenarioSelector.tsx, apps/web/src/app/(shell)/prospects/[prospectId]/proposal/builder/components/SectionEditor.tsx, apps/web/src/app/(shell)/prospects/[prospectId]/proposal/preview/page.tsx</files>
  <read_first>
    - .planning/phases/43-prospect-keyword-pipeline/GSD-PLAN-REVISION.md (proposal builder UX)
  </read_first>
  <action>
1. Create `apps/web/src/app/(shell)/prospects/[prospectId]/proposal/builder/actions.ts`:

```typescript
"use server";

export type ProposalScenario = "focused" | "full_audit" | "competitor_only";
export type AwarenessLevel = "unaware" | "problem-aware" | "solution-aware" | "product-aware" | "most-aware";

export interface GeneratedSection {
  type: string;
  content: string;
  language: string;
}

export interface GenerateProposalInput {
  prospectId: string;
  scenario: ProposalScenario;
  awarenessLevel?: AwarenessLevel;
  pricing: {
    setupFee: number;
    monthlyFee: number;
    contractMonths: number;
  };
  agencyInfo?: {
    name?: string;
    positioning?: string;
    differentiators?: string[];
  };
}

export async function generateProposal(input: GenerateProposalInput): Promise<{
  proposalId: string;
  sections: GeneratedSection[];
  awarenessLevel: AwarenessLevel;
}> {
  const openSeoUrl = process.env.OPEN_SEO_API_URL || "http://localhost:3001";
  
  const response = await fetch(`${openSeoUrl}/api/proposals/generate`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(input),
  });

  if (!response.ok) {
    throw new Error("Failed to generate proposal");
  }

  const result = await response.json();
  return result.data;
}

export async function regenerateSection(
  proposalId: string,
  sectionType: string
): Promise<GeneratedSection> {
  const openSeoUrl = process.env.OPEN_SEO_API_URL || "http://localhost:3001";
  
  const response = await fetch(`${openSeoUrl}/api/proposals/${proposalId}/generate`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ sectionType }),
  });

  if (!response.ok) {
    throw new Error("Failed to regenerate section");
  }

  const result = await response.json();
  return result.data;
}

export async function updateSection(
  proposalId: string,
  sectionType: string,
  content: string
): Promise<void> {
  const openSeoUrl = process.env.OPEN_SEO_API_URL || "http://localhost:3001";
  
  const response = await fetch(`${openSeoUrl}/api/proposals/${proposalId}/sections`, {
    method: "PATCH",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ sectionType, content }),
  });

  if (!response.ok) {
    throw new Error("Failed to update section");
  }
}
```

2. Create scenario selector, section editor components, and main builder page with:
   - Step 1: Scenario selection (focused/full_audit/competitor_only)
   - Step 2: Content customization (awareness level, tone, agency info)
   - Step 3: Pricing configuration
   - Step 4: Section list with AI generate, edit, reorder functionality
   - Preview and save as template options

3. Create proposal preview page.
  </action>
  <verify>
    <automated>cd apps/web && pnpm build && pnpm tsc --noEmit</automated>
  </verify>
  <done>Proposal builder UI with scenario selection, AI section generation, and editing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete proposal generation pipeline:
  - AwarenessClassifier using Schwartz framework
  - SectionGenerator with 5 XML prompts (Halbert/Kennedy copywriting)
  - ProposalGeneratorService orchestrating full proposals
  - Three proposal scenarios (focused/full_audit/competitor_only)
  - Proposal builder UI with section editing
  </what-built>
  <how-to-verify>
  1. Navigate to a prospect with completed analysis
  2. Click "Generate Proposal"
  3. Select "Full Audit" scenario
  4. Configure pricing (e.g., 500 EUR setup, 800 EUR/month)
  5. Click "Generate"
  6. Verify each section generates with Lithuanian text
  7. Edit the executive summary section
  8. Click "Regenerate" on a section
  9. Preview the proposal
  10. Check agreement section has legal structure
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with proposal generation</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> AI prompts | Pricing, company info injected into prompts |
| AI output -> Proposal | Generated content displayed to prospects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-17 | Injection | Prompt templates | mitigate | Escape user input before template substitution |
| T-43-18 | Info Disclosure | Agreement | mitigate | Review generated agreements before sending |
| T-43-19 | Tampering | Section content | accept | Users can edit sections; audit trail exists |
</threat_model>

<verification>
1. Run service tests: `cd open-seo-main && pnpm test -- --grep "Awareness|Section|ProposalGenerator"`
2. Verify UI builds: `cd apps/web && pnpm build`
3. Manual: Generate a proposal with each scenario type
4. Manual: Verify awareness classification affects hook strategy
5. Manual: Edit and regenerate sections
6. Manual: Check Lithuanian agreement has required legal sections
</verification>

<success_criteria>
- [ ] AwarenessClassifier determines 5 Schwartz levels correctly
- [ ] SectionGenerator uses XML prompts with copywriting frameworks
- [ ] Three scenarios generate with appropriate sections
- [ ] AI sections are in Lithuanian with professional tone
- [ ] Sections can be edited and regenerated
- [ ] Agreement has legal structure (parties, scope, payment, termination)
- [ ] Proposal builder UI works with drag-reorder
- [ ] Human verification checkpoint passes
- [ ] TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/43-prospect-keyword-pipeline/43-06-SUMMARY.md`
</output>
