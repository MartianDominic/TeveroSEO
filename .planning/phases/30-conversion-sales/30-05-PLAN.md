---
phase: 30-conversion-sales
plan: 05
type: execute
wave: 3
depends_on:
  - 29-05
  - 30-04
files_modified:
  - open-seo-main/src/server/services/prospect-report/prospect-report-renderer.ts
  - open-seo-main/src/server/services/prospect-report/index.ts
  - open-seo-main/src/routes/api/prospects/$id.report.ts
  - apps/web/src/app/(shell)/prospects/[prospectId]/page.tsx
  - apps/web/src/app/(shell)/prospects/[prospectId]/actions.ts
autonomous: true
requirements:
  - PROSP-25

must_haves:
  truths:
    - "PDF contains domain metrics summary card"
    - "PDF shows top 20 keyword gaps sorted by achievability"
    - "PDF includes opportunity keywords section"
    - "AI executive summary generated from business info"
    - "Uses existing Puppeteer PDF pipeline"
  artifacts:
    - path: "open-seo-main/src/server/services/prospect-report/prospect-report-renderer.ts"
      provides: "HTML renderer for prospect analysis PDF"
      exports: ["renderProspectReportToHTML", "ProspectReportData", "ProspectReportLabels"]
    - path: "open-seo-main/src/routes/api/prospects/$id.report.ts"
      provides: "PDF download endpoint"
---

<objective>
Create PDF export functionality for prospect analysis reports.

The PDF report is a sales tool for agencies to share analysis results with prospects. It should present data professionally with:
- Domain health overview
- Top keyword opportunities (gaps)
- AI-discovered opportunity keywords
- Executive summary

Reuse existing PDF generation infrastructure (Puppeteer + report-renderer pattern).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create prospect report renderer</name>
  <files>
    open-seo-main/src/server/services/prospect-report/prospect-report-renderer.ts
    open-seo-main/src/server/services/prospect-report/index.ts
  </files>
  <action>
    1. Create `open-seo-main/src/server/services/prospect-report/` directory
    
    2. Create `prospect-report-renderer.ts` with:
       - ProspectReportData interface (analysis + prospect info)
       - ProspectReportLabels interface for localization
       - renderProspectReportToHTML() function
       - Sections: header, domain metrics, keyword gaps table, opportunity keywords, AI summary, footer
       - Use RGB colors for Puppeteer compatibility
       - Professional styling matching existing reports
    
    3. Create `index.ts` barrel export
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create PDF download API endpoint</name>
  <files>
    open-seo-main/src/routes/api/prospects/$id.report.ts
  </files>
  <action>
    1. Create GET endpoint `/api/prospects/:id/report`
    
    2. Implementation:
       - Validate prospect exists and user has access
       - Fetch prospect with latest completed analysis
       - Render HTML using prospect-report-renderer
       - Generate PDF using existing generatePDF()
       - Return PDF with Content-Disposition header
    
    3. Error handling:
       - 404 if prospect not found
       - 400 if no completed analysis
       - 500 if PDF generation fails
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add export button to prospect detail page</name>
  <files>
    apps/web/src/app/(shell)/prospects/[prospectId]/page.tsx
    apps/web/src/app/(shell)/prospects/[prospectId]/actions.ts
  </files>
  <action>
    1. Add server action `downloadAnalysisPDF(prospectId)` in actions.ts:
       - Proxy to open-seo API
       - Return blob URL for download
    
    2. Update page.tsx:
       - Add "Export PDF" button near analysis header
       - Show only when analysis is completed
       - Handle loading state during PDF generation
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Add unit tests</name>
  <files>
    open-seo-main/src/server/services/prospect-report/prospect-report-renderer.test.ts
  </files>
  <action>
    1. Test renderProspectReportToHTML:
       - Outputs valid HTML document
       - Includes all required sections
       - Escapes HTML in user content
       - Handles missing optional fields
       - Respects locale for number formatting
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm test prospect-report-renderer</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- [ ] prospect-report-renderer.ts renders valid HTML with all sections
- [ ] GET /api/prospects/:id/report returns PDF binary
- [ ] Export button visible on prospect detail when analysis complete
- [ ] PDF contains domain metrics, keyword gaps, opportunities, summary
- [ ] TypeScript compiles without errors
- [ ] Unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-conversion-sales/30-05-SUMMARY.md`
</output>
