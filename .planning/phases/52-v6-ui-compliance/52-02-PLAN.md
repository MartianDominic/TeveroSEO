---
phase: 52-v6-ui-compliance
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - apps/web/src/app/(shell)/prospects/[prospectId]/scrape-config/components/RuleEditor.tsx
  - apps/web/src/app/(shell)/prospects/[prospectId]/scrape-config/page.tsx
autonomous: true
requirements:
  - All UI consistent before reports

must_haves:
  truths:
    - "RuleEditor Card uses shadow-card instead of default shadcn card border"
    - "FieldEditor nested panels use bg-surface-2 instead of bg-muted/30"
    - "All Badge components use v6 semantic styling"
    - "All text >= 12px (text-xs converted to text-[12px])"
    - "Form labels use text-text-3 for muted appearance"
  artifacts:
    - path: "apps/web/src/app/(shell)/prospects/[prospectId]/scrape-config/components/RuleEditor.tsx"
      provides: "v6 compliant extraction rule editor"
      contains: "shadow-card"
    - path: "apps/web/src/app/(shell)/prospects/[prospectId]/scrape-config/page.tsx"
      provides: "v6 compliant scrape config page"
      contains: "bg-surface"
  key_links:
    - from: "scrape-config/page.tsx"
      to: "RuleEditor.tsx"
      via: "component import"
      pattern: "import.*RuleEditor"
---

<objective>
Update the scrape configuration components (RuleEditor and scrape-config page) to v6 design system compliance.

Purpose: Ensure the extraction rule editor matches v6 visual standards with ghost-edge shadows and semantic colors.

Output: 2 files updated with v6 tokens, consistent with Wave 1 keyword components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/design-system-v6.md
@packages/ui/src/lib/tokens.css
@.planning/phases/52-v6-ui-compliance/52-01-SUMMARY.md
</context>

<interfaces>
<!-- v6 Design System Key Patterns for Cards and Forms -->

From design-system-v6.md:
```css
/* Card shadow (replaces border) */
--shadow-card: 0 1px 2px rgba(20,20,26,0.04), 0 2px 4px rgba(20,20,26,0.02), 0 0 0 1px var(--hairline-3);

/* Surface ladder for nested elements */
--surface: #FFFFFF;      /* card background */
--surface-2: #F8F8F3;    /* nested panels, hover states */
--surface-3: #F2F1EB;    /* deeper insets */

/* Text hierarchy */
--text-1: #14141A;   /* headings, card titles */
--text-2: #54545A;   /* body text */
--text-3: #93939A;   /* labels, descriptions, muted */

/* Semantic for destructive actions */
--error: #9B2C2C;
```

Tailwind utility mappings:
```
shadow-card       -> ghost-edge card shadow
bg-surface        -> white card background
bg-surface-2      -> nested panel background (instead of bg-muted/30)
text-text-1       -> card titles
text-text-3       -> labels, descriptions
text-error        -> destructive actions
text-[12px]       -> 12px WCAG floor
rounded-[var(--radius-card)]  -> 12px card radius
rounded-[var(--radius-input)] -> 6px input radius
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update RuleEditor Card and FieldEditor panel styling</name>
  <files>apps/web/src/app/(shell)/prospects/[prospectId]/scrape-config/components/RuleEditor.tsx</files>
  <read_first>
    - apps/web/src/app/(shell)/prospects/[prospectId]/scrape-config/components/RuleEditor.tsx
    - .planning/design/design-system-v6.md (sections 4.1 Card, 2.1 Color)
  </read_first>
  <action>
Update RuleEditor.tsx with these specific changes:

1. **Update Card wrapper to use shadow-card:**
   Line 258-259: Add shadow-card class:
   ```tsx
   <Card className={cn("shadow-card", !rule.enabled && "opacity-60")}>
   ```
   
   Import `cn` from "@/lib/utils" if not already imported.

2. **Update FieldEditor panel background:**
   Line 95: Replace `bg-muted/30` with `bg-surface-2`:
   ```tsx
   <div className="border rounded-[var(--radius-input)] p-4 bg-surface-2 space-y-3">
   ```

3. **Update Label text styling for v6:**
   Lines 99, 108, 152, 200, 304, 314, 335, 347: Labels use `text-xs` which is okay BUT ensure they have proper color:
   ```tsx
   <Label className="text-[12px] text-text-3">Field Name</Label>
   ```
   
   Apply to all Label elements with `text-xs` class.

4. **Update destructive button colors:**
   Lines 130-135 and 278-286: The destructive button uses `text-destructive`. This is fine as shadcn maps to our error color, but verify:
   ```tsx
   className="text-error hover:text-error h-8"
   ```

5. **Update Badge variant styling:**
   Line 180: Primary badge uses `variant="secondary"`, update to use v6 colors:
   ```tsx
   <Badge className="text-[12px] bg-accent-soft text-accent-ink">
     Primary
   </Badge>
   ```
   
   Line 270: Page type badge uses `variant="outline"`, keep but ensure text size:
   ```tsx
   <Badge variant="outline" className="text-[12px]">{rule.pageType}</Badge>
   ```

6. **Update muted text descriptions:**
   Line 343-344: Change `text-muted-foreground` to `text-text-3`:
   ```tsx
   <p className="text-[12px] text-text-3 mt-1">
     Use * for wildcards. Example: /products/* matches all product URLs.
   </p>
   ```

7. **Ensure all text-xs instances are at least 12px:**
   Search for `text-xs` and replace with `text-[12px]` throughout the file.
  </action>
  <verify>
    <automated>grep -E "bg-muted/30|text-xs[^0-9]" apps/web/src/app/(shell)/prospects/[prospectId]/scrape-config/components/RuleEditor.tsx || echo "PASS: No legacy patterns"</automated>
  </verify>
  <acceptance_criteria>
    - RuleEditor.tsx Card has "shadow-card" class
    - FieldEditor div uses "bg-surface-2" instead of "bg-muted/30"
    - All Label elements have "text-[12px]" and "text-text-3"
    - All text-xs replaced with text-[12px]
    - Primary Badge uses "bg-accent-soft text-accent-ink"
    - Description text uses "text-text-3"
  </acceptance_criteria>
  <done>RuleEditor uses v6 shadow-card, surface-2 for nested panels, 12px text floor</done>
</task>

<task type="auto">
  <name>Task 2: Update scrape-config page layout to v6</name>
  <files>apps/web/src/app/(shell)/prospects/[prospectId]/scrape-config/page.tsx</files>
  <read_first>
    - apps/web/src/app/(shell)/prospects/[prospectId]/scrape-config/page.tsx
    - .planning/design/design-system-v6.md (section 3.3 Content padding)
  </read_first>
  <action>
Update scrape-config/page.tsx with these specific changes:

1. **Update page container styling:**
   If using container class, ensure it follows v6 spacing:
   ```tsx
   <div className="space-y-[var(--space-6)]">
   ```

2. **Update any Card wrappers to use shadow-card:**
   Add `shadow-card` class to any Card components in the page.

3. **Update any loading states:**
   Replace any `animate-pulse` with `skeleton` class.

4. **Update muted text colors:**
   Replace `text-muted-foreground` with `text-text-3` for consistency.

5. **Update any section headers:**
   Ensure page title uses appropriate size and color:
   ```tsx
   <h1 className="text-[var(--type-h1)] text-text-1 font-display">...</h1>
   ```
   Or if using smaller heading:
   ```tsx
   <h2 className="text-[var(--type-h2)] text-text-1">...</h2>
   ```

6. **Ensure empty state styling:**
   If there's an empty state message, use:
   ```tsx
   <p className="text-text-3 text-center py-8">No extraction rules configured.</p>
   ```
  </action>
  <verify>
    <automated>grep -E "animate-pulse|text-muted-foreground" apps/web/src/app/(shell)/prospects/[prospectId]/scrape-config/page.tsx || echo "PASS: No legacy patterns"</automated>
  </verify>
  <acceptance_criteria>
    - scrape-config/page.tsx has no animate-pulse
    - Any Card components have shadow-card class
    - Muted text uses text-text-3 instead of text-muted-foreground
    - Page builds without errors
  </acceptance_criteria>
  <done>Scrape config page uses v6 spacing, text colors, and no animate-pulse</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure UI styling update - no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02 | (N/A) | UI components | accept | Styling-only changes; no security impact |
</threat_model>

<verification>
After all tasks complete:

```bash
# 1. Verify no bg-muted/30 patterns
grep -r "bg-muted/30" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/scrape-config/ && echo "FAIL" || echo "PASS: No bg-muted/30"

# 2. Verify no text-xs (should be text-[12px])
grep -r "text-xs[^0-9]" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/scrape-config/ && echo "FAIL" || echo "PASS: No text-xs"

# 3. Verify shadow-card in RuleEditor
grep "shadow-card" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/scrape-config/components/RuleEditor.tsx && echo "PASS: shadow-card present"

# 4. Verify no animate-pulse
grep -r "animate-pulse" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/scrape-config/ && echo "FAIL" || echo "PASS: No animate-pulse"

# 5. Verify v6 surface colors
grep -r "bg-surface-2" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/scrape-config/components/RuleEditor.tsx && echo "PASS: bg-surface-2 in use"
```
</verification>

<success_criteria>
1. Both scrape-config files updated with v6 patterns
2. RuleEditor Card uses shadow-card class
3. FieldEditor nested panels use bg-surface-2
4. All text-xs replaced with text-[12px] (12px floor)
5. Labels use text-text-3 for muted styling
6. No animate-pulse in any file
7. Build passes: `pnpm --filter @tevero/web build`
</success_criteria>

<output>
After completion, create `.planning/phases/52-v6-ui-compliance/52-02-SUMMARY.md`
</output>
