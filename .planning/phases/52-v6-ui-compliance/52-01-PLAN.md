---
phase: 52-v6-ui-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/KeywordTable.tsx
  - apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/ScoreWeightEditor.tsx
  - apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/TierFilter.tsx
  - apps/web/src/app/(shell)/prospects/[prospectId]/keywords/page.tsx
  - apps/web/src/app/(shell)/prospects/[prospectId]/keywords/error.tsx
  - apps/web/src/components/keywords/PositionBadge.tsx
  - apps/web/src/components/keywords/RankHistoryChart.tsx
  - apps/web/src/components/keywords/RankSparkline.tsx
autonomous: true
requirements:
  - All UI consistent before reports

must_haves:
  truths:
    - "KeywordTable uses shadow-card instead of rounded-md border"
    - "TierFilter badges use v6 semantic colors (success-soft, warning-soft, error-soft, surface-2)"
    - "ScoreWeightEditor card uses shadow-card with 12px radius"
    - "All text >= 12px (no text-xs smaller than type-tiny)"
    - "No animate-pulse in any component"
    - "Quick win icons use v6 accent colors"
  artifacts:
    - path: "apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/KeywordTable.tsx"
      provides: "v6 compliant keyword data table"
      contains: "shadow-card"
    - path: "apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/TierFilter.tsx"
      provides: "v6 compliant tier badges"
      contains: "bg-success-soft"
    - path: "apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/ScoreWeightEditor.tsx"
      provides: "v6 compliant weight editor card"
      contains: "shadow-card"
  key_links:
    - from: "KeywordTable.tsx"
      to: "TierFilter.tsx"
      via: "getTierBadge() import"
      pattern: "import.*getTierBadge.*TierFilter"
---

<objective>
Update the core keyword pipeline components (KeywordTable, ScoreWeightEditor, TierFilter) and related keyword components (PositionBadge, RankHistoryChart, RankSparkline) to v6 design system compliance.

Purpose: Ensure visual consistency with the rest of the platform before report generation phase.

Output: 8 files updated with v6 tokens, ghost-edge shadows, 12px text floor, and emerald accent color.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design/design-system-v6.md
@packages/ui/src/lib/tokens.css
@apps/web/src/app/globals.css
</context>

<interfaces>
<!-- v6 Design System Key Patterns -->

From tokens.css (available via Tailwind utilities):
```css
/* Shadow classes (use via shadow-card, shadow-lift) */
--shadow-card: 0 1px 2px rgba(20,20,26,0.04), 0 2px 4px rgba(20,20,26,0.02), 0 0 0 1px var(--hairline-3);
--shadow-lift: 0 2px 8px rgba(20,20,26,0.08), 0 4px 16px rgba(20,20,26,0.04), 0 0 0 1px var(--hairline-2);

/* Color classes (use via bg-success-soft, text-success, etc.) */
--success: #1B6E45;  --success-soft: #EAF2EE;
--error: #9B2C2C;    --error-soft: #F4E6E6;
--warning: #A87F1A;  --warning-soft: #F4EDDA;
--info: #2D5A87;     --info-soft: #EFF4F9;
--accent: #0F4F3D;   --accent-soft: #EAF1ED;

/* Text classes (use via text-text-1, text-text-2, text-text-3) */
--text-1: #14141A;   /* headings */
--text-2: #54545A;   /* body */
--text-3: #93939A;   /* labels, muted */

/* Type scale (12px floor) */
--type-tiny: 12px;   /* minimum visible text */
--type-body: 14px;   /* default body */
```

Tailwind utility mappings (from globals.css @theme inline):
```
bg-surface, bg-surface-2, bg-surface-3
bg-success-soft, bg-error-soft, bg-warning-soft, bg-info-soft
text-success, text-error, text-warning, text-info
text-text-1, text-text-2, text-text-3, text-text-4
text-accent, text-accent-ink
shadow-card, shadow-lift, shadow-pop
rounded-[var(--radius-card)] for 12px radius
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update KeywordTable to v6</name>
  <files>apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/KeywordTable.tsx</files>
  <read_first>
    - apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/KeywordTable.tsx
    - .planning/design/design-system-v6.md (sections 2.9, 8.1, 8.2)
  </read_first>
  <action>
Update KeywordTable.tsx with these specific changes:

1. **Replace border wrapper with shadow-card:**
   - Line 103: Change `<div className="rounded-md border">` to `<div className="rounded-[var(--radius-card)] shadow-card bg-surface">`

2. **Update quick win icon colors to v6 semantic colors:**
   - Line 37-51: Replace Tailwind color classes with v6 tokens:
     - `text-orange-500` -> `text-warning`
     - `text-green-500` -> `text-success`
     - `text-blue-500` -> `text-info`

3. **Update KD (keyword difficulty) colors to v6:**
   - Lines 164-170: Replace:
     - `text-green-600` -> `text-success`
     - `text-yellow-600` -> `text-warning`
     - `text-red-600` -> `text-error`

4. **Update composite score colors to v6:**
   - Lines 204-214: Replace:
     - `text-red-600` -> `text-error`
     - `text-orange-600` -> `text-warning`
     - `text-blue-600` -> `text-info`
     - `text-gray-500` -> `text-text-3`

5. **Update mapped URL link color:**
   - Line 228: Change `text-blue-600` to `text-accent`

6. **Ensure text sizes >= 12px:**
   - Line 134: `text-muted-foreground` is fine (inherits body size)
   - All TableCell content inherits --type-body (14px)
  </action>
  <verify>
    <automated>grep -E "rounded-md border|text-orange-500|text-green-500|text-blue-500|text-yellow-600|text-gray-500" apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/KeywordTable.tsx || echo "PASS: No legacy patterns found"</automated>
  </verify>
  <acceptance_criteria>
    - KeywordTable.tsx contains "shadow-card bg-surface"
    - KeywordTable.tsx contains "text-success" (not text-green-600)
    - KeywordTable.tsx contains "text-warning" (not text-yellow-600 or text-orange-600)
    - KeywordTable.tsx contains "text-error" (not text-red-600)
    - KeywordTable.tsx contains "text-accent" (not text-blue-600 for links)
    - No "rounded-md border" pattern exists
  </acceptance_criteria>
  <done>KeywordTable uses v6 shadow-card, semantic colors for all status indicators</done>
</task>

<task type="auto">
  <name>Task 2: Update TierFilter badges to v6</name>
  <files>apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/TierFilter.tsx</files>
  <read_first>
    - apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/TierFilter.tsx
    - .planning/design/design-system-v6.md (section 6.2 Effort/impact pills)
  </read_first>
  <action>
Update TierFilter.tsx with these specific changes:

1. **Replace tier badge colors with v6 semantic tokens:**
   Lines 19-23, update TIERS array colors:
   
   ```typescript
   const TIERS = [
     { id: "must_do", label: "Must-Do", color: "bg-error-soft text-error" },
     { id: "should_do", label: "Should-Do", color: "bg-warning-soft text-warning" },
     { id: "nice_to_have", label: "Nice-to-Have", color: "bg-info-soft text-info" },
     { id: "ignore", label: "Ignore", color: "bg-surface-2 text-text-3" },
   ] as const;
   ```

2. **Ensure badge text is >= 12px:**
   Line 48: Change `text-xs` to `text-[12px]` to enforce 12px floor
   
   ```tsx
   className={`inline-block px-1.5 py-0.5 rounded text-[12px] font-medium ${tier.color}`}
   ```

3. **Update count badge text size:**
   Line 52-54: Change `text-xs` to `text-[12px]`:
   ```tsx
   <span className="text-[12px] text-text-3">
     ({counts[tier.id] || 0})
   </span>
   ```

4. **Update "All" button count text size:**
   Line 37: Change `text-xs` to `text-[12px]`
  </action>
  <verify>
    <automated>grep -E "bg-red-100|bg-orange-100|bg-blue-100|bg-gray-100|dark:bg-red-900|dark:bg-orange-900" apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/TierFilter.tsx || echo "PASS: No legacy color patterns"</automated>
  </verify>
  <acceptance_criteria>
    - TierFilter.tsx contains "bg-error-soft text-error" for must_do tier
    - TierFilter.tsx contains "bg-warning-soft text-warning" for should_do tier
    - TierFilter.tsx contains "bg-info-soft text-info" for nice_to_have tier
    - TierFilter.tsx contains "bg-surface-2 text-text-3" for ignore tier
    - All text-xs replaced with text-[12px]
    - No dark mode variants (dark:bg-*) present
  </acceptance_criteria>
  <done>TierFilter badges use v6 semantic-soft backgrounds with 12px minimum text</done>
</task>

<task type="auto">
  <name>Task 3: Update ScoreWeightEditor to v6</name>
  <files>apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/ScoreWeightEditor.tsx</files>
  <read_first>
    - apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/ScoreWeightEditor.tsx
    - .planning/design/design-system-v6.md (sections 4.1 Card, 2.9 Shadows)
  </read_first>
  <action>
Update ScoreWeightEditor.tsx with these specific changes:

1. **Add shadow-card to Card wrapper:**
   Line 70: Add shadow-card class to Card
   ```tsx
   <Card className="w-80 shadow-card">
   ```

2. **Update validation feedback colors to v6:**
   Lines 100-108: Replace Tailwind colors with v6 semantic tokens:
   ```tsx
   <div
     className={`text-[12px] p-2 rounded-[var(--radius-input)] ${
       isValid
         ? "bg-success-soft text-success"
         : "bg-error-soft text-error"
     }`}
   >
   ```
   
   Remove dark mode variants (dark:bg-green-950, dark:bg-red-950).

3. **Ensure text sizes >= 12px:**
   - Line 72: CardTitle `text-sm` is fine (14px)
   - Line 82-87: The flex justify-between text-sm is fine
   - Line 86: `font-mono text-muted-foreground` inherits body size (fine)
  </action>
  <verify>
    <automated>grep -E "bg-green-50|bg-red-50|dark:bg-green-950|dark:bg-red-950" apps/web/src/app/(shell)/prospects/[prospectId]/keywords/components/ScoreWeightEditor.tsx || echo "PASS: No legacy color patterns"</automated>
  </verify>
  <acceptance_criteria>
    - ScoreWeightEditor.tsx Card has "shadow-card" class
    - Validation div uses "bg-success-soft text-success" for valid state
    - Validation div uses "bg-error-soft text-error" for invalid state
    - No dark mode color variants present
    - Validation text uses text-[12px] for 12px floor
  </acceptance_criteria>
  <done>ScoreWeightEditor uses v6 shadow-card and semantic validation colors</done>
</task>

<task type="auto">
  <name>Task 4: Update keyword page and PositionBadge to v6</name>
  <files>
    apps/web/src/app/(shell)/prospects/[prospectId]/keywords/page.tsx
    apps/web/src/app/(shell)/prospects/[prospectId]/keywords/error.tsx
    apps/web/src/components/keywords/PositionBadge.tsx
  </files>
  <read_first>
    - apps/web/src/app/(shell)/prospects/[prospectId]/keywords/page.tsx
    - apps/web/src/components/keywords/PositionBadge.tsx
    - .planning/design/design-system-v6.md (sections 6.1, 2.1)
  </read_first>
  <action>
**PositionBadge.tsx updates:**

1. **Replace position indicator colors with v6 semantic tokens:**
   Lines 40-42:
   ```tsx
   improved && "text-success border-accent-line bg-success-soft",
   declined && "text-error border-error-soft bg-error-soft",
   ```

2. **Ensure delta text >= 12px:**
   Lines 47, 53: Change `text-xs` to `text-[12px]`:
   ```tsx
   <span className="text-[12px] ml-0.5">+{change}</span>
   ```

**Keywords page.tsx updates:**

1. Scan for any non-v6 patterns (likely minimal as it primarily composes other components)
2. Ensure any loading states use `skeleton` class instead of `animate-pulse`
3. If any direct color classes exist, update to v6 tokens

**Keywords error.tsx updates:**

1. Ensure error display uses v6 error colors (bg-error-soft, text-error)
2. Update any text-xs to text-[12px]
  </action>
  <verify>
    <automated>grep -E "text-green-600|text-red-500|border-green-200|border-red-200|bg-green-50|bg-red-50" apps/web/src/components/keywords/PositionBadge.tsx || echo "PASS: No legacy colors in PositionBadge"</automated>
  </verify>
  <acceptance_criteria>
    - PositionBadge.tsx uses "text-success" and "bg-success-soft" for improved state
    - PositionBadge.tsx uses "text-error" and "bg-error-soft" for declined state
    - All text-xs in PositionBadge replaced with text-[12px]
    - No border-green-200 or border-red-200 patterns
  </acceptance_criteria>
  <done>PositionBadge and keyword page/error use v6 semantic colors with 12px text floor</done>
</task>

<task type="auto">
  <name>Task 5: Update RankHistoryChart and RankSparkline to v6</name>
  <files>
    apps/web/src/components/keywords/RankHistoryChart.tsx
    apps/web/src/components/keywords/RankSparkline.tsx
  </files>
  <read_first>
    - apps/web/src/components/keywords/RankHistoryChart.tsx
    - apps/web/src/components/keywords/RankSparkline.tsx
    - .planning/design/design-system-v6.md (sections 9.1, 9.2, 2.1)
  </read_first>
  <action>
**RankHistoryChart.tsx updates:**

1. **Update chart colors to v6 accent:**
   - Replace any blue/green chart line colors with `var(--accent)` (#0F4F3D)
   - Replace any area fills with `var(--accent-soft)` (#EAF1ED)

2. **Update axis/label text:**
   - Ensure axis labels use `var(--text-3)` for muted labels
   - Ensure values use `var(--text-1)` for emphasis

3. **Ensure loading skeleton uses shimmer:**
   - Replace any `animate-pulse` with `skeleton` class

**RankSparkline.tsx updates:**

1. **Update sparkline colors:**
   - Positive trend line: `var(--success)` (#1B6E45)
   - Negative trend line: `var(--error)` (#9B2C2C)
   - Neutral line: `var(--text-3)` (#93939A)

2. **Ensure endpoint labels >= 12px:**
   - Any text labels should use min 12px

3. **Replace any animate-pulse with skeleton shimmer**
  </action>
  <verify>
    <automated>grep -E "animate-pulse|#3b82f6|#22c55e|#ef4444" apps/web/src/components/keywords/RankHistoryChart.tsx apps/web/src/components/keywords/RankSparkline.tsx 2>/dev/null || echo "PASS: No legacy colors or animate-pulse"</automated>
  </verify>
  <acceptance_criteria>
    - RankHistoryChart.tsx uses var(--accent) or equivalent for chart lines
    - RankSparkline.tsx uses var(--success), var(--error), var(--text-3) for trend colors
    - No animate-pulse class in either file
    - No hardcoded hex colors like #3b82f6, #22c55e, #ef4444
  </acceptance_criteria>
  <done>Chart components use v6 color tokens and skeleton shimmer for loading</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure UI styling update - no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | (N/A) | UI components | accept | Styling-only changes; no security impact |
</threat_model>

<verification>
After all tasks complete:

```bash
# 1. Verify no legacy border patterns on tables/cards
grep -r "rounded-md border" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/keywords/ && echo "FAIL: Legacy border pattern found" || echo "PASS"

# 2. Verify no legacy Tailwind colors
grep -rE "text-(green|blue|red|orange|yellow|gray)-(100|200|400|500|600|800|900)" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/keywords/ apps/web/src/components/keywords/ && echo "FAIL: Legacy colors found" || echo "PASS"

# 3. Verify no dark mode color variants
grep -r "dark:bg-" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/keywords/ && echo "FAIL: Dark mode variants found" || echo "PASS"

# 4. Verify no animate-pulse
grep -r "animate-pulse" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/keywords/ apps/web/src/components/keywords/ && echo "FAIL: animate-pulse found" || echo "PASS"

# 5. Verify shadow-card usage
grep -r "shadow-card" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/keywords/components/ && echo "PASS: shadow-card in use"

# 6. Verify v6 semantic colors in use
grep -rE "bg-(success|error|warning|info)-soft" apps/web/src/app/\(shell\)/prospects/\[prospectId\]/keywords/components/ && echo "PASS: v6 semantic colors in use"
```
</verification>

<success_criteria>
1. All 8 Wave 1 files updated with v6 patterns
2. No `rounded-md border` patterns remain - replaced with `shadow-card`
3. All Tailwind color utilities replaced with v6 semantic tokens
4. No dark: mode variants (system handles via tokens)
5. No text smaller than 12px (text-xs replaced with text-[12px])
6. No animate-pulse (replaced with skeleton shimmer where needed)
7. Build passes: `pnpm --filter @tevero/web build`
</success_criteria>

<output>
After completion, create `.planning/phases/52-v6-ui-compliance/52-01-SUMMARY.md`
</output>
