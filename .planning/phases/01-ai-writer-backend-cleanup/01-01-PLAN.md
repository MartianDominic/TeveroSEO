---
phase: 01-ai-writer-backend-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AI-Writer/backend/app.py
  - AI-Writer/backend/main.py
  - AI-Writer/backend/alwrity_utils/router_manager.py
  - AI-Writer/backend/alwrity_utils/feature_registry.py
  - AI-Writer/backend/alwrity_utils/feature_profiles.py
  - AI-Writer/backend/logging_config.py
  - AI-Writer/backend/start_alwrity_backend.py
  - AI-Writer/backend/start_linkedin_service.py
autonomous: true
requirements:
  - CLEAN-06
must_haves:
  truths:
    - "Neither app.py nor main.py imports from any legacy package (blog_writer, podcast, youtube, story_writer, linkedin, linkedin_image_generation)"
    - "router_manager/feature_registry/feature_profiles registries contain no entries for legacy features"
    - "logging_config.py has no logger entries for legacy module loggers"
    - "start_alwrity_backend.py has no imports from services.story_writer"
    - "start_linkedin_service.py is deleted (obsolete launcher)"
  artifacts:
    - path: "AI-Writer/backend/app.py"
      provides: "FastAPI app factory with no legacy router registrations and no podcast-only demo mode"
    - path: "AI-Writer/backend/main.py"
      provides: "Alternate app entrypoint with no legacy router registrations"
    - path: "AI-Writer/backend/alwrity_utils/router_manager.py"
      provides: "Router registry without blog_writer/podcast/youtube/story_writer/linkedin entries"
  key_links:
    - from: "AI-Writer/backend/app.py"
      to: "alwrity_utils/router_manager.py"
      via: "get_routers() call during app init"
      pattern: "router_manager|get_routers"
---

<objective>
Remove all legacy router registrations, feature-registry entries, and logger configurations for the five deprecated services (blog_writer, podcast, youtube, story_writer, linkedin) from the FastAPI entry points and startup scripts. This isolates the legacy directories so Wave 2 can delete them without breaking app startup.

Purpose: Prepares `AI-Writer/backend/app.py` and `main.py` to import cleanly after Wave 2 deletions. Without this, `docker compose run --rm ai-writer-backend python -c "from main import app"` fails.
Output: Entry points, router registrations, feature registry, and logging config scrubbed of legacy references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

Working directory note: all paths below are relative to the TeveroSEO repo root; actual code lives under `AI-Writer/`. When running Python/pytest/docker commands, invoke them with the `AI-Writer/backend/` prefix or `cd AI-Writer/backend && ...`.

<interfaces>
<!-- Legacy import surface to be REMOVED. After this plan, none of these lines may remain. -->

From AI-Writer/backend/app.py (to be removed):
- Line 32: `if os.getenv("ALWRITY_ENABLED_FEATURES", "").strip().lower() == "podcast":` (podcast-only demo mode)
- Lines 49-60, 105-112: `is_podcast_only_demo_mode()`, `should_include_non_podcast_features()`, `PODCAST_ONLY_DEMO_MODE` constant
- Line 144: `from routers.linkedin import router as linkedin_router`
- Line 145: `from api.linkedin_image_generation import router as linkedin_image_router`
- All `if not is_podcast_only_demo_mode():` and `if PODCAST_ONLY_DEMO_MODE` branches (unwrap to keep the NON-podcast path, i.e. keep code inside `if not is_podcast_only_demo_mode():` blocks, delete the `else`/`if is_podcast_only_demo_mode` branches)
- Any `podcast_only_demo_mode` field inserted into health response dicts (lines ~349, 366, 398, 417-424)

From AI-Writer/backend/main.py (to be removed):
- Line 72: `from routers.linkedin import router as linkedin_router` and its include_router call
- Line 74: `from api.linkedin_image_generation import router as linkedin_image_router` and its include_router call
- Lines 403-404: `from api.podcast.router import router as podcast_router; app.include_router(podcast_router)`
- Lines 407-408: `from api.youtube.router import router as youtube_router; app.include_router(youtube_router, prefix="/api")`

From AI-Writer/backend/alwrity_utils/router_manager.py (entries to DELETE from registry list):
- Line 28: linkedin
- Line 29: linkedin_image
- Line 45: blog_writer
- Line 46: story_writer
- Line 48: blog_seo_analysis
- Line 59: podcast
- Line 60: youtube
- Line 17: also remove "podcast", "blog-writer", "youtube" feature tags from `subscription` router entry
- Line 19: remove "podcast" feature tag from `step4_assets` router entry

From AI-Writer/backend/alwrity_utils/feature_registry.py:
- Lines 39-45: DELETE FeatureGroup entries for "podcast" and "youtube"
- Lines 60-61: DELETE keys "podcast" and "youtube" from the feature-tag alias map

From AI-Writer/backend/alwrity_utils/feature_profiles.py:
- Line 48: comment mentions podcast — update comment to remove legacy feature names (cosmetic; acceptable as comment but safer to drop the example)

From AI-Writer/backend/logging_config.py:
- Line 126: `'linkedin_persona_service',` — KEEP (this is the persona module, not legacy linkedin)
- Line 148: `'linkedin_service',` — DELETE
- Line 151: `'linkedin_prompt_generator',` — DELETE
- Line 152: `'linkedin_image_storage',` — DELETE
- Line 161: `'linkedin_image_generator',` — DELETE
- Line 192: `or "services.story_writer.video_generation_service" in name` — DELETE (drop that `or` clause)

From AI-Writer/backend/start_alwrity_backend.py:
- Line 33: remove "linkedin" and "blog_writer" from LINGUISTIC_REQUIRED_FEATURES (keep content_planning, strategy_copilot, facebook, persona)
- Line 61: remove `if enabled_features == {"podcast"}:` early-return branch
- Lines 66-67: remove "podcast" and "youtube" from feature-name alias dict
- Line 73: remove "linkedin" and "blog-writer" from linguistic_features set
- Lines 88, 290-295, 388-392: remove all podcast-only mode branching
- Lines 363-387: remove the `from services.story_writer.video_preflight import (...)` block and its preflight call entirely

From AI-Writer/backend/start_linkedin_service.py:
- DELETE the entire file (it is a standalone launcher for the removed linkedin service)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip legacy router imports and podcast-only demo mode from app.py and main.py</name>
  <files>AI-Writer/backend/app.py, AI-Writer/backend/main.py</files>
  <read_first>
    - AI-Writer/backend/app.py (entire file — 853 lines)
    - AI-Writer/backend/main.py (entire file — 479 lines)
  </read_first>
  <action>
    In AI-Writer/backend/app.py:
    1. DELETE the early-exit block at lines ~30-34 that sets LOG_LEVEL=WARNING based on `ALWRITY_ENABLED_FEATURES == "podcast"`.
    2. DELETE the functions `is_podcast_only_demo_mode()` (lines ~49-60) and `should_include_non_podcast_features()` (lines ~105-112).
    3. DELETE the module-level constant `PODCAST_ONLY_DEMO_MODE = is_podcast_only_demo_mode()` (appears twice, lines ~60 and ~112).
    4. For every `if not is_podcast_only_demo_mode():` block: REMOVE the guard (dedent the body — keep the code inside).
    5. For every `if is_podcast_only_demo_mode():` or `if PODCAST_ONLY_DEMO_MODE:` block: DELETE the entire block (we are no longer in podcast mode).
    6. DELETE lines ~144-145: `from routers.linkedin import router as linkedin_router` and `from api.linkedin_image_generation import router as linkedin_image_router` along with their `app.include_router(...)` calls (inspect following lines to find the include_router calls and remove them too).
    7. DELETE any field insertion of `"podcast_only_demo_mode"` into health-response dicts (lines ~349, 366, 398, 417-424). Remove the entire dict key-value lines.
    8. Verify no `podcast_only_demo_mode`, `is_podcast_only_demo_mode`, `PODCAST_ONLY_DEMO_MODE`, `should_include_non_podcast_features`, `linkedin_router`, `linkedin_image_router` identifiers remain.

    In AI-Writer/backend/main.py:
    1. DELETE line 72: `from routers.linkedin import router as linkedin_router` AND its corresponding `app.include_router(linkedin_router...)` call (scan following lines).
    2. DELETE line 74: `from api.linkedin_image_generation import router as linkedin_image_router` AND its corresponding `app.include_router(linkedin_image_router...)` call.
    3. DELETE lines 403-404: `from api.podcast.router import router as podcast_router` AND `app.include_router(podcast_router)`.
    4. DELETE lines 407-408: `from api.youtube.router import router as youtube_router` AND `app.include_router(youtube_router, prefix="/api")`.

    After edits, run both files through `python -m py_compile` to catch syntax errors.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && python -m py_compile app.py main.py && grep -En "blog_writer|podcast|youtube|story_writer|linkedin" app.py main.py | grep -vE "^[^:]+:[0-9]+:\s*#" | grep -vE "linkedin_persona|LinkedInPersona"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "podcast_only_demo_mode|PODCAST_ONLY_DEMO_MODE|is_podcast_only_demo_mode|should_include_non_podcast_features" AI-Writer/backend/app.py` returns `0`
    - `grep -cE "^from routers\.linkedin|^from api\.linkedin_image_generation|^from api\.podcast|^from api\.youtube|^from api\.blog_writer|^from api\.story_writer" AI-Writer/backend/app.py` returns `0`
    - `grep -cE "^from routers\.linkedin|^from api\.linkedin_image_generation|^from api\.podcast|^from api\.youtube|^from api\.blog_writer|^from api\.story_writer" AI-Writer/backend/main.py` returns `0`
    - `grep -cE "include_router\(\s*(linkedin_router|linkedin_image_router|podcast_router|youtube_router|blog_writer_router|story_writer_router)" AI-Writer/backend/app.py AI-Writer/backend/main.py` returns `0`
    - `python -m py_compile AI-Writer/backend/app.py AI-Writer/backend/main.py` exits 0
  </acceptance_criteria>
  <done>Both app.py and main.py compile; zero legacy router imports/registrations remain; podcast-only demo mode fully excised from app.py.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub legacy entries from router_manager, feature_registry, feature_profiles, and logging_config</name>
  <files>AI-Writer/backend/alwrity_utils/router_manager.py, AI-Writer/backend/alwrity_utils/feature_registry.py, AI-Writer/backend/alwrity_utils/feature_profiles.py, AI-Writer/backend/logging_config.py</files>
  <read_first>
    - AI-Writer/backend/alwrity_utils/router_manager.py
    - AI-Writer/backend/alwrity_utils/feature_registry.py
    - AI-Writer/backend/alwrity_utils/feature_profiles.py
    - AI-Writer/backend/logging_config.py
  </read_first>
  <action>
    In AI-Writer/backend/alwrity_utils/router_manager.py (229 lines total):
    1. DELETE registry entries for names: "linkedin" (line 28), "linkedin_image" (line 29), "blog_writer" (line 45), "story_writer" (line 46), "blog_seo_analysis" (line 48), "podcast" (line 59), "youtube" (line 60). Delete the entire dict literal for each entry including trailing comma.
    2. On the remaining `subscription` entry (line 17), edit its `"features"` set from `{"all", "core", "podcast", "blog-writer", "youtube"}` to `{"all", "core"}`.
    3. On the remaining `step4_assets` entry (line 19), edit its `"features"` set from `{"all", "core", "podcast"}` to `{"all", "core"}`.
    4. In the docstring (lines ~90-91), remove the example values "podcast,blog-writer,youtube" and "podcast" (replace with e.g. "core" or drop the examples).

    In AI-Writer/backend/alwrity_utils/feature_registry.py:
    1. DELETE the entire FeatureGroup literal for "podcast" (lines 39-41 approximately — the key "podcast" and its FeatureGroup(features=..., routers=...) value).
    2. DELETE the entire FeatureGroup literal for "youtube" (lines 43-45 approximately).
    3. DELETE dict entries `"podcast": ("core", "podcast"),` and `"youtube": ("core", "youtube"),` (lines 60-61).

    In AI-Writer/backend/alwrity_utils/feature_profiles.py:
    1. On line 48, update the example comment from `Supports comma-separated feature names, e.g. \`podcast,core\`.` to `Supports comma-separated feature names, e.g. \`core\`.` (drop legacy example).

    In AI-Writer/backend/logging_config.py (do NOT delete `linkedin_persona_service` — that is a separate live module):
    1. DELETE line 148: `'linkedin_service',`
    2. DELETE line 151: `'linkedin_prompt_generator',`
    3. DELETE line 152: `'linkedin_image_storage',`
    4. DELETE line 161: `'linkedin_image_generator',`
    5. On line 192, remove the boolean clause `or "services.story_writer.video_generation_service" in name` so that the surrounding `if` or `return` expression keeps its other clauses but drops that one.

    After edits, compile-check all four files.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && python -m py_compile alwrity_utils/router_manager.py alwrity_utils/feature_registry.py alwrity_utils/feature_profiles.py logging_config.py && grep -cE "api\.(blog_writer|podcast|youtube|story_writer)|routers\.linkedin|api\.linkedin_image_generation" alwrity_utils/router_manager.py alwrity_utils/feature_registry.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\"name\":\s*\"(linkedin|linkedin_image|blog_writer|story_writer|blog_seo_analysis|podcast|youtube)\"" AI-Writer/backend/alwrity_utils/router_manager.py` returns `0`
    - `grep -cE "\"(podcast|youtube|blog-writer)\"" AI-Writer/backend/alwrity_utils/router_manager.py` returns `0`
    - `grep -cE "^\s*\"(podcast|youtube)\"\s*:\s*FeatureGroup" AI-Writer/backend/alwrity_utils/feature_registry.py` returns `0`
    - `grep -cE "^\s*\"(podcast|youtube)\"\s*:\s*\(\"core\"" AI-Writer/backend/alwrity_utils/feature_registry.py` returns `0`
    - `grep -cE "^\s*'linkedin_service'|^\s*'linkedin_prompt_generator'|^\s*'linkedin_image_storage'|^\s*'linkedin_image_generator'" AI-Writer/backend/logging_config.py` returns `0`
    - `grep -cE "services\.story_writer\.video_generation_service" AI-Writer/backend/logging_config.py` returns `0`
    - `grep -cE "linkedin_persona_service" AI-Writer/backend/logging_config.py` returns `1` (KEPT — separate module)
    - `python -m py_compile AI-Writer/backend/alwrity_utils/router_manager.py AI-Writer/backend/alwrity_utils/feature_registry.py AI-Writer/backend/alwrity_utils/feature_profiles.py AI-Writer/backend/logging_config.py` exits 0
  </acceptance_criteria>
  <done>All four files compile; legacy registry/logger entries removed; persona linkedin module preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Clean startup scripts (start_alwrity_backend.py edit, start_linkedin_service.py delete)</name>
  <files>AI-Writer/backend/start_alwrity_backend.py, AI-Writer/backend/start_linkedin_service.py</files>
  <read_first>
    - AI-Writer/backend/start_alwrity_backend.py
    - AI-Writer/backend/start_linkedin_service.py
  </read_first>
  <action>
    In AI-Writer/backend/start_alwrity_backend.py:
    1. Line 33: change `LINGUISTIC_REQUIRED_FEATURES = {"content_planning", "strategy_copilot", "facebook", "linkedin", "blog_writer", "persona"}` to `LINGUISTIC_REQUIRED_FEATURES = {"content_planning", "strategy_copilot", "facebook", "persona"}` (remove "linkedin" and "blog_writer").
    2. Lines 41-42: in the docstring examples, remove references to "podcast,blog-writer,youtube" and "podcast" feature names (replace example with "core" or drop examples).
    3. Lines 61-63: DELETE the early-return branch `if enabled_features == {"podcast"}: ...` in full (including its body).
    4. Lines 66-67: DELETE dict entries `"podcast": "podcast",` and `"youtube": "youtube",`.
    5. Line 73: change `linguistic_features = {"content_planning", "facebook", "linkedin", "blog-writer", "persona"}` to `linguistic_features = {"content_planning", "facebook", "persona"}`.
    6. Lines 80-88: review the SIF/Story Writer skip block — since story_writer is gone, DELETE the entire block and any comment/logic about skipping video preflight for podcast mode.
    7. Lines 290-295: DELETE the entire `podcast_only_demo_mode` detection block (env-var read + the `if podcast_only_demo_mode:` branch that skips non-podcast routers).
    8. Lines 363-387: DELETE the entire `from services.story_writer.video_preflight import (...)` import and the preflight invocation it gates. If there's an enclosing `try/except` that only wraps this import, remove that too.
    9. Lines 388-392: DELETE the `is_podcast = os.getenv("ALWRITY_ENABLED_FEATURES", ...).strip().lower() == "podcast"` variable and the `if is_podcast:` branch.
    10. Compile-check.

    For AI-Writer/backend/start_linkedin_service.py:
    1. DELETE the file entirely (standalone launcher for the removed linkedin service).
    2. Use `git rm AI-Writer/backend/start_linkedin_service.py` if the repo is git-tracked, otherwise `rm AI-Writer/backend/start_linkedin_service.py`.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && python -m py_compile start_alwrity_backend.py && test ! -f start_linkedin_service.py && grep -cE "blog_writer|podcast|youtube|story_writer|linkedin" start_alwrity_backend.py</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f AI-Writer/backend/start_linkedin_service.py` succeeds (file is gone)
    - `grep -cE "services\.story_writer|podcast_only_demo_mode|PODCAST_ONLY_DEMO_MODE" AI-Writer/backend/start_alwrity_backend.py` returns `0`
    - `grep -cE "\"(podcast|youtube|blog-writer|blog_writer|linkedin)\"" AI-Writer/backend/start_alwrity_backend.py` returns `0`
    - `grep -cE "LINGUISTIC_REQUIRED_FEATURES\s*=" AI-Writer/backend/start_alwrity_backend.py` returns `1` and the line does NOT contain "linkedin" or "blog_writer"
    - `python -m py_compile AI-Writer/backend/start_alwrity_backend.py` exits 0
  </acceptance_criteria>
  <done>start_alwrity_backend.py has no legacy-feature references or story_writer imports; start_linkedin_service.py deleted.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `python -m py_compile AI-Writer/backend/app.py AI-Writer/backend/main.py AI-Writer/backend/alwrity_utils/router_manager.py AI-Writer/backend/alwrity_utils/feature_registry.py AI-Writer/backend/alwrity_utils/feature_profiles.py AI-Writer/backend/logging_config.py AI-Writer/backend/start_alwrity_backend.py` exits 0.
- None of the modified files import from `routers.linkedin`, `api.linkedin_image_generation`, `api.podcast`, `api.youtube`, `api.blog_writer`, `api.story_writer`, or `services.story_writer`.
- `start_linkedin_service.py` is removed from the filesystem.
</verification>

<success_criteria>
- [ ] All seven modified files compile with `python -m py_compile`.
- [ ] Zero `import` or `from X import` statements target `routers.linkedin`, `api.linkedin_image_generation`, `api.podcast`, `api.youtube`, `api.blog_writer`, `api.story_writer`, `services.story_writer`, `services.blog_writer`, `services.linkedin`, `services.podcast` in these files.
- [ ] Podcast-only demo mode branching fully removed from app.py and start_alwrity_backend.py.
- [ ] `start_linkedin_service.py` deleted.
- [ ] `services/persona/linkedin/linkedin_persona_service.py` and the `linkedin_persona_service` logger entry are UNTOUCHED (separate live module).
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-writer-backend-cleanup/01-01-SUMMARY.md` documenting exact line-range deletions and any surprises found in app.py's podcast-mode branching.
</output>
