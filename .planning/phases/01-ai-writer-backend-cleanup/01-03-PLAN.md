---
phase: 01-ai-writer-backend-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - AI-Writer/backend/scripts/run_podcast_billing_sequence.py
  - AI-Writer/backend/scripts/create_podcast_tables.py
  - AI-Writer/backend/scripts/smoke_test_podcast_demo.py
  - AI-Writer/backend/scripts/verify_podcast_table.py
  - AI-Writer/backend/scripts/create_story_project_tables.py
  - AI-Writer/backend/models/linkedin_models.py
  - AI-Writer/backend/models/podcast_models.py
  - AI-Writer/backend/models/podcast_bible_models.py
  - AI-Writer/backend/models/story_models.py
  - AI-Writer/backend/models/story_project_models.py
  - AI-Writer/backend/services/database.py
  - AI-Writer/backend/tests/test_clients.py
  - AI-Writer/backend/tests/test_no_import_time_mkdir.py
autonomous: true
requirements:
  - CLEAN-06
  - CLEAN-07
must_haves:
  truths:
    - "All podcast-only and story-only one-off scripts under backend/scripts/ are deleted"
    - "Legacy model files (linkedin_models, podcast_models, podcast_bible_models, story_models, story_project_models) are deleted"
    - "services/database.py no longer imports PodcastProject from the deleted models.podcast_models"
    - "Test fixtures no longer set ALWRITY_ENABLED_FEATURES=podcast and no longer reference youtube handler paths"
    - "models/blog_models.py is PRESERVED (still imported by non-legacy research engine + parameter_optimizer)"
  artifacts:
    - path: "AI-Writer/backend/services/database.py"
      provides: "Database module without legacy model imports"
    - path: "AI-Writer/backend/tests/test_clients.py"
      provides: "Client test fixture without podcast-mode env hack"
  key_links:
    - from: "AI-Writer/backend/services/database.py"
      to: "SQLAlchemy Base / ORM registry"
      via: "remove PodcastProject registration"
      pattern: "from models\\.podcast_models"
---

<objective>
Delete all legacy one-off scripts and model files whose only consumers are the five legacy packages slated for deletion, and update two non-legacy files that still reference them (services/database.py and test fixtures). `models/blog_models.py` is explicitly KEPT because non-legacy code (research_engine, parameter_optimizer) imports its `ResearchSource`, `ResearchConfig`, `ResearchProvider`, and `ResearchMode` types.

Purpose: Removes remaining stale callers so the Wave 2 directory deletions cannot leave dangling imports.
Output: 5 scripts deleted, 5 model files deleted, services/database.py edited, 2 test files edited.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Files to DELETE: -->
- AI-Writer/backend/scripts/run_podcast_billing_sequence.py (imports api.podcast.*)
- AI-Writer/backend/scripts/create_podcast_tables.py (imports models.podcast_models)
- AI-Writer/backend/scripts/smoke_test_podcast_demo.py (podcast demo harness)
- AI-Writer/backend/scripts/verify_podcast_table.py (podcast schema verifier)
- AI-Writer/backend/scripts/create_story_project_tables.py (imports models.story_project_models)
- AI-Writer/backend/models/linkedin_models.py (only consumed by deleted routers/linkedin.py and deleted services/linkedin_service.py)
- AI-Writer/backend/models/podcast_models.py (consumed by services/database.py — fix that import first)
- AI-Writer/backend/models/podcast_bible_models.py (legacy only)
- AI-Writer/backend/models/story_models.py (legacy only)
- AI-Writer/backend/models/story_project_models.py (legacy only)

<!-- Files to EDIT: -->
- AI-Writer/backend/services/database.py (line 32: `from models.podcast_models import PodcastProject`)
- AI-Writer/backend/tests/test_clients.py (line 42-44: ALWRITY_ENABLED_FEATURES=podcast hack)
- AI-Writer/backend/tests/test_no_import_time_mkdir.py (lines 14-17: youtube handler path constants)

<!-- Files to PRESERVE (explicitly NOT in scope): -->
- AI-Writer/backend/models/blog_models.py (KEEP — non-legacy consumers: research_engine.py line 40 & 465, parameter_optimizer.py line 31)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy scripts and fix services/database.py</name>
  <files>AI-Writer/backend/scripts/run_podcast_billing_sequence.py, AI-Writer/backend/scripts/create_podcast_tables.py, AI-Writer/backend/scripts/smoke_test_podcast_demo.py, AI-Writer/backend/scripts/verify_podcast_table.py, AI-Writer/backend/scripts/create_story_project_tables.py, AI-Writer/backend/services/database.py</files>
  <read_first>
    - AI-Writer/backend/scripts/run_podcast_billing_sequence.py (confirm all imports are api.podcast.*)
    - AI-Writer/backend/scripts/create_podcast_tables.py
    - AI-Writer/backend/scripts/smoke_test_podcast_demo.py
    - AI-Writer/backend/scripts/verify_podcast_table.py
    - AI-Writer/backend/scripts/create_story_project_tables.py
    - AI-Writer/backend/services/database.py (full file — to understand if PodcastProject is referenced anywhere beyond the import line, and to ensure removing only the import line leaves the file syntactically valid)
    - Output of: `grep -rn "PodcastProject" AI-Writer/backend/services/ --include="*.py" | grep -v __pycache__` (must show ONLY the database.py line 32 outside legacy dirs; if there are others, also update them)
  </read_first>
  <action>
    1. Delete the five scripts (use `git rm` if repo is git-tracked, else `rm`):
       - `AI-Writer/backend/scripts/run_podcast_billing_sequence.py`
       - `AI-Writer/backend/scripts/create_podcast_tables.py`
       - `AI-Writer/backend/scripts/smoke_test_podcast_demo.py`
       - `AI-Writer/backend/scripts/verify_podcast_table.py`
       - `AI-Writer/backend/scripts/create_story_project_tables.py`

    2. In AI-Writer/backend/services/database.py:
       - DELETE line 32 exactly: `from models.podcast_models import PodcastProject`.
       - Scan the rest of database.py for any other reference to `PodcastProject` or `podcast_models`. If there are none, you're done. If there are (e.g. a `PodcastProject.__table__` registration), DELETE those lines too.
       - `python -m py_compile AI-Writer/backend/services/database.py` must exit 0.

    3. Also verify that no other non-legacy script under `AI-Writer/backend/scripts/` imports from the five legacy packages. Run:
       `grep -rEl "services\.blog_writer|services\.podcast\b|services\.youtube|services\.story_writer|services\.linkedin[^_]|api\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)|routers\.linkedin\b|models\.(linkedin_models|podcast_models|podcast_bible_models|story_models|story_project_models)" AI-Writer/backend/scripts/ --include="*.py" 2>/dev/null | grep -v __pycache__`
       Must return nothing after the five deletions above. If it returns any file, open it and either delete the file (if legacy-only) or delete the specific imports.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && test ! -f scripts/run_podcast_billing_sequence.py && test ! -f scripts/create_podcast_tables.py && test ! -f scripts/smoke_test_podcast_demo.py && test ! -f scripts/verify_podcast_table.py && test ! -f scripts/create_story_project_tables.py && python -m py_compile services/database.py && ! grep -E "from models\.podcast_models|PodcastProject" services/database.py</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f AI-Writer/backend/scripts/run_podcast_billing_sequence.py` succeeds
    - `test ! -f AI-Writer/backend/scripts/create_podcast_tables.py` succeeds
    - `test ! -f AI-Writer/backend/scripts/smoke_test_podcast_demo.py` succeeds
    - `test ! -f AI-Writer/backend/scripts/verify_podcast_table.py` succeeds
    - `test ! -f AI-Writer/backend/scripts/create_story_project_tables.py` succeeds
    - `grep -c "from models\.podcast_models\|PodcastProject" AI-Writer/backend/services/database.py` returns `0`
    - `python -m py_compile AI-Writer/backend/services/database.py` exits 0
    - `grep -rEl "services\.blog_writer|services\.podcast\b|services\.youtube|services\.story_writer|services\.linkedin[^_]|api\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)|routers\.linkedin\b|models\.(linkedin_models|podcast_models|podcast_bible_models|story_models|story_project_models)" AI-Writer/backend/scripts/ --include="*.py" 2>/dev/null | grep -v __pycache__` returns NOTHING.
  </acceptance_criteria>
  <done>Five legacy scripts deleted; services/database.py no longer imports PodcastProject; database.py compiles; no remaining script imports any legacy module.</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy model files (preserve blog_models.py)</name>
  <files>AI-Writer/backend/models/linkedin_models.py, AI-Writer/backend/models/podcast_models.py, AI-Writer/backend/models/podcast_bible_models.py, AI-Writer/backend/models/story_models.py, AI-Writer/backend/models/story_project_models.py</files>
  <read_first>
    - Output of: `grep -rn "from models.linkedin_models\|import linkedin_models" AI-Writer/backend/ --include="*.py" | grep -v __pycache__ | grep -v "backend/services/linkedin\|backend/services/blog_writer\|backend/services/podcast\|backend/services/youtube\|backend/services/story_writer\|backend/api/linkedin_image_generation\|backend/api/podcast\|backend/api/youtube\|backend/api/story_writer\|backend/api/blog_writer"`
    - Same grep pattern for `models.podcast_models`, `models.podcast_bible_models`, `models.story_models`, `models.story_project_models`
    - Each grep must return EMPTY (confirming no non-legacy consumers remain after Plans 01, 02, and Task 1 of Plan 03). If any non-legacy consumer remains, stop and clean that consumer first.
    - AI-Writer/backend/models/blog_models.py (the first 50 lines — just to confirm its existence; it is KEPT, not read for deletion)
  </read_first>
  <action>
    1. Run the grep checks listed in read_first. Each must return empty. If any returns a file outside the legacy directories, read that file, delete the offending import (and any code that depends on the imported symbol), and re-run the grep until it is empty.

    2. Delete the five model files (git rm preferred):
       - `AI-Writer/backend/models/linkedin_models.py`
       - `AI-Writer/backend/models/podcast_models.py`
       - `AI-Writer/backend/models/podcast_bible_models.py`
       - `AI-Writer/backend/models/story_models.py`
       - `AI-Writer/backend/models/story_project_models.py`

    3. DO NOT delete `AI-Writer/backend/models/blog_models.py`. It exports `ResearchSource`, `ResearchConfig`, `ResearchProvider`, and `ResearchMode`, which are imported by non-legacy code:
       - `services/research/core/research_engine.py` lines 40 and 465
       - `services/research/core/parameter_optimizer.py` line 31
       Verify at end of task: `grep -l "ResearchSource\|ResearchConfig\|ResearchProvider\|ResearchMode" AI-Writer/backend/models/blog_models.py` returns the file path (it still exists).

    4. If `AI-Writer/backend/models/__init__.py` re-exports any deleted symbol, edit it to drop those exports. Compile-check `python -m py_compile AI-Writer/backend/models/__init__.py`.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && test ! -f models/linkedin_models.py && test ! -f models/podcast_models.py && test ! -f models/podcast_bible_models.py && test ! -f models/story_models.py && test ! -f models/story_project_models.py && test -f models/blog_models.py && python -m py_compile models/__init__.py models/blog_models.py</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f AI-Writer/backend/models/linkedin_models.py` succeeds
    - `test ! -f AI-Writer/backend/models/podcast_models.py` succeeds
    - `test ! -f AI-Writer/backend/models/podcast_bible_models.py` succeeds
    - `test ! -f AI-Writer/backend/models/story_models.py` succeeds
    - `test ! -f AI-Writer/backend/models/story_project_models.py` succeeds
    - `test -f AI-Writer/backend/models/blog_models.py` succeeds (PRESERVED)
    - `grep -rEl "from models\.(linkedin_models|podcast_models|podcast_bible_models|story_models|story_project_models)|import (linkedin_models|podcast_models|podcast_bible_models|story_models|story_project_models)" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep -vE "backend/(services/(blog_writer|podcast|youtube|story_writer|linkedin)|api/(blog_writer|podcast|youtube|story_writer|linkedin_image_generation))"` returns NOTHING.
    - `python -m py_compile AI-Writer/backend/models/__init__.py AI-Writer/backend/models/blog_models.py` exits 0.
  </acceptance_criteria>
  <done>Five legacy model files deleted; blog_models.py preserved; models/__init__.py compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Update test fixtures to drop podcast/youtube references</name>
  <files>AI-Writer/backend/tests/test_clients.py, AI-Writer/backend/tests/test_no_import_time_mkdir.py</files>
  <read_first>
    - AI-Writer/backend/tests/test_clients.py (full file — line 42-44 area with ALWRITY_ENABLED_FEATURES)
    - AI-Writer/backend/tests/test_no_import_time_mkdir.py (full file — lines 14-17 with youtube handler paths, plus understand the test's overall intent)
  </read_first>
  <action>
    In AI-Writer/backend/tests/test_clients.py:
    1. The file currently sets `os.environ.setdefault("ALWRITY_ENABLED_FEATURES", "podcast")` (line ~44) with a comment about preventing heavy onboarding chain. Since podcast-only mode has been removed in Plan 01, this hack no longer applies.
    2. DELETE lines 42-44 (the comment about podcast mode + the `os.environ.setdefault` call). If alternative env setup is needed to keep onboarding chain fast, replace with: `os.environ.setdefault("ALWRITY_ENABLED_FEATURES", "core")` — but only if setting "core" actually accomplishes the same isolation. Otherwise just delete the three lines and rely on default test environment.
    3. Run `python -m py_compile AI-Writer/backend/tests/test_clients.py`.

    In AI-Writer/backend/tests/test_no_import_time_mkdir.py:
    1. The file has a constant list (around lines 14-17) that includes `"backend/api/youtube/router.py"`, `"backend/api/youtube/handlers/avatar.py"`, `"backend/api/youtube/handlers/images.py"`, `"backend/api/youtube/handlers/audio.py"`. These paths point to files that Wave 2 will delete.
    2. DELETE those four list entries (plus any trailing comma on the previous kept entry so the list stays syntactically valid).
    3. Scan the same file for any additional entries matching `backend/api/(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)` or `backend/services/(blog_writer|podcast|youtube|story_writer|linkedin)/`. DELETE those entries too.
    4. Run `python -m py_compile AI-Writer/backend/tests/test_no_import_time_mkdir.py`.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && python -m py_compile tests/test_clients.py tests/test_no_import_time_mkdir.py && ! grep -E "ALWRITY_ENABLED_FEATURES.*podcast" tests/test_clients.py && ! grep -E "backend/api/(blog_writer|podcast|youtube|story_writer)|backend/services/(blog_writer|podcast|youtube|story_writer|linkedin)/" tests/test_no_import_time_mkdir.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ALWRITY_ENABLED_FEATURES.*"podcast"' AI-Writer/backend/tests/test_clients.py` returns `0`.
    - `grep -cE "backend/api/(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)|backend/services/(blog_writer|podcast|youtube|story_writer|linkedin)/" AI-Writer/backend/tests/test_no_import_time_mkdir.py` returns `0`.
    - `python -m py_compile AI-Writer/backend/tests/test_clients.py AI-Writer/backend/tests/test_no_import_time_mkdir.py` exits 0.
  </acceptance_criteria>
  <done>test_clients.py no longer sets podcast-only env; test_no_import_time_mkdir.py no longer references legacy paths.</done>
</task>

</tasks>

<verification>
After all three tasks, both these commands must return empty (no matches):
- `grep -rEl "from models\.(linkedin_models|podcast_models|podcast_bible_models|story_models|story_project_models)" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep -vE "backend/(services/(blog_writer|podcast|youtube|story_writer|linkedin)|api/(blog_writer|podcast|youtube|story_writer|linkedin_image_generation))"`
- `grep -rEl "services\.blog_writer|services\.podcast\b|services\.youtube|services\.story_writer|services\.linkedin[^_]|api\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)|routers\.linkedin\b" AI-Writer/backend/scripts/ --include="*.py" 2>/dev/null | grep -v __pycache__`

All modified files must compile with `python -m py_compile`.
</verification>

<success_criteria>
- [ ] Five scripts deleted.
- [ ] Five legacy model files deleted; blog_models.py PRESERVED.
- [ ] services/database.py no longer imports from legacy models; compiles.
- [ ] test_clients.py and test_no_import_time_mkdir.py updated; both compile.
- [ ] No non-legacy file imports any deleted model or module.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-writer-backend-cleanup/01-03-SUMMARY.md` listing every file deleted, every file edited with exact line numbers, and noting that `models/blog_models.py` was intentionally kept (with the two consumer files that required it).
</output>
