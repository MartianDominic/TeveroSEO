---
phase: 01-ai-writer-backend-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AI-Writer/backend/routers/linkedin.py
  - AI-Writer/backend/routers/video_studio/tasks/avatar_generation.py
  - AI-Writer/backend/routers/video_studio/tasks/video_generation.py
  - AI-Writer/backend/routers/video_studio/endpoints/avatar.py
  - AI-Writer/backend/routers/video_studio/endpoints/tasks.py
  - AI-Writer/backend/routers/video_studio/endpoints/create.py
  - AI-Writer/backend/routers/video_studio/endpoints/social.py
  - AI-Writer/backend/services/linkedin_service.py
  - AI-Writer/backend/services/podcast_service.py
  - AI-Writer/backend/services/podcast_bible_service.py
  - AI-Writer/backend/services/product_marketing/product_avatar_service.py
  - AI-Writer/backend/services/research/core/research_engine.py
  - AI-Writer/backend/services/llm_providers/gemini_provider.py
  - AI-Writer/backend/services/llm_providers/gemini_grounded_provider.py
  - AI-Writer/backend/services/llm_providers/_exceptions.py
  - AI-Writer/backend/services/llm_providers/_retry.py
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05
must_haves:
  truths:
    - "No non-legacy service/router file imports from services.blog_writer, services.podcast, services.youtube, services.story_writer, or services.linkedin (the content generation module — not persona.linkedin)"
    - "Top-level wrapper service modules (linkedin_service.py, podcast_service.py, podcast_bible_service.py) are deleted"
    - "routers/linkedin.py and the story-writer-dependent video_studio task files are deleted; remaining video_studio endpoints compile"
    - "research_engine.py, gemini_provider.py, and gemini_grounded_provider.py no longer reference services.blog_writer submodules"
  artifacts:
    - path: "AI-Writer/backend/services/llm_providers/_exceptions.py"
      provides: "Local exception classes (APIRateLimitException, APITimeoutException, ValidationException, ContentGenerationException, ResearchFailedException)"
    - path: "AI-Writer/backend/services/llm_providers/_retry.py"
      provides: "Local retry_with_backoff + CONTENT_RETRY_CONFIG + RESEARCH_RETRY_CONFIG"
    - path: "AI-Writer/backend/services/research/core/research_engine.py"
      provides: "Research engine with blog_writer provider imports removed (code paths raise NotImplementedError or deleted)"
  key_links:
    - from: "AI-Writer/backend/services/llm_providers/gemini_provider.py"
      to: "AI-Writer/backend/services/llm_providers/_exceptions.py"
      via: "relative-ish import path services.llm_providers._exceptions"
      pattern: "from services\\.llm_providers\\._exceptions import"
---

<objective>
Unwind all cross-module imports from non-legacy files into the five legacy packages. Delete top-level wrapper services that only existed to bridge to legacy code, delete the LinkedIn and story-writer-dependent video_studio routers, inline the small set of exception classes and retry utilities that gemini providers consume from `services.blog_writer`, and excise `services.blog_writer.research.*` usage from `research_engine.py`. After this plan, no live code path still reaches into legacy directories.

Purpose: Isolates the legacy directories so Wave 2 (deletion) will not break any remaining import.
Output: 6 files deleted outright; 2 new files created (_exceptions.py, _retry.py); 8 files edited to drop or replace legacy imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

Working-directory note: execute all commands from the TeveroSEO repo root; code lives under `AI-Writer/`.

<interfaces>
<!-- Files to DELETE entirely (no replacement — these wrapped now-removed services): -->
- AI-Writer/backend/services/linkedin_service.py (wrapper around services.linkedin.*)
- AI-Writer/backend/services/podcast_service.py (wrapper around services.podcast_bible_service + legacy podcast)
- AI-Writer/backend/services/podcast_bible_service.py (only consumed by podcast_service.py and legacy podcast API)
- AI-Writer/backend/routers/linkedin.py (entire LinkedIn content generation router)
- AI-Writer/backend/routers/video_studio/tasks/avatar_generation.py (depends on api.story_writer.task_manager)
- AI-Writer/backend/routers/video_studio/tasks/video_generation.py (depends on api.story_writer.task_manager)

<!-- Files to EDIT (remove or replace specific lines): -->
- AI-Writer/backend/routers/video_studio/endpoints/avatar.py (line 18: `from api.story_writer.task_manager import task_manager`)
- AI-Writer/backend/routers/video_studio/endpoints/tasks.py (line 10: same import)
- AI-Writer/backend/routers/video_studio/endpoints/create.py (line 16: same import)
- AI-Writer/backend/routers/video_studio/endpoints/social.py (verify any legacy imports; remove if present)
- AI-Writer/backend/services/product_marketing/product_avatar_service.py (line 15: `from services.story_writer.audio_generation_service import StoryAudioGenerationService`)
- AI-Writer/backend/services/research/core/research_engine.py (lines 287-288, 332-333, 378-379, 412-415: blog_writer imports)
- AI-Writer/backend/services/llm_providers/gemini_provider.py (lines 410, 456, 463, 470, 477: blog_writer imports)
- AI-Writer/backend/services/llm_providers/gemini_grounded_provider.py (lines 173, 185, 192, 200, 211: blog_writer imports)

<!-- NEW files to create: -->
- AI-Writer/backend/services/llm_providers/_exceptions.py — exports: APIRateLimitException, APITimeoutException, ValidationException, ContentGenerationException, ResearchFailedException
- AI-Writer/backend/services/llm_providers/_retry.py — exports: retry_with_backoff, CONTENT_RETRY_CONFIG, RESEARCH_RETRY_CONFIG
</interfaces>

Non-goals: we are NOT preserving LinkedIn content generation, podcast generation, or story_writer video features. The phase goal is to DELETE them. The video_studio avatar/video routers that depended on `api.story_writer.task_manager` are part of the story-writer subsystem and go with it.

IMPORTANT — "linkedin" disambiguation: `AI-Writer/backend/services/persona/linkedin/` (persona subdirectory) is a DIFFERENT module and must NOT be touched. This plan targets only: `services/linkedin_service.py`, `routers/linkedin.py`, and (in Wave 2) `services/linkedin/` + `api/linkedin_image_generation.py`. Do not edit any file under `services/persona/linkedin/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete wrapper/router files that bridge to legacy services</name>
  <files>AI-Writer/backend/services/linkedin_service.py, AI-Writer/backend/services/podcast_service.py, AI-Writer/backend/services/podcast_bible_service.py, AI-Writer/backend/routers/linkedin.py, AI-Writer/backend/routers/video_studio/tasks/avatar_generation.py, AI-Writer/backend/routers/video_studio/tasks/video_generation.py</files>
  <read_first>
    - AI-Writer/backend/services/linkedin_service.py (confirm it is purely a wrapper — ~400 lines importing from services.linkedin.*)
    - AI-Writer/backend/services/podcast_service.py (confirm it imports podcast_bible_service + legacy podcast)
    - AI-Writer/backend/services/podcast_bible_service.py
    - AI-Writer/backend/routers/linkedin.py (top 50 lines confirm it imports LinkedInService + models.linkedin_models)
    - AI-Writer/backend/routers/video_studio/tasks/avatar_generation.py
    - AI-Writer/backend/routers/video_studio/tasks/video_generation.py
    - AI-Writer/backend/routers/video_studio/__init__.py (to understand how tasks/avatar_generation + tasks/video_generation are registered)
    - AI-Writer/backend/routers/video_studio/tasks/__init__.py (if present)
    - AI-Writer/backend/routers/video_studio/router.py (if present)
  </read_first>
  <action>
    1. Confirm no live (non-legacy) code still imports any of the six files to be deleted. Run:
       `grep -rn "from services.linkedin_service\|import linkedin_service\|from services.podcast_service\|from services.podcast_bible_service\|from routers.linkedin\b\|routers.video_studio.tasks.avatar_generation\|routers.video_studio.tasks.video_generation" AI-Writer/backend/ --include="*.py" | grep -v __pycache__`
       After Plan 01 is applied, this should only surface references inside the six files themselves or inside the legacy directories (which Wave 2 deletes). If any OTHER live file still imports one of these, stop and report back — do not delete until that caller is also cleaned.

    2. Inspect `AI-Writer/backend/routers/video_studio/__init__.py`, `routers/video_studio/tasks/__init__.py`, and `routers/video_studio/router.py` (whichever exist). If they register or re-export the `avatar_generation` or `video_generation` task modules, REMOVE those registrations (import lines + any `include_router` / registry-append lines). Edit in place — do NOT delete the directory or the __init__ that still has non-legacy siblings.

    3. Delete the six files (prefer `git rm` if the repo is git-tracked; otherwise `rm`):
       - `AI-Writer/backend/services/linkedin_service.py`
       - `AI-Writer/backend/services/podcast_service.py`
       - `AI-Writer/backend/services/podcast_bible_service.py`
       - `AI-Writer/backend/routers/linkedin.py`
       - `AI-Writer/backend/routers/video_studio/tasks/avatar_generation.py`
       - `AI-Writer/backend/routers/video_studio/tasks/video_generation.py`

    4. If `AI-Writer/backend/routers/video_studio/tasks/` becomes empty (only `__init__.py` with no siblings), leave the directory and `__init__.py` as-is.

    5. Compile-check remaining video_studio router files: `cd AI-Writer/backend && python -m py_compile routers/video_studio/__init__.py routers/video_studio/*.py routers/video_studio/endpoints/*.py 2>&1 | head -40` — zero errors expected.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && test ! -f services/linkedin_service.py && test ! -f services/podcast_service.py && test ! -f services/podcast_bible_service.py && test ! -f routers/linkedin.py && test ! -f routers/video_studio/tasks/avatar_generation.py && test ! -f routers/video_studio/tasks/video_generation.py && python -m py_compile routers/video_studio/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f AI-Writer/backend/services/linkedin_service.py` succeeds
    - `test ! -f AI-Writer/backend/services/podcast_service.py` succeeds
    - `test ! -f AI-Writer/backend/services/podcast_bible_service.py` succeeds
    - `test ! -f AI-Writer/backend/routers/linkedin.py` succeeds
    - `test ! -f AI-Writer/backend/routers/video_studio/tasks/avatar_generation.py` succeeds
    - `test ! -f AI-Writer/backend/routers/video_studio/tasks/video_generation.py` succeeds
    - `grep -rEl "from services\.linkedin_service|from services\.podcast_service\b|from services\.podcast_bible_service|from routers\.linkedin\b|routers\.video_studio\.tasks\.(avatar|video)_generation" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__` returns NOTHING (no stale callers).
    - Every file under `AI-Writer/backend/routers/video_studio/` compiles with `python -m py_compile`.
  </acceptance_criteria>
  <done>Six wrapper/router files deleted; video_studio registry updated so the deleted task modules are no longer imported or registered.</done>
</task>

<task type="auto">
  <name>Task 2: Remove story_writer imports from remaining video_studio endpoints and product_avatar_service</name>
  <files>AI-Writer/backend/routers/video_studio/endpoints/avatar.py, AI-Writer/backend/routers/video_studio/endpoints/tasks.py, AI-Writer/backend/routers/video_studio/endpoints/create.py, AI-Writer/backend/routers/video_studio/endpoints/social.py, AI-Writer/backend/services/product_marketing/product_avatar_service.py</files>
  <read_first>
    - AI-Writer/backend/routers/video_studio/endpoints/avatar.py (entire file)
    - AI-Writer/backend/routers/video_studio/endpoints/tasks.py (entire file)
    - AI-Writer/backend/routers/video_studio/endpoints/create.py (entire file)
    - AI-Writer/backend/routers/video_studio/endpoints/social.py (entire file)
    - AI-Writer/backend/services/product_marketing/product_avatar_service.py (entire file)
    - Output of: `grep -rn "task_manager" AI-Writer/backend/api AI-Writer/backend/routers --include="*.py" | grep -v story_writer | grep -v __pycache__` (to discover a non-story_writer task_manager replacement if one exists)
  </read_first>
  <action>
    For each of avatar.py, tasks.py, create.py (the three files with `from api.story_writer.task_manager import task_manager`):
    1. DELETE the `from api.story_writer.task_manager import task_manager` line.
    2. Inspect the rest of the file for usages of `task_manager`. For each usage:
       - (a) If the project has a non-story_writer `task_manager` module (from the grep in read_first — e.g. a generic background-jobs task manager under `services/background_jobs.py` or `api/video_studio/task_manager.py`), rewrite the import to point there. Adapt method calls if the new module's signature differs.
       - (b) If no replacement exists, DELETE every FastAPI endpoint function (including its `@router.{get,post,put,delete}(...)` decorator and the function body) that references `task_manager`. Downstream nav/API docs will lose those endpoints — that is acceptable since the story_writer feature is gone.
    3. Run `python -m py_compile` on each file after edits.

    For social.py:
    1. Run `grep -nE "story_writer|blog_writer|^from services\.podcast|^from api\.podcast|^from services\.youtube|^from api\.youtube|^from services\.linkedin\b|^from api\.linkedin_image_generation|^from routers\.linkedin\b" AI-Writer/backend/routers/video_studio/endpoints/social.py`.
    2. If any matches appear, DELETE those imports and every function/method that depended on them, following the same "delete import + delete dependent function" pattern.
    3. `python -m py_compile` the file.

    For AI-Writer/backend/services/product_marketing/product_avatar_service.py:
    1. DELETE line 15: `from services.story_writer.audio_generation_service import StoryAudioGenerationService`.
    2. In the same file, find every usage of `StoryAudioGenerationService`. For each usage:
       - If the enclosing method's sole purpose is to delegate to `StoryAudioGenerationService`, DELETE the entire method.
       - Otherwise, DELETE only the line(s) that reference `StoryAudioGenerationService` and any variable assigned from its calls. Leave other logic intact.
    3. Search consumers with `grep -rn "product_avatar_service\." AI-Writer/backend/services/product_marketing/ --include="*.py" | grep -v __pycache__` and delete any call sites to methods you removed in step 2.
    4. `python -m py_compile AI-Writer/backend/services/product_marketing/product_avatar_service.py`.

    If any file becomes a trivial stub (no meaningful code remaining), DELETE it and remove its registrations upstream (chase the import chain with `grep -rn "from <relative_path>" AI-Writer/backend/`).
  </action>
  <verify>
    <automated>cd AI-Writer/backend && (! grep -rEl "from api\.story_writer|from services\.story_writer\." routers/video_studio/ services/product_marketing/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep .) && python -m py_compile routers/video_studio/endpoints/avatar.py routers/video_studio/endpoints/tasks.py routers/video_studio/endpoints/create.py routers/video_studio/endpoints/social.py services/product_marketing/product_avatar_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rEl "from api\.story_writer|from services\.story_writer\." AI-Writer/backend/routers/video_studio/ AI-Writer/backend/services/product_marketing/ --include="*.py" 2>/dev/null | grep -v __pycache__` returns NOTHING.
    - All five listed files compile with `python -m py_compile`.
    - `grep -c "StoryAudioGenerationService" AI-Writer/backend/services/product_marketing/product_avatar_service.py` returns `0`.
    - `grep -c "api.story_writer.task_manager" AI-Writer/backend/routers/video_studio/endpoints/avatar.py AI-Writer/backend/routers/video_studio/endpoints/tasks.py AI-Writer/backend/routers/video_studio/endpoints/create.py` returns `0` on each file.
  </acceptance_criteria>
  <done>Remaining video_studio endpoints and product_avatar_service.py import only from non-legacy modules; all five files compile.</done>
</task>

<task type="auto">
  <name>Task 3: Inline exception classes and retry utility into llm_providers (replace blog_writer imports)</name>
  <files>AI-Writer/backend/services/llm_providers/_exceptions.py, AI-Writer/backend/services/llm_providers/_retry.py, AI-Writer/backend/services/llm_providers/gemini_provider.py, AI-Writer/backend/services/llm_providers/gemini_grounded_provider.py</files>
  <read_first>
    - AI-Writer/backend/services/blog_writer/exceptions.py (source of truth — read BEFORE Wave 2 deletes it)
    - AI-Writer/backend/services/blog_writer/retry_utils.py (source of truth — read BEFORE Wave 2 deletes it)
    - AI-Writer/backend/services/llm_providers/gemini_provider.py (lines 400-490 where blog_writer imports live)
    - AI-Writer/backend/services/llm_providers/gemini_grounded_provider.py (lines 170-220 where blog_writer imports live)
  </read_first>
  <action>
    1. Create `AI-Writer/backend/services/llm_providers/_exceptions.py`. Copy the EXACT class definitions of `APIRateLimitException`, `APITimeoutException`, `ValidationException`, `ContentGenerationException`, and `ResearchFailedException` from `services/blog_writer/exceptions.py`. If the source classes share a base class (e.g. all inherit from `BlogWriterException`), include that base class too, renamed to `LLMProviderException(Exception)`. Preserve every `__init__` signature verbatim.

    2. Create `AI-Writer/backend/services/llm_providers/_retry.py`. Copy the EXACT implementation of `retry_with_backoff` and the constants `CONTENT_RETRY_CONFIG` and `RESEARCH_RETRY_CONFIG` from `services/blog_writer/retry_utils.py`. If the copied `retry_with_backoff` references exception classes from blog_writer, change those references to `from services.llm_providers._exceptions import ...`.

    3. In AI-Writer/backend/services/llm_providers/gemini_provider.py, edit line-by-line:
       - Line 410: replace `from services.blog_writer.retry_utils import retry_with_backoff, CONTENT_RETRY_CONFIG` with `from services.llm_providers._retry import retry_with_backoff, CONTENT_RETRY_CONFIG`.
       - Line 456: replace `from services.blog_writer.exceptions import APIRateLimitException` with `from services.llm_providers._exceptions import APIRateLimitException`.
       - Line 463: replace `from services.blog_writer.exceptions import APITimeoutException` with `from services.llm_providers._exceptions import APITimeoutException`.
       - Line 470: replace `from services.blog_writer.exceptions import ValidationException` with `from services.llm_providers._exceptions import ValidationException`.
       - Line 477: replace `from services.blog_writer.exceptions import ContentGenerationException` with `from services.llm_providers._exceptions import ContentGenerationException`.

    4. In AI-Writer/backend/services/llm_providers/gemini_grounded_provider.py, edit line-by-line:
       - Line 173: replace `from services.blog_writer.exceptions import APITimeoutException` with `from services.llm_providers._exceptions import APITimeoutException`.
       - Line 185: replace `from services.blog_writer.exceptions import ValidationException` with `from services.llm_providers._exceptions import ValidationException`.
       - Line 192: same substitution for `ValidationException`.
       - Line 200: replace `from services.blog_writer.retry_utils import retry_with_backoff, RESEARCH_RETRY_CONFIG` with `from services.llm_providers._retry import retry_with_backoff, RESEARCH_RETRY_CONFIG`.
       - Line 211: replace `from services.blog_writer.exceptions import ResearchFailedException` with `from services.llm_providers._exceptions import ResearchFailedException`.

    5. Compile-check all four files: `cd AI-Writer/backend && python -m py_compile services/llm_providers/_exceptions.py services/llm_providers/_retry.py services/llm_providers/gemini_provider.py services/llm_providers/gemini_grounded_provider.py`.

    6. Do NOT port any other symbols from `services/blog_writer/exceptions.py` or `services/blog_writer/retry_utils.py`. Scope is strictly the five exceptions + the retry utility + its two config constants.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && python -m py_compile services/llm_providers/_exceptions.py services/llm_providers/_retry.py services/llm_providers/gemini_provider.py services/llm_providers/gemini_grounded_provider.py && ! grep -E "from services\.blog_writer" services/llm_providers/gemini_provider.py services/llm_providers/gemini_grounded_provider.py</automated>
  </verify>
  <acceptance_criteria>
    - `test -f AI-Writer/backend/services/llm_providers/_exceptions.py` succeeds.
    - `test -f AI-Writer/backend/services/llm_providers/_retry.py` succeeds.
    - `grep -cE "^class (APIRateLimitException|APITimeoutException|ValidationException|ContentGenerationException|ResearchFailedException)\b" AI-Writer/backend/services/llm_providers/_exceptions.py` returns `5`.
    - `grep -cE "^def retry_with_backoff\b" AI-Writer/backend/services/llm_providers/_retry.py` returns `1`.
    - `grep -cE "^CONTENT_RETRY_CONFIG\s*=|^RESEARCH_RETRY_CONFIG\s*=" AI-Writer/backend/services/llm_providers/_retry.py` returns `2`.
    - `grep -cE "from services\.blog_writer" AI-Writer/backend/services/llm_providers/gemini_provider.py` returns `0`.
    - `grep -cE "from services\.blog_writer" AI-Writer/backend/services/llm_providers/gemini_grounded_provider.py` returns `0`.
    - `grep -cE "from services\.llm_providers\._(exceptions|retry)" AI-Writer/backend/services/llm_providers/gemini_provider.py` is `>= 2`.
    - `grep -cE "from services\.llm_providers\._(exceptions|retry)" AI-Writer/backend/services/llm_providers/gemini_grounded_provider.py` is `>= 2`.
    - All four files compile with `python -m py_compile`.
  </acceptance_criteria>
  <done>gemini_provider.py and gemini_grounded_provider.py import exceptions/retry from the new local _exceptions.py/_retry.py; blog_writer import statements fully removed from both provider files.</done>
</task>

<task type="auto">
  <name>Task 4: Remove services.blog_writer.research.* usage from research_engine.py</name>
  <files>AI-Writer/backend/services/research/core/research_engine.py</files>
  <read_first>
    - AI-Writer/backend/services/research/core/research_engine.py (full file; specifically lines 280-420)
    - Directory listing: `ls AI-Writer/backend/services/blog_writer/research/` (to know what's being retired: exa_provider.py, tavily_provider.py, google_provider.py, research_strategies.py, keyword_analyzer.py, competitor_analyzer.py, content_angle_generator.py, data_filter.py)
    - Callers of research_engine: `grep -rn "research_engine\|ResearchEngine\|UnifiedResearchEngine" AI-Writer/backend/ --include="*.py" | grep -v __pycache__` (to understand which methods are externally invoked before deleting them)
  </read_first>
  <action>
    The research engine lazy-imports providers from `services.blog_writer.research.*`. Since blog_writer is being deleted and there is no replacement in scope, every code path that depends on those imports must be removed.

    1. Identify each import block and the method it lives inside. From the prior grep:
       - Lines 287-288: exa_provider + research_strategies (inside some method; call it `exa_method`)
       - Lines 332-333: tavily_provider + research_strategies (inside `tavily_method`)
       - Lines 378-379: google_provider + research_strategies (inside `google_method`)
       - Lines 412-415: keyword_analyzer + competitor_analyzer + content_angle_generator + data_filter (inside `analyzer_method`)

    2. For each method (`exa_method`, `tavily_method`, `google_method`):
       - DELETE the `from services.blog_writer.research...` import lines.
       - DELETE every subsequent line in the method body that uses the imported symbols (provider instantiation, `provider.search(...)` calls, strategy.apply calls, etc.).
       - Replace the method body with the exact two lines:
         ```python
         logger.warning("%s provider was removed in Phase 1 cleanup; returning empty result", "<provider_name>")
         return []
         ```
         where `<provider_name>` is literally `"exa"`, `"tavily"`, or `"google"`. Keep the method signature unchanged so existing callers don't break.

    3. For `analyzer_method` (lines 412-415):
       - DELETE the `from services.blog_writer.research import ...` line.
       - DELETE every line in the method body that uses `KeywordAnalyzer`, `CompetitorAnalyzer`, `ContentAngleGenerator`, or `ResearchDataFilter`.
       - Replace with:
         ```python
         logger.warning("Research analyzers were removed in Phase 1 cleanup; returning empty analysis")
         return {}
         ```
         (or `return []` if the original return type was a list — inspect the method's type hints to choose correctly).

    4. After edits, run `grep -nE "services\.blog_writer|blog_writer\." AI-Writer/backend/services/research/core/research_engine.py` — must return nothing.

    5. Cascade check: `grep -rnE "services\.blog_writer|blog_writer\." AI-Writer/backend/services/research/ --include="*.py" | grep -v __pycache__`. If any other file in `services/research/` still references blog_writer, apply the same treatment (delete import + replace usage with logged empty return).

    6. Compile: `cd AI-Writer/backend && python -m py_compile services/research/core/research_engine.py`.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && ! grep -rE "services\.blog_writer|blog_writer\." services/research/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep . && python -m py_compile services/research/core/research_engine.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "services\.blog_writer|blog_writer\." AI-Writer/backend/services/research/core/research_engine.py` returns `0`.
    - `grep -rEl "services\.blog_writer|blog_writer\." AI-Writer/backend/services/research/ --include="*.py" 2>/dev/null | grep -v __pycache__` returns NOTHING.
    - `grep -cE "was removed in Phase 1 cleanup" AI-Writer/backend/services/research/core/research_engine.py` returns at least `4` (one per neutered method).
    - `python -m py_compile AI-Writer/backend/services/research/core/research_engine.py` exits 0.
  </acceptance_criteria>
  <done>research_engine.py (and any cascade-affected sibling files) no longer reference services.blog_writer; all four disabled methods log a warning and return an empty result; the file compiles.</done>
</task>

</tasks>

<verification>
After all four tasks in this plan:
- No file under `AI-Writer/backend/` outside the five legacy directories imports `services.blog_writer`, `services.podcast`, `services.youtube`, `services.story_writer`, or `services.linkedin` (the content module, not persona.linkedin).
  Check: `grep -rEl "^\s*(from|import)\s+(services\.blog_writer|services\.podcast|services\.youtube|services\.story_writer|services\.linkedin[^_])|from\s+services\s+import\s+(blog_writer|podcast|youtube|story_writer)|from\s+api\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)|from\s+routers\.linkedin\b" AI-Writer/backend/ --include="*.py" | grep -v __pycache__ | grep -vE "backend/services/(blog_writer|podcast|youtube|story_writer|linkedin)/|backend/api/(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)"` returns NOTHING.
- Every modified file compiles with `python -m py_compile`.
</verification>

<success_criteria>
- [ ] Six files deleted (linkedin_service.py, podcast_service.py, podcast_bible_service.py, routers/linkedin.py, two video_studio/tasks files).
- [ ] Two new files created and populated (_exceptions.py, _retry.py).
- [ ] All eight edited files compile cleanly.
- [ ] Zero non-legacy files import from `services.{blog_writer,podcast,youtube,story_writer,linkedin}` or `api.{blog_writer,podcast,youtube,story_writer,linkedin_image_generation}` or `routers.linkedin`.
- [ ] `services/persona/linkedin/` untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-writer-backend-cleanup/01-02-SUMMARY.md` listing: files deleted, files created, exact line ranges edited in each file, and any unexpected cascade removals encountered in steps that say "chase the import chain".
</output>
