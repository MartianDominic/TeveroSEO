---
phase: 01-ai-writer-backend-cleanup
plan: 05
type: execute
wave: 3
depends_on:
  - 01-04
files_modified: []
autonomous: true
requirements:
  - CLEAN-06
  - CLEAN-07
must_haves:
  truths:
    - "`grep -rE \"^\\s*(from|import)\\s+(services|api|routers)\\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)\" AI-Writer/backend/ --include=*.py` returns ZERO matches"
    - "`grep -rE \"^\\s*from\\s+services\\.linkedin(\\.|\\s+import)\" AI-Writer/backend/ --include=*.py` returns ZERO matches (excluding persona.linkedin)"
    - "`python -c \"from app import app\"` and `python -c \"from main import app\"` both succeed from AI-Writer/backend/"
    - "`python -m pytest AI-Writer/backend/tests -x --collect-only` completes without ImportError"
    - "Full test run `python -m pytest AI-Writer/backend/tests -x` passes (or fails only on pre-existing non-import issues documented in the summary)"
  artifacts:
    - path: ".planning/phases/01-ai-writer-backend-cleanup/VERIFICATION.md"
      provides: "Evidence log of all verification command outputs"
  key_links:
    - from: "app.py"
      to: "uvicorn/FastAPI"
      via: "python -c from app import app"
      pattern: "app\\s*=\\s*FastAPI"
    - from: "pytest collector"
      to: "entire backend test tree"
      via: "collect-only succeeds with no ImportError"
      pattern: "collected \\d+ items"
---

<objective>
Final verification that Phase 1 succeeded. Run the three ROADMAP success-criteria commands, the CLEAN-06 grep check, the CLEAN-07 pytest run, and produce a VERIFICATION.md recording every command's exact output. This plan makes NO code modifications — it only reads and reports.

Purpose: Prove the phase goal is met before marking phase complete.
Output: VERIFICATION.md with pass/fail evidence for each acceptance criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

ROADMAP Phase 1 Success Criteria (verbatim):
1. `grep -r "blog_writer\|podcast\|youtube\|story_writer\|linkedin" AI-Writer/backend/api/` returns zero matches
2. `ls AI-Writer/backend/services/` shows no legacy directories (blog_writer, podcast, youtube, story_writer, linkedin)
3. Backend starts without import errors: `docker compose run --rm ai-writer-backend python -c "from main import app"` succeeds

Nuance for Criterion 1: raw grep on `AI-Writer/backend/api/` may still surface strings in comments, docstrings, or non-legacy file bodies that mention these words cosmetically (e.g. a docstring saying "supports podcast feature groups" somewhere in `persona.py`). The deep_work_rules context explicitly states: "what matters is no Python `import` or `from X import` statements". This plan therefore uses TWO grep checks: (a) the raw ROADMAP check (for audit trail) and (b) a stricter "import-only" check that MUST return zero.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all grep-based acceptance checks and record outputs</name>
  <files>.planning/phases/01-ai-writer-backend-cleanup/VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (re-read Phase 1 success criteria at lines 13-18)
    - .planning/REQUIREMENTS.md (re-read CLEAN-01 through CLEAN-07 at lines 12-18)
  </read_first>
  <action>
    Create `.planning/phases/01-ai-writer-backend-cleanup/VERIFICATION.md` with the following sections. For each command, run it and paste the exact output (or "(no output)") below the command fence. Do NOT redact or summarize — paste raw output.

    ```markdown
    # Phase 1 Verification — AI-Writer Backend Cleanup

    Date: <YYYY-MM-DD of execution>

    ## Criterion 1 — Zero legacy imports in api/

    ### 1a. Raw ROADMAP check (may include cosmetic string matches — informational):
    Command:
      grep -r "blog_writer\|podcast\|youtube\|story_writer\|linkedin" AI-Writer/backend/api/ --include="*.py" 2>/dev/null | grep -v __pycache__
    Output:
    <paste>

    ### 1b. Strict import-only check (MUST be empty):
    Command:
      grep -rEn "^\s*(from|import)\s+(services|api|routers|models)\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__
    Output:
    <paste>

    ### 1c. services.linkedin (content module) import check:
    Command:
      grep -rEn "^\s*(from|import)\s+services\.linkedin(\.|\s+import)" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep -vE "services\.persona\.linkedin|services\.linkedin_persona"
    Output:
    <paste>

    ### 1d. routers.linkedin import check:
    Command:
      grep -rEn "^\s*(from|import)\s+routers\.linkedin\b" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__
    Output:
    <paste>

    ## Criterion 2 — services/ has no legacy subdirectories

    Command:
      ls AI-Writer/backend/services/ | grep -xE "blog_writer|podcast|youtube|story_writer|linkedin"
    Output (must be empty):
    <paste>

    Command:
      ls AI-Writer/backend/api/ | grep -xE "blog_writer|podcast|youtube|story_writer|linkedin_image_generation.py"
    Output (must be empty):
    <paste>

    ## Preservation checks — these MUST still exist

    Command: `test -d AI-Writer/backend/services/persona/linkedin && echo OK`
    Output:
    <paste>

    Command: `test -f AI-Writer/backend/models/blog_models.py && echo OK`
    Output:
    <paste>

    ## Status
    Pass / Fail: <decision>
    ```

    Fill every `<paste>` placeholder with the literal command output. Mark Status = PASS only if all strict import checks return empty and both preservation checks echo OK.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-ai-writer-backend-cleanup/VERIFICATION.md && (! grep -rEn "^\s*(from|import)\s+(services|api|routers|models)\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep .) && test -d AI-Writer/backend/services/persona/linkedin && test -f AI-Writer/backend/models/blog_models.py</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md exists with all six command sections filled in.
    - `grep -rEn "^\s*(from|import)\s+(services|api|routers|models)\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__` returns NOTHING.
    - `grep -rEn "^\s*(from|import)\s+services\.linkedin(\.|\s+import)" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep -vE "services\.persona\.linkedin|services\.linkedin_persona"` returns NOTHING.
    - `grep -rEn "^\s*(from|import)\s+routers\.linkedin\b" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__` returns NOTHING.
    - `ls AI-Writer/backend/services/ | grep -xE "blog_writer|podcast|youtube|story_writer|linkedin"` returns NOTHING.
    - `ls AI-Writer/backend/api/ | grep -xE "blog_writer|podcast|youtube|story_writer|linkedin_image_generation.py"` returns NOTHING.
    - `test -d AI-Writer/backend/services/persona/linkedin` succeeds.
    - `test -f AI-Writer/backend/models/blog_models.py` succeeds.
    - VERIFICATION.md "Status" line reads `Pass / Fail: PASS`.
  </acceptance_criteria>
  <done>VERIFICATION.md created with all grep-based acceptance evidence pasted; status PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Verify backend imports cleanly (python -c from app/main import app)</name>
  <files>.planning/phases/01-ai-writer-backend-cleanup/VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-ai-writer-backend-cleanup/VERIFICATION.md (append to existing file; do NOT recreate)
    - AI-Writer/backend/app.py (first 20 lines — confirm it still defines `app = FastAPI(...)` after Plan 01 edits)
    - AI-Writer/backend/main.py (first 20 lines — confirm same)
  </read_first>
  <action>
    Append a new section `## Criterion 3 — Backend imports cleanly` to VERIFICATION.md.

    Run these commands in order. Paste exact stdout+stderr output of each after the command.

    Command 1 (preferred — via docker compose, matching ROADMAP verbatim):
      `docker compose run --rm ai-writer-backend python -c "from main import app; print('main OK')"`
    Command 2 (fallback — local python if docker compose is not available in this environment):
      `cd AI-Writer/backend && python -c "from main import app; print('main OK')"`
    Command 3 (also check app.py entrypoint):
      `cd AI-Writer/backend && python -c "from app import app; print('app OK')"`

    Expected: each prints exactly `main OK` or `app OK` to stdout and exits 0. If docker compose is unavailable, note the fallback was used and run commands 2 and 3.

    If ANY command exits non-zero with an `ImportError` / `ModuleNotFoundError`:
    - Paste the full traceback into VERIFICATION.md under the failed command.
    - Set the Status line at the top of VERIFICATION.md to `Pass / Fail: FAIL`.
    - Do NOT attempt to edit any source file to fix the import — that is out of scope for this plan. Instead, report the failure to the parent orchestrator so a gap-closure plan can be created.

    If all three commands succeed: update VERIFICATION.md Status to remain `PASS`.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && python -c "from main import app; print('main OK')" 2>&1 | tail -1 | grep -q "main OK" && python -c "from app import app; print('app OK')" 2>&1 | tail -1 | grep -q "app OK"</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md contains a `## Criterion 3 — Backend imports cleanly` section with at least two command outputs (local-python fallback acceptable).
    - Running `cd AI-Writer/backend && python -c "from main import app"` exits 0.
    - Running `cd AI-Writer/backend && python -c "from app import app"` exits 0.
    - Neither command produces an `ImportError` or `ModuleNotFoundError`.
  </acceptance_criteria>
  <done>Backend imports cleanly via both `main` and `app` entrypoints; output logged in VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 3: Run pytest on the backend test tree (CLEAN-07)</name>
  <files>.planning/phases/01-ai-writer-backend-cleanup/VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-ai-writer-backend-cleanup/VERIFICATION.md (append, do not recreate)
    - AI-Writer/backend/tests/ directory listing (`ls AI-Writer/backend/tests/`)
    - AI-Writer/backend/pytest.ini or AI-Writer/backend/pyproject.toml (confirm pytest config if present)
  </read_first>
  <action>
    Append a new section `## Criterion 4 — Pytest passes with no ImportError (CLEAN-07)` to VERIFICATION.md.

    Step 1 — collect-only (proves no ImportError):
    Command: `cd AI-Writer/backend && python -m pytest tests/ --collect-only -q 2>&1 | tail -40`
    Paste the last 40 lines of output. Expected: a line like `X tests collected` with no `ERROR`/`ImportError` lines.

    Step 2 — full run (CLEAN-07 acceptance):
    Command: `cd AI-Writer/backend && python -m pytest tests/ -x 2>&1 | tail -80`
    Paste the last 80 lines. Expected: either `X passed` (ideal) OR `X passed, Y skipped`. Test failures unrelated to imports (e.g. flaky network tests, missing API keys, pre-existing bugs) are ACCEPTABLE if and only if:
      - The failure does NOT mention `ImportError` or `ModuleNotFoundError`.
      - The failure would have existed before Phase 1 began (document with a note: "Pre-existing failure — unrelated to cleanup").

    If any test fails with `ImportError` or `ModuleNotFoundError`:
    - Mark VERIFICATION.md Status = FAIL.
    - Paste the full failure + traceback.
    - Report to parent orchestrator for gap-closure plan.

    If tests pass OR fail only for non-import reasons:
    - Update VERIFICATION.md Status to `PASS (with N pre-existing failures documented)` or `PASS` if fully green.
    - List every non-import failure explicitly with a one-line justification.

    Final step — also scan pytest output for `ERRORS` section (collection-time failures) and append any such errors to the report.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && python -m pytest tests/ --collect-only -q 2>&1 | grep -qE "tests collected|^no tests" && ! python -m pytest tests/ --collect-only 2>&1 | grep -qE "ImportError|ModuleNotFoundError"</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md contains a `## Criterion 4 — Pytest passes with no ImportError (CLEAN-07)` section with both the collect-only and full-run outputs.
    - `cd AI-Writer/backend && python -m pytest tests/ --collect-only 2>&1 | grep -cE "ImportError|ModuleNotFoundError"` returns `0`.
    - Any test failures in the full run are annotated as pre-existing/non-import failures, OR the full run is green.
    - VERIFICATION.md final Status line is `PASS` or `PASS (with N pre-existing failures documented)`.
  </acceptance_criteria>
  <done>Pytest collection and execution verified; VERIFICATION.md updated with pytest evidence and final Status.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `.planning/phases/01-ai-writer-backend-cleanup/VERIFICATION.md` exists with four criterion sections, each with pasted command outputs and a final PASS/FAIL status.
- All seven CLEAN-XX requirements from REQUIREMENTS.md are provably satisfied OR the VERIFICATION.md clearly identifies which ones failed and why.
</verification>

<success_criteria>
- [ ] VERIFICATION.md exists and covers all four criteria (grep checks, ls checks, backend import, pytest).
- [ ] Strict import-only grep checks return zero matches.
- [ ] `python -c "from main import app"` and `python -c "from app import app"` both exit 0.
- [ ] Pytest collection produces zero ImportError/ModuleNotFoundError.
- [ ] services/persona/linkedin/ and models/blog_models.py are preserved.
- [ ] VERIFICATION.md final Status is PASS (or PASS with documented pre-existing failures).
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-writer-backend-cleanup/01-05-SUMMARY.md` that:
- Summarizes which criteria passed and which (if any) failed.
- Links to VERIFICATION.md for raw evidence.
- Lists any pre-existing test failures that must be addressed in a future phase (with issue-tracker-style bullets).
- Confirms the phase is ready to be marked complete in ROADMAP.md.
</output>
