---
phase: 01-ai-writer-backend-cleanup
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - AI-Writer/backend/services/blog_writer/
  - AI-Writer/backend/services/podcast/
  - AI-Writer/backend/services/youtube/
  - AI-Writer/backend/services/story_writer/
  - AI-Writer/backend/services/linkedin/
  - AI-Writer/backend/api/blog_writer/
  - AI-Writer/backend/api/podcast/
  - AI-Writer/backend/api/youtube/
  - AI-Writer/backend/api/story_writer/
  - AI-Writer/backend/api/linkedin_image_generation.py
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05
  - CLEAN-06
must_haves:
  truths:
    - "Five legacy service directories under AI-Writer/backend/services/ are gone"
    - "Four legacy API route directories under AI-Writer/backend/api/ are gone"
    - "The standalone file AI-Writer/backend/api/linkedin_image_generation.py is gone"
    - "services/persona/linkedin/ is untouched (separate module)"
  artifacts:
    - path: "AI-Writer/backend/services/"
      provides: "Services directory with no blog_writer, podcast, youtube, story_writer, or linkedin subdirectory"
    - path: "AI-Writer/backend/api/"
      provides: "API directory with no blog_writer, podcast, youtube, story_writer subdirectory and no linkedin_image_generation.py"
  key_links:
    - from: "Wave 1 plans (01, 02, 03)"
      to: "this plan"
      via: "consumer-cleanup prerequisite"
      pattern: "depends_on: [01-01, 01-02, 01-03]"
---

<objective>
Physically delete the five legacy service directories, four legacy API route directories, and the `linkedin_image_generation.py` router file. Preceded by Wave 1 plans that removed every non-legacy import of these paths; this plan is the final rm step.

Purpose: Removes the legacy code from the repository.
Output: 9 directories + 1 file removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

This plan MUST run after Plans 01-01, 01-02, and 01-03 have completed successfully. Before running the `rm` commands in Task 1, the executor MUST confirm that no live (non-legacy) Python module imports from any of the paths about to be deleted.

<interfaces>
<!-- Paths to delete (services): -->
- AI-Writer/backend/services/blog_writer/
- AI-Writer/backend/services/podcast/
- AI-Writer/backend/services/youtube/
- AI-Writer/backend/services/story_writer/
- AI-Writer/backend/services/linkedin/   (NOT services/persona/linkedin/)

<!-- Paths to delete (api): -->
- AI-Writer/backend/api/blog_writer/
- AI-Writer/backend/api/podcast/
- AI-Writer/backend/api/youtube/
- AI-Writer/backend/api/story_writer/
- AI-Writer/backend/api/linkedin_image_generation.py  (single file, not a directory)

<!-- Paths to PRESERVE (explicit NOT-touch list): -->
- AI-Writer/backend/services/persona/linkedin/  (persona module — unrelated)
- AI-Writer/backend/models/blog_models.py       (still used by research_engine)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion safety check — confirm zero non-legacy consumers</name>
  <files>(no file modifications in this task; runs verification commands only)</files>
  <read_first>
    - Output of: `ls -d AI-Writer/backend/services/blog_writer AI-Writer/backend/services/podcast AI-Writer/backend/services/youtube AI-Writer/backend/services/story_writer AI-Writer/backend/services/linkedin AI-Writer/backend/api/blog_writer AI-Writer/backend/api/podcast AI-Writer/backend/api/youtube AI-Writer/backend/api/story_writer 2>/dev/null`
    - Output of: `ls AI-Writer/backend/api/linkedin_image_generation.py 2>/dev/null`
  </read_first>
  <action>
    Run the following checks. EVERY check must pass with zero matches before proceeding to Task 2. If any check fails, open the offending file, fix it (following the patterns in Plans 01-02/01-03), then rerun.

    Check 1 — services-directory imports by non-legacy code:
    ```
    grep -rEn "from\s+services\.(blog_writer|podcast|youtube|story_writer)(\b|\.)|^import\s+services\.(blog_writer|podcast|youtube|story_writer)(\b|\.)" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep -vE "^AI-Writer/backend/(services/(blog_writer|podcast|youtube|story_writer|linkedin)|api/(blog_writer|podcast|youtube|story_writer|linkedin_image_generation))"
    ```
    Expected: empty output.

    Check 2 — services.linkedin (content generation) imports by non-legacy code. Exclude persona.linkedin, linkedin_persona_service, linkedin_models (which should be already deleted), linkedin_image_generation, LinkedIn as a string in comments/type literals.
    ```
    grep -rEn "from\s+services\.linkedin(\.|\s+import)|^import\s+services\.linkedin\b" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep -vE "services\.persona\.linkedin|services\.linkedin_persona" | grep -vE "^AI-Writer/backend/(services/(blog_writer|podcast|youtube|story_writer|linkedin)|api/(blog_writer|podcast|youtube|story_writer|linkedin_image_generation))"
    ```
    Expected: empty output.

    Check 3 — api imports by non-legacy code:
    ```
    grep -rEn "from\s+api\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)(\b|\.)|^import\s+api\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)(\b|\.)" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep -vE "^AI-Writer/backend/(services/(blog_writer|podcast|youtube|story_writer|linkedin)|api/(blog_writer|podcast|youtube|story_writer|linkedin_image_generation))"
    ```
    Expected: empty output.

    Check 4 — routers.linkedin still referenced anywhere:
    ```
    grep -rEn "from\s+routers\.linkedin\b|^import\s+routers\.linkedin\b" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__
    ```
    Expected: empty output (routers/linkedin.py was deleted in Plan 02).

    If any check fails:
    - STOP. Do not delete anything.
    - Report the stale references to the parent orchestrator.
    - Do NOT attempt to self-heal by editing files outside this plan's `files_modified` list without explicit approval.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && (! grep -rEn "from\s+services\.(blog_writer|podcast|youtube|story_writer)(\b|\.)" . --include="*.py" 2>/dev/null | grep -v __pycache__ | grep -vE "\./(services/(blog_writer|podcast|youtube|story_writer|linkedin)|api/(blog_writer|podcast|youtube|story_writer|linkedin_image_generation))" | grep .) && (! grep -rEn "from\s+api\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)(\b|\.)" . --include="*.py" 2>/dev/null | grep -v __pycache__ | grep -vE "\./(services/(blog_writer|podcast|youtube|story_writer|linkedin)|api/(blog_writer|podcast|youtube|story_writer|linkedin_image_generation))" | grep .) && (! grep -rEn "from\s+routers\.linkedin\b" . --include="*.py" 2>/dev/null | grep -v __pycache__ | grep .)</automated>
  </verify>
  <acceptance_criteria>
    - All four grep checks (documented in action) return EMPTY.
    - No file outside the legacy directories imports from the legacy packages.
  </acceptance_criteria>
  <done>Pre-deletion safety verified; no non-legacy consumers remain.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the five legacy service directories</name>
  <files>AI-Writer/backend/services/blog_writer/, AI-Writer/backend/services/podcast/, AI-Writer/backend/services/youtube/, AI-Writer/backend/services/story_writer/, AI-Writer/backend/services/linkedin/</files>
  <read_first>
    - Output of: `ls -la AI-Writer/backend/services/blog_writer AI-Writer/backend/services/podcast AI-Writer/backend/services/youtube AI-Writer/backend/services/story_writer AI-Writer/backend/services/linkedin 2>/dev/null` (confirm they still exist)
    - Output of: `ls -la AI-Writer/backend/services/persona/linkedin 2>/dev/null` (confirm this exists and is separate; it must remain after the task)
  </read_first>
  <action>
    Delete each directory recursively. Prefer `git rm -r` if the repo is git-tracked; otherwise `rm -rf`. Execute EXACTLY these five commands (one at a time so any failure is surfaced):

    ```
    git rm -r AI-Writer/backend/services/blog_writer   # or: rm -rf AI-Writer/backend/services/blog_writer
    git rm -r AI-Writer/backend/services/podcast       # or: rm -rf AI-Writer/backend/services/podcast
    git rm -r AI-Writer/backend/services/youtube       # or: rm -rf AI-Writer/backend/services/youtube
    git rm -r AI-Writer/backend/services/story_writer  # or: rm -rf AI-Writer/backend/services/story_writer
    git rm -r AI-Writer/backend/services/linkedin      # or: rm -rf AI-Writer/backend/services/linkedin
    ```

    After each deletion, run: `ls AI-Writer/backend/services/ | grep -E "^(blog_writer|podcast|youtube|story_writer|linkedin)$"` — must return nothing for the corresponding name.

    CRITICAL: `AI-Writer/backend/services/persona/linkedin/` must NOT be touched. Verify after all five deletions: `test -d AI-Writer/backend/services/persona/linkedin` exits 0.
  </action>
  <verify>
    <automated>cd AI-Writer/backend && test ! -e services/blog_writer && test ! -e services/podcast && test ! -e services/youtube && test ! -e services/story_writer && test ! -e services/linkedin && test -d services/persona/linkedin</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e AI-Writer/backend/services/blog_writer` succeeds
    - `test ! -e AI-Writer/backend/services/podcast` succeeds
    - `test ! -e AI-Writer/backend/services/youtube` succeeds
    - `test ! -e AI-Writer/backend/services/story_writer` succeeds
    - `test ! -e AI-Writer/backend/services/linkedin` succeeds
    - `test -d AI-Writer/backend/services/persona/linkedin` succeeds (preserved)
    - `ls AI-Writer/backend/services/ | grep -xE "blog_writer|podcast|youtube|story_writer|linkedin"` returns NOTHING.
  </acceptance_criteria>
  <done>Five legacy service directories removed; persona.linkedin preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the four legacy API route directories and linkedin_image_generation.py</name>
  <files>AI-Writer/backend/api/blog_writer/, AI-Writer/backend/api/podcast/, AI-Writer/backend/api/youtube/, AI-Writer/backend/api/story_writer/, AI-Writer/backend/api/linkedin_image_generation.py</files>
  <read_first>
    - Output of: `ls -la AI-Writer/backend/api/blog_writer AI-Writer/backend/api/podcast AI-Writer/backend/api/youtube AI-Writer/backend/api/story_writer AI-Writer/backend/api/linkedin_image_generation.py 2>/dev/null`
    - AI-Writer/backend/api/__init__.py (to check for any re-exports of the deleted subpackages; if present, edit in this task)
  </read_first>
  <action>
    1. Delete the four directories and one file (git rm preferred):
       ```
       git rm -r AI-Writer/backend/api/blog_writer
       git rm -r AI-Writer/backend/api/podcast
       git rm -r AI-Writer/backend/api/youtube
       git rm -r AI-Writer/backend/api/story_writer
       git rm AI-Writer/backend/api/linkedin_image_generation.py
       ```

    2. Inspect `AI-Writer/backend/api/__init__.py`. If it contains any `from .blog_writer`, `from .podcast`, `from .youtube`, `from .story_writer`, or `from .linkedin_image_generation` lines (or `import` equivalents), DELETE those lines. Run `python -m py_compile AI-Writer/backend/api/__init__.py` after.

    3. Final scan — `grep -rEl "blog_writer|podcast|youtube|story_writer|linkedin_image_generation" AI-Writer/backend/api/ --include="*.py" 2>/dev/null | grep -v __pycache__` — any remaining non-empty output indicates either leftover comments/strings (acceptable if not imports) or import statements that need removal. For each match, open the file and verify: if it is a `from` / `import` statement, delete it. If it is a comment or string literal, leave it (cosmetic).
  </action>
  <verify>
    <automated>cd AI-Writer/backend && test ! -e api/blog_writer && test ! -e api/podcast && test ! -e api/youtube && test ! -e api/story_writer && test ! -f api/linkedin_image_generation.py && python -m py_compile api/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e AI-Writer/backend/api/blog_writer` succeeds
    - `test ! -e AI-Writer/backend/api/podcast` succeeds
    - `test ! -e AI-Writer/backend/api/youtube` succeeds
    - `test ! -e AI-Writer/backend/api/story_writer` succeeds
    - `test ! -f AI-Writer/backend/api/linkedin_image_generation.py` succeeds
    - `grep -cE "^from\s+\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)|^import\s+\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)" AI-Writer/backend/api/__init__.py` returns `0`
    - `python -m py_compile AI-Writer/backend/api/__init__.py` exits 0
    - `grep -rEn "^\s*(from|import)\s+api\.(blog_writer|podcast|youtube|story_writer|linkedin_image_generation)" AI-Writer/backend/ --include="*.py" 2>/dev/null | grep -v __pycache__` returns NOTHING
  </acceptance_criteria>
  <done>Four legacy API route directories and linkedin_image_generation.py deleted; api/__init__.py re-exports cleaned.</done>
</task>

</tasks>

<verification>
After this plan:
- `ls AI-Writer/backend/services/` contains NO entries named blog_writer, podcast, youtube, story_writer, or linkedin.
- `ls AI-Writer/backend/api/` contains NO entries named blog_writer, podcast, youtube, story_writer, or linkedin_image_generation.py.
- `AI-Writer/backend/services/persona/linkedin/` still exists.
- `AI-Writer/backend/models/blog_models.py` still exists.
</verification>

<success_criteria>
- [ ] Five service directories deleted.
- [ ] Four API route directories deleted.
- [ ] `linkedin_image_generation.py` deleted.
- [ ] api/__init__.py no longer re-exports deleted subpackages.
- [ ] `services/persona/linkedin/` and `models/blog_models.py` preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-writer-backend-cleanup/01-04-SUMMARY.md` listing every deleted path with its approximate file count (from `git rm -r` output), and explicitly confirming that `services/persona/linkedin/` and `models/blog_models.py` were preserved.
</output>
