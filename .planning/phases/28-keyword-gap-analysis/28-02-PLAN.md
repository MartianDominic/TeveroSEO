---
phase: 28-keyword-gap-analysis
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - open-seo-main/src/server/lib/dataforseoKeywordGap.ts
  - open-seo-main/src/db/prospect-schema.ts
  - open-seo-main/src/server/workers/prospect-analysis-processor.ts
autonomous: true
requirements:
  - PROSP-16

must_haves:
  truths:
    - "Achievability score uses prospect's Domain Authority"
    - "Formula: 100 - max(0, difficulty - DA)"
    - "Higher score = more achievable keyword"
    - "DA comes from domain_metrics.domainRank in prospect analysis"
  artifacts:
    - path: "open-seo-main/src/server/lib/dataforseoKeywordGap.ts"
      provides: "calculateAchievability function using DA"
      exports: ["calculateAchievability", "enrichGapsWithAchievability"]
---

<objective>
Update keyword gap scoring to use Domain Authority-based achievability formula.

Current: `opportunityScore = searchVolume * cpc * (100 - difficulty) / 100`
Target: `achievability = 100 - max(0, difficulty - domainAuthority)`

The achievability score tells agencies which keywords are realistic targets based on the prospect's current domain strength.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add calculateAchievability function</name>
  <files>
    open-seo-main/src/server/lib/dataforseoKeywordGap.ts
  </files>
  <action>
    1. Add calculateAchievability function:
       ```typescript
       export function calculateAchievability(
         difficulty: number,
         domainAuthority: number
       ): number {
         // Higher DA = can tackle harder keywords
         // DA 50, Difficulty 40 → achievability = 100 - max(0, 40-50) = 100
         // DA 30, Difficulty 60 → achievability = 100 - max(0, 60-30) = 70
         return 100 - Math.max(0, difficulty - domainAuthority);
       }
       ```
    
    2. Add enrichGapsWithAchievability function:
       ```typescript
       export function enrichGapsWithAchievability(
         gaps: KeywordGap[],
         domainAuthority: number
       ): KeywordGapWithAchievability[] {
         return gaps.map(gap => ({
           ...gap,
           achievability: calculateAchievability(gap.difficulty, domainAuthority),
         }));
       }
       ```
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update KeywordGap type to include achievability</name>
  <files>
    open-seo-main/src/db/prospect-schema.ts
  </files>
  <action>
    1. Update KeywordGap interface to include optional achievability field:
       ```typescript
       export interface KeywordGap {
         keyword: string;
         competitorDomain: string;
         competitorPosition: number;
         searchVolume: number;
         cpc: number;
         difficulty: number;
         trafficPotential: number;
         opportunityScore: number;  // Keep existing
         achievability?: number;     // NEW: 0-100, higher = more achievable
       }
       ```
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Wire achievability into prospect-analysis-processor</name>
  <files>
    open-seo-main/src/server/workers/prospect-analysis-processor.ts
  </files>
  <action>
    1. After fetching keyword gaps, enrich with achievability:
       ```typescript
       import { enrichGapsWithAchievability } from "@/server/lib/dataforseoKeywordGap";
       
       // After gap analysis (existing code):
       const rawGaps = await fetchDomainIntersectionRaw(domain, competitors, ...);
       
       // Get DA from domain metrics (already fetched earlier in processor)
       const domainAuthority = domainMetrics?.domainRank ?? 0;
       
       // Enrich gaps with achievability
       const enrichedGaps = enrichGapsWithAchievability(rawGaps, domainAuthority);
       
       // Store enriched gaps
       await AnalysisService.updateAnalysisResult(analysisId, {
         keywordGaps: enrichedGaps,
       });
       ```
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm tsc --noEmit</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- [ ] calculateAchievability function uses formula: 100 - max(0, difficulty - DA)
- [ ] KeywordGap type includes optional achievability field
- [ ] Processor enriches gaps with achievability before storing
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/28-keyword-gap-analysis/28-02-SUMMARY.md`
</output>
