---
phase: 70-frontend-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/connect/success-screen.tsx
  - apps/web/src/app/(shell)/settings/components/*.tsx
  - apps/web/src/components/proposals/ServiceLineItems.tsx
  - apps/web/src/components/dashboard/PatternsPanel.tsx
  - apps/web/src/app/(shell)/clients/new/page.tsx
autonomous: true
requirements:
  - CRITICAL-MEMORY-01
  - CRITICAL-RENDER-01
  - HIGH-KEYS-01
  - HIGH-ARIA-01
must_haves:
  truths:
    - All setTimeout cleaned up on unmount
    - No infinite re-renders in settings tabs
    - Index keys replaced with stable IDs
    - Forms have aria-invalid and aria-describedby
  artifacts:
    - apps/web/src/components/connect/success-screen.tsx (fixed useEffect)
  key_links:
    - useEffect cleanup function
    - React key best practices
---

# Plan 70-01: React Component Fixes

<objective>
Fix memory leaks, infinite re-renders, index keys, and ARIA accessibility issues in React components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

## Task 1: Fix Memory Leak in success-screen.tsx

<read_first>
- apps/web/src/components/connect/success-screen.tsx
</read_first>

<action>
Add cleanup function to useEffect to clear all setTimeout on unmount:
```typescript
useEffect(() => {
  const timer1 = setTimeout(() => {...}, 1000);
  const timer2 = setTimeout(() => {...}, 2000);
  
  return () => {
    clearTimeout(timer1);
    clearTimeout(timer2);
  };
}, []);
```
</action>

<acceptance_criteria>
- [ ] Cleanup returns clearTimeout for all timers
- [ ] No console warnings on unmount
- [ ] File updated: apps/web/src/components/connect/success-screen.tsx
</acceptance_criteria>

## Task 2: Audit GlobalSettings Tab Components

<read_first>
- apps/web/src/app/(shell)/settings/components/api-integrations-tab.tsx
- apps/web/src/app/(shell)/settings/components/voice-templates-tab.tsx
- apps/web/src/app/(shell)/settings/components/model-defaults-tab.tsx
</read_first>

<action>
Fix object dependencies causing re-renders:
- Move object creation outside useEffect or useMemo
- Use primitive values in dependency arrays
- Memoize callbacks with useCallback
</action>

<acceptance_criteria>
- [ ] Stable useEffect dependencies (primitives or memoized values)
- [ ] No infinite render loops detected
- [ ] useCallback for event handlers passed as props
</acceptance_criteria>

## Task 3: Replace Index Keys in Lists

<read_first>
- apps/web/src/components/proposals/ServiceLineItems.tsx
- apps/web/src/components/dashboard/PatternsPanel.tsx
</read_first>

<action>
Fix 10 files with index keys - replace with stable IDs:
1. ServiceLineItems.tsx - use item.id
2. PatternsPanel.tsx - use pattern.id
3. PredictiveAlertsPanel.tsx - use alert.id
4. PortfolioHealthSummary.tsx - use item.id
5. OpportunitiesPanel.tsx - use opportunity.id
6. AppShellNavItem.tsx - use item.href
7. AppShellSidebar.tsx - use item.href
8. error-screen.tsx - use unique identifier
9. oauth-enhancement.tsx - use step.id or step.name
10. ProposalPreview.tsx - use section.id
</action>

<acceptance_criteria>
- [ ] All 10 files use stable IDs instead of array index
- [ ] Keys are unique within the list scope
- [ ] No React key warnings in console
</acceptance_criteria>

## Task 4: Add ARIA to Form Components

<read_first>
- apps/web/src/app/(shell)/clients/new/page.tsx
</read_first>

<action>
Fix 5 files with ARIA attributes:
1. clients/new/page.tsx
2. prospects/new/page.tsx
3. ProposalForm.tsx
4. KeywordImportForm.tsx
5. VoiceProfileForm.tsx

Add to input fields:
```typescript
aria-invalid={errors.field ? "true" : undefined}
aria-describedby={errors.field ? "field-error" : undefined}
```

Add to error messages:
```typescript
<span id="field-error" role="alert">{errors.field}</span>
```
</action>

<acceptance_criteria>
- [ ] aria-invalid on inputs with errors
- [ ] aria-describedby linking to error message IDs
- [ ] role="alert" on error messages
</acceptance_criteria>

</tasks>
