---
phase: 57-proposal-editor-revolution
plan: 03
type: execute
wave: 2
depends_on: [57-01, 57-02]
files_modified:
  - apps/web/src/components/proposals/ProposalInlineEditor.tsx
  - apps/web/src/components/proposals/extensions/VariableExtension.ts
  - apps/web/src/components/proposals/VariableChip.tsx
  - apps/web/src/hooks/useVariableValue.ts
  - apps/web/package.json
autonomous: true
requirements:
  - SC-2
  - SC-3

must_haves:
  truths:
    - "TipTap editor with StarterKit, Placeholder, Typography extensions"
    - "VariableExtension renders {{variable}} as inline atom nodes"
    - "Variable chips show category color and resolved value on hover"
    - "Click any text to edit inline (WYSIWYG)"
    - "Drag variable from palette into editor inserts chip"
    - "Red dashed border on unresolved variables"
    - "Localized placeholder text"
  artifacts:
    - path: "apps/web/src/components/proposals/ProposalInlineEditor.tsx"
      provides: "Main TipTap editor component"
      exports: ["ProposalInlineEditor"]
    - path: "apps/web/src/components/proposals/extensions/VariableExtension.ts"
      provides: "TipTap extension for variable chips"
      exports: ["VariableExtension"]
    - path: "apps/web/src/components/proposals/VariableChip.tsx"
      provides: "React component for variable chip"
      exports: ["VariableChip"]
  key_links:
    - from: "ProposalInlineEditor.tsx"
      to: "VariableExtension.ts"
      reason: "Editor uses variable extension"
    - from: "VariableExtension.ts"
      to: "VariableChip.tsx"
      reason: "Extension renders chip component"
---

# Plan 57-03: Inline Editing (TipTap) + Variable Chips

## Goal
Implement rich text inline editing with TipTap and variable chip rendering.

## Dependencies

```bash
pnpm add @tiptap/react @tiptap/starter-kit @tiptap/extension-placeholder @tiptap/extension-typography @tiptap/extension-link @tiptap/extension-highlight
```

## Tasks

### Task 1: TipTap Setup (2h)

Create ProposalInlineEditor component:
- StarterKit (bold, italic, lists, headings)
- Placeholder with localized text
- Typography (smart quotes, dashes)
- Link extension
- Highlight extension
- Custom VariableExtension

```typescript
const editor = useEditor({
  extensions: [
    StarterKit,
    Placeholder.configure({
      placeholder: locale === 'lt' 
        ? 'Pradėkite rašyti arba vilkite kintamąjį...'
        : 'Start typing or drag a variable...',
    }),
    Typography,
    VariableExtension,
    Link,
    Highlight,
  ],
  content,
  onUpdate: ({ editor }) => onUpdate(editor.getHTML()),
});
```

### Task 2: VariableExtension (3h)

TipTap Node extension:
- `name: 'variable'`
- `group: 'inline'`, `inline: true`, `atom: true`
- Attributes: `key`, `category`, `label`
- Parse/render HTML with `data-variable` attribute
- ReactNodeViewRenderer for VariableChip

### Task 3: VariableChip Component (2h)

React component for inline variable:
- Category-based colors (blue/green/orange/purple/gray/teal)
- Tooltip with resolved value
- Red dashed border if unresolved
- Non-editable (atom node)
- useVariableValue hook for resolution

### Task 4: Drop Target (1.5h)

Enable dropping variables from palette:
- TipTap drop handler
- Insert variable node at drop position
- Visual drop indicator

### Task 5: Character Count (0.5h)

Real-time character count:
- Display in footer
- Exclude variable markup from count

## Verification

```bash
# Editor renders with placeholder
# Click text to edit
# Type to add content with formatting
# Drag variable → chip appears
# Hover chip → shows resolved value
# Unresolved variables have red border
```
