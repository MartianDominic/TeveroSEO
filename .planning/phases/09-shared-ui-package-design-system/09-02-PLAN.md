---
phase: 09-shared-ui-package-design-system
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - packages/ui/src/components/badge.tsx
  - packages/ui/src/components/button.tsx
  - packages/ui/src/components/card.tsx
  - packages/ui/src/components/chart.tsx
  - packages/ui/src/components/cms-health-badge.tsx
  - packages/ui/src/components/command.tsx
  - packages/ui/src/components/dialog.tsx
  - packages/ui/src/components/error-banner.tsx
  - packages/ui/src/components/input.tsx
  - packages/ui/src/components/label.tsx
  - packages/ui/src/components/page-header.tsx
  - packages/ui/src/components/popover.tsx
  - packages/ui/src/components/select.tsx
  - packages/ui/src/components/separator.tsx
  - packages/ui/src/components/sheet.tsx
  - packages/ui/src/components/skeleton.tsx
  - packages/ui/src/components/slider.tsx
  - packages/ui/src/components/status-chip.tsx
  - packages/ui/src/components/switch.tsx
  - packages/ui/src/components/table.tsx
  - packages/ui/src/components/tabs.tsx
  - packages/ui/src/components/textarea.tsx
  - packages/ui/src/index.ts
autonomous: true
requirements: [UI-01, UI-03, UI-04]
must_haves:
  truths:
    - "All 22 component files from apps/web/src/components/ui/ exist under packages/ui/src/components/ with identical behavior"
    - "Every component's internal cn import resolves within packages/ui (no @/lib/utils imports remain in the moved files)"
    - "packages/ui/src/index.ts exports every component named export that apps/web consumes"
    - "packages/ui typechecks clean in isolation — no references back to apps/web"
  artifacts:
    - path: "packages/ui/src/components/button.tsx"
      provides: "Button + buttonVariants + ButtonProps"
      exports: ["Button", "buttonVariants", "ButtonProps"]
    - path: "packages/ui/src/components/page-header.tsx"
      provides: "PageHeader shell component (next/navigation router)"
      exports: ["PageHeader", "PageHeaderProps"]
    - path: "packages/ui/src/components/status-chip.tsx"
      provides: "StatusChip with status map"
      exports: ["StatusChip", "StatusChipProps"]
    - path: "packages/ui/src/index.ts"
      provides: "Barrel export of all components + cn"
      contains: "export { Button"
  key_links:
    - from: "packages/ui/src/components/*.tsx"
      to: "packages/ui/src/lib/utils.ts"
      via: "relative import"
      pattern: "from \"../lib/utils\""
    - from: "packages/ui/src/index.ts"
      to: "packages/ui/src/components/*.tsx"
      via: "re-export"
      pattern: "export .* from \"./components/"
---

<objective>
Move every component file from `apps/web/src/components/ui/` into `packages/ui/src/components/`, rewrite each file's `cn` import to the package-local path, and produce a barrel `packages/ui/src/index.ts` that re-exports every symbol apps/web currently consumes.

Purpose: Single source of truth for all shadcn primitives + project-specific shell components (PageHeader, StatusChip, ErrorBanner, CmsHealthBadge). After this plan, `apps/web` still compiles (its local `components/ui/` copies remain untouched) — plan 03 is the one that flips import paths and deletes the old copies.

Output:
- 22 component files under `packages/ui/src/components/` with `cn` imported from `../lib/utils`
- `packages/ui/src/index.ts` exporting every named symbol consumed from apps/web
- `pnpm --filter @tevero/ui typecheck` passes in isolation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-shared-ui-package-design-system/09-CONTEXT.md
@.planning/phases/09-shared-ui-package-design-system/09-01-SUMMARY.md
@apps/web/src/components/ui/badge.tsx
@apps/web/src/components/ui/button.tsx
@apps/web/src/components/ui/card.tsx
@apps/web/src/components/ui/chart.tsx
@apps/web/src/components/ui/cms-health-badge.tsx
@apps/web/src/components/ui/command.tsx
@apps/web/src/components/ui/dialog.tsx
@apps/web/src/components/ui/error-banner.tsx
@apps/web/src/components/ui/input.tsx
@apps/web/src/components/ui/label.tsx
@apps/web/src/components/ui/page-header.tsx
@apps/web/src/components/ui/popover.tsx
@apps/web/src/components/ui/select.tsx
@apps/web/src/components/ui/separator.tsx
@apps/web/src/components/ui/sheet.tsx
@apps/web/src/components/ui/skeleton.tsx
@apps/web/src/components/ui/slider.tsx
@apps/web/src/components/ui/status-chip.tsx
@apps/web/src/components/ui/switch.tsx
@apps/web/src/components/ui/table.tsx
@apps/web/src/components/ui/tabs.tsx
@apps/web/src/components/ui/textarea.tsx
@packages/ui/src/lib/utils.ts
@packages/ui/src/index.ts

<interfaces>
<!-- The 22 files that must move, with their expected public exports. -->
<!-- Derived from `grep -rn "from .@/components/ui" apps/web/src` — every import below is already in use. -->

Named exports apps/web consumes (executor MUST keep every one of these in the new barrel):

- badge.tsx            → { Badge, badgeVariants, BadgeProps }
- button.tsx           → { Button, buttonVariants, ButtonProps }
- card.tsx             → { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent }
- chart.tsx            → { ChartContainer, ChartTooltip, ChartTooltipContent, ChartConfig }
- cms-health-badge.tsx → { CmsHealthBadge }
- command.tsx          → { Command, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem, CommandSeparator, CommandShortcut, CommandDialog }
- dialog.tsx           → { Dialog, DialogTrigger, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription, DialogClose, DialogPortal, DialogOverlay }
- error-banner.tsx     → { ErrorBanner }
- input.tsx            → { Input }
- label.tsx            → { Label }
- page-header.tsx      → { PageHeader, PageHeaderProps }
- popover.tsx          → { Popover, PopoverTrigger, PopoverContent, PopoverAnchor }
- select.tsx           → { Select, SelectGroup, SelectValue, SelectTrigger, SelectContent, SelectLabel, SelectItem, SelectSeparator, SelectScrollUpButton, SelectScrollDownButton }
- separator.tsx        → { Separator }
- sheet.tsx            → { Sheet, SheetTrigger, SheetClose, SheetContent, SheetHeader, SheetFooter, SheetTitle, SheetDescription, SheetPortal, SheetOverlay }
- skeleton.tsx         → { Skeleton }
- slider.tsx           → { Slider }
- status-chip.tsx      → { StatusChip, StatusChipProps }
- switch.tsx           → { Switch }
- table.tsx            → { Table, TableHeader, TableBody, TableFooter, TableHead, TableRow, TableCell, TableCaption }
- tabs.tsx             → { Tabs, TabsList, TabsTrigger, TabsContent }
- textarea.tsx         → { Textarea }

Internal pattern every source file uses today (apps/web):
```typescript
import { cn } from "@/lib/utils";
```
The executor rewrites every occurrence to:
```typescript
import { cn } from "../lib/utils";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy all 22 components into packages/ui/src/components/ with rewritten cn import</name>
  <files>packages/ui/src/components/badge.tsx, packages/ui/src/components/button.tsx, packages/ui/src/components/card.tsx, packages/ui/src/components/chart.tsx, packages/ui/src/components/cms-health-badge.tsx, packages/ui/src/components/command.tsx, packages/ui/src/components/dialog.tsx, packages/ui/src/components/error-banner.tsx, packages/ui/src/components/input.tsx, packages/ui/src/components/label.tsx, packages/ui/src/components/page-header.tsx, packages/ui/src/components/popover.tsx, packages/ui/src/components/select.tsx, packages/ui/src/components/separator.tsx, packages/ui/src/components/sheet.tsx, packages/ui/src/components/skeleton.tsx, packages/ui/src/components/slider.tsx, packages/ui/src/components/status-chip.tsx, packages/ui/src/components/switch.tsx, packages/ui/src/components/table.tsx, packages/ui/src/components/tabs.tsx, packages/ui/src/components/textarea.tsx</files>
  <action>
Copy every `.tsx` file in `apps/web/src/components/ui/` byte-for-byte into `packages/ui/src/components/`, with exactly one mechanical edit per file:

Find:
```
from "@/lib/utils"
```
Replace with:
```
from "../lib/utils"
```

Concrete procedure (use filesystem ops, not manual rewrites):

```bash
mkdir -p packages/ui/src/components
cp apps/web/src/components/ui/*.tsx packages/ui/src/components/

# Rewrite cn import in-place (the ONLY mechanical edit)
# sed -i works on GNU sed (Linux). On macOS use sed -i '' ''.
for f in packages/ui/src/components/*.tsx; do
  sed -i 's|from "@/lib/utils"|from "../lib/utils"|g' "$f"
done
```

After the copy+rewrite, audit each file for any OTHER `@/` import still present — there should be zero. The sole cross-file internal reference inside this component set is `cms-health-badge.tsx` importing `StatusChip` from `./status-chip` (it already uses a relative path — leave it).

Verify no stray path-alias imports remain:
```bash
grep -n "from \"@/" packages/ui/src/components/*.tsx
# Expected: no output
```

If ANY `@/` import remains after the sed pass, STOP and investigate. Likely candidates if the source diverges from what this plan documented:
- `page-header.tsx` imports `next/navigation` — that's fine (bare module specifier, not a path alias). Leave it.
- `error-banner.tsx` imports `./button` — relative, fine.
- If a component pulls in anything from `@/stores`, `@/types`, or `@/lib/*` besides utils, escalate — that's new drift since plan 09-CONTEXT was captured and the executor should note it in the SUMMARY rather than auto-rewriting.

Do NOT modify any file in `apps/web/src/components/ui/` — leave the originals in place so apps/web keeps compiling between plans. Plan 03 deletes them.

Do NOT edit component bodies, prop types, or className strings. The goal of this plan is zero behavioral change.
  </action>
  <verify>
    <automated>test $(ls packages/ui/src/components/*.tsx 2>/dev/null | wc -l) -eq 22 &amp;&amp; ! grep -rn "from \"@/" packages/ui/src/components/ &amp;&amp; grep -l "from \"../lib/utils\"" packages/ui/src/components/*.tsx | wc -l | grep -qE "^(1[5-9]|2[0-2])$"</automated>
  </verify>
  <done>22 .tsx files exist under packages/ui/src/components/. Grep for `from "@/` against that directory returns zero matches. At least 15 files import cn via `from "../lib/utils"` (not all 22 import cn — e.g., sheet.tsx re-exports dialog primitives; cms-health-badge.tsx doesn't use cn directly; table.tsx does). `git diff --stat apps/web/src/components/ui/` shows zero changes.</done>
</task>

<task type="auto">
  <name>Task 2: Write packages/ui/src/index.ts barrel re-exporting all 22 components</name>
  <files>packages/ui/src/index.ts</files>
  <action>
Overwrite `packages/ui/src/index.ts` (currently only re-exporting `cn`) with a complete barrel. Keep the `cn` re-export on line 1 so existing call sites (added in plan 03) can still import `cn` from `@tevero/ui`. Use explicit named re-exports so tree-shaking stays clean and downstream type IDE navigation works.

Exact content to write:

```typescript
// @tevero/ui — shared design system
// Barrel: every symbol apps/web imports from @/components/ui/* must be re-exported here.
// Keep sorted alphabetically by component file.

export { cn } from "./lib/utils";

export { Badge, badgeVariants } from "./components/badge";
export type { BadgeProps } from "./components/badge";

export { Button, buttonVariants } from "./components/button";
export type { ButtonProps } from "./components/button";

export {
  Card,
  CardHeader,
  CardFooter,
  CardTitle,
  CardDescription,
  CardContent,
} from "./components/card";

export {
  ChartContainer,
  ChartTooltip,
  ChartTooltipContent,
} from "./components/chart";
export type { ChartConfig } from "./components/chart";

export { CmsHealthBadge } from "./components/cms-health-badge";

export {
  Command,
  CommandDialog,
  CommandInput,
  CommandList,
  CommandEmpty,
  CommandGroup,
  CommandItem,
  CommandShortcut,
  CommandSeparator,
} from "./components/command";

export {
  Dialog,
  DialogPortal,
  DialogOverlay,
  DialogTrigger,
  DialogClose,
  DialogContent,
  DialogHeader,
  DialogFooter,
  DialogTitle,
  DialogDescription,
} from "./components/dialog";

export { ErrorBanner } from "./components/error-banner";

export { Input } from "./components/input";

export { Label } from "./components/label";

export { PageHeader } from "./components/page-header";
export type { PageHeaderProps } from "./components/page-header";

export {
  Popover,
  PopoverTrigger,
  PopoverContent,
  PopoverAnchor,
} from "./components/popover";

export {
  Select,
  SelectGroup,
  SelectValue,
  SelectTrigger,
  SelectContent,
  SelectLabel,
  SelectItem,
  SelectSeparator,
  SelectScrollUpButton,
  SelectScrollDownButton,
} from "./components/select";

export { Separator } from "./components/separator";

export {
  Sheet,
  SheetPortal,
  SheetOverlay,
  SheetTrigger,
  SheetClose,
  SheetContent,
  SheetHeader,
  SheetFooter,
  SheetTitle,
  SheetDescription,
} from "./components/sheet";

export { Skeleton } from "./components/skeleton";

export { Slider } from "./components/slider";

export { StatusChip } from "./components/status-chip";
export type { StatusChipProps } from "./components/status-chip";

export { Switch } from "./components/switch";

export {
  Table,
  TableHeader,
  TableBody,
  TableFooter,
  TableHead,
  TableRow,
  TableCell,
  TableCaption,
} from "./components/table";

export { Tabs, TabsList, TabsTrigger, TabsContent } from "./components/tabs";

export { Textarea } from "./components/textarea";
```

Notes for the executor:
- If a source file exports additional named symbols not listed above (e.g., drift between this plan and the actual copied file), add them to the barrel rather than dropping them. The compiler flag in task 3 will catch missed exports.
- If a source file exports FEWER names than listed above (e.g., `CommandShortcut` turns out to be missing from command.tsx), remove the offender from the barrel and record it in the SUMMARY so plan 03 knows not to import it in apps/web call sites.
- This is the ONLY place consumers should reach into the package; do NOT create subpath exports like `@tevero/ui/button` in packages/ui/package.json — keep the single `.` entry from plan 01.
  </action>
  <verify>
    <automated>grep -q "export { Button" packages/ui/src/index.ts &amp;&amp; grep -q "export { PageHeader" packages/ui/src/index.ts &amp;&amp; grep -q "export { StatusChip" packages/ui/src/index.ts &amp;&amp; grep -q "export { cn }" packages/ui/src/index.ts</automated>
  </verify>
  <done>packages/ui/src/index.ts re-exports cn plus every symbol documented in the interfaces block above. `grep -c "^export" packages/ui/src/index.ts` returns ≥ 25 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Verify packages/ui typechecks in isolation</name>
  <files>(no file writes — verification gate)</files>
  <action>
Install dependencies (new `@tevero/ui` runtime deps from plan 01 must be resolved) and confirm the package compiles standalone with `tsc --noEmit`. This is the gate that catches:
- A missing Radix package in packages/ui/package.json (plan 01's dep list is wrong)
- A lingering `@/…` import the sed pass missed
- A symbol named in the barrel that the component doesn't actually export
- A React 19 type incompatibility

Run:

```bash
pnpm install                       # refreshes workspace symlinks, installs new packages/ui deps
pnpm --filter @tevero/ui typecheck # runs `tsc --noEmit` against packages/ui/src/**
```

Expected: both commands exit 0 with no diagnostics.

Failure playbook:
1. `Cannot find module 'X'` → add X to packages/ui/package.json dependencies (record the addition in SUMMARY so plan 01's dep list is corrected in hindsight), re-run `pnpm install`.
2. `Cannot find name 'Foo' / has no exported member 'Foo'` in packages/ui/src/index.ts → the barrel names a symbol the source component doesn't export; remove it from the barrel, record in SUMMARY.
3. Path-alias residue (`Cannot find module '@/lib/utils'`) → re-run the sed rewrite; confirm `grep -rn "@/" packages/ui/src/components/` returns zero.

Do NOT run `pnpm --filter @tevero/web typecheck` here. apps/web is still using its local `@/components/ui/*` copies — its typecheck status is unchanged by this plan. It becomes the gate in plan 03.
  </action>
  <verify>
    <automated>pnpm install --silent &amp;&amp; pnpm --filter @tevero/ui typecheck</automated>
  </verify>
  <done>`pnpm install` completes with no errors. `pnpm --filter @tevero/ui typecheck` exits 0. Every symbol in the barrel resolves to a real exported binding in its source file.</done>
</task>

</tasks>

<verification>
End-of-plan gates (automated):

```bash
# 22 files exist under packages/ui/src/components
test $(ls packages/ui/src/components/*.tsx | wc -l) -eq 22

# Every file that previously imported cn now imports from ../lib/utils
! grep -rn "from \"@/" packages/ui/src/components/

# Barrel re-exports the critical components + cn
grep -q "export { cn }" packages/ui/src/index.ts
grep -q "export { Button" packages/ui/src/index.ts
grep -q "export { PageHeader" packages/ui/src/index.ts
grep -q "export { StatusChip" packages/ui/src/index.ts

# packages/ui compiles cleanly in isolation
pnpm --filter @tevero/ui typecheck

# apps/web untouched this plan
! git diff --quiet apps/web/src/components/ui/ && exit 1 || true
# (above line asserts apps/web/src/components/ui had no edits)
```
</verification>

<success_criteria>
1. `packages/ui/src/components/` contains exactly 22 `.tsx` files (the full set from apps/web/src/components/ui/).
2. No file under `packages/ui/src/components/` imports anything via the `@/` path alias. `cn` is imported via `from "../lib/utils"` everywhere it was imported before.
3. `packages/ui/src/index.ts` re-exports `cn` plus every named export/type listed in the interfaces block (Button, Card + subcomponents, Dialog + subcomponents, PageHeader, StatusChip, etc.).
4. `pnpm install` resolves all new `@tevero/ui` runtime deps without errors.
5. `pnpm --filter @tevero/ui typecheck` passes with zero TypeScript errors.
6. `apps/web/src/components/ui/` is byte-identical to what it was at the start of the plan — apps/web still builds unchanged.
7. Any drift between the documented export list and actual component source is recorded in the SUMMARY (added exports, missing exports, or extra deps), so plan 03 starts with accurate information.
</success_criteria>

<output>
After completion, create `.planning/phases/09-shared-ui-package-design-system/09-02-SUMMARY.md` with:
- Full list of copied files with line counts (`wc -l packages/ui/src/components/*.tsx`)
- Complete list of every symbol re-exported from `packages/ui/src/index.ts`
- Any export additions/removals vs. the interfaces block in this plan (drift signals for plan 03)
- Any dependencies added to packages/ui/package.json beyond plan 01's list (and why)
- Confirmation that apps/web was not modified (`git status apps/web/src/components/ui/` is clean)
- Confirmation that `pnpm --filter @tevero/ui typecheck` exits 0
</output>
