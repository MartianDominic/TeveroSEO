---
phase: 60-payment-flexibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - open-seo-main/src/routes/api/invoices/$id.pay.ts
  - open-seo-main/src/routes/api/invoices/$id.schedule.ts
  - open-seo-main/src/components/payment/PaymentPlanSelector.tsx
  - open-seo-main/src/components/payment/PaymentScheduleView.tsx
  - open-seo-main/src/components/payment/InstallmentCard.tsx
  - open-seo-main/src/lib/format-currency.ts
autonomous: true
requirements:
  - P60-PLAN-SELECTOR-UI
  - P60-CHECKOUT-FLOW
  - P60-SCHEDULE-VIEW

must_haves:
  truths:
    - "Client sees payment plan options with exact amounts and dates"
    - "Client can select full, 2-payment, or 3-payment plan"
    - "Continue button creates schedule and redirects to payment"
    - "Client can view payment schedule after first payment"
  artifacts:
    - path: "open-seo-main/src/components/payment/PaymentPlanSelector.tsx"
      provides: "Radio card selector showing plan breakdown"
      min_lines: 80
    - path: "open-seo-main/src/components/payment/PaymentScheduleView.tsx"
      provides: "Client-facing schedule with paid/pending installments"
      min_lines: 60
    - path: "open-seo-main/src/routes/api/invoices/$id.schedule.ts"
      provides: "API for schedule creation and retrieval"
      exports: ["Route"]
  key_links:
    - from: "PaymentPlanSelector"
      to: "/api/invoices/:id/schedule"
      via: "fetch POST on Continue click"
      pattern: "fetch.*schedule"
    - from: "$id.schedule.ts POST"
      to: "PaymentScheduleService.createScheduleForInvoice"
      via: "service call"
      pattern: "createScheduleForInvoice"
---

<objective>
Build client-facing payment plan selector UI and checkout flow integration.

Purpose: Enable clients to choose payment plans and see their schedules.
Output: PaymentPlanSelector component, PaymentScheduleView component, schedule API routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-payment-flexibility/DESIGN.md
@.planning/phases/60-payment-flexibility/60-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From PaymentScheduleService (created in 60-01):
```typescript
export interface PaymentPlan {
  type: 'full' | 'split_2' | 'split_3';
  installments: {
    number: number;
    amount: number;
    dueDate: Date;
    label: string;
  }[];
  totalAmount: number;
}

export function calculatePlan(totalCents: number, planType: string): PaymentPlan;
export function createScheduleForInvoice(invoiceId: string, planType: string): Promise<Schedule>;
export function getScheduleWithInstallments(invoiceId: string): Promise<ScheduleWithInstallments>;
```

From open-seo-main/src/routes/api/invoices/$id.pay.ts:
```typescript
// GET returns:
{
  invoice: { id, invoiceNumber, totalCents, currency, dueAt, lineItems },
  payment: { availableProviders, primaryProvider, existingCheckoutUrl }
}

// POST creates payment session, returns:
{ sessionId, checkoutUrl, token }
```

From packages/ui/src/components (v6 design system):
```typescript
import { Card, CardContent } from "@tevero/ui/card";
import { Badge } from "@tevero/ui/badge";
import { Button } from "@tevero/ui/button";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create payment plan selector component</name>
  <files>
    open-seo-main/src/components/payment/PaymentPlanSelector.tsx
    open-seo-main/src/components/payment/InstallmentCard.tsx
    open-seo-main/src/lib/format-currency.ts
  </files>
  <read_first>
    - .planning/phases/60-payment-flexibility/DESIGN.md (UI mockup for plan selector)
    - open-seo-main/src/routes/api/invoices/$id.pay.ts (existing payment page data)
    - packages/ui/src/components/card.tsx (Card component pattern)
  </read_first>
  <action>
Create format-currency.ts utility:
- formatCents(cents, currency) -> "4,200.00" or "4.200,00" for LT
- formatCurrency(cents, currency) -> "EUR 4,200.00"

Create InstallmentCard.tsx:
- Props: installment { number, amount, dueDate, label, isPaid? }
- Shows check icon if paid, circle if pending
- Formats amount using formatCurrency
- Formats date using date-fns format

Create PaymentPlanSelector.tsx per D-09:
- Props: totalCents, currency, availablePlans[], selectedPlan, onPlanChange, onContinue
- Uses calculatePlan to compute breakdowns for each available plan
- Radio card layout per DESIGN.md mockup:
  - "Pay in full" showing total today
  - "Pay in 2 installments" showing 50/50 split with dates
  - "Pay in 3 installments" showing 40/30/30 split with dates
- Each card shows:
  - Radio indicator (selected state)
  - Plan name
  - First payment amount ("EUR 2,100.00 today")
  - Divider
  - List of all installments with dates
- Selected card has border-accent highlight
- Continue button at bottom disabled until plan selected
- Loading state while creating schedule
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && npx tsc --noEmit src/components/payment/PaymentPlanSelector.tsx src/components/payment/InstallmentCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PaymentPlanSelector" open-seo-main/src/components/payment/PaymentPlanSelector.tsx
    - grep -q "availablePlans" open-seo-main/src/components/payment/PaymentPlanSelector.tsx
    - grep -q "onPlanChange" open-seo-main/src/components/payment/PaymentPlanSelector.tsx
    - grep -q "formatCents" open-seo-main/src/lib/format-currency.ts
    - grep -q "InstallmentCard" open-seo-main/src/components/payment/InstallmentCard.tsx
  </acceptance_criteria>
  <done>
    - PaymentPlanSelector shows all available plans with calculated breakdowns
    - Radio selection works with visual feedback
    - Amounts formatted correctly with currency
    - Dates formatted in user-friendly format
    - Continue button triggers onContinue callback
    - Loading state prevents double-submission
  </done>
</task>

<task type="auto">
  <name>Task 2: Create payment schedule view component</name>
  <files>
    open-seo-main/src/components/payment/PaymentScheduleView.tsx
  </files>
  <read_first>
    - .planning/phases/60-payment-flexibility/DESIGN.md (Client Payment Schedule View mockup)
    - open-seo-main/src/components/payment/InstallmentCard.tsx (from Task 1)
  </read_first>
  <action>
Create PaymentScheduleView.tsx per D-10:
- Props: schedule { planType, installments[] }, invoiceNumber
- Header: "Your Payment Schedule" with invoice number
- List of InstallmentCard components for each installment:
  - Paid installments: check icon, green "Paid" badge, paidAt date
  - Pending installments: circle icon, "Due {date}" text
  - Next due installment: highlight with "Pay Now" button
- Footer note: "We'll send you a reminder 3 days before your next payment."
- Uses v6 design tokens (bg-surface, border-hairline, text-primary)

Payment status mapping:
- paid -> Badge variant="success", check icon
- pending -> Badge variant="outline", circle icon
- processing -> Badge variant="warning", spinner icon
- overdue -> Badge variant="destructive", alert icon
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && npx tsc --noEmit src/components/payment/PaymentScheduleView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PaymentScheduleView" open-seo-main/src/components/payment/PaymentScheduleView.tsx
    - grep -q "installments" open-seo-main/src/components/payment/PaymentScheduleView.tsx
    - grep -q "Pay Now" open-seo-main/src/components/payment/PaymentScheduleView.tsx
    - grep -q "reminder" open-seo-main/src/components/payment/PaymentScheduleView.tsx
  </acceptance_criteria>
  <done>
    - PaymentScheduleView shows all installments with correct status
    - Paid installments show checkmark and paid date
    - Next unpaid installment has "Pay Now" button
    - Reminder note displayed at bottom
    - Uses v6 design tokens consistently
  </done>
</task>

<task type="auto">
  <name>Task 3: Create schedule API routes and integrate with payment page</name>
  <files>
    open-seo-main/src/routes/api/invoices/$id.schedule.ts
    open-seo-main/src/routes/api/invoices/$id.pay.ts
  </files>
  <read_first>
    - open-seo-main/src/routes/api/invoices/$id.pay.ts (existing payment endpoint)
    - open-seo-main/src/server/features/payments/services/PaymentScheduleService.ts (service from 60-01)
    - open-seo-main/src/server/features/payments/repositories/WorkspacePaymentSettingsRepository.ts (for split settings)
  </read_first>
  <action>
Create $id.schedule.ts:
- GET /api/invoices/:id/schedule
  - Returns existing schedule with installments if exists
  - Returns null if no schedule (pay-in-full or not created yet)
  - Public endpoint (client access via shared link)

- POST /api/invoices/:id/schedule
  - Body: { planType: 'full' | 'split_2' | 'split_3' }
  - Validates planType is in workspace's availablePlans
  - Calls PaymentScheduleService.createScheduleForInvoice
  - Returns created schedule with installments
  - Creates checkout session for first installment
  - Returns { schedule, checkoutUrl }

Update $id.pay.ts GET handler:
- Add splitPaymentsEnabled and availablePlans to response
- Add existingSchedule if schedule exists for invoice
- Response shape:
```typescript
{
  invoice: { ... },
  payment: { ... },
  splitPayments: {
    enabled: boolean,
    availablePlans: string[],
    defaultPlan: string,
    existingSchedule: Schedule | null
  }
}
```

Update $id.pay.ts POST handler:
- If planType provided and not 'full':
  - Create schedule first via PaymentScheduleService
  - Create checkout for first installment amount (not full invoice)
  - Store installmentId in checkout metadata
- If 'full' or no planType: existing behavior (full amount)
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && npx tsc --noEmit src/routes/api/invoices/\$id.schedule.ts src/routes/api/invoices/\$id.pay.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "createFileRoute.*schedule" open-seo-main/src/routes/api/invoices/\$id.schedule.ts
    - grep -q "createScheduleForInvoice" open-seo-main/src/routes/api/invoices/\$id.schedule.ts
    - grep -q "splitPayments" open-seo-main/src/routes/api/invoices/\$id.pay.ts
    - grep -q "availablePlans" open-seo-main/src/routes/api/invoices/\$id.pay.ts
    - grep -q "existingSchedule" open-seo-main/src/routes/api/invoices/\$id.pay.ts
  </acceptance_criteria>
  <done>
    - GET /api/invoices/:id/schedule returns schedule or null
    - POST /api/invoices/:id/schedule creates schedule and first installment checkout
    - GET /api/invoices/:id/pay includes splitPayments configuration
    - POST /api/invoices/:id/pay handles split payment checkout correctly
    - Installment-specific checkout sessions created with correct amounts
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser -> API | Plan selection from unauthenticated client |
| API -> Payment provider | Creating checkout sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-05 | Tampering | planType selection | mitigate | Validate planType against workspace.availablePlans whitelist |
| T-60-06 | Spoofing | invoice access | mitigate | Invoice ID in URL acts as capability token; no sensitive data exposed |
| T-60-07 | Repudiation | schedule creation | mitigate | Log schedule creation with invoiceId and planType |
| T-60-08 | DoS | schedule creation | mitigate | Check if schedule already exists before creating; return existing |
</threat_model>

<verification>
- TypeScript compiles: `cd open-seo-main && npx tsc --noEmit`
- Components render: Manual check in browser
- API responds: `curl http://localhost:3001/api/invoices/test-id/pay`
</verification>

<success_criteria>
1. PaymentPlanSelector shows all available plans with correct calculations
2. PaymentScheduleView shows installments with correct status badges
3. GET /api/invoices/:id/pay includes split payment configuration
4. POST /api/invoices/:id/schedule creates schedule and checkout
5. Checkout amount matches first installment (not full invoice)
6. All components use v6 design tokens
</success_criteria>

<output>
After completion, create `.planning/phases/60-payment-flexibility/60-02-SUMMARY.md`
</output>
