---
phase: 60-payment-flexibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - open-seo-main/src/db/payment-schedule-schema.ts
  - open-seo-main/src/db/discount-code-schema.ts
  - open-seo-main/src/db/workspace-payment-settings-schema.ts
  - open-seo-main/src/server/features/payments/services/PaymentScheduleService.ts
  - open-seo-main/src/server/features/payments/services/PaymentScheduleService.test.ts
  - open-seo-main/src/server/features/payments/repositories/PaymentScheduleRepository.ts
  - open-seo-main/drizzle/migrations/0060_payment_schedules.sql
autonomous: true
requirements:
  - P60-SCHEDULE-SCHEMA
  - P60-INSTALLMENT-TRACKING
  - P60-SPLIT-LOGIC

must_haves:
  truths:
    - "Payment schedules can be created with planType full/split_2/split_3"
    - "Installments calculate correct amounts using Math.ceil for first payment"
    - "Installment status transitions work: pending -> processing -> paid | overdue | failed"
    - "Workspace settings have splitPaymentsEnabled and availablePlans fields"
  artifacts:
    - path: "open-seo-main/src/db/payment-schedule-schema.ts"
      provides: "paymentSchedules and paymentInstallments tables"
      exports: ["paymentSchedules", "paymentInstallments", "PLAN_TYPES", "INSTALLMENT_STATUS"]
    - path: "open-seo-main/src/db/discount-code-schema.ts"
      provides: "discountCodes table"
      exports: ["discountCodes", "DISCOUNT_TYPES"]
    - path: "open-seo-main/src/server/features/payments/services/PaymentScheduleService.ts"
      provides: "Schedule creation and calculation logic"
      exports: ["PaymentScheduleService", "calculatePlan"]
  key_links:
    - from: "PaymentScheduleService"
      to: "paymentSchedules table"
      via: "PaymentScheduleRepository.insertSchedule"
      pattern: "PaymentScheduleRepository\\.(insert|get)"
    - from: "paymentInstallments"
      to: "invoices"
      via: "FK paymentSchedules.invoiceId -> invoices.id"
      pattern: "references.*invoices"
---

<objective>
Create database schema for split payments and payment schedule service with installment calculations.

Purpose: Foundation for split payment functionality - schema and core business logic.
Output: Drizzle schema files, PaymentScheduleService with calculatePlan function, migration file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-payment-flexibility/DESIGN.md
@.planning/phases/60-payment-flexibility/60-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From open-seo-main/src/db/invoice-schema.ts:
```typescript
export const invoices = pgTable("invoices", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull(),
  clientId: uuid("client_id").notNull(),
  totalCents: integer("total_cents").notNull(),
  currency: text("currency").default("EUR"),
  status: text("status").notNull().default("draft"),
  // ... other fields
});

export type InvoiceSelect = typeof invoices.$inferSelect;
```

From open-seo-main/src/db/workspace-payment-settings-schema.ts:
```typescript
export const workspacePaymentSettings = pgTable("workspace_payment_settings", {
  id: text("id").primaryKey(),
  workspaceId: text("workspace_id").notNull().unique(),
  defaultProvider: text("default_provider").notNull().default("stripe"),
  stripeEnabled: boolean("stripe_enabled").notNull().default(false),
  revolutEnabled: boolean("revolut_enabled").notNull().default(false),
  // ... credentials fields
});
```

From open-seo-main/src/server/features/payments/types.ts:
```typescript
export type PaymentProviderType = "stripe" | "revolut";
export type PaymentStatus = "pending" | "processing" | "paid" | "failed" | "cancelled" | "refunded";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create payment schedule and discount code schemas</name>
  <files>
    open-seo-main/src/db/payment-schedule-schema.ts
    open-seo-main/src/db/discount-code-schema.ts
    open-seo-main/src/db/workspace-payment-settings-schema.ts
  </files>
  <read_first>
    - open-seo-main/src/db/invoice-schema.ts (FK reference pattern)
    - open-seo-main/src/db/workspace-payment-settings-schema.ts (existing schema to extend)
    - .planning/phases/60-payment-flexibility/DESIGN.md (schema definitions)
  </read_first>
  <behavior>
    - Test 1: paymentSchedules table has planType with valid values (full, split_2, split_3)
    - Test 2: paymentInstallments has status enum (pending, processing, paid, overdue, failed)
    - Test 3: discountCodes has discountType enum (percentage, fixed)
    - Test 4: workspace_payment_settings includes splitPaymentsEnabled boolean
  </behavior>
  <action>
Create payment-schedule-schema.ts per D-01 and D-02:
- paymentSchedules table with id, invoiceId (FK to invoices), planType (text with CHECK), totalInstallments
- paymentInstallments table with id, scheduleId (FK), installmentNumber, amountCents, dueAt, status (text with CHECK), paidAt, paymentId, paymentProvider, paymentUrl, reminderSentAt
- Index on (status, due_at) per D-03 for efficient overdue queries
- Relations for type-safe Drizzle queries

Create discount-code-schema.ts per D-13-D-16:
- discountCodes table with id, workspaceId, code (unique per workspace), discountType, discountValue, maxUses, usedCount, minAmountCents, validFrom, validUntil, isActive
- CHECK constraints for discountType (percentage/fixed)

Extend workspace-payment-settings-schema.ts per D-04:
- Add splitPaymentsEnabled boolean default false
- Add availablePlans jsonb default ['full', 'split_2', 'split_3']
- Add defaultPlan text default 'full'
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && npx tsc --noEmit src/db/payment-schedule-schema.ts src/db/discount-code-schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "paymentSchedules = pgTable" open-seo-main/src/db/payment-schedule-schema.ts
    - grep -q "paymentInstallments = pgTable" open-seo-main/src/db/payment-schedule-schema.ts
    - grep -q "discountCodes = pgTable" open-seo-main/src/db/discount-code-schema.ts
    - grep -q "splitPaymentsEnabled" open-seo-main/src/db/workspace-payment-settings-schema.ts
    - grep -q "idx_installments_status_due" open-seo-main/src/db/payment-schedule-schema.ts
  </acceptance_criteria>
  <done>
    - paymentSchedules table created with planType CHECK constraint
    - paymentInstallments table created with status CHECK and index on (status, due_at)
    - discountCodes table created with discountType CHECK
    - workspace_payment_settings extended with split payment fields
    - All Drizzle relations defined
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PaymentScheduleRepository and Service</name>
  <files>
    open-seo-main/src/server/features/payments/repositories/PaymentScheduleRepository.ts
    open-seo-main/src/server/features/payments/services/PaymentScheduleService.ts
    open-seo-main/src/server/features/payments/services/PaymentScheduleService.test.ts
  </files>
  <read_first>
    - open-seo-main/src/db/payment-schedule-schema.ts (schema from Task 1)
    - open-seo-main/src/server/features/contracts/repositories/InvoiceRepository.ts (repository pattern)
    - open-seo-main/src/server/features/invoices/services/InvoiceService.ts (service pattern)
    - .planning/phases/60-payment-flexibility/DESIGN.md (calculatePlan logic)
  </read_first>
  <behavior>
    - Test 1: calculatePlan('full', 420000) returns 1 installment of 420000 cents due today
    - Test 2: calculatePlan('split_2', 420000) returns [210000 today, 210000 in 30 days]
    - Test 3: calculatePlan('split_3', 420000) returns [168000 (40%), 126000 (30%), 126000 (30%)]
    - Test 4: calculatePlan uses Math.ceil for first installment to avoid rounding issues
    - Test 5: createSchedule returns schedule with correct installment records
    - Test 6: updateInstallmentStatus transitions pending -> processing -> paid
  </behavior>
  <action>
Create PaymentScheduleRepository with:
- insertSchedule(data) - creates schedule record
- insertInstallments(scheduleId, installments[]) - creates all installment records
- getScheduleByInvoiceId(invoiceId) - returns schedule with installments
- getInstallmentById(id) - single installment lookup
- getUpcomingInstallments(daysAhead) - for reminder worker per D-20
- getOverdueInstallments() - status pending + dueAt < now
- updateInstallmentStatus(id, status, paymentData) - status transition

Create PaymentScheduleService with:
- calculatePlan(totalCents, planType) per D-05, D-06, D-07:
  - full: 1 installment, 100% today
  - split_2: 50% Math.ceil today, remainder in 30 days
  - split_3: 40% Math.ceil today, 30% Math.ceil in 30 days, remainder in 60 days
- createScheduleForInvoice(invoiceId, planType) - creates schedule and installments
- getScheduleWithInstallments(invoiceId) - returns full payment plan view
- recordPayment(installmentId, paymentProvider, paymentId) - marks installment paid
- getFirstUnpaidInstallment(scheduleId) - for checkout flow

Create test file with vitest testing all calculation edge cases and status transitions.
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && npm test -- --run src/server/features/payments/services/PaymentScheduleService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "calculatePlan" open-seo-main/src/server/features/payments/services/PaymentScheduleService.ts
    - grep -q "Math.ceil" open-seo-main/src/server/features/payments/services/PaymentScheduleService.ts
    - grep -q "split_2.*50" open-seo-main/src/server/features/payments/services/PaymentScheduleService.ts
    - grep -q "split_3.*40" open-seo-main/src/server/features/payments/services/PaymentScheduleService.ts
    - grep -q "insertSchedule" open-seo-main/src/server/features/payments/repositories/PaymentScheduleRepository.ts
    - Tests pass with coverage >= 80%
  </acceptance_criteria>
  <done>
    - PaymentScheduleRepository implements all CRUD operations
    - calculatePlan correctly calculates splits for all plan types
    - Math.ceil used for first installment per D-07
    - createScheduleForInvoice creates schedule + all installments in transaction
    - Test coverage >= 80% on PaymentScheduleService
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate Drizzle migration</name>
  <files>
    open-seo-main/drizzle/migrations/0060_payment_schedules.sql
  </files>
  <read_first>
    - open-seo-main/src/db/payment-schedule-schema.ts (tables to migrate)
    - open-seo-main/src/db/discount-code-schema.ts (tables to migrate)
    - open-seo-main/src/db/workspace-payment-settings-schema.ts (columns to add)
    - open-seo-main/drizzle/migrations/ (existing migration naming pattern)
  </read_first>
  <action>
Run drizzle-kit generate to create migration, or manually create 0060_payment_schedules.sql with:

1. CREATE TABLE payment_schedules with all columns and FK to invoices
2. CREATE TABLE payment_installments with all columns, FK to payment_schedules, and index on (status, due_at)
3. CREATE TABLE discount_codes with all columns and unique constraint on (workspace_id, code)
4. ALTER TABLE workspace_payment_settings ADD COLUMN split_payments_enabled, available_plans, default_plan

Add CHECK constraints:
- payment_schedules.plan_type IN ('full', 'split_2', 'split_3')
- payment_installments.status IN ('pending', 'processing', 'paid', 'overdue', 'failed')
- discount_codes.discount_type IN ('percentage', 'fixed')

Note: Do NOT run the migration - just generate the file.
  </action>
  <verify>
    <automated>ls -la /home/dominic/Documents/TeveroSEO/open-seo-main/drizzle/migrations/0060_payment_schedules.sql && head -50 /home/dominic/Documents/TeveroSEO/open-seo-main/drizzle/migrations/0060_payment_schedules.sql</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CREATE TABLE.*payment_schedules" open-seo-main/drizzle/migrations/0060_payment_schedules.sql
    - grep -q "CREATE TABLE.*payment_installments" open-seo-main/drizzle/migrations/0060_payment_schedules.sql
    - grep -q "CREATE TABLE.*discount_codes" open-seo-main/drizzle/migrations/0060_payment_schedules.sql
    - grep -q "CREATE INDEX.*idx_installments_status_due" open-seo-main/drizzle/migrations/0060_payment_schedules.sql
    - grep -q "ALTER TABLE.*workspace_payment_settings" open-seo-main/drizzle/migrations/0060_payment_schedules.sql
  </acceptance_criteria>
  <done>
    - Migration file 0060_payment_schedules.sql exists
    - Creates payment_schedules, payment_installments, discount_codes tables
    - Adds split payment columns to workspace_payment_settings
    - Includes all CHECK constraints and indexes
    - Migration NOT executed (ready for manual run)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> PaymentScheduleService | Plan selection and calculation from user input |
| PaymentScheduleService -> Database | Installment amounts stored for billing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | calculatePlan | mitigate | Validate planType against PLAN_TYPES enum before calculation; use CHECK constraint at DB |
| T-60-02 | Tampering | installment amounts | mitigate | Calculate amounts server-side only; never accept client-provided amounts |
| T-60-03 | Elevation | schedule creation | mitigate | Verify invoice belongs to authenticated workspace before creating schedule |
| T-60-04 | DoS | overdue queries | mitigate | Index on (status, due_at) ensures query performance |
</threat_model>

<verification>
- TypeScript compiles: `cd open-seo-main && npx tsc --noEmit`
- Tests pass: `cd open-seo-main && npm test -- --run src/server/features/payments/services/PaymentScheduleService.test.ts`
- Schema exports correct: `grep -q "export const paymentSchedules" src/db/payment-schedule-schema.ts`
- Service exports correct: `grep -q "export.*PaymentScheduleService" src/server/features/payments/services/PaymentScheduleService.ts`
</verification>

<success_criteria>
1. paymentSchedules and paymentInstallments tables defined with proper constraints
2. discountCodes table defined with validation constraints
3. workspace_payment_settings extended with split payment fields
4. PaymentScheduleService.calculatePlan returns correct amounts for all plan types
5. PaymentScheduleRepository implements all required queries
6. Migration file ready (not executed)
7. Test coverage >= 80%
</success_criteria>

<output>
After completion, create `.planning/phases/60-payment-flexibility/60-01-SUMMARY.md`
</output>
