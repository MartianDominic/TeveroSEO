---
phase: 69-data-integrity-performance
plan: 04
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - AI-Writer/backend/services/auto_publish_executor.py
  - AI-Writer/backend/services/job_state_machine.py
  - open-seo-main/src/server/lib/job-deduplication.ts
  - open-seo-main/src/server/lib/dead-letter-queue.ts
  - open-seo-main/src/server/lib/circuit-breaker.ts
  - open-seo-main/src/db/schema.ts
autonomous: true
requirements:
  - CRIT-JOB-01
  - CRIT-JOB-02
  - HIGH-JOB-01
  - HIGH-JOB-02
  - HIGH-JOB-03
must_haves:
  truths:
    - Optimistic locking prevents concurrent job claims
    - Job state transitions are atomic
    - Duplicate jobs are deduplicated via Redis
    - Failed jobs go to dead letter queue
    - Circuit breaker shared across workers
  artifacts:
    - AI-Writer/backend/services/job_state_machine.py (transition_job_status)
    - open-seo-main/src/server/lib/job-deduplication.ts (acquireJobLock)
    - open-seo-main/src/server/lib/dead-letter-queue.ts (moveToDeadLetter)
    - open-seo-main/src/server/lib/circuit-breaker.ts (RedisCircuitBreaker)
  key_links:
    - Redis SET NX EX for locks
    - dead_letter_jobs table schema
---

# Plan 69-04: Background Job Reliability

<objective>
Implement optimistic locking, atomic state transitions, job deduplication, dead letter queue, and shared circuit breaker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

## Task 1: Add Optimistic Locking to Article Claims

<read_first>
- AI-Writer/backend/services/auto_publish_executor.py
- AI-Writer/backend/models/article.py
</read_first>

<action>
Update `auto_publish_executor.py`:
- Add version column check in WHERE clause for claim_article
- Increment version on successful claim
- Return None on version mismatch (concurrent claim)
- Add version column to articles table if not exists
</action>

<acceptance_criteria>
- [ ] Version check in WHERE clause
- [ ] Returns None on version mismatch (concurrent claim)
- [ ] Version incremented on successful claim
</acceptance_criteria>

## Task 2: Implement Job State Machine

<read_first>
- AI-Writer/backend/services/auto_publish_executor.py
</read_first>

<action>
Create `AI-Writer/backend/services/job_state_machine.py`:
- Define JobStatus enum (pending, processing, completed, failed, cancelled, retrying)
- Define VALID_TRANSITIONS dict mapping from_status to allowed to_statuses
- Create InvalidTransitionError exception
- Create validate_transition function
- Create transition_job_status async function with atomic WHERE check
</action>

<acceptance_criteria>
- [ ] VALID_TRANSITIONS defines allowed state changes
- [ ] InvalidTransitionError raised for invalid transitions
- [ ] transition_job_status is atomic (WHERE checks current status)
- [ ] File exists: AI-Writer/backend/services/job_state_machine.py
</acceptance_criteria>

## Task 3: Implement Job Deduplication

<read_first>
- open-seo-main/src/server/lib/redis.ts
</read_first>

<action>
Create `open-seo-main/src/server/lib/job-deduplication.ts`:
- acquireJobLock using Redis SET NX EX for atomic lock
- releaseJobLock using Redis Lua script (redis.call) for atomic release
- extendJobLock for long-running jobs
- Use crypto.randomUUID() for lock IDs
</action>

<acceptance_criteria>
- [ ] acquireJobLock uses SET NX EX for atomic lock
- [ ] releaseJobLock only releases if caller owns lock
- [ ] extendJobLock extends TTL for long-running jobs
- [ ] File exists: open-seo-main/src/server/lib/job-deduplication.ts
</acceptance_criteria>

## Task 4: Implement Dead Letter Queue

<read_first>
- open-seo-main/src/db/schema.ts
</read_first>

<action>
1. Add dead_letter_jobs table to schema with columns:
   - id, originalJobId, queue, jobName, data (jsonb)
   - error, stackTrace, failedAt, retryCount, metadata

2. Create `open-seo-main/src/server/lib/dead-letter-queue.ts`:
   - moveToDeadLetter to store failed job with error info
   - replayFromDeadLetter to re-queue and remove from DLQ
</action>

<acceptance_criteria>
- [ ] dead_letter_jobs table schema added
- [ ] moveToDeadLetter stores failed job with error info
- [ ] replayFromDeadLetter re-queues and removes from DLQ
- [ ] File exists: open-seo-main/src/server/lib/dead-letter-queue.ts
</acceptance_criteria>

## Task 5: Implement Redis Circuit Breaker

<read_first>
- open-seo-main/src/server/lib/redis.ts
</read_first>

<action>
Create `open-seo-main/src/server/lib/circuit-breaker.ts`:
- RedisCircuitBreaker class with state in Redis
- States: closed, open, half-open
- Config: failureThreshold (5), resetTimeoutMs (30s), halfOpenRequests (3)
- Methods: getState, isOpen, recordSuccess, recordFailure, execute
- Use Redis INCR for failure counting, SET with PX for state timeout
</action>

<acceptance_criteria>
- [ ] Circuit state shared across workers via Redis
- [ ] Opens after failureThreshold failures
- [ ] Half-open allows limited requests after timeout
- [ ] File exists: open-seo-main/src/server/lib/circuit-breaker.ts
</acceptance_criteria>

</tasks>
