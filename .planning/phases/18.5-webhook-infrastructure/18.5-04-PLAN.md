---
phase: 18.5-webhook-infrastructure
plan: 04
title: Wire Tier 1 Events
subsystem: webhook-integration
tags: [webhooks, events, alerts, rankings]
wave: 3
depends_on: [18.5-02, 18.5-03]
---

# Plan 18.5-04: Wire Tier 1 Events

## Objective
Integrate webhook dispatcher into existing processors to emit events for alerts, rankings, reports, and connections.

## Tasks

### Task 1: Alert Processor Integration

In `alert-processor.ts`, after creating an alert:
- Call `emitAlertTriggered()` from webhook-dispatcher

### Task 2: Ranking Processor Integration

In `ranking-processor.ts`, when detecting position changes:
- Emit `ranking.drop` for significant drops
- Emit `ranking.entered_top_10` when entering top 10
- Emit `ranking.position_1` when reaching #1

### Task 3: API Route Integration

Create webhook API routes for CRUD operations.

## Self-Check

- [ ] Alert processor emits alert.triggered
- [ ] Ranking processor emits ranking events
- [ ] Webhook API routes working
- [ ] TypeScript compiles clean

## Commit Message
```
feat(18.5-04): wire Tier 1 webhook events

- Alert processor emits alert.triggered
- Ranking processor emits ranking.drop
- Webhook CRUD API routes
```
