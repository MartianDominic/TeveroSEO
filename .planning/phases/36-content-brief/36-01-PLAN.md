---
phase: 36-content-brief
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - open-seo-main/src/db/brief-schema.ts
  - open-seo-main/src/db/brief-schema.test.ts
  - open-seo-main/src/server/features/briefs/services/SerpAnalyzer.ts
  - open-seo-main/src/server/features/briefs/services/SerpAnalyzer.test.ts
  - open-seo-main/src/server/lib/cache/serp-cache.ts
  - open-seo-main/src/server/lib/cache/serp-cache.test.ts
autonomous: true
requirements:
  - BRIEF-01
  - BRIEF-02
  - BRIEF-04
  - BRIEF-05

must_haves:
  truths:
    - "content_briefs table exists with correct schema"
    - "SERP analysis extracts competitor data from top 10 results"
    - "SERP results are cached in Redis with 24h TTL"
    - "Status workflow enum values are correctly defined"
  artifacts:
    - path: "open-seo-main/src/db/brief-schema.ts"
      provides: "content_briefs table with FK to keyword_page_mapping"
      contains: "pgTable"
    - path: "open-seo-main/src/server/features/briefs/services/SerpAnalyzer.ts"
      provides: "SERP analysis extraction logic"
      exports: ["analyzeSerpForKeyword", "SerpAnalysis"]
    - path: "open-seo-main/src/server/lib/cache/serp-cache.ts"
      provides: "Redis caching for SERP results"
      exports: ["getCachedSerp", "setCachedSerp", "SERP_CACHE_TTL"]
  key_links:
    - from: "open-seo-main/src/db/brief-schema.ts"
      to: "open-seo-main/src/db/mapping-schema.ts"
      via: "FK reference to keywordPageMapping"
      pattern: "references.*keywordPageMapping"
    - from: "open-seo-main/src/server/features/briefs/services/SerpAnalyzer.ts"
      to: "open-seo-main/src/server/lib/dataforseo.ts"
      via: "fetchLiveSerpItemsRaw import"
      pattern: "fetchLiveSerpItemsRaw"
---

<objective>
Create the content briefs database schema and SERP analysis service with Redis caching.

Purpose: Establish the data layer for content briefs (content_briefs table) and the core SERP analysis capability that extracts competitor patterns (word counts, H2 headings, PAA questions) from DataForSEO results. Redis caching reduces API costs per D-01 decision.

Output: Schema migration for content_briefs table, SerpAnalyzer service with caching, and comprehensive test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-content-brief/36-CONTEXT.md
@.planning/phases/36-content-brief/36-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From open-seo-main/src/db/mapping-schema.ts:
```typescript
export const keywordPageMapping = pgTable(
  "keyword_page_mapping",
  {
    id: text("id").primaryKey(),
    projectId: text("project_id").notNull(),
    keyword: text("keyword").notNull(),
    targetUrl: text("target_url"),
    action: text("action").notNull(), // 'optimize' or 'create'
    relevanceScore: real("relevance_score"),
    searchVolume: integer("search_volume"),
    difficulty: integer("difficulty"),
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().defaultNow(),
    updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().defaultNow(),
  }
);
```

From open-seo-main/src/server/lib/dataforseo.ts:
```typescript
export async function fetchLiveSerpItemsRaw(
  keyword: string,
  locationCode: number,
  languageCode: string,
): Promise<DataforseoApiResponse<SerpLiveItem[]>>;
```

From open-seo-main/src/server/lib/redis.ts:
```typescript
export const redis: Redis;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create content_briefs schema with Wave 0 tests</name>
  <files>open-seo-main/src/db/brief-schema.ts, open-seo-main/src/db/brief-schema.test.ts</files>
  <behavior>
    - Test: BRIEF_STATUSES contains ['draft', 'ready', 'generating', 'published']
    - Test: VOICE_MODES contains ['preservation', 'application', 'best_practices']
    - Test: contentBriefs table has required columns (id, mappingId, keyword, targetWordCount, voiceMode, status, serpAnalysis, articleId, createdAt, updatedAt)
    - Test: serpAnalysis JSONB type matches SerpAnalysisData interface
  </behavior>
  <action>
Create the brief schema following patterns from change-schema.ts:

1. Create `open-seo-main/src/db/brief-schema.test.ts` first with tests for:
   - Status enum values
   - Voice mode enum values  
   - Schema column presence verification
   - JSONB type structure

2. Create `open-seo-main/src/db/brief-schema.ts`:
   - Import from `drizzle-orm/pg-core`
   - Define `BRIEF_STATUSES = ["draft", "ready", "generating", "published"] as const`
   - Define `VOICE_MODES = ["preservation", "application", "best_practices"] as const`
   - Create `contentBriefs` pgTable with:
     - `id: text("id").primaryKey()` (use nanoid at insertion time)
     - `mappingId: text("mapping_id").notNull().references(() => keywordPageMapping.id, { onDelete: "cascade" })`
     - `keyword: text("keyword").notNull()` (denormalized for quick access)
     - `targetWordCount: integer("target_word_count").notNull()`
     - `voiceMode: text("voice_mode").notNull()` (one of VOICE_MODES)
     - `status: text("status").notNull().default("draft")` (one of BRIEF_STATUSES)
     - `serpAnalysis: jsonb("serp_analysis").$type<SerpAnalysisData>()` (see interface below)
     - `articleId: text("article_id")` (nullable FK to AI-Writer articles)
     - `createdAt, updatedAt` with timezone timestamps
   - Define `SerpAnalysisData` interface:
     ```typescript
     interface SerpAnalysisData {
       commonH2s: { heading: string; frequency: number }[];
       paaQuestions: string[];
       competitorWordCounts: number[];
       metaLengths: { title: number; description: number };
       analyzedAt: string; // ISO timestamp
       location: string;
     }
     ```
   - Add indexes on mappingId and status
   - Export inferred types: `ContentBriefSelect`, `ContentBriefInsert`

3. Import and re-export from `open-seo-main/src/db/index.ts`
  </action>
  <verify>
    <automated>npx vitest run open-seo-main/src/db/brief-schema.test.ts</automated>
  </verify>
  <done>Schema file exists with all columns, enums exported, tests pass, types inferred correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SERP cache service with Redis TTL</name>
  <files>open-seo-main/src/server/lib/cache/serp-cache.ts, open-seo-main/src/server/lib/cache/serp-cache.test.ts</files>
  <behavior>
    - Test: getCachedSerp returns null for non-existent key
    - Test: setCachedSerp stores data with correct TTL (24h = 86400s)
    - Test: getCachedSerp returns cached data after setCachedSerp
    - Test: cache key format is "serp:{mappingId}:{keyword}"
    - Test: SERP_CACHE_TTL = 86400 (24 hours in seconds)
  </behavior>
  <action>
Create the SERP cache service following ioredis patterns from redis.ts:

1. Create `open-seo-main/src/server/lib/cache/serp-cache.test.ts` first:
   - Mock redis using vi.mock
   - Test getCachedSerp returns null when redis.get returns null
   - Test setCachedSerp calls redis.setex with correct TTL
   - Test cache key generation

2. Create `open-seo-main/src/server/lib/cache/serp-cache.ts`:
   - Import `redis` singleton from `@/server/lib/redis`
   - Import `SerpAnalysisData` type from brief-schema
   - Export `SERP_CACHE_TTL = 24 * 60 * 60` (86400 seconds)
   - Export `buildSerpCacheKey(mappingId: string, keyword: string): string`
     - Returns `serp:${mappingId}:${keyword}`
   - Export `getCachedSerp(key: string): Promise<SerpAnalysisData | null>`
     - Call `redis.get(key)`
     - Return `JSON.parse(result)` or null
   - Export `setCachedSerp(key: string, data: SerpAnalysisData): Promise<void>`
     - Call `redis.setex(key, SERP_CACHE_TTL, JSON.stringify(data))`
   - Export `invalidateSerpCache(key: string): Promise<void>`
     - Call `redis.del(key)`
  </action>
  <verify>
    <automated>npx vitest run open-seo-main/src/server/lib/cache/serp-cache.test.ts</automated>
  </verify>
  <done>Cache service exports getCachedSerp, setCachedSerp, invalidateSerpCache; tests pass; 24h TTL enforced</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create SerpAnalyzer service with competitor extraction</name>
  <files>open-seo-main/src/server/features/briefs/services/SerpAnalyzer.ts, open-seo-main/src/server/features/briefs/services/SerpAnalyzer.test.ts</files>
  <behavior>
    - Test: extractCommonH2s returns headings with frequency >= 3
    - Test: extractPAAQuestions extracts all PAA items from SERP response
    - Test: calculateWordCountStats returns min/max/avg from competitor word counts
    - Test: calculateMetaLengths returns average title/description lengths
    - Test: analyzeSerpForKeyword uses cache when available
    - Test: analyzeSerpForKeyword calls DataForSEO when cache miss
    - Test: analyzeSerpForKeyword stores result in cache after API call
  </behavior>
  <action>
Create the SERP analyzer service per RESEARCH.md patterns:

1. Create `open-seo-main/src/server/features/briefs/services/SerpAnalyzer.test.ts` first:
   - Mock fetchLiveSerpItemsRaw from dataforseo.ts
   - Mock getCachedSerp/setCachedSerp from serp-cache
   - Test extraction functions with fixture data
   - Test cache hit/miss behavior

2. Create directories: `mkdir -p open-seo-main/src/server/features/briefs/services`

3. Create `open-seo-main/src/server/features/briefs/services/SerpAnalyzer.ts`:
   - Import fetchLiveSerpItemsRaw from dataforseo.ts
   - Import cache functions from serp-cache.ts
   - Import SerpAnalysisData type from brief-schema
   
   Pure extraction functions:
   - `extractCommonH2s(items: SerpLiveItem[]): { heading: string; frequency: number }[]`
     - Collect h2 headings from `item.items` where `type === "organic"`
     - Count frequencies, filter frequency >= 3, sort by frequency desc
   - `extractPAAQuestions(items: SerpLiveItem[]): string[]`
     - Filter items where `type === "people_also_ask"`
     - Extract question text from each PAA item
   - `calculateWordCountStats(items: SerpLiveItem[]): { min: number; max: number; avg: number }`
     - Filter organic items with word_count
     - Calculate min/max/avg
   - `calculateMetaLengths(items: SerpLiveItem[]): { title: number; description: number }`
     - Average title and description lengths from organic items
   
   Main function:
   - `analyzeSerpForKeyword(mappingId: string, keyword: string, locationCode: number = 2840): Promise<SerpAnalysisData>`
     - Build cache key with buildSerpCacheKey
     - Check cache first with getCachedSerp
     - If cached, return cached data
     - Else call fetchLiveSerpItemsRaw(keyword, locationCode, "en")
     - Extract data using pure functions above
     - Build SerpAnalysisData object with analyzedAt timestamp
     - Cache with setCachedSerp
     - Return analysis

4. Export types and main function
  </action>
  <verify>
    <automated>npx vitest run open-seo-main/src/server/features/briefs/services/SerpAnalyzer.test.ts</automated>
  </verify>
  <done>SerpAnalyzer service extracts H2s, PAA, word counts, meta lengths from SERP; uses Redis cache with 24h TTL; all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataForSEO API | External service, untrusted response data |
| Redis Cache | Internal storage, trusted but validate on read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | SerpAnalyzer | mitigate | Validate DataForSEO response with Zod schema before extraction |
| T-36-02 | Information Disclosure | Cache Keys | mitigate | Use mappingId (internal ID) not raw keyword in cache keys to prevent enumeration |
| T-36-03 | Denial of Service | Cache | mitigate | TTL prevents unbounded cache growth; max 24h retention |
</threat_model>

<verification>
1. Schema migration can be generated: `cd open-seo-main && npx drizzle-kit generate`
2. All unit tests pass: `npx vitest run src/db/brief-schema.test.ts src/server/lib/cache/serp-cache.test.ts src/server/features/briefs/services/SerpAnalyzer.test.ts`
3. TypeScript compiles: `cd open-seo-main && npx tsc --noEmit`
</verification>

<success_criteria>
- content_briefs schema exists with FK to keyword_page_mapping
- Status enum: draft, ready, generating, published
- Voice mode enum: preservation, application, best_practices
- SerpAnalyzer extracts top 10 competitor data
- Redis cache with 24h TTL reduces API costs
- All Wave 0 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/36-content-brief/36-01-SUMMARY.md`
</output>
