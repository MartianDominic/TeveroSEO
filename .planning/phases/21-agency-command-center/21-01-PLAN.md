---
phase: 21-agency-command-center
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - open-seo-main/src/db/dashboard-schema.ts
  - open-seo-main/src/db/schema.ts
  - open-seo-main/drizzle/0009_dashboard_metrics.sql
  - open-seo-main/src/lib/dashboard/health-score.ts
  - open-seo-main/src/lib/dashboard/health-score.test.ts
  - open-seo-main/src/server/queues/dashboardMetricsQueue.ts
  - open-seo-main/src/server/workers/dashboard-metrics-processor.ts
  - open-seo-main/src/server/workers/dashboard-metrics-processor.test.ts
  - open-seo-main/src/server/workers/dashboard-metrics-worker.ts
  - open-seo-main/src/worker-entry.ts
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-12
user_setup: []

must_haves:
  truths:
    - "Pre-computed metrics exist for all clients"
    - "Health score calculation produces 0-100 value"
    - "BullMQ job runs every 5 minutes"
    - "Metrics include traffic, keywords, alerts data"
  artifacts:
    - path: "open-seo-main/src/db/dashboard-schema.ts"
      provides: "Dashboard metrics Drizzle schema"
      contains: "client_dashboard_metrics"
    - path: "open-seo-main/src/lib/dashboard/health-score.ts"
      provides: "Health score calculation algorithm"
      exports: ["computeHealthScore"]
    - path: "open-seo-main/src/server/workers/dashboard-metrics-worker.ts"
      provides: "BullMQ worker for metrics computation"
      exports: ["startDashboardMetricsWorker", "stopDashboardMetricsWorker"]
  key_links:
    - from: "open-seo-main/src/server/workers/dashboard-metrics-processor.ts"
      to: "open-seo-main/src/lib/dashboard/health-score.ts"
      via: "import computeHealthScore"
      pattern: "computeHealthScore\\("
    - from: "open-seo-main/src/worker-entry.ts"
      to: "open-seo-main/src/server/workers/dashboard-metrics-worker.ts"
      via: "startDashboardMetricsWorker call"
      pattern: "startDashboardMetricsWorker\\(\\)"
---

<objective>
Create the data foundation for the Agency Command Center dashboard. This includes:
1. Database schema for pre-computed client metrics (client_dashboard_metrics, portfolio_activity, dashboard_views)
2. Health score algorithm with weighted components (traffic 30%, rankings 25%, technical 20%, backlinks 15%, content 10%)
3. BullMQ worker that computes metrics every 5 minutes

Purpose: Enable < 1s dashboard load for 100+ clients by pre-computing expensive aggregations rather than computing on each page load.

Output: Drizzle migration, health score module with tests, BullMQ worker integrated into worker-entry.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 21 section)
@.planning/phases/21-agency-command-center/21-RESEARCH.md

<interfaces>
<!-- Existing schema patterns from alert-schema.ts -->
From open-seo-main/src/db/alert-schema.ts:
```typescript
import {
  pgTable,
  text,
  integer,
  boolean,
  timestamp,
  jsonb,
  index,
  uniqueIndex,
} from "drizzle-orm/pg-core";

export const alerts = pgTable(
  "alerts",
  {
    id: text("id").primaryKey(),
    clientId: text("client_id").notNull(),
    // ... columns
    createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().defaultNow(),
  },
  (table) => [
    index("ix_alerts_client_status").on(table.clientId, table.status),
  ],
);
```

<!-- BullMQ queue pattern from rankingQueue.ts -->
From open-seo-main/src/server/queues/rankingQueue.ts:
```typescript
import { Queue, type JobsOptions } from "bullmq";
import { getSharedBullMQConnection } from "@/server/lib/redis";
import { createLogger } from "@/server/lib/logger";

const log = createLogger({ module: "rankingQueue" });

export const RANKING_QUEUE_NAME = "keyword-ranking" as const;

const DEFAULT_JOB_OPTIONS: JobsOptions = {
  attempts: 3,
  backoff: { type: "exponential", delay: 10_000 },
  removeOnComplete: { count: 50 },
  removeOnFail: { count: 100 },
};

export const rankingQueue = new Queue<RankingJobData>(
  RANKING_QUEUE_NAME,
  {
    connection: getSharedBullMQConnection("queue:ranking"),
    defaultJobOptions: DEFAULT_JOB_OPTIONS,
  },
);

export async function initRankingScheduler(): Promise<void> {
  const repeatableJobs = await rankingQueue.getRepeatableJobs();
  for (const job of repeatableJobs) {
    await rankingQueue.removeRepeatableByKey(job.key);
  }
  await rankingQueue.add(
    "compute-metrics",
    { triggeredAt: new Date().toISOString() },
    {
      repeat: { pattern: "*/5 * * * *" }, // Every 5 minutes
      jobId: "dashboard-metrics-compute",
    },
  );
}
```

<!-- Worker startup pattern from worker-entry.ts -->
From open-seo-main/src/worker-entry.ts:
```typescript
import { startRankingWorker, stopRankingWorker } from "./server/workers/ranking-worker";

// Startup
await startRankingWorker();

// Shutdown
process.on("SIGTERM", async () => {
  await stopRankingWorker();
  process.exit(0);
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create dashboard schema with migrations</name>
  <files>
    open-seo-main/src/db/dashboard-schema.ts,
    open-seo-main/src/db/schema.ts,
    open-seo-main/drizzle/0009_dashboard_metrics.sql
  </files>
  <read_first>
    open-seo-main/src/db/alert-schema.ts,
    open-seo-main/src/db/schema.ts,
    open-seo-main/drizzle/0008_webhooks.sql
  </read_first>
  <behavior>
    - Test 1: clientDashboardMetrics table has all required columns (health_score, traffic_current, traffic_previous, keywords_total, keywords_top_10, etc.)
    - Test 2: portfolioActivity table has workspace_id, client_id, event_type, event_data columns
    - Test 3: dashboardViews table has workspace_id, user_id, name, filters, layout, is_default columns
  </behavior>
  <action>
Create `open-seo-main/src/db/dashboard-schema.ts` with three tables:

1. `client_dashboard_metrics` table:
   - id: text("id").primaryKey() - use crypto.randomUUID() format
   - clientId: text("client_id").notNull().unique() - one row per client
   - healthScore: integer("health_score").notNull().default(100)
   - healthBreakdown: jsonb("health_breakdown").$type<{ traffic: number; rankings: number; technical: number; backlinks: number; content: number }>()
   - trafficCurrent: integer("traffic_current").default(0)
   - trafficPrevious: integer("traffic_previous").default(0)
   - trafficTrendPct: text("traffic_trend_pct") - store as text, convert to decimal in app
   - keywordsTotal: integer("keywords_total").default(0)
   - keywordsTop10: integer("keywords_top_10").default(0)
   - keywordsTop3: integer("keywords_top_3").default(0)
   - keywordsPosition1: integer("keywords_position_1").default(0)
   - keywordsDistribution: jsonb("keywords_distribution").$type<Record<string, number>>()
   - backlinksTotal: integer("backlinks_total").default(0)
   - backlinksNewMonth: integer("backlinks_new_month").default(0)
   - alertsOpen: integer("alerts_open").default(0)
   - alertsCritical: integer("alerts_critical").default(0)
   - lastReportAt: timestamp("last_report_at", { withTimezone: true, mode: "date" })
   - lastAuditAt: timestamp("last_audit_at", { withTimezone: true, mode: "date" })
   - computedAt: timestamp("computed_at", { withTimezone: true, mode: "date" }).notNull().defaultNow()
   - Indexes: ix_dashboard_metrics_computed on (computedAt)

2. `portfolio_activity` table:
   - id: text("id").primaryKey()
   - workspaceId: text("workspace_id").notNull()
   - clientId: text("client_id") - nullable for workspace-level events
   - eventType: text("event_type").notNull()
   - eventData: jsonb("event_data").$type<Record<string, unknown>>().notNull()
   - createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().defaultNow()
   - Indexes: ix_activity_workspace_created on (workspaceId, createdAt), ix_activity_client on (clientId)

3. `dashboard_views` table:
   - id: text("id").primaryKey()
   - workspaceId: text("workspace_id").notNull()
   - userId: text("user_id") - nullable for shared views
   - name: text("name").notNull()
   - filters: jsonb("filters").$type<Record<string, unknown>>().notNull()
   - layout: jsonb("layout").$type<{ cardOrder: string[] }>()
   - isDefault: boolean("is_default").notNull().default(false)
   - createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().defaultNow()
   - updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().defaultNow()
   - Indexes: ix_views_workspace_user on (workspaceId, userId), ix_views_default on (workspaceId, isDefault)

Export types: ClientDashboardMetricsSelect, ClientDashboardMetricsInsert, PortfolioActivitySelect, PortfolioActivityInsert, DashboardViewSelect, DashboardViewInsert

Update `open-seo-main/src/db/schema.ts` to export from dashboard-schema.ts.

Generate migration via `pnpm db:generate` or manually create 0009_dashboard_metrics.sql following 0008_webhooks.sql pattern.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm tsc --noEmit && grep -q "client_dashboard_metrics" src/db/dashboard-schema.ts && grep -q "portfolio_activity" src/db/dashboard-schema.ts && grep -q "dashboard_views" src/db/dashboard-schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `grep -c "pgTable" open-seo-main/src/db/dashboard-schema.ts` returns 3
    - [ ] `grep "health_score" open-seo-main/src/db/dashboard-schema.ts` shows integer column
    - [ ] `grep "export.*from.*dashboard-schema" open-seo-main/src/db/schema.ts` returns match
    - [ ] Migration file 0009_dashboard_metrics.sql exists with CREATE TABLE statements
    - [ ] TypeScript compiles without errors: `cd open-seo-main && pnpm tsc --noEmit`
  </acceptance_criteria>
  <done>Three Drizzle tables defined, types exported, migration created, schema.ts updated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create health score algorithm with tests</name>
  <files>
    open-seo-main/src/lib/dashboard/health-score.ts,
    open-seo-main/src/lib/dashboard/health-score.test.ts
  </files>
  <read_first>
    .planning/phases/21-agency-command-center/21-RESEARCH.md (Pattern 5: Health Score Calculation)
  </read_first>
  <behavior>
    - Test 1: Perfect client (no alerts, good traffic, >80% top 10) scores 90-100
    - Test 2: Client with critical alert scores < 60
    - Test 3: Client with >20% traffic drop scores < 70
    - Test 4: Client with stale connection scores < 85
    - Test 5: Score is clamped between 0 and 100
    - Test 6: Empty inputs return 100 (healthy default)
  </behavior>
  <action>
Create `open-seo-main/src/lib/dashboard/health-score.ts`:

```typescript
/**
 * Health score calculation for client dashboard metrics.
 * Weighted composite: traffic 30%, rankings 25%, technical 20%, backlinks 15%, content 10%
 */

export interface HealthInputs {
  trafficTrend: number;         // WoW change as decimal (-0.2 = 20% drop)
  alertsCritical: number;       // Count of critical alerts
  alertsWarning: number;        // Count of warning alerts
  keywordsTop10Pct: number;     // % of keywords in top 10 (0-100)
  backlinksLostPct: number;     // % backlinks lost this month (0-1)
  lastReportDaysAgo: number;    // Days since last report
  connectionStale: boolean;     // GSC/GA4 token expired or stale
}

export interface HealthBreakdown {
  traffic: number;    // 0-30 points
  rankings: number;   // 0-25 points  
  technical: number;  // 0-20 points
  backlinks: number;  // 0-15 points
  content: number;    // 0-10 points
}

export interface HealthResult {
  score: number;         // 0-100
  breakdown: HealthBreakdown;
}

export function computeHealthScore(inputs: Partial<HealthInputs>): HealthResult {
  // Default values for missing inputs
  const {
    trafficTrend = 0,
    alertsCritical = 0,
    alertsWarning = 0,
    keywordsTop10Pct = 100,
    backlinksLostPct = 0,
    lastReportDaysAgo = 0,
    connectionStale = false,
  } = inputs;

  // Traffic health (30 points max)
  let trafficScore = 30;
  if (trafficTrend < -0.2) trafficScore -= 20;       // >20% drop
  else if (trafficTrend < -0.1) trafficScore -= 10; // >10% drop
  else if (trafficTrend > 0.1) trafficScore = Math.min(30, trafficScore + 5); // >10% gain bonus

  // Technical health (20 points max)
  let technicalScore = 20;
  technicalScore -= Math.min(20, alertsCritical * 10); // -10 per critical, max -20
  technicalScore -= Math.min(technicalScore, alertsWarning * 5); // -5 per warning

  // Ranking health (25 points max)
  const rankingScore = Math.round((keywordsTop10Pct / 100) * 25);

  // Backlink health (15 points max)
  let backlinkScore = 15;
  if (backlinksLostPct > 0.1) backlinkScore -= 10;      // >10% lost
  else if (backlinksLostPct > 0.05) backlinkScore -= 5; // >5% lost

  // Content freshness (10 points max)
  let contentScore = 10;
  if (lastReportDaysAgo > 30) contentScore -= 10;       // No report 30+ days
  else if (lastReportDaysAgo > 14) contentScore -= 5;   // No report 14+ days

  // Connection penalty (applied to total)
  const connectionPenalty = connectionStale ? 15 : 0;

  const breakdown: HealthBreakdown = {
    traffic: Math.max(0, trafficScore),
    rankings: Math.max(0, rankingScore),
    technical: Math.max(0, technicalScore),
    backlinks: Math.max(0, backlinkScore),
    content: Math.max(0, contentScore),
  };

  const rawScore = breakdown.traffic + breakdown.rankings + breakdown.technical + breakdown.backlinks + breakdown.content - connectionPenalty;
  const score = Math.max(0, Math.min(100, rawScore));

  return { score, breakdown };
}
```

Create test file with all behavior tests.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm test src/lib/dashboard/health-score.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `grep "computeHealthScore" open-seo-main/src/lib/dashboard/health-score.ts` shows exported function
    - [ ] Test file has 6+ test cases covering all behavior
    - [ ] All tests pass: `cd open-seo-main && pnpm test src/lib/dashboard/health-score.test.ts --run`
    - [ ] Perfect client (empty inputs) returns score >= 90
    - [ ] Client with alertsCritical: 2 returns score < 60
  </acceptance_criteria>
  <done>Health score algorithm implemented with comprehensive test coverage</done>
</task>

<task type="auto">
  <name>Task 3: Create BullMQ metrics queue and processor</name>
  <files>
    open-seo-main/src/server/queues/dashboardMetricsQueue.ts,
    open-seo-main/src/server/workers/dashboard-metrics-processor.ts,
    open-seo-main/src/server/workers/dashboard-metrics-processor.test.ts
  </files>
  <read_first>
    open-seo-main/src/server/queues/rankingQueue.ts,
    open-seo-main/src/server/workers/ranking-processor.ts,
    open-seo-main/src/db/dashboard-schema.ts
  </read_first>
  <action>
Create `open-seo-main/src/server/queues/dashboardMetricsQueue.ts`:

- DASHBOARD_METRICS_QUEUE_NAME = "dashboard-metrics"
- DashboardMetricsJobData interface: { triggeredAt: string }
- DashboardMetricsDLQJobData for dead-letter queue
- DEFAULT_JOB_OPTIONS: 3 attempts, exponential backoff (30s, 60s, 120s)
- dashboardMetricsQueue using getSharedBullMQConnection("queue:dashboard-metrics")
- initDashboardMetricsScheduler(): Creates repeatable job with pattern "*/5 * * * *" (every 5 minutes)
- triggerDashboardMetricsCompute(): Manual trigger for testing

Create `open-seo-main/src/server/workers/dashboard-metrics-processor.ts`:

```typescript
import { Job } from "bullmq";
import { db } from "@/db";
import { clientDashboardMetrics } from "@/db/dashboard-schema";
import { alerts } from "@/db/alert-schema";
import { savedKeywords, keywordRankings } from "@/db/ranking-schema";
import { gscSnapshots } from "@/db/analytics-schema";
import { computeHealthScore } from "@/lib/dashboard/health-score";
import { eq, sql, and, gte, desc } from "drizzle-orm";
import { createLogger } from "@/server/lib/logger";

const log = createLogger({ module: "dashboard-metrics-processor" });

export async function processDashboardMetrics(job: Job): Promise<void> {
  log.info("Starting dashboard metrics computation", { jobId: job.id });
  
  // Get all unique client IDs from alerts table (proxy for active clients)
  // In production, this should query the clients table from AI-Writer
  const clientsResult = await db
    .selectDistinct({ clientId: alerts.clientId })
    .from(alerts);
  
  const clientIds = clientsResult.map(r => r.clientId);
  log.info(`Processing ${clientIds.length} clients`);

  for (const clientId of clientIds) {
    try {
      await computeClientMetrics(clientId);
    } catch (error) {
      log.error(`Failed to compute metrics for client ${clientId}`, { error });
      // Continue with other clients
    }
  }

  log.info("Dashboard metrics computation complete", { 
    clientCount: clientIds.length,
    jobId: job.id 
  });
}

async function computeClientMetrics(clientId: string): Promise<void> {
  // 1. Get traffic data (last 30 days vs previous 30 days)
  const now = new Date();
  const thirtyDaysAgo = new Date(now.getTime() - 30 * 24 * 60 * 60 * 1000);
  const sixtyDaysAgo = new Date(now.getTime() - 60 * 24 * 60 * 60 * 1000);

  // Query GSC snapshots for traffic
  const trafficCurrent = await db
    .select({ total: sql<number>`COALESCE(SUM(clicks), 0)` })
    .from(gscSnapshots)
    .where(and(
      eq(gscSnapshots.clientId, clientId),
      gte(gscSnapshots.date, thirtyDaysAgo.toISOString().split('T')[0])
    ));

  const trafficPrevious = await db
    .select({ total: sql<number>`COALESCE(SUM(clicks), 0)` })
    .from(gscSnapshots)
    .where(and(
      eq(gscSnapshots.clientId, clientId),
      gte(gscSnapshots.date, sixtyDaysAgo.toISOString().split('T')[0]),
      sql`date < ${thirtyDaysAgo.toISOString().split('T')[0]}`
    ));

  const currentClicks = Number(trafficCurrent[0]?.total ?? 0);
  const previousClicks = Number(trafficPrevious[0]?.total ?? 0);
  const trafficTrendPct = previousClicks > 0 
    ? ((currentClicks - previousClicks) / previousClicks) 
    : 0;

  // 2. Get keyword stats
  const keywordStats = await db
    .select({
      total: sql<number>`COUNT(*)`,
    })
    .from(savedKeywords)
    .where(eq(savedKeywords.projectId, clientId)); // Note: may need to join projects

  const keywordsTotal = Number(keywordStats[0]?.total ?? 0);

  // Get latest rankings for position distribution
  // Simplified: in production, join with latest ranking per keyword
  const top10Count = 0; // Placeholder - needs ranking join
  const top3Count = 0;
  const position1Count = 0;

  // 3. Get alert counts
  const alertCounts = await db
    .select({
      severity: alerts.severity,
      count: sql<number>`COUNT(*)`,
    })
    .from(alerts)
    .where(and(
      eq(alerts.clientId, clientId),
      eq(alerts.status, "pending")
    ))
    .groupBy(alerts.severity);

  const alertsCritical = Number(alertCounts.find(a => a.severity === "critical")?.count ?? 0);
  const alertsWarning = Number(alertCounts.find(a => a.severity === "warning")?.count ?? 0);
  const alertsOpen = alertsCritical + alertsWarning + Number(alertCounts.find(a => a.severity === "info")?.count ?? 0);

  // 4. Compute health score
  const healthResult = computeHealthScore({
    trafficTrend: trafficTrendPct,
    alertsCritical,
    alertsWarning,
    keywordsTop10Pct: keywordsTotal > 0 ? (top10Count / keywordsTotal) * 100 : 100,
    backlinksLostPct: 0, // Placeholder - needs backlink data
    lastReportDaysAgo: 0, // Placeholder - needs report query
    connectionStale: false, // Placeholder - needs OAuth token check
  });

  // 5. Upsert metrics
  const id = crypto.randomUUID();
  await db
    .insert(clientDashboardMetrics)
    .values({
      id,
      clientId,
      healthScore: healthResult.score,
      healthBreakdown: healthResult.breakdown,
      trafficCurrent: currentClicks,
      trafficPrevious: previousClicks,
      trafficTrendPct: trafficTrendPct.toFixed(4),
      keywordsTotal,
      keywordsTop10: top10Count,
      keywordsTop3: top3Count,
      keywordsPosition1: position1Count,
      keywordsDistribution: { top10: top10Count, top3: top3Count, position1: position1Count },
      backlinksTotal: 0,
      backlinksNewMonth: 0,
      alertsOpen,
      alertsCritical,
      computedAt: new Date(),
    })
    .onConflictDoUpdate({
      target: clientDashboardMetrics.clientId,
      set: {
        healthScore: healthResult.score,
        healthBreakdown: healthResult.breakdown,
        trafficCurrent: currentClicks,
        trafficPrevious: previousClicks,
        trafficTrendPct: trafficTrendPct.toFixed(4),
        keywordsTotal,
        keywordsTop10: top10Count,
        keywordsTop3: top3Count,
        keywordsPosition1: position1Count,
        keywordsDistribution: { top10: top10Count, top3: top3Count, position1: position1Count },
        alertsOpen,
        alertsCritical,
        computedAt: new Date(),
      },
    });
}
```

Create basic test file verifying processor exports and basic structure.
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm tsc --noEmit && grep -q "DASHBOARD_METRICS_QUEUE_NAME" src/server/queues/dashboardMetricsQueue.ts && grep -q "processDashboardMetrics" src/server/workers/dashboard-metrics-processor.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `grep "initDashboardMetricsScheduler" open-seo-main/src/server/queues/dashboardMetricsQueue.ts` returns match
    - [ ] `grep "*/5 \* \* \* \*" open-seo-main/src/server/queues/dashboardMetricsQueue.ts` shows 5-minute cron
    - [ ] `grep "processDashboardMetrics" open-seo-main/src/server/workers/dashboard-metrics-processor.ts` shows exported function
    - [ ] `grep "computeHealthScore" open-seo-main/src/server/workers/dashboard-metrics-processor.ts` shows health score import
    - [ ] TypeScript compiles without errors
  </acceptance_criteria>
  <done>BullMQ queue defined with 5-minute repeatable job, processor computes and upserts metrics</done>
</task>

<task type="auto">
  <name>Task 4: Create worker and integrate into worker-entry.ts</name>
  <files>
    open-seo-main/src/server/workers/dashboard-metrics-worker.ts,
    open-seo-main/src/worker-entry.ts
  </files>
  <read_first>
    open-seo-main/src/server/workers/ranking-worker.ts,
    open-seo-main/src/worker-entry.ts
  </read_first>
  <action>
Create `open-seo-main/src/server/workers/dashboard-metrics-worker.ts`:

```typescript
/**
 * BullMQ Worker for dashboard metrics computation.
 * Runs every 5 minutes to pre-compute client dashboard metrics.
 */

import { Worker } from "bullmq";
import { getSharedBullMQConnection } from "@/server/lib/redis";
import { createLogger } from "@/server/lib/logger";
import {
  DASHBOARD_METRICS_QUEUE_NAME,
  initDashboardMetricsScheduler,
  dashboardMetricsQueue,
  type DashboardMetricsJobData,
  type DashboardMetricsDLQJobData,
} from "@/server/queues/dashboardMetricsQueue";
import { processDashboardMetrics } from "./dashboard-metrics-processor";

const log = createLogger({ module: "dashboard-metrics-worker" });

let worker: Worker | null = null;

export async function startDashboardMetricsWorker(): Promise<void> {
  // Initialize the scheduler (adds repeatable job)
  await initDashboardMetricsScheduler();

  worker = new Worker<DashboardMetricsJobData>(
    DASHBOARD_METRICS_QUEUE_NAME,
    async (job) => {
      if (job.name === "dlq:dashboard-metrics") {
        log.warn("DLQ job received", { jobId: job.id, data: job.data });
        return;
      }
      await processDashboardMetrics(job);
    },
    {
      connection: getSharedBullMQConnection("worker:dashboard-metrics"),
      concurrency: 1, // Single metrics computation at a time
      lockDuration: 300_000, // 5 minutes (must complete before next run)
      maxStalledCount: 2,
    },
  );

  worker.on("completed", (job) => {
    log.info("Dashboard metrics job completed", { jobId: job?.id });
  });

  worker.on("failed", async (job, error) => {
    log.error("Dashboard metrics job failed", { 
      jobId: job?.id, 
      error: error.message,
      attemptsMade: job?.attemptsMade 
    });

    // Route to DLQ after max retries
    if (job && job.attemptsMade >= 3) {
      const dlqData: DashboardMetricsDLQJobData = {
        originalJobId: job.id,
        originalJobName: job.name,
        data: job.data as DashboardMetricsJobData,
        error: error.message,
        stack: error.stack,
        failedAt: new Date().toISOString(),
        attemptsMade: job.attemptsMade,
      };
      await dashboardMetricsQueue.add("dlq:dashboard-metrics", dlqData);
    }
  });

  log.info("Dashboard metrics worker started");
}

export async function stopDashboardMetricsWorker(): Promise<void> {
  if (worker) {
    await worker.close(false); // Graceful shutdown, wait for current job
    log.info("Dashboard metrics worker stopped");
    worker = null;
  }
}
```

Update `open-seo-main/src/worker-entry.ts`:
- Add import: `import { startDashboardMetricsWorker, stopDashboardMetricsWorker } from "./server/workers/dashboard-metrics-worker";`
- Add to startup: `await startDashboardMetricsWorker();`
- Add to SIGTERM handler: `await stopDashboardMetricsWorker();`
- Add to SIGINT handler: `await stopDashboardMetricsWorker();`
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm tsc --noEmit && grep -q "startDashboardMetricsWorker" src/worker-entry.ts && grep -q "stopDashboardMetricsWorker" src/worker-entry.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `grep "startDashboardMetricsWorker" open-seo-main/src/server/workers/dashboard-metrics-worker.ts` shows exported function
    - [ ] `grep "stopDashboardMetricsWorker" open-seo-main/src/server/workers/dashboard-metrics-worker.ts` shows exported function
    - [ ] `grep "dashboard-metrics-worker" open-seo-main/src/worker-entry.ts` shows import
    - [ ] `grep -c "startDashboardMetricsWorker" open-seo-main/src/worker-entry.ts` returns at least 2 (import + call)
    - [ ] TypeScript compiles without errors
  </acceptance_criteria>
  <done>Worker created with start/stop functions, integrated into worker-entry.ts startup and shutdown</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker -> Database | Worker writes computed metrics; untrusted if worker compromised |
| Database -> Frontend | Pre-computed metrics served to dashboard; data integrity critical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | dashboard-metrics-processor | mitigate | Metrics computed server-side only; no client input to computation |
| T-21-02 | Information Disclosure | client_dashboard_metrics | mitigate | Query scoped by workspace_id in downstream plans; row-level access control |
| T-21-03 | Denial of Service | BullMQ worker | accept | Worker has concurrency 1, 5-min schedule; cannot be triggered externally |
</threat_model>

<verification>
After completing all tasks, verify:

```bash
# Schema verification
cd open-seo-main
grep -q "client_dashboard_metrics" src/db/dashboard-schema.ts
grep -q "portfolio_activity" src/db/dashboard-schema.ts
grep -q "dashboard_views" src/db/dashboard-schema.ts

# Health score tests pass
pnpm test src/lib/dashboard/health-score.test.ts --run

# TypeScript compiles
pnpm tsc --noEmit

# Worker integration
grep "startDashboardMetricsWorker" src/worker-entry.ts
grep "stopDashboardMetricsWorker" src/worker-entry.ts
```
</verification>

<success_criteria>
- [ ] Three database tables created: client_dashboard_metrics, portfolio_activity, dashboard_views
- [ ] Migration file 0009_dashboard_metrics.sql exists
- [ ] Health score algorithm passes all tests (6+ test cases)
- [ ] BullMQ queue configured with 5-minute repeatable job
- [ ] Processor computes metrics and upserts to database
- [ ] Worker integrated into worker-entry.ts with graceful shutdown
- [ ] TypeScript compiles without errors across all new files
</success_criteria>

<output>
After completion, create `.planning/phases/21-agency-command-center/21-01-SUMMARY.md`
</output>
