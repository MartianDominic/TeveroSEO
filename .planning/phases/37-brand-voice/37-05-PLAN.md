---
phase: 37-brand-voice
plan: 05
type: execute
wave: 1
depends_on: [37-01, 37-02, 37-03]
files_modified:
  - open-seo-main/src/server/features/voice/services/VoiceAnalysisService.ts
  - open-seo-main/src/server/features/voice/services/ProtectionEnforcementService.ts
  - open-seo-main/src/server/workers/voice-analysis-processor.ts
  - AI-Writer/backend/services/article_generation_service.py
autonomous: true
requirements: []
gap_closure: true
gaps_addressed:
  - "Gap 1: Voice learning extraction logic (VoiceAnalysisService)"
  - "Gap 2: Protection enforcement (ProtectionEnforcementService)"
  - "Gap 3: AI-Writer voice integration"

must_haves:
  truths:
    - "Voice learning: analyze 5-10 pages → extract 40+ dimensions → create profile"
    - "Preservation mode: protect tagged content"
    - "Application mode: generate in client voice using profile"
  artifacts:
    - path: "open-seo-main/src/server/features/voice/services/VoiceAnalysisService.ts"
      provides: "Voice learning service that wraps VoiceAnalyzer"
      min_lines: 50
    - path: "open-seo-main/src/server/features/voice/services/ProtectionEnforcementService.ts"
      provides: "Content protection logic for preservation mode"
      min_lines: 80
    - path: "AI-Writer/backend/services/article_generation_service.py"
      provides: "Voice-constrained generation"
      contains: ["fetch_voice_profile", "voice_constraints"]
  key_links:
    - from: "voice-analysis-processor.ts"
      to: "VoiceAnalysisService"
      via: "import and call"
      pattern: "VoiceAnalysisService.*analyzePages"
    - from: "article_generation_service.py"
      to: "open-seo voice API"
      via: "httpx fetch"
      pattern: "fetch_voice_profile"
---

<objective>
Close gaps 1, 2, and 3 from Phase 37 verification. Implement missing voice learning extraction logic, protection enforcement service, and AI-Writer voice integration per original plan 37-04 specifications.

Purpose: Enable voice learning from existing content and voice-constrained content generation
Output: VoiceAnalysisService, ProtectionEnforcementService, AI-Writer integration
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-brand-voice/37-CONTEXT.md
@.planning/phases/37-brand-voice/37-RESEARCH.md
@.planning/phases/37-brand-voice/37-VERIFICATION.md

<interfaces>
From VoiceAnalyzer (existing):
```typescript
export async function analyzePageVoice(
  page: PageAnalysis,
  domain: string,
): Promise<VoiceExtractionResult>;

export function aggregateVoiceResults(
  results: VoiceExtractionResult[],
): ExtractedVoiceDimensions;
```

From voice-schema.ts:
```typescript
export interface VoiceProfileSelect {
  id: string;
  clientId: string;
  mode: "preservation" | "application" | "best_practices";
  tonePrimary: string | null;
  // ... 40+ fields
}
```

From ProtectionRulesService (existing):
```typescript
export const protectionRulesService = {
  async getActiveRules(profileId: string): Promise<ProtectionRuleSelect[]>;
  async getRulesForPage(profileId: string, url: string): Promise<ProtectionRuleSelect[]>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create VoiceAnalysisService wrapper</name>
  <files>open-seo-main/src/server/features/voice/services/VoiceAnalysisService.ts, open-seo-main/src/server/features/voice/services/VoiceAnalysisService.test.ts</files>
  <behavior>
    - Test 1: analyzePages() scrapes 5 URLs and aggregates voice dimensions
    - Test 2: analyzePages() saves individual analyses to voiceAnalysis table
    - Test 3: analyzePages() updates voiceProfiles with aggregated results
    - Test 4: analyzePages() returns aggregated dimensions with confidence score
  </behavior>
  <action>
Create VoiceAnalysisService.ts that orchestrates voice learning. This service wraps VoiceAnalyzer (which handles single-page AI analysis) and adds multi-page orchestration, database persistence, and profile updating. Uses existing VoiceAnalyzer.analyzePageVoice() and aggregateVoiceResults() functions. Saves each page analysis to voiceAnalysis table, then updates voiceProfiles with aggregated dimensions. Includes onProgress callback for BullMQ job progress updates.

Create test file VoiceAnalysisService.test.ts with mocked scraper and DB.
  </action>
  <verify>
    <automated>pnpm test -- VoiceAnalysisService</automated>
  </verify>
  <done>VoiceAnalysisService created with multi-page analysis orchestration</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ProtectionEnforcementService</name>
  <files>open-seo-main/src/server/features/voice/services/ProtectionEnforcementService.ts, open-seo-main/src/server/features/voice/services/ProtectionEnforcementService.test.ts</files>
  <behavior>
    - Test 1: isContentProtected() returns true for protected page URL
    - Test 2: isContentProtected() returns true for content with voice:protected tag
    - Test 3: extractProtectedSections() finds all HTML comment tags
    - Test 4: shouldPreserveContent() checks both URL rules and inline tags
  </behavior>
  <action>
Create ProtectionEnforcementService.ts for preservation mode. Implements three protection layers: (1) Page-level protection via URL pattern matching using ProtectionRulesService, (2) Section-level protection via CSS selectors, (3) Text-level protection via inline HTML comments. Main function shouldPreserveContent() returns whether to preserve entire page plus array of protected sections. Uses regex to find inline tags. For CSS selector matching, document pattern for future cheerio integration. Export protectionEnforcementService singleton.

Create test file with mock protection rules and HTML samples containing voice:protected tags.
  </action>
  <verify>
    <automated>pnpm test -- ProtectionEnforcementService</automated>
  </verify>
  <done>ProtectionEnforcementService created with URL, tag, and selector-based protection</done>
</task>

<task type="auto">
  <name>Task 3: Wire VoiceAnalysisService into voice-analysis-processor</name>
  <files>open-seo-main/src/server/workers/voice-analysis-processor.ts</files>
  <action>
Refactor voice-analysis-processor.ts to use VoiceAnalysisService instead of direct VoiceAnalyzer calls. Replace entire processVoiceAnalysisJob body with single call to voiceAnalysisService.analyzePages(). Pass onProgress callback that calls job.updateProgress(). Remove all inline scraping/analysis logic (100+ lines) — it's now in VoiceAnalysisService. Result: processor becomes thin orchestration layer (~30 lines) that delegates to service.
  </action>
  <verify>
    <automated>grep -l "voiceAnalysisService.analyzePages" open-seo-main/src/server/workers/voice-analysis-processor.ts</automated>
  </verify>
  <done>Voice analysis processor refactored to use VoiceAnalysisService</done>
</task>

<task type="auto">
  <name>Task 4: Integrate voice constraints into AI-Writer article generation</name>
  <files>AI-Writer/backend/services/article_generation_service.py</files>
  <action>
Add voice profile fetching and constraint building to article_generation_service.py per original Plan 37-04 specification. Add two module-level helper functions: (1) fetch_voice_profile(client_id) uses httpx to call open-seo API, (2) build_voice_constraints_from_profile(profile) formats voice data into AI prompt constraints. In generate_article() method, fetch voice profile, build constraints, pass to _build_article_prompt() as new voice_constraints parameter. Update _build_article_prompt() signature to accept voice_constraints and inject into system prompt AFTER topic but BEFORE structure requirements. Maintain backwards compatibility with legacy brand_voice text field. Handle all three modes: best_practices (generic guidelines), preservation (minimal changes), application (full voice constraints with tone, formality, vocabulary, forbidden phrases).
  </action>
  <verify>
    <automated>grep -l "fetch_voice_profile\|build_voice_constraints_from_profile\|voice_constraints" AI-Writer/backend/services/article_generation_service.py</automated>
  </verify>
  <done>Voice constraints integrated into AI-Writer article generation with backwards compatibility</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-Writer -> open-seo API | Internal service call for voice profile |
| VoiceAnalyzer -> Claude API | AI voice extraction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Information Disclosure | URL scraping | mitigate | Validate URLs belong to client domain before scraping |
| T-37-04 | Denial of Service | Voice analysis | mitigate | 10-page limit enforced in queue; don't store full content in DB |
| T-37-09 | Tampering | voice_constraints | mitigate | Escape special characters in signature/forbidden phrases via VoiceConstraintBuilder |
</threat_model>

<verification>
- [ ] VoiceAnalysisService.ts created with analyzePages method
- [ ] VoiceAnalysisService tests pass
- [ ] ProtectionEnforcementService.ts created with protection checks
- [ ] ProtectionEnforcementService tests pass
- [ ] voice-analysis-processor.ts uses VoiceAnalysisService
- [ ] article_generation_service.py fetches voice profile and builds constraints
- [ ] All tests pass: pnpm test -- voice
</verification>

<success_criteria>
- Voice learning extracts 40+ dimensions from 5-10 pages via VoiceAnalysisService
- Protection enforcement checks URL rules, inline tags, and CSS selectors
- AI-Writer generation calls open-seo API for voice profile before generation
- Voice constraints injected into AI prompts in application mode
- Backwards compatible with legacy brand_voice text field
</success_criteria>

<output>
After completion, create `.planning/phases/37-brand-voice/37-05-SUMMARY.md`
</output>
