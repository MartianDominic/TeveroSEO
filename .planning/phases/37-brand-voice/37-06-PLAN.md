---
phase: 37-brand-voice
plan: 06
type: execute
wave: 2
depends_on: [37-05]
files_modified:
  - apps/web/src/app/(shell)/clients/[clientId]/settings/voice/components/VoicePreviewPanel.tsx
  - apps/web/src/app/(shell)/clients/[clientId]/settings/voice/components/ComplianceScoreBadge.tsx
  - apps/web/src/app/(shell)/clients/[clientId]/settings/voice/page.tsx
  - open-seo-main/src/routes/api/seo/voice/[clientId]/preview.ts
  - AI-Writer/backend/api/voice_preview.py
  - AI-Writer/backend/main.py
autonomous: false
requirements: []
gap_closure: true
gaps_addressed:
  - "Gap 4: Voice preview system (VoicePreviewPanel + APIs)"
  - "Gap 5: Voice analysis queue processor (already fixed in Plan 37-05)"

must_haves:
  truths:
    - "Voice preview: test generation before applying to real content"
    - "Preview tab generates 3 sample content types (headline, paragraph, CTA)"
    - "Compliance scores show per-dimension breakdown"
  artifacts:
    - path: "apps/web/src/app/(shell)/clients/[clientId]/settings/voice/components/VoicePreviewPanel.tsx"
      provides: "Preview generation UI with 3 types"
      min_lines: 150
    - path: "apps/web/src/app/(shell)/clients/[clientId]/settings/voice/components/ComplianceScoreBadge.tsx"
      provides: "Color-coded compliance badge"
      min_lines: 30
    - path: "AI-Writer/backend/api/voice_preview.py"
      provides: "Preview endpoint with compliance scoring"
      min_lines: 100
    - path: "open-seo-main/src/routes/api/seo/voice/[clientId]/preview.ts"
      provides: "Preview proxy to AI-Writer"
      min_lines: 50
  key_links:
    - from: "VoicePreviewPanel.tsx"
      to: "/api/voice/{clientId}/preview"
      via: "fetch call"
      pattern: "fetch.*preview"
    - from: "open-seo preview.ts"
      to: "AI-Writer voice_preview.py"
      via: "HTTP proxy"
      pattern: "AI_WRITER_API_URL"
---

<objective>
Close gap 4 from Phase 37 verification. Implement voice preview system with 3 preview types (headline, paragraph, CTA) and compliance scoring per original Plan 37-04 specifications.

Purpose: Enable users to test voice settings before applying to real content generation
Output: Full preview suite with UI, API routes, and compliance scoring
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-brand-voice/37-CONTEXT.md
@.planning/phases/37-brand-voice/37-RESEARCH.md
@.planning/phases/37-brand-voice/37-VERIFICATION.md
@.planning/phases/37-brand-voice/37-UI-SPEC.md

<interfaces>
From Plan 37-02 voiceApi.ts:
```typescript
export interface VoiceProfile {
  mode: "preservation" | "application" | "best_practices";
  primaryTone: string | null;
  secondaryTones: string[];
  formalityLevel: number | null;
  personalityTraits: string[];
  signaturePhrases: string[];
  forbiddenPhrases: string[];
  // ... all fields
}
```

From 37-UI-SPEC.md Voice Preview Panel Labels:
```
Preview type selector: "Preview Type"
Headline option: "Headline"
Paragraph option: "Paragraph"  
CTA option: "Call to Action"
Generate button: "Generate Preview"
Regenerate button: "Try Again"
Compliance score label: "Voice Compliance"
Score passing (>=70): "Matches voice profile"
Score warning (50-69): "Partial match - review suggestions"
Score failing (<50): "Does not match - adjust settings"
```

From existing VoiceConstraintBuilder (open-seo-main):
```typescript
export function buildVoiceConstraints(options: VoiceConstraintOptions): string {
  const { profile, templateBlend = 0, templateId } = options;
  // Returns formatted string for AI prompt injection
}
```

From AI-Writer GeminiClient pattern:
```python
class GeminiClient:
    async def generate(self, prompt: str, max_tokens: int = 1000) -> str:
        # Returns generated text
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create preview API in open-seo and AI-Writer</name>
  <files>open-seo-main/src/routes/api/seo/voice/[clientId]/preview.ts, AI-Writer/backend/api/voice_preview.py, AI-Writer/backend/main.py</files>
  <action>
1. Create open-seo preview route at open-seo-main/src/routes/api/seo/voice/[clientId]/preview.ts that validates request, fetches voice profile, builds voice constraints using VoiceConstraintBuilder, and proxies to AI-Writer preview API. Use h3 defineEventHandler pattern. Validate with zod previewSchema (previewType enum, topic string, keywords array). Return success/data envelope.

2. Create AI-Writer preview endpoint at AI-Writer/backend/api/voice_preview.py with FastAPI router. Define PreviewRequest Pydantic model (preview_type, topic, keywords, voice_constraints, voice_profile). Define ComplianceScore model (overall, tone_consistency, vocabulary_alignment, structure_compliance, issues array). Implement generate_preview endpoint that builds prompt based on preview_type (headline=5-10 words, paragraph=50-80 words, cta=10-20 words), calls GeminiClient.generate(), scores compliance via _score_compliance() helper, returns PreviewResponse.

3. Implement _score_compliance() function: base scores 80%, check forbidden phrases (deduct 15 per match), check signature phrases (bonus 10% weighted), clamp scores 0-100, calculate overall as weighted average (tone 40%, vocab 35%, structure 25%), return ComplianceScore with issues array.

4. Register router in AI-Writer/backend/main.py: import voice_preview router, call app.include_router(voice_preview_router).
  </action>
  <verify>
    <automated>grep -l "generate_preview\|preview_type" AI-Writer/backend/api/voice_preview.py && grep -l "preview" open-seo-main/src/routes/api/seo/voice/\[clientId\]/preview.ts</automated>
  </verify>
  <done>Preview API endpoints created in both services with compliance scoring</done>
</task>

<task type="auto">
  <name>Task 2: Create VoicePreviewPanel and ComplianceScoreBadge components</name>
  <files>apps/web/src/app/(shell)/clients/[clientId]/settings/voice/components/VoicePreviewPanel.tsx, apps/web/src/app/(shell)/clients/[clientId]/settings/voice/components/ComplianceScoreBadge.tsx</files>
  <action>
1. Create ComplianceScoreBadge.tsx component: accepts score (number) and showLabel (boolean) props. Uses Badge from @tevero/ui. Shows CheckCircle icon (green) if score >=70, AlertTriangle icon (amber) if 50-69, XCircle icon (red) if <50. Displays score as percentage. Applies color classes: green-500/10 bg, green-600 text for passing; amber-500/10, amber-600 for warning; red-500/10, red-600 for failing. Shows label text per UI spec.

2. Create VoicePreviewPanel.tsx component: accepts clientId and profile props. useState for previewType (headline/paragraph/cta), topic, generating, result, error. RadioGroup from @tevero/ui for preview type selection with 3 options per UI spec labels. Input for topic with placeholder. Generate Preview button calls handleGenerate() which fetches /api/voice/{clientId}/preview with POST. Loading state shows Skeleton components. Result state shows Card with ComplianceScoreBadge, generated content in muted background, score breakdown grid (3 columns: Tone, Vocabulary, Structure), issues list if any, Try Again button. Error state shows destructive Card.

3. Update voice/page.tsx: import VoicePreviewPanel, replace placeholder in preview TabsContent with VoicePreviewPanel component passing clientId and profile props.
  </action>
  <verify>
    <automated>grep -l "ComplianceScoreBadge\|VoicePreviewPanel" apps/web/src/app/\(shell\)/clients/\[clientId\]/settings/voice/components/*.tsx</automated>
  </verify>
  <done>Preview panel and compliance badge components created and wired into voice settings page</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Voice Preview System</name>
  <what-built>
Complete voice preview system:
1. Preview API in open-seo-main that calls AI-Writer
2. AI-Writer preview endpoint with compliance scoring
3. VoicePreviewPanel component with 3 preview types
4. ComplianceScoreBadge with color-coded scores
5. Integration into voice settings page Preview tab
  </what-built>
  <how-to-verify>
1. Start both services:
   - Terminal 1: cd open-seo-main && pnpm dev
   - Terminal 2: cd AI-Writer/backend && python main.py
   - Terminal 3: cd apps/web && pnpm dev

2. Navigate to voice settings Preview tab:
   - Go to http://localhost:3000/clients/test-client/settings/voice
   - Click "Preview" tab

3. Generate a preview:
   - Select "Paragraph" preview type
   - Enter topic: "Benefits of our SEO platform"
   - Click "Generate Preview"

4. Verify:
   - Content generates without errors
   - Compliance score badge shows (green/amber/red based on score)
   - Score breakdown shows Tone, Vocabulary, Structure percentages
   - Try Again button regenerates

5. Test compliance scoring:
   - Go to Vocabulary tab
   - Add a forbidden phrase (e.g., "cheap")
   - Save profile
   - Return to Preview tab
   - Generate preview with topic mentioning that phrase
   - Verify issue appears in compliance feedback with warning badge

6. Test all preview types:
   - Generate Headline (should be 5-10 words)
   - Generate Paragraph (should be 50-80 words)
   - Generate CTA (should be 10-20 words)
  </how-to-verify>
  <resume-signal>Type "approved" to confirm preview system works, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| open-seo -> AI-Writer | Internal service call for preview |
| AI-Writer -> Gemini API | Prompt with user content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-09 | Tampering | voice_constraints | mitigate | VoiceConstraintBuilder escapes special characters |
| T-37-10 | Denial of Service | preview endpoint | mitigate | Rate limit: max 10 previews/minute per client (implement in future plan) |
| T-37-11 | Information Disclosure | voice profile fetch | accept | Internal API, profile scoped to client |
</threat_model>

<verification>
- [ ] Preview API created in open-seo
- [ ] Preview endpoint created in AI-Writer
- [ ] Compliance scoring implemented
- [ ] VoicePreviewPanel renders correctly
- [ ] ComplianceScoreBadge shows correct colors
- [ ] Preview tab integrated into voice/page.tsx
- [ ] Human verification confirms full system works
</verification>

<success_criteria>
- Preview generates content based on voice profile
- Compliance scores calculated and displayed
- Issues identified (e.g., forbidden phrases detected)
- All 3 preview types work (headline, paragraph, CTA)
- User can test voice settings before applying to real content
</success_criteria>

<output>
After completion, create `.planning/phases/37-brand-voice/37-06-SUMMARY.md`
</output>
