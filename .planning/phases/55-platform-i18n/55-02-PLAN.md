---
phase: 55-platform-i18n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - open-seo-main/src/server/services/translation/types.ts
  - open-seo-main/src/server/services/translation/prompts.ts
  - open-seo-main/src/server/services/translation/TranslationService.ts
  - open-seo-main/src/server/services/translation/TranslationService.test.ts
  - open-seo-main/src/db/translation-cache-schema.ts
  - open-seo-main/src/routes/api/translate.ts
autonomous: true
requirements:
  - I18N-02
must_haves:
  truths:
    - "Translation service translates English to Lithuanian via Gemini API"
    - "Translations are cached in database with >80% hit rate on repeated requests"
    - "Placeholders like {{name}} and {count} are preserved in translations"
    - "Translation API endpoint accepts POST requests and returns translated text"
    - "Quality score is calculated and stored with each translation"
  artifacts:
    - path: "open-seo-main/src/server/services/translation/TranslationService.ts"
      provides: "Main translation service with caching"
      exports: ["TranslationService", "getTranslationService"]
      min_lines: 100
    - path: "open-seo-main/src/server/services/translation/types.ts"
      provides: "TypeScript types for translation"
      exports: ["TranslationRequest", "TranslationResult", "TranslationContext"]
    - path: "open-seo-main/src/db/translation-cache-schema.ts"
      provides: "Drizzle schema for translation cache"
      contains: "translationCache"
    - path: "open-seo-main/src/routes/api/translate.ts"
      provides: "Translation API endpoint"
      contains: "POST"
  key_links:
    - from: "open-seo-main/src/server/services/translation/TranslationService.ts"
      to: "open-seo-main/src/db/translation-cache-schema.ts"
      via: "Drizzle ORM queries"
      pattern: "db\\.select.*translationCache"
    - from: "open-seo-main/src/routes/api/translate.ts"
      to: "open-seo-main/src/server/services/translation/TranslationService.ts"
      via: "getTranslationService import"
      pattern: "getTranslationService"
---

<objective>
Build a robust translation service using the Gemini API with caching, quality validation, placeholder preservation, and Lithuanian-specific prompt engineering.

Purpose: Gemini has superior Lithuanian language support due to EU institutional training data. Caching ensures cost efficiency and consistent translations.
Output: Working translation service with database caching, API endpoint, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-platform-i18n/DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create translation types and cache schema</name>
  <files>open-seo-main/src/server/services/translation/types.ts, open-seo-main/src/db/translation-cache-schema.ts</files>
  <read_first>open-seo-main/src/db/schema.ts, open-seo-main/package.json</read_first>
  <action>
Create open-seo-main/src/server/services/translation/types.ts with:
- SupportedLocale = 'en' | 'lt'
- ContextType = 'ui' | 'proposal' | 'agreement' | 'email' | 'report'
- Formality = 'formal' | 'informal'
- Domain = 'seo' | 'business' | 'legal' | 'technical'
- Tone = 'professional' | 'friendly' | 'urgent' | 'celebratory'
- TranslationRequest interface with text, sourceLang, targetLang, context, maxLength?, preservePlaceholders?
- TranslationContext interface with type, formality, domain?, tone?, workspaceId?
- TranslationResult interface with text, cached, confidence, alternatives?, sourceHash?
- BatchTranslationResult interface with results, totalCached, totalTranslated, durationMs

Create open-seo-main/src/db/translation-cache-schema.ts with Drizzle pgTable:
- translationCache table with: id, sourceHash, sourceText, sourceLang, targetLang, translatedText, contextType, formality, translator, modelVersion, qualityScore, reviewedBy, reviewedAt, createdAt, updatedAt, lastUsedAt, useCount
- Add unique constraint on (sourceHash, targetLang, contextType, formality)
- Add indexes for lookup and review queue
- workspaceTranslationOverrides table for custom per-workspace translations
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && pnpm tsc --noEmit 2>&1 | grep -E "error|types.ts|translation-cache" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - types.ts exports TranslationRequest, TranslationResult, TranslationContext
    - translation-cache-schema.ts exports translationCache table
    - Schema includes sourceHash, targetLang, contextType columns
  </acceptance_criteria>
  <done>Translation types and database schema defined</done>
</task>

<task type="auto">
  <name>Task 2: Create Lithuanian prompt templates and translation service</name>
  <files>open-seo-main/src/server/services/translation/prompts.ts, open-seo-main/src/server/services/translation/TranslationService.ts</files>
  <read_first>open-seo-main/src/server/services/translation/types.ts</read_first>
  <action>
Create open-seo-main/src/server/services/translation/prompts.ts with:
- LITHUANIAN_SYSTEM_PROMPT constant with linguistic rules: formality (jus/tu), case agreement, technical terms to keep in English (SEO, URL, API), business terms (pasiulymas, sutartis, saskaita faktura), placeholder preservation rules, length constraints, tone guidance
- buildTranslationPrompt function that substitutes formality, maxLength, tone, context description into the system prompt
- LITHUANIAN_ABBREVIATIONS map for space-constrained contexts

Create open-seo-main/src/server/services/translation/TranslationService.ts with:
- TranslationService class using GoogleGenerativeAI from @google/generative-ai
- Constructor initializes Gemini model with temperature: 0.3 for consistency
- translate() method: check same-lang skip, check cache, check workspace override, build prompt, call Gemini, validate placeholders, retry if too long, calculate quality score, cache result
- translateBatch() method for batch translations with rate limiting (60 RPM)
- Private methods: buildSourceHash (SHA256), checkCache, updateCacheStats, validatePlaceholders, retryWithShorter, calculateQualityScore, cacheTranslation
- Singleton getter: getTranslationService()
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && grep -l "getTranslationService\|translateBatch\|LITHUANIAN_SYSTEM_PROMPT" src/server/services/translation/*.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - prompts.ts contains LITHUANIAN_SYSTEM_PROMPT with formality rules
    - TranslationService.ts exports TranslationService class
    - TranslationService.ts exports getTranslationService singleton
    - translate() method checks cache before calling Gemini
    - validatePlaceholders() preserves {{name}}, {count}, %s patterns
  </acceptance_criteria>
  <done>Translation service with Lithuanian prompts and caching implemented</done>
</task>

<task type="auto">
  <name>Task 3: Create translation API endpoint and unit tests</name>
  <files>open-seo-main/src/routes/api/translate.ts, open-seo-main/src/server/services/translation/TranslationService.test.ts</files>
  <read_first>open-seo-main/src/routes/api/health.ts</read_first>
  <action>
Create open-seo-main/src/routes/api/translate.ts with:
- Zod schema for request validation: text (string 1-10000), targetLang (enum 'en'|'lt'), contextType, formality, maxLength?
- POST handler that validates input, calls getTranslationService().translate(), returns JSON response
- Error handling with appropriate status codes

Create open-seo-main/src/server/services/translation/TranslationService.test.ts with Vitest tests:
- Mock @google/generative-ai to return controlled translation responses
- Mock database module for cache operations
- Test: returns original text when source === target language
- Test: translates text to Lithuanian (mocked)
- Test: validates placeholders are preserved
- Test: calculates quality score correctly (ratio checks, Lithuanian character detection)

Generate and note migration for translation_cache table: `pnpm drizzle-kit generate`
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && pnpm vitest run src/server/services/translation/TranslationService.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - API route at /api/translate accepts POST with text and targetLang
    - Tests mock Gemini API and verify cache behavior
    - At least 3 test cases pass
    - Migration file generated for translation_cache table
  </acceptance_criteria>
  <done>Translation API endpoint created and unit tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/translate POST body | Untrusted user input for translation |
| Gemini API response | External service response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Injection | Translation text input | mitigate | Validate text length (max 10000 chars), sanitize before prompt |
| T-55-04 | Denial of Service | Translation API | mitigate | Rate limit endpoint, cache aggressively to reduce API calls |
| T-55-05 | Information Disclosure | Cached translations | accept | Cache contains translated UI strings, not sensitive PII |
</threat_model>

<verification>
- [ ] `pnpm drizzle-kit generate` creates migration for translation_cache
- [ ] `pnpm vitest run src/server/services/translation/` tests pass
- [ ] Manual API test with curl returns translated text
- [ ] Second identical request returns cached: true
</verification>

<success_criteria>
- TranslationService translates English to Lithuanian via Gemini
- Translations are cached in PostgreSQL
- Repeated requests return cached results
- Placeholders are preserved in translations
- Quality scores are calculated and stored
- API endpoint accepts POST and returns JSON
</success_criteria>

<output>
After completion, create `.planning/phases/55-platform-i18n/55-02-SUMMARY.md`
</output>
