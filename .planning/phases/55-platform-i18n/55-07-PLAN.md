---
phase: 55-platform-i18n
plan: 07
type: execute
wave: 4
depends_on:
  - "55-03"
  - "55-04"
files_modified:
  - apps/web/src/components/LanguageSwitcher.tsx
  - apps/web/src/components/PreviewLanguageToggle.tsx
  - apps/web/src/components/prospects/ProspectLanguageField.tsx
  - apps/web/src/components/layout/Header.tsx
  - apps/web/src/lib/locale-storage.ts
  - apps/web/src/hooks/useLocaleSync.ts
  - apps/web/src/contexts/LanguageContext.tsx
  - apps/web/src/app/globals.css
autonomous: true
requirements:
  - I18N-07
must_haves:
  truths:
    - "Language switcher appears in header for all authenticated pages"
    - "Clicking language switcher instantly changes UI language"
    - "Language preference persists across page refreshes (cookie + localStorage)"
    - "Prospect edit form has communication language field"
    - "Preview toggle allows EN/LT switching in proposal editor"
  artifacts:
    - path: "apps/web/src/components/LanguageSwitcher.tsx"
      provides: "Global language switcher dropdown"
      contains: "handleLanguageChange"
      min_lines: 50
    - path: "apps/web/src/lib/locale-storage.ts"
      provides: "Locale persistence utilities"
      exports: ["getStoredLocale", "setStoredLocale"]
    - path: "apps/web/src/components/prospects/ProspectLanguageField.tsx"
      provides: "Prospect communication language form field"
      contains: "preferredLanguage"
  key_links:
    - from: "apps/web/src/components/layout/Header.tsx"
      to: "apps/web/src/components/LanguageSwitcher.tsx"
      via: "component import"
      pattern: "LanguageSwitcher"
    - from: "apps/web/src/components/LanguageSwitcher.tsx"
      to: "apps/web/src/lib/locale-storage.ts"
      via: "setStoredLocale call"
      pattern: "setStoredLocale"
---

<objective>
Implement intuitive language switching UI with proper state persistence, prospect-specific language settings, and preview toggles for proposals/agreements.

Purpose: Users need instant, persistent language switching. Prospects need per-contact language preferences for outbound communications.
Output: Language switcher component, locale persistence, prospect language field, preview toggle, and header integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-platform-i18n/DESIGN.md
@.planning/phases/55-platform-i18n/55-03-SUMMARY.md
@.planning/phases/55-platform-i18n/55-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LanguageSwitcher component and locale storage utilities</name>
  <files>apps/web/src/components/LanguageSwitcher.tsx, apps/web/src/lib/locale-storage.ts</files>
  <read_first>apps/web/src/components/layout/Header.tsx</read_first>
  <action>
Create apps/web/src/lib/locale-storage.ts:
- LOCALE_COOKIE_NAME = 'NEXT_LOCALE'
- LOCALE_STORAGE_KEY = 'preferred-locale'
- MAX_AGE = 60 * 60 * 24 * 365 (1 year)
- getStoredLocale(): check cookie first, then localStorage, return string | null
- setStoredLocale(locale): set both cookie and localStorage
- clearStoredLocale(): remove from both

Create apps/web/src/components/LanguageSwitcher.tsx:
- 'use client' directive
- Import useLocale, useRouter, usePathname from next-intl and next/navigation
- Import useTransition for pending state
- LANGUAGES array: [{ code: 'en', name: 'English', native: 'English', flag: 'GB' }, { code: 'lt', name: 'Lithuanian', native: 'Lietuviu', flag: 'LT' }]
- Props: variant ('default' | 'compact'), className
- handleLanguageChange(newLocale):
  - Remove current locale prefix from pathname
  - Build new path with locale prefix (none for EN, /lt/ for LT)
  - Call setStoredLocale(newLocale)
  - router.push(newPath) wrapped in startTransition
  - router.refresh()
- Render DropdownMenu with:
  - Trigger: Button showing current flag + native name (or just Globe icon for compact)
  - Content: DropdownMenuItem for each language with flag, native name, checkmark for current
- getFlagEmoji(countryCode) helper: convert country code to flag emoji using code points
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -E "handleLanguageChange|setStoredLocale|getFlagEmoji|DropdownMenu" apps/web/src/components/LanguageSwitcher.tsx apps/web/src/lib/locale-storage.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - locale-storage.ts exports getStoredLocale and setStoredLocale
    - LanguageSwitcher uses DropdownMenu from shadcn/ui
    - handleLanguageChange updates URL and persists to storage
    - Both default and compact variants supported
  </acceptance_criteria>
  <done>Language switcher component and storage utilities created</done>
</task>

<task type="auto">
  <name>Task 2: Integrate LanguageSwitcher into Header and create ProspectLanguageField</name>
  <files>apps/web/src/components/layout/Header.tsx, apps/web/src/components/prospects/ProspectLanguageField.tsx, apps/web/src/hooks/useLocaleSync.ts</files>
  <read_first>apps/web/src/components/layout/Header.tsx</read_first>
  <action>
Update apps/web/src/components/layout/Header.tsx:
- Import LanguageSwitcher from '@/components/LanguageSwitcher'
- Add LanguageSwitcher to header right side, before UserMenu
- For mobile: add compact variant in sidebar/drawer

Create apps/web/src/hooks/useLocaleSync.ts:
- 'use client' directive
- Import useLocale from next-intl, useUser from @clerk/nextjs
- useLocaleSync hook:
  - On locale change, sync to localStorage via setStoredLocale
  - If user is logged in, optionally sync to user.publicMetadata.preferredLocale

Create apps/web/src/components/prospects/ProspectLanguageField.tsx:
- 'use client' directive
- Props: form (UseFormReturn), workspaceDefaultLanguage
- Use useTranslations('prospects')
- Render FormField with:
  - FormLabel for communicationLanguage
  - Select with options: 'inherit' (shows workspace default), 'en', 'lt'
  - FormDescription explaining what this affects
- getFlagEmoji helper for flag display
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -l "LanguageSwitcher\|ProspectLanguageField\|useLocaleSync" apps/web/src/components/layout/Header.tsx apps/web/src/components/prospects/ProspectLanguageField.tsx apps/web/src/hooks/useLocaleSync.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Header imports and renders LanguageSwitcher
    - ProspectLanguageField has inherit/en/lt options
    - useLocaleSync hook persists locale changes
    - Mobile header has compact switcher variant
  </acceptance_criteria>
  <done>Language switcher integrated into header and prospect form field created</done>
</task>

<task type="auto">
  <name>Task 3: Create PreviewLanguageToggle and LanguageContext</name>
  <files>apps/web/src/components/PreviewLanguageToggle.tsx, apps/web/src/contexts/LanguageContext.tsx, apps/web/src/app/globals.css</files>
  <read_first>apps/web/src/app/globals.css</read_first>
  <action>
Create apps/web/src/components/PreviewLanguageToggle.tsx:
- Props: value ('en' | 'lt'), onChange, isLoading?, disabled?
- Use useTranslations('common') for label
- Render Label + ToggleGroup with EN/LT items
- Show Loader2 spinner when isLoading

Create apps/web/src/contexts/LanguageContext.tsx:
- LanguageContextValue interface: currentLocale, isLithuanian, formality
- LanguageContext with createContext
- LanguageProvider component:
  - Props: children, formality (default 'formal')
  - Use useLocale() to get current locale
  - Provide value with currentLocale, isLithuanian: locale === 'lt', formality
- useLanguage hook: useContext with error if outside provider

Update apps/web/src/app/globals.css:
- Add transition styles for locale switching:
  ```css
  [data-locale-transitioning="true"] main {
    opacity: 0.7;
    transition: opacity 150ms ease-out;
  }
  ```
- Add loading overlay styles for language switch
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -E "PreviewLanguageToggle|LanguageProvider|useLanguage|locale-transitioning" apps/web/src/components/PreviewLanguageToggle.tsx apps/web/src/contexts/LanguageContext.tsx apps/web/src/app/globals.css | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - PreviewLanguageToggle renders EN/LT toggle with loading state
    - LanguageContext provides currentLocale and isLithuanian
    - useLanguage hook available for components
    - CSS transition styles for locale change
  </acceptance_criteria>
  <done>Preview toggle and language context created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Locale cookie | Client-controlled, could be manipulated |
| localStorage | Client-controlled storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-15 | Tampering | Locale cookie | mitigate | Validate locale against whitelist ['en', 'lt'] in middleware |
| T-55-16 | Spoofing | User preference sync | accept | Locale is non-sensitive preference data |
</threat_model>

<verification>
- [ ] Language switcher visible in header
- [ ] Clicking LT switches UI to Lithuanian immediately
- [ ] Refresh page - language preference persists
- [ ] Mobile view has compact switcher
- [ ] Prospect form has communication language dropdown
- [ ] Preview toggle switches between EN/LT content
</verification>

<success_criteria>
- Language switcher in header for all authenticated pages
- Instant language switching with URL update
- Preference persists via cookie and localStorage
- Prospect form has language field
- Preview toggle component available
- Visual feedback during language switch
</success_criteria>

<output>
After completion, create `.planning/phases/55-platform-i18n/55-07-SUMMARY.md`
</output>
