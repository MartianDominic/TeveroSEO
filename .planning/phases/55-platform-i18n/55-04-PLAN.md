---
phase: 55-platform-i18n
plan: 04
type: execute
wave: 2
depends_on:
  - "55-01"
files_modified:
  - open-seo-main/src/db/schema.ts
  - open-seo-main/src/db/prospect-schema.ts
  - open-seo-main/src/server/services/LanguageResolutionService.ts
  - open-seo-main/src/routes/api/settings/language.ts
  - apps/web/src/app/[locale]/(shell)/settings/language/page.tsx
autonomous: true
requirements:
  - I18N-03
  - I18N-04
must_haves:
  truths:
    - "Workspace has default_language and formality settings in database"
    - "Prospects have preferred_language field for communication override"
    - "Language resolution follows hierarchy: user > prospect > workspace > browser > default"
    - "Settings UI allows workspace language configuration"
    - "API endpoints for reading/updating language settings work"
  artifacts:
    - path: "open-seo-main/src/server/services/LanguageResolutionService.ts"
      provides: "Language resolution service with hierarchy"
      exports: ["LanguageResolutionService", "getLanguageResolutionService"]
      min_lines: 80
    - path: "open-seo-main/src/routes/api/settings/language.ts"
      provides: "Language settings API endpoint"
      contains: "GET"
    - path: "apps/web/src/app/[locale]/(shell)/settings/language/page.tsx"
      provides: "Workspace language settings UI"
      contains: "defaultLanguage"
  key_links:
    - from: "open-seo-main/src/server/services/LanguageResolutionService.ts"
      to: "open-seo-main/src/db/schema.ts"
      via: "Drizzle query for organization"
      pattern: "organization\\.defaultLanguage"
    - from: "apps/web/src/app/[locale]/(shell)/settings/language/page.tsx"
      to: "open-seo-main/src/routes/api/settings/language.ts"
      via: "fetch API call"
      pattern: "fetch.*api/settings/language"
---

<objective>
Implement workspace-level and prospect-level language preferences with proper inheritance, resolution logic, and settings UI.

Purpose: Multi-tenant language support ensures each agency can configure their default language and formality, while individual prospects can have communication preferences.
Output: Database schema for language settings, resolution service, API endpoints, and settings UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-platform-i18n/DESIGN.md
@.planning/phases/55-platform-i18n/55-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add language columns to organization and prospects tables</name>
  <files>open-seo-main/src/db/schema.ts, open-seo-main/src/db/prospect-schema.ts</files>
  <read_first>open-seo-main/src/db/schema.ts, open-seo-main/src/db/prospect-schema.ts</read_first>
  <action>
Update open-seo-main/src/db/schema.ts to add columns to organization table:
- defaultLanguage: text('default_language').default('en')
- supportedLanguages: text('supported_languages').array().default(['en'])
- country: text('country') -- ISO 3166-1 alpha-2 (LT, US, etc.)
- formality: text('formality').default('formal') -- 'formal' | 'informal'

Update open-seo-main/src/db/prospect-schema.ts (or wherever prospects table is defined):
- preferredLanguage: text('preferred_language') -- null = inherit from workspace
- country: text('country')

If there's a separate clients table, add same columns:
- preferredLanguage: text('preferred_language')
- country: text('country')

Generate migration: `cd open-seo-main && pnpm drizzle-kit generate`
Apply migration: `pnpm drizzle-kit migrate`
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && grep -E "defaultLanguage|preferredLanguage|formality" src/db/schema.ts src/db/prospect-schema.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - organization table has defaultLanguage, supportedLanguages, formality columns
    - prospects table has preferredLanguage, country columns
    - Migration file generated for schema changes
  </acceptance_criteria>
  <done>Database schema updated with language preference columns</done>
</task>

<task type="auto">
  <name>Task 2: Create LanguageResolutionService with hierarchy logic</name>
  <files>open-seo-main/src/server/services/LanguageResolutionService.ts</files>
  <read_first>open-seo-main/src/db/schema.ts</read_first>
  <action>
Create open-seo-main/src/server/services/LanguageResolutionService.ts with:

Types:
- SupportedLocale = 'en' | 'lt'
- Formality = 'formal' | 'informal'
- LanguageContext interface: userId?, userSelection?, prospectId?, clientId?, workspaceId, acceptLanguage?
- ResolvedLanguage interface: locale, formality, source ('user'|'prospect'|'client'|'workspace'|'browser'|'default')

LanguageResolutionService class:
- SUPPORTED_LOCALES static array
- resolveLanguage(context: LanguageContext): Promise<ResolvedLanguage>
  - Resolution order: userSelection > prospect > client > workspace > browser > default
  - Each step checks isSupported() before using
- resolveForCommunication(workspaceId, prospectOrClientId, type): Promise<ResolvedLanguage>
  - Convenience method for outbound comms
- Private helper methods:
  - isSupported(locale): boolean
  - parseAcceptLanguage(header): string[]
  - getWorkspaceLanguageSettings(workspaceId): Promise with defaultLanguage, supportedLanguages, formality
  - getWorkspaceFormality(workspaceId): Promise<Formality>
  - getProspectLanguage(prospectId): Promise with preferredLanguage, country
  - getClientLanguage(clientId): Promise with preferredLanguage, country

Export singleton: getLanguageResolutionService()
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && grep -E "resolveLanguage|resolveForCommunication|getLanguageResolutionService" src/server/services/LanguageResolutionService.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - LanguageResolutionService class exported
    - resolveLanguage method implements 6-step resolution hierarchy
    - resolveForCommunication convenience method exists
    - getLanguageResolutionService singleton exported
  </acceptance_criteria>
  <done>Language resolution service with hierarchy logic implemented</done>
</task>

<task type="auto">
  <name>Task 3: Create language settings API and UI</name>
  <files>open-seo-main/src/routes/api/settings/language.ts, apps/web/src/app/[locale]/(shell)/settings/language/page.tsx</files>
  <read_first>open-seo-main/src/routes/api/health.ts, apps/web/src/app/(shell)/settings/page.tsx</read_first>
  <action>
Create open-seo-main/src/routes/api/settings/language.ts:
- Zod schema for update: defaultLanguage (enum), supportedLanguages (array), country?, formality (enum)
- GET handler: fetch workspace language settings, return JSON
- PUT handler: validate input, ensure defaultLanguage in supportedLanguages, update organization record

Create apps/web/src/app/[locale]/(shell)/settings/language/page.tsx:
- 'use client' directive
- useTranslations for settings.language namespace
- useForm with zodResolver for form validation
- Form fields:
  - defaultLanguage: Select with EN/LT options
  - supportedLanguages: Checkbox group for EN/LT
  - formality: Select with formal (jus) / informal (tu) options
- Load settings on mount via fetch('/api/settings/language')
- Submit handler calls PUT to update settings
- Toast notifications for success/error

Use shadcn/ui components: Card, Form, Select, Checkbox, Button, useToast
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -l "defaultLanguage\|supportedLanguages" apps/web/src/app/*/settings/language/page.tsx open-seo-main/src/routes/api/settings/language.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - API GET returns current language settings
    - API PUT validates and updates settings
    - Settings page has form for defaultLanguage, supportedLanguages, formality
    - Form submits to API and shows success/error toast
  </acceptance_criteria>
  <done>Language settings API and UI created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings API request body | User-controlled input for workspace settings |
| Workspace ID from auth | Must be validated against authenticated user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-08 | Elevation of Privilege | Settings API | mitigate | Validate workspaceId from auth, not request body |
| T-55-09 | Tampering | Language settings | mitigate | Validate enum values against whitelist |
</threat_model>

<verification>
- [ ] Migration applied successfully: `pnpm drizzle-kit migrate`
- [ ] GET /api/settings/language returns current settings
- [ ] PUT /api/settings/language updates settings correctly
- [ ] Settings UI loads at /settings/language
- [ ] Changing default language persists after refresh
</verification>

<success_criteria>
- Database schema has language columns on organization and prospects
- LanguageResolutionService resolves language by hierarchy
- API endpoints for CRUD on language settings
- Settings UI allows workspace configuration
- Migration applied without errors
</success_criteria>

<output>
After completion, create `.planning/phases/55-platform-i18n/55-04-SUMMARY.md`
</output>
