---
phase: 55-platform-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/middleware.ts
  - apps/web/i18n.ts
  - apps/web/messages/en.json
  - apps/web/messages/lt.json
  - apps/web/src/app/[locale]/layout.tsx
  - apps/web/src/lib/language-resolution.ts
  - apps/web/types/i18n.d.ts
  - apps/web/src/test-utils/i18n.tsx
  - open-seo-main/src/i18n/index.ts
  - open-seo-main/src/i18n/locales/en.json
  - open-seo-main/src/i18n/locales/lt.json
autonomous: true
requirements:
  - I18N-01
  - I18N-07
must_haves:
  truths:
    - "Locale routing works for /lt/* paths"
    - "Accept-Language header detection redirects to correct locale"
    - "English is served by default without locale prefix"
    - "Lithuanian is served with /lt/ prefix"
    - "Message files load correctly for each locale"
  artifacts:
    - path: "apps/web/middleware.ts"
      provides: "Locale routing middleware for Next.js"
      contains: "createMiddleware"
    - path: "apps/web/i18n.ts"
      provides: "next-intl request config"
      contains: "getRequestConfig"
    - path: "apps/web/messages/en.json"
      provides: "English translations skeleton"
      min_lines: 50
    - path: "apps/web/messages/lt.json"
      provides: "Lithuanian translations skeleton"
      min_lines: 50
    - path: "open-seo-main/src/i18n/index.ts"
      provides: "i18next configuration for TanStack Start"
      contains: "i18n.init"
  key_links:
    - from: "apps/web/middleware.ts"
      to: "apps/web/src/app/[locale]/layout.tsx"
      via: "locale parameter routing"
      pattern: "locales.*en.*lt"
    - from: "apps/web/src/app/[locale]/layout.tsx"
      to: "apps/web/messages/*.json"
      via: "getMessages import"
      pattern: "NextIntlClientProvider"
---

<objective>
Set up internationalization frameworks for both Next.js (apps/web) and TanStack Start (open-seo-main) with locale routing, middleware, and detection.

Purpose: This is the foundation for all subsequent i18n work - without proper framework setup, no translations can be displayed.
Output: Working locale routing, message file structure, and i18n hooks available in both apps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-platform-i18n/DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install next-intl and configure locale routing</name>
  <files>apps/web/middleware.ts, apps/web/i18n.ts, apps/web/src/app/[locale]/layout.tsx, apps/web/types/i18n.d.ts</files>
  <read_first>apps/web/package.json, apps/web/src/app/layout.tsx</read_first>
  <action>
Install next-intl: `pnpm add next-intl --filter @tevero/web`

Create middleware.ts at apps/web/middleware.ts with createMiddleware from next-intl/middleware. Configure:
- locales: ['en', 'lt']
- defaultLocale: 'en'
- localePrefix: 'as-needed' (no prefix for EN, /lt/ prefix for LT)
- localeDetection: true

Create i18n.ts at apps/web/i18n.ts with getRequestConfig from next-intl/server. Import messages from ./messages/{locale}.json.

Create [locale] layout at apps/web/src/app/[locale]/layout.tsx with NextIntlClientProvider wrapping children. Use getMessages() to load messages. Add generateStaticParams for ['en', 'lt'].

Update root layout at apps/web/src/app/layout.tsx to pass locale to html lang attribute.

Create TypeScript type definitions at apps/web/types/i18n.d.ts to extend IntlMessages with the message shape.
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && pnpm tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - middleware.ts contains createMiddleware with locales: ['en', 'lt']
    - i18n.ts contains getRequestConfig
    - [locale]/layout.tsx contains NextIntlClientProvider
    - types/i18n.d.ts declares IntlMessages interface
  </acceptance_criteria>
  <done>Next.js i18n framework installed, middleware routes locales, layout provides translations</done>
</task>

<task type="auto">
  <name>Task 2: Create message file structure with initial translations</name>
  <files>apps/web/messages/en.json, apps/web/messages/lt.json</files>
  <action>
Create apps/web/messages/en.json with organized namespaces:
- common: save, cancel, delete, edit, loading, submit, back, next, close, confirm, search, filter
- nav: dashboard, prospects, clients, proposals, agreements, invoices, reports, settings
- dashboard: title, welcome (with {name} placeholder), recentActivity, quickActions
- prospects: title, addNew, noProspects, contactInfo, communicationLanguage, communicationLanguageDesc
- errors: notFound, unauthorized, generic

Create apps/web/messages/lt.json with Lithuanian skeleton translations for the same structure. Use proper Lithuanian characters (a, c, e, i, s, u, z).

Ensure both files have identical key structure.
  </action>
  <verify>
    <automated>node -e "const en=require('./apps/web/messages/en.json'); const lt=require('./apps/web/messages/lt.json'); console.log('EN keys:', Object.keys(en).length, 'LT keys:', Object.keys(lt).length); if(Object.keys(en).length !== Object.keys(lt).length) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - en.json exists with common, nav, dashboard, prospects, errors namespaces
    - lt.json exists with identical key structure
    - Both files are valid JSON
  </acceptance_criteria>
  <done>Message files created with matching structure for EN and LT</done>
</task>

<task type="auto">
  <name>Task 3: Set up i18next in open-seo-main with locale files</name>
  <files>open-seo-main/src/i18n/index.ts, open-seo-main/src/i18n/locales/en.json, open-seo-main/src/i18n/locales/lt.json</files>
  <read_first>open-seo-main/package.json, open-seo-main/src/client.tsx</read_first>
  <action>
Install i18next dependencies: `pnpm add i18next react-i18next i18next-browser-languagedetector --filter open-seo-main`

Create open-seo-main/src/i18n/index.ts with:
- Import i18n from 'i18next'
- Import initReactI18next from 'react-i18next'
- Import LanguageDetector from 'i18next-browser-languagedetector'
- Configure with resources for en and lt
- Set fallbackLng: 'en', supportedLngs: ['en', 'lt']
- Configure detection order: cookie, localStorage, navigator

Create locale files at open-seo-main/src/i18n/locales/en.json and lt.json with similar structure to apps/web messages (common, nav, errors).

Initialize i18n in the app entry point by importing './i18n' early in the module.
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO/open-seo-main && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - open-seo-main/src/i18n/index.ts contains i18n.init configuration
    - locales/en.json and locales/lt.json exist and are valid JSON
    - i18n is imported in the app entry point
  </acceptance_criteria>
  <done>i18next configured for open-seo-main with EN/LT locale files</done>
</task>

<task type="auto">
  <name>Task 4: Create language resolution utility and test utilities</name>
  <files>apps/web/src/lib/language-resolution.ts, apps/web/src/test-utils/i18n.tsx</files>
  <action>
Create apps/web/src/lib/language-resolution.ts with:
- Type SupportedLocale = 'en' | 'lt'
- Interface LanguageContext with userSelection, prospectLanguage, workspaceDefault, acceptLanguage
- Interface ResolvedLanguage with locale, formality, source
- Function resolveLanguage(context) implementing resolution order: user > prospect > workspace > browser > default
- Helper function parseAcceptLanguage(header) to parse Accept-Language header

Create apps/web/src/test-utils/i18n.tsx with:
- TestI18nProvider component wrapping NextIntlClientProvider
- Import messages from ../../messages/en.json
- Export for use in tests
  </action>
  <verify>
    <automated>cd /home/dominic/Documents/TeveroSEO && grep -l "resolveLanguage\|SupportedLocale" apps/web/src/lib/language-resolution.ts && grep -l "TestI18nProvider" apps/web/src/test-utils/i18n.tsx</automated>
  </verify>
  <acceptance_criteria>
    - language-resolution.ts exports resolveLanguage function
    - language-resolution.ts exports SupportedLocale type
    - test-utils/i18n.tsx exports TestI18nProvider component
  </acceptance_criteria>
  <done>Language resolution utility and test utilities created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Accept-Language header | Browser-controlled, untrusted input |
| Locale path parameter | URL-controlled, could be manipulated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | Locale parameter | mitigate | Validate locale against whitelist ['en', 'lt'] in middleware |
| T-55-02 | Information Disclosure | Message files | accept | Message files contain UI strings, not sensitive data |
</threat_model>

<verification>
- [ ] `pnpm build --filter @tevero/web` passes
- [ ] `pnpm tsc --noEmit` passes in apps/web
- [ ] `curl -I http://localhost:3000/dashboard` returns 200 (EN default)
- [ ] `curl -I http://localhost:3000/lt/dashboard` returns 200 (LT locale)
- [ ] `curl -H "Accept-Language: lt" http://localhost:3000/` redirects to /lt/
</verification>

<success_criteria>
- next-intl installed and configured in apps/web
- i18next installed and configured in open-seo-main
- Locale routing works for /lt/* paths
- Message files exist for both EN and LT
- Language resolution utility available
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/55-platform-i18n/55-01-SUMMARY.md`
</output>
