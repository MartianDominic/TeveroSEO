---
phase: 18-monitoring-alerts
plan: 04
title: Alert UI Components
subsystem: ui
tags: [nextjs, alerts, dashboard, shadcn]
wave: 3
dependency_graph:
  requires: [alert schema from 18-01, alert API from 18-01]
  provides: [AlertBadge, AlertDrawer, AlertsPage]
  affects: [Dashboard UX]
---

# Phase 18 Plan 04: Alert UI Components

## Objective

Add alert badge to dashboard, create alert drawer for quick access, and build full alerts history page.

## Tasks

### Task 1: Create alert badge component

**Files:**
- `apps/web/src/components/alerts/AlertBadge.tsx`
- `apps/web/src/actions/alerts.ts`

**Changes:**
1. Server action `getAlertCount(clientId)` — returns unacknowledged count
2. AlertBadge component:
   - Shows count in red badge
   - Pulse animation for critical alerts
   - Click opens drawer
   - Empty state shows checkmark

**Commit:** `feat(18-04): add alert badge component`

---

### Task 2: Create alert drawer component

**Files:**
- `apps/web/src/components/alerts/AlertDrawer.tsx`
- `apps/web/src/components/alerts/AlertItem.tsx`

**Changes:**
1. AlertDrawer using shadcn Sheet:
   - Lists recent alerts (last 20)
   - Quick actions: acknowledge, dismiss
   - Severity color coding
   - Link to full alerts page
2. AlertItem component:
   - Shows title, message, severity, time
   - Action buttons

**Commit:** `feat(18-04): add alert drawer`

---

### Task 3: Integrate badge into dashboard

**Files:**
- `apps/web/src/app/(shell)/clients/[clientId]/layout.tsx` or appropriate nav component

**Changes:**
1. Add AlertBadge to client navigation
2. Include AlertDrawer at layout level
3. Pass clientId to components

**Commit:** `feat(18-04): integrate alert badge in dashboard`

---

### Task 4: Create alerts history page

**Files:**
- `apps/web/src/app/(shell)/clients/[clientId]/alerts/page.tsx`
- `apps/web/src/components/alerts/AlertsTable.tsx`

**Changes:**
1. Server action `getClientAlerts(clientId, filters)` — paginated
2. AlertsTable component:
   - Columns: severity, type, title, status, created, actions
   - Filter by status, type
   - Bulk actions
3. Alerts page with table and filters

**Commit:** `feat(18-04): add alerts history page`

---

### Task 5: Add i18n messages

**Files:**
- `apps/web/src/i18n/messages/en.json`
- `apps/web/src/i18n/messages/de.json`
- `apps/web/src/i18n/messages/lt.json`

**Changes:**
1. Add alerts namespace with labels for:
   - Severity levels
   - Status values
   - Action buttons
   - Empty states

**Commit:** `feat(18-04): add alerts i18n messages`

---

## Self-Check

- [ ] AlertBadge shows count with animation
- [ ] AlertDrawer opens from badge click
- [ ] Dashboard has alert badge in nav
- [ ] /clients/[id]/alerts page renders
- [ ] Actions update alert status
- [ ] i18n messages for EN/DE/LT
- [ ] TypeScript compiles clean
