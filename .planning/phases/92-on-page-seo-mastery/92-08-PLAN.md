---
phase: 92-on-page-seo-mastery
plan: 08
type: execute
wave: 3
depends_on:
  - 92-02
  - 92-03
files_modified:
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-01-reddit-test.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-02-info-gain.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-03-prove-it.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-04-not-for-you.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-05-qdd.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-06-thin-content.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-07-fluff.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-08-ai-slop.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-09-voice-consistency.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-10-tone.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-11-audience.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-12-sentence-length.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/T5-13-paragraph-length.ts
  - open-seo-main/src/server/lib/audit/checks/tier5/index.ts
  - open-seo-main/src/server/lib/audit/checks/types.ts
autonomous: true
requirements:
  - OPM-19

must_haves:
  truths:
    - "13 Tier 5 checks registered (T5-01 to T5-13)"
    - "Tier 5 checks use QualityGateService for evaluation"
    - "Blocking checks (T5-01, T5-02, T5-03, T5-06, T5-08) return blocking=true when failed"
    - "Checks are async to support LLM/embedding operations"
  artifacts:
    - path: "open-seo-main/src/server/lib/audit/checks/tier5/T5-01-reddit-test.ts"
      provides: "Reddit Test check using QualityGateService"
      contains: "registerCheck"
    - path: "open-seo-main/src/server/lib/audit/checks/tier5/index.ts"
      provides: "Tier 5 check registration barrel"
      contains: "import"
  key_links:
    - from: "T5-01-reddit-test.ts"
      to: "QualityGateService"
      via: "Service call for evaluation"
      pattern: "getQualityGateService\\(\\)"
    - from: "tier5/index.ts"
      to: "registry.ts"
      via: "Check registration"
      pattern: "import.*from"
---

<objective>
Implement 13 Tier 5 content quality checks (T5-01 to T5-13) that wrap the QualityGateService methods as audit checks.

Purpose: Tier 5 checks are opt-in content quality gates that integrate with the existing audit system. They leverage QualityGateService for the actual evaluation but present results in the standard check format.

Output: 13 check files + barrel export that register with the audit system and delegate to QualityGateService.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-on-page-seo-mastery/92-CONTEXT.md
@.planning/phases/92-on-page-seo-mastery/92-RESEARCH.md

<interfaces>
<!-- QualityGateService from 92-03 -->
From open-seo-main/src/server/features/onpage-mastery/services/QualityGateService.ts:
```typescript
export class QualityGateService {
  async evaluateRedditTest(content: string, vertical: Vertical): Promise<GateResult>;
  async evaluateInformationGain(content: string, serpContent: string[]): Promise<GateResult>;
  async evaluateProveItDetails(content: string, vertical: Vertical): Promise<GateResult>;
  evaluateNotForYou(content: string): GateResult;
  async evaluateQDDVulnerability(content: string, serpContent: string[]): Promise<GateResult>;
  evaluateThinContent(content: string, vertical: Vertical): GateResult;
  evaluateFluffDetection(content: string): GateResult;
}

export interface GateResult {
  passed: boolean;
  score: number;
  message: string;
  method: "embedding" | "llm" | "llm-fallback" | "rule";
}
```

<!-- Check registration pattern (async) -->
From open-seo-main/src/server/lib/audit/checks/types.ts:
```typescript
export interface CheckDefinition {
  id: string;
  name: string;
  tier: CheckTier;
  category: string;
  severity: Severity;
  autoEditable: boolean;
  editRecipe?: string;
  run: (ctx: CheckContext) => CheckResult | Promise<CheckResult>;
  // Phase 92 addition for Tier 5
  blocking?: boolean;  // If true and failed, blocks publication
}

export type CheckTier = 1 | 2 | 3 | 4 | 5;
```

<!-- Tier 5 check specs from CONTEXT.md -->
| ID | Name | Blocking | Cost |
|----|------|----------|------|
| T5-01 | Reddit Test | Yes (<50) | ~$0.002 |
| T5-02 | Information Gain vs SERP | Yes (<40) | ~$0.0001 |
| T5-03 | Prove-It Details | Yes (<30) | ~$0.003 |
| T5-04 | Not For You Block | No | $0 |
| T5-05 | QDD Vulnerability | No | ~$0.002 |
| T5-06 | Thin Content Detection | Yes (<20) | ~$0.0001 |
| T5-07 | Fluff Detection | No | ~$0.001 |
| T5-08 | AI Slop Detection | Yes (<40) | ~$0.002 |
| T5-09 | Voice Consistency | No | ~$0.001 |
| T5-10 | Tone Appropriateness | No | ~$0.001 |
| T5-11 | Audience Alignment | No | ~$0.001 |
| T5-12 | Sentence Length Distribution | No | $0 |
| T5-13 | Paragraph Length Optimization | No | $0 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update types and create T5-01 to T5-07 checks</name>
  <files>
    open-seo-main/src/server/lib/audit/checks/types.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-01-reddit-test.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-02-info-gain.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-03-prove-it.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-04-not-for-you.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-05-qdd.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-06-thin-content.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-07-fluff.ts
  </files>
  <read_first>
    - open-seo-main/src/server/lib/audit/checks/types.ts (existing types)
    - open-seo-main/src/server/lib/audit/checks/registry.ts (registerCheck function)
    - open-seo-main/src/server/features/onpage-mastery/services/QualityGateService.ts (from 92-03)
    - .planning/phases/92-on-page-seo-mastery/92-CONTEXT.md (Tier 5 specs)
  </read_first>
  <action>
**Update types.ts** to add Tier 5 support:

```typescript
// Add to existing types
export type CheckTier = 1 | 2 | 3 | 4 | 5;

export interface CheckDefinition {
  // ... existing fields
  blocking?: boolean;  // Tier 5: If true and failed, blocks publication
}

export interface CheckContext {
  // ... existing fields
  vertical?: Vertical;
  serpContent?: string[];  // For information gain checks
}

export interface CheckResult {
  // ... existing fields
  blocking?: boolean;  // Indicates if this failure should block publication
}
```

**Create tier5 directory**: `mkdir -p open-seo-main/src/server/lib/audit/checks/tier5`

**Create T5-01-reddit-test.ts**:

```typescript
import { registerCheck } from "../registry";
import type { CheckContext, CheckResult } from "../types";
import { getQualityGateService } from "@/server/features/onpage-mastery/services/QualityGateService";

registerCheck({
  id: "T5-01",
  name: "Reddit Test",
  tier: 5,
  category: "content-quality",
  severity: "high",
  autoEditable: true,
  editRecipe: "Add specific examples, numbers, case studies; avoid generic advice",
  blocking: true,
  run: async (ctx: CheckContext): Promise<CheckResult> => {
    const { $, vertical } = ctx;
    
    if (!vertical) {
      return {
        checkId: "T5-01",
        passed: true,
        severity: "info",
        message: "Reddit Test requires vertical classification",
        autoEditable: false,
      };
    }

    // Extract text content
    const text = $("body").text().replace(/\s+/g, " ").trim();
    
    // Get quality gate service
    const gateService = getQualityGateService();
    const result = await gateService.evaluateRedditTest(text, vertical);

    const isBlocking = !result.passed && result.score < 50;

    return {
      checkId: "T5-01",
      passed: result.passed,
      severity: result.passed ? "info" : "high",
      message: result.message,
      details: {
        score: result.score,
        method: result.method,
        embeddingSimilarity: result.embeddingSimilarity,
      },
      autoEditable: !result.passed,
      blocking: isBlocking,
    };
  },
});
```

**Create T5-02-info-gain.ts**:

```typescript
import { registerCheck } from "../registry";
import type { CheckContext, CheckResult } from "../types";
import { getQualityGateService } from "@/server/features/onpage-mastery/services/QualityGateService";

registerCheck({
  id: "T5-02",
  name: "Information Gain vs SERP",
  tier: 5,
  category: "content-quality",
  severity: "high",
  autoEditable: true,
  editRecipe: "Add unique insights, data, or perspectives not found in top SERP results",
  blocking: true,
  run: async (ctx: CheckContext): Promise<CheckResult> => {
    const { $, serpContent } = ctx;
    
    if (!serpContent || serpContent.length === 0) {
      return {
        checkId: "T5-02",
        passed: true,
        severity: "info",
        message: "Information Gain check requires SERP content for comparison",
        autoEditable: false,
      };
    }

    const text = $("body").text().replace(/\s+/g, " ").trim();
    
    const gateService = getQualityGateService();
    const result = await gateService.evaluateInformationGain(text, serpContent);

    const isBlocking = !result.passed && result.score < 40;

    return {
      checkId: "T5-02",
      passed: result.passed,
      severity: result.passed ? "info" : "high",
      message: result.message,
      details: {
        score: result.score,
        method: result.method,
        similarity: result.embeddingSimilarity,
      },
      autoEditable: !result.passed,
      blocking: isBlocking,
    };
  },
});
```

**Create T5-03-prove-it.ts**:

```typescript
import { registerCheck } from "../registry";
import type { CheckContext, CheckResult } from "../types";
import { getQualityGateService } from "@/server/features/onpage-mastery/services/QualityGateService";

registerCheck({
  id: "T5-03",
  name: "Prove-It Details",
  tier: 5,
  category: "content-quality",
  severity: "high",
  autoEditable: true,
  editRecipe: "Add statistics, citations, or specific examples to back up each claim",
  blocking: true,
  run: async (ctx: CheckContext): Promise<CheckResult> => {
    const { $, vertical } = ctx;
    
    if (!vertical) {
      return {
        checkId: "T5-03",
        passed: true,
        severity: "info",
        message: "Prove-It check requires vertical classification",
        autoEditable: false,
      };
    }

    const text = $("body").text().replace(/\s+/g, " ").trim();
    
    const gateService = getQualityGateService();
    const result = await gateService.evaluateProveItDetails(text, vertical);

    const isBlocking = !result.passed && result.score < 30;

    return {
      checkId: "T5-03",
      passed: result.passed,
      severity: result.passed ? "info" : "high",
      message: result.message,
      details: { score: result.score, method: result.method },
      autoEditable: !result.passed,
      blocking: isBlocking,
    };
  },
});
```

**Create T5-04 through T5-07** following same pattern:

- **T5-04-not-for-you.ts**: Calls evaluateNotForYou, NOT blocking
- **T5-05-qdd.ts**: Calls evaluateQDDVulnerability, NOT blocking
- **T5-06-thin-content.ts**: Calls evaluateThinContent, BLOCKING if score < 20
- **T5-07-fluff.ts**: Calls evaluateFluffDetection, NOT blocking
  </action>
  <verify>
    <automated>ls -la open-seo-main/src/server/lib/audit/checks/tier5/T5-0*.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `blocking?: boolean`
    - types.ts contains `CheckTier = 1 | 2 | 3 | 4 | 5`
    - T5-01-reddit-test.ts contains `blocking: true`
    - T5-01-reddit-test.ts contains `getQualityGateService()`
    - T5-02-info-gain.ts contains `evaluateInformationGain`
    - T5-03-prove-it.ts contains `evaluateProveItDetails`
    - T5-04-not-for-you.ts contains `blocking` is NOT true (or absent)
    - T5-06-thin-content.ts contains `blocking: isBlocking`
    - 7 files created (T5-01 to T5-07)
  </acceptance_criteria>
  <done>T5-01 to T5-07 checks created with QualityGateService integration and blocking flags</done>
</task>

<task type="auto">
  <name>Task 2: Create T5-08 to T5-13 checks and barrel export</name>
  <files>
    open-seo-main/src/server/lib/audit/checks/tier5/T5-08-ai-slop.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-09-voice-consistency.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-10-tone.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-11-audience.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-12-sentence-length.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/T5-13-paragraph-length.ts,
    open-seo-main/src/server/lib/audit/checks/tier5/index.ts
  </files>
  <read_first>
    - .planning/phases/92-on-page-seo-mastery/92-CONTEXT.md (T5-08 to T5-13 specs)
    - .planning/phases/92-on-page-seo-mastery/92-EXTRACTED-RULES.md (AI slop patterns, writing rules)
    - open-seo-main/src/server/features/onpage-mastery/utils/ReadabilityScorer.ts (from 92-02)
  </read_first>
  <action>
**Create T5-08-ai-slop.ts** (BLOCKING):

```typescript
import { registerCheck } from "../registry";
import type { CheckContext, CheckResult } from "../types";

// AI-generated content patterns
const AI_SLOP_PATTERNS = [
  // Opening phrases
  /^(in today's digital (age|world|landscape))/i,
  /^(in the world of)/i,
  /^(in the realm of)/i,
  /^(when it comes to)/i,
  /^(it goes without saying)/i,
  /^(needless to say)/i,
  /^(as we all know)/i,
  
  // Filler phrases
  /\b(it is important to note that)\b/gi,
  /\b(it is worth noting that)\b/gi,
  /\b(it's no secret that)\b/gi,
  /\b(at the end of the day)\b/gi,
  /\b(the bottom line is)\b/gi,
  /\b(all in all)\b/gi,
  
  // Generic conclusions
  /\b(in conclusion|to conclude|to sum up|in summary)\b/gi,
  /\b(take your .* to the next level)\b/gi,
  /\b(unlock (the|your) (full )?potential)\b/gi,
  /\b(embark on (a|your) journey)\b/gi,
  /\b(dive (deep )?into)\b/gi,
];

registerCheck({
  id: "T5-08",
  name: "AI Slop Detection",
  tier: 5,
  category: "content-quality",
  severity: "high",
  autoEditable: true,
  editRecipe: "Remove generic AI-generated phrases and replace with specific, original language",
  blocking: true,
  run: (ctx: CheckContext): CheckResult => {
    const { $ } = ctx;
    const text = $("body").text();
    const wordCount = text.split(/\s+/).length;
    
    // Count AI slop patterns
    let slopCount = 0;
    const foundPatterns: string[] = [];
    
    for (const pattern of AI_SLOP_PATTERNS) {
      const matches = text.match(pattern);
      if (matches) {
        slopCount += matches.length;
        foundPatterns.push(...matches.slice(0, 2)); // Max 2 examples per pattern
      }
    }

    // Calculate slop density (per 1000 words)
    const slopDensity = (slopCount / wordCount) * 1000;
    
    // Score: 100 = no slop, 0 = excessive slop
    const score = Math.max(0, 100 - slopDensity * 10);
    const passed = score >= 60;
    const isBlocking = !passed && score < 40;

    return {
      checkId: "T5-08",
      passed,
      severity: passed ? "info" : "high",
      message: passed
        ? `Low AI slop density: ${slopDensity.toFixed(1)} per 1000 words`
        : `High AI slop density: ${slopDensity.toFixed(1)} per 1000 words - ${foundPatterns.slice(0, 3).map(p => `"${p}"`).join(", ")}`,
      details: { slopCount, slopDensity, score, foundPatterns: foundPatterns.slice(0, 5) },
      autoEditable: !passed,
      blocking: isBlocking,
    };
  },
});
```

**Create T5-09-voice-consistency.ts** (NOT blocking, uses AI-Writer voice profile):

```typescript
import { registerCheck } from "../registry";
import type { CheckContext, CheckResult } from "../types";

// This check would integrate with AI-Writer voice profiles
// For now, implement basic voice consistency heuristics

registerCheck({
  id: "T5-09",
  name: "Voice Consistency",
  tier: 5,
  category: "content-quality",
  severity: "medium",
  autoEditable: false,
  run: (ctx: CheckContext): CheckResult => {
    const { $ } = ctx;
    const text = $("body").text();
    
    // Check POV consistency (first person, second person, third person)
    const firstPerson = (text.match(/\b(I|we|our|us|my)\b/gi) || []).length;
    const secondPerson = (text.match(/\b(you|your|yours)\b/gi) || []).length;
    const thirdPerson = (text.match(/\b(they|their|one should|it is)\b/gi) || []).length;
    
    const total = firstPerson + secondPerson + thirdPerson;
    if (total < 10) {
      return {
        checkId: "T5-09",
        passed: true,
        severity: "info",
        message: "Not enough text to analyze voice consistency",
        autoEditable: false,
      };
    }

    // Calculate dominant voice
    const povRatios = [
      { pov: "first", count: firstPerson },
      { pov: "second", count: secondPerson },
      { pov: "third", count: thirdPerson },
    ].sort((a, b) => b.count - a.count);
    
    const dominantRatio = povRatios[0].count / total;
    const passed = dominantRatio >= 0.5; // Dominant POV should be > 50%

    return {
      checkId: "T5-09",
      passed,
      severity: passed ? "info" : "medium",
      message: passed
        ? `Consistent ${povRatios[0].pov} person voice (${Math.round(dominantRatio * 100)}%)`
        : `Mixed voice POV - dominant is only ${Math.round(dominantRatio * 100)}%`,
      details: { firstPerson, secondPerson, thirdPerson, dominantRatio },
      autoEditable: false,
    };
  },
});
```

**Create T5-10 through T5-13**:

- **T5-10-tone.ts**: Analyze sentiment consistency (use simple positive/negative word ratios)
- **T5-11-audience.ts**: Check for audience-appropriate language complexity
- **T5-12-sentence-length.ts**: Distribution analysis (avg 15-25 words, max 40)
- **T5-13-paragraph-length.ts**: Distribution analysis (3-5 sentences, max 7)

**Create tier5/index.ts** (barrel export):

```typescript
// Tier 5: Content Quality Intelligence Checks
// These checks are opt-in and require vertical classification

// Blocking checks (can prevent publication)
import "./T5-01-reddit-test";
import "./T5-02-info-gain";
import "./T5-03-prove-it";
import "./T5-06-thin-content";
import "./T5-08-ai-slop";

// Non-blocking checks (recommendations only)
import "./T5-04-not-for-you";
import "./T5-05-qdd";
import "./T5-07-fluff";
import "./T5-09-voice-consistency";
import "./T5-10-tone";
import "./T5-11-audience";
import "./T5-12-sentence-length";
import "./T5-13-paragraph-length";

// Note: To enable Tier 5 checks, import this file in the main checks loader
// and ensure client has tier5Enabled: true in clientSeoSettings
```
  </action>
  <verify>
    <automated>ls -la open-seo-main/src/server/lib/audit/checks/tier5/*.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - T5-08-ai-slop.ts contains `AI_SLOP_PATTERNS`
    - T5-08-ai-slop.ts contains `blocking: isBlocking`
    - T5-09-voice-consistency.ts contains POV analysis
    - T5-12-sentence-length.ts contains sentence length analysis
    - T5-13-paragraph-length.ts contains paragraph analysis
    - tier5/index.ts imports all 13 checks
    - tier5/index.ts contains comment about opt-in nature
    - 14 files total (13 checks + index.ts)
  </acceptance_criteria>
  <done>T5-08 to T5-13 checks created with barrel export. All 13 Tier 5 checks registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML content -> Tier 5 checks | Untrusted content evaluated for quality |
| Tier 5 checks -> QualityGateService | Delegation to service with LLM calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | LLM prompts | mitigate | QualityGateService handles PII stripping and prompt sanitization |
| T-92-11 | Denial of Service | Async checks | mitigate | Timeouts on QualityGateService calls, circuit breaker protection |
</threat_model>

<verification>
1. Count check files: `ls open-seo-main/src/server/lib/audit/checks/tier5/T5-*.ts | wc -l` should return 13
2. Verify barrel export: `cat open-seo-main/src/server/lib/audit/checks/tier5/index.ts | grep import | wc -l` should return 13
3. Run type check: `cd open-seo-main && pnpm tsc --noEmit`
</verification>

<success_criteria>
- [ ] 13 Tier 5 check files created (T5-01 to T5-13)
- [ ] Blocking checks marked correctly (T5-01, T5-02, T5-03, T5-06, T5-08)
- [ ] Non-blocking checks have no blocking flag
- [ ] QualityGateService integration for T5-01 to T5-07
- [ ] AI slop detection patterns comprehensive
- [ ] Barrel export imports all checks
- [ ] types.ts updated with blocking field
</success_criteria>

<output>
After completion, create `.planning/phases/92-on-page-seo-mastery/92-08-SUMMARY.md`
</output>
