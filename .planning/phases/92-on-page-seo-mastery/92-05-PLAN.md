---
phase: 92-on-page-seo-mastery
plan: 05
type: execute
wave: 2
depends_on:
  - 92-01
  - 92-02
files_modified:
  - open-seo-main/src/server/features/onpage-mastery/utils/SchemaGenerator.ts
  - open-seo-main/src/server/features/onpage-mastery/utils/SchemaGenerator.test.ts
  - open-seo-main/src/server/features/onpage-mastery/utils/index.ts
  - open-seo-main/package.json
autonomous: true
requirements:
  - OPM-17

must_haves:
  truths:
    - "Schema.org JSON-LD generated with TypeScript type safety"
    - "Vertical-specific schema selection (Article, Product, LocalBusiness, etc.)"
    - "Generated JSON-LD validates against Schema.org spec"
  artifacts:
    - path: "open-seo-main/src/server/features/onpage-mastery/utils/SchemaGenerator.ts"
      provides: "Type-safe Schema.org JSON-LD generation"
      exports: ["generateArticleSchema", "generateProductSchema", "generateLocalBusinessSchema", "getSchemaForVertical"]
  key_links:
    - from: "SchemaGenerator.ts"
      to: "schema-dts"
      via: "TypeScript type imports"
      pattern: "import type { Article, Product } from 'schema-dts'"
---

<objective>
Create SchemaGenerator utility for type-safe Schema.org JSON-LD generation with vertical-specific schema selection.

Purpose: Proper structured data is essential for rich snippets and AI visibility. Type-safe generation prevents Schema.org property typos that would break structured data.

Output: SchemaGenerator with Article, Product, LocalBusiness, Service, Course, and Organization schema generators using schema-dts types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-on-page-seo-mastery/92-CONTEXT.md
@.planning/phases/92-on-page-seo-mastery/92-RESEARCH.md
@.planning/phases/92-on-page-seo-mastery/92-PATTERNS.md

<interfaces>
<!-- Types from 92-01 -->
From open-seo-main/src/server/features/onpage-mastery/types.ts:
```typescript
export type Vertical = "healthcare" | "legal" | "financial" | "ecommerce" | "saas" | /* ... */;
```

<!-- schema-dts pattern from RESEARCH.md -->
From 92-RESEARCH.md:
```typescript
import type { Article, Person } from 'schema-dts';

export function generateArticleSchema(data: ArticleData): string {
  const schema: Article = {
    '@type': 'Article',
    headline: data.headline,
    author: { '@type': 'Person', name: data.author } as Person,
    // TypeScript validates all properties at compile time
  };
  return JSON.stringify({ '@context': 'https://schema.org', ...schema });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SchemaGenerator utility with vertical-specific schemas</name>
  <files>
    open-seo-main/src/server/features/onpage-mastery/utils/SchemaGenerator.ts,
    open-seo-main/src/server/features/onpage-mastery/utils/SchemaGenerator.test.ts,
    open-seo-main/src/server/features/onpage-mastery/utils/index.ts,
    open-seo-main/package.json
  </files>
  <read_first>
    - open-seo-main/package.json (check if schema-dts installed)
    - .planning/phases/92-on-page-seo-mastery/92-RESEARCH.md (schema-dts code example)
    - .planning/phases/92-on-page-seo-mastery/92-CONTEXT.md (vertical classification)
  </read_first>
  <behavior>
    - Test: generateArticleSchema returns valid JSON-LD with @context and @type
    - Test: generateProductSchema includes price, availability, brand
    - Test: generateLocalBusinessSchema includes name, address, telephone
    - Test: getSchemaForVertical("healthcare") returns Article or MedicalBusiness
    - Test: getSchemaForVertical("ecommerce") returns Product
    - Test: TypeScript catches invalid property names at compile time
  </behavior>
  <action>
**Install schema-dts** (if not already installed):
```bash
cd open-seo-main && pnpm add schema-dts
```

**Create SchemaGenerator.ts**:

```typescript
import type {
  Article,
  Person,
  Organization,
  Product,
  Offer,
  LocalBusiness,
  Service,
  Course,
  MedicalOrganization,
  LegalService,
  FinancialService,
  SoftwareApplication,
  WithContext,
  Thing,
} from "schema-dts";
import type { Vertical } from "../types";

// Input data interfaces
export interface ArticleData {
  headline: string;
  description?: string;
  author: string;
  authorUrl?: string;
  datePublished: string;
  dateModified?: string;
  image?: string;
  url: string;
  publisher?: string;
  publisherLogo?: string;
}

export interface ProductData {
  name: string;
  description?: string;
  image?: string;
  brand?: string;
  sku?: string;
  price?: number;
  priceCurrency?: string;
  availability?: "InStock" | "OutOfStock" | "PreOrder";
  url: string;
  reviewCount?: number;
  ratingValue?: number;
}

export interface LocalBusinessData {
  name: string;
  description?: string;
  image?: string;
  telephone?: string;
  email?: string;
  address: {
    street: string;
    city: string;
    state: string;
    postalCode: string;
    country: string;
  };
  geo?: { latitude: number; longitude: number };
  openingHours?: string[];
  priceRange?: string;
  url: string;
}

export interface ServiceData {
  name: string;
  description?: string;
  provider: string;
  areaServed?: string[];
  serviceType?: string;
  url: string;
}

export interface CourseData {
  name: string;
  description?: string;
  provider: string;
  duration?: string;
  educationalLevel?: string;
  url: string;
}

export interface SoftwareData {
  name: string;
  description?: string;
  applicationCategory?: string;
  operatingSystem?: string;
  offers?: { price: number; priceCurrency: string };
  url: string;
}

/**
 * Generate Article schema (for blog posts, news, healthcare content)
 */
export function generateArticleSchema(data: ArticleData): string {
  const schema: WithContext<Article> = {
    "@context": "https://schema.org",
    "@type": "Article",
    headline: data.headline,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author,
      url: data.authorUrl,
    } as Person,
    datePublished: data.datePublished,
    dateModified: data.dateModified || data.datePublished,
    image: data.image,
    url: data.url,
    publisher: data.publisher
      ? ({
          "@type": "Organization",
          name: data.publisher,
          logo: data.publisherLogo
            ? {
                "@type": "ImageObject",
                url: data.publisherLogo,
              }
            : undefined,
        } as Organization)
      : undefined,
  };

  return JSON.stringify(schema, null, 2);
}

/**
 * Generate Product schema (for e-commerce)
 */
export function generateProductSchema(data: ProductData): string {
  const schema: WithContext<Product> = {
    "@context": "https://schema.org",
    "@type": "Product",
    name: data.name,
    description: data.description,
    image: data.image,
    brand: data.brand
      ? {
          "@type": "Brand",
          name: data.brand,
        }
      : undefined,
    sku: data.sku,
    url: data.url,
    offers: data.price
      ? ({
          "@type": "Offer",
          price: data.price,
          priceCurrency: data.priceCurrency || "USD",
          availability: data.availability
            ? `https://schema.org/${data.availability}`
            : undefined,
        } as Offer)
      : undefined,
    aggregateRating:
      data.reviewCount && data.ratingValue
        ? {
            "@type": "AggregateRating",
            ratingValue: data.ratingValue,
            reviewCount: data.reviewCount,
          }
        : undefined,
  };

  return JSON.stringify(schema, null, 2);
}

/**
 * Generate LocalBusiness schema (for home services, restaurants, etc.)
 */
export function generateLocalBusinessSchema(data: LocalBusinessData): string {
  const schema: WithContext<LocalBusiness> = {
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    name: data.name,
    description: data.description,
    image: data.image,
    telephone: data.telephone,
    email: data.email,
    url: data.url,
    priceRange: data.priceRange,
    address: {
      "@type": "PostalAddress",
      streetAddress: data.address.street,
      addressLocality: data.address.city,
      addressRegion: data.address.state,
      postalCode: data.address.postalCode,
      addressCountry: data.address.country,
    },
    geo: data.geo
      ? {
          "@type": "GeoCoordinates",
          latitude: data.geo.latitude,
          longitude: data.geo.longitude,
        }
      : undefined,
    openingHoursSpecification: data.openingHours?.map((hours) => ({
      "@type": "OpeningHoursSpecification",
      dayOfWeek: hours.split(" ")[0],
      opens: hours.split(" ")[1]?.split("-")[0],
      closes: hours.split(" ")[1]?.split("-")[1],
    })),
  };

  return JSON.stringify(schema, null, 2);
}

/**
 * Generate Service schema (for professional services)
 */
export function generateServiceSchema(data: ServiceData): string {
  const schema: WithContext<Service> = {
    "@context": "https://schema.org",
    "@type": "Service",
    name: data.name,
    description: data.description,
    provider: {
      "@type": "Organization",
      name: data.provider,
    } as Organization,
    areaServed: data.areaServed,
    serviceType: data.serviceType,
    url: data.url,
  };

  return JSON.stringify(schema, null, 2);
}

/**
 * Generate Course schema (for education)
 */
export function generateCourseSchema(data: CourseData): string {
  const schema: WithContext<Course> = {
    "@context": "https://schema.org",
    "@type": "Course",
    name: data.name,
    description: data.description,
    provider: {
      "@type": "Organization",
      name: data.provider,
    } as Organization,
    timeRequired: data.duration,
    educationalLevel: data.educationalLevel,
    url: data.url,
  };

  return JSON.stringify(schema, null, 2);
}

/**
 * Generate SoftwareApplication schema (for SaaS)
 */
export function generateSoftwareSchema(data: SoftwareData): string {
  const schema: WithContext<SoftwareApplication> = {
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    name: data.name,
    description: data.description,
    applicationCategory: data.applicationCategory,
    operatingSystem: data.operatingSystem,
    url: data.url,
    offers: data.offers
      ? ({
          "@type": "Offer",
          price: data.offers.price,
          priceCurrency: data.offers.priceCurrency,
        } as Offer)
      : undefined,
  };

  return JSON.stringify(schema, null, 2);
}

/**
 * Get recommended schema type(s) for a vertical
 */
export function getSchemaTypesForVertical(vertical: Vertical): string[] {
  const VERTICAL_SCHEMAS: Record<Vertical, string[]> = {
    healthcare: ["Article", "MedicalOrganization"],
    legal: ["Article", "LegalService"],
    financial: ["Article", "FinancialService"],
    ecommerce: ["Product", "ItemList"],
    saas: ["SoftwareApplication", "Article"],
    real_estate: ["LocalBusiness", "RealEstateListing"],
    home_services: ["LocalBusiness", "Service"],
    hospitality: ["LocalBusiness", "Hotel", "Restaurant"],
    education: ["Course", "EducationalOrganization"],
    professional: ["Service", "LocalBusiness"],
    manufacturing: ["Product", "Organization"],
    nonprofit: ["Organization", "Article"],
    general: ["Article", "WebPage"],
  };

  return VERTICAL_SCHEMAS[vertical] || ["Article"];
}

/**
 * Validate generated JSON-LD structure
 */
export function validateJsonLd(jsonLd: string): { valid: boolean; errors: string[] } {
  const errors: string[] = [];

  try {
    const parsed = JSON.parse(jsonLd);

    // Required fields
    if (!parsed["@context"]) {
      errors.push("Missing @context");
    }
    if (!parsed["@type"]) {
      errors.push("Missing @type");
    }

    // Context should be schema.org
    if (parsed["@context"] && !parsed["@context"].includes("schema.org")) {
      errors.push("@context should reference schema.org");
    }

    return { valid: errors.length === 0, errors };
  } catch (e) {
    return { valid: false, errors: [`Invalid JSON: ${(e as Error).message}`] };
  }
}
```

**Create test file** `SchemaGenerator.test.ts`:
- Test generateArticleSchema output structure
- Test generateProductSchema with all fields
- Test generateLocalBusinessSchema with address
- Test getSchemaTypesForVertical mappings
- Test validateJsonLd catches missing @context
- Test type safety (compile-time only, not runtime)

**Update utils/index.ts**:
```typescript
export * from './ChunkExtractor';
export * from './ReadabilityScorer';
export * from './EntityExtractor';
export * from './SchemaGenerator';
```
  </action>
  <verify>
    <automated>cd open-seo-main && pnpm test -- src/server/features/onpage-mastery/utils/SchemaGenerator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - open-seo-main/package.json contains `"schema-dts":`
    - SchemaGenerator.ts contains `import type { Article, Product } from "schema-dts"`
    - SchemaGenerator.ts contains `export function generateArticleSchema(`
    - SchemaGenerator.ts contains `export function generateProductSchema(`
    - SchemaGenerator.ts contains `export function generateLocalBusinessSchema(`
    - SchemaGenerator.ts contains `export function getSchemaTypesForVertical(`
    - SchemaGenerator.ts contains `"@context": "https://schema.org"`
    - utils/index.ts contains `export * from './SchemaGenerator'`
    - Tests pass: `pnpm test -- SchemaGenerator.test.ts` exits 0
  </acceptance_criteria>
  <done>SchemaGenerator utility with type-safe Schema.org JSON-LD generation for 6 schema types. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User data -> SchemaGenerator | User-provided content used in JSON-LD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-08 | Tampering | JSON-LD output | mitigate | Use schema-dts types for validation, escape user strings, never render raw JSON-LD from untrusted sources in browser |
</threat_model>

<verification>
1. Run SchemaGenerator tests: `cd open-seo-main && pnpm test -- SchemaGenerator.test.ts`
2. Verify schema-dts types: `cd open-seo-main && pnpm tsc --noEmit` (catches property typos)
3. Validate output: paste generated JSON-LD into Google's Rich Results Test
</verification>

<success_criteria>
- [ ] schema-dts installed and imported
- [ ] Article, Product, LocalBusiness, Service, Course, SoftwareApplication generators
- [ ] getSchemaTypesForVertical maps all 13 verticals
- [ ] validateJsonLd function for basic validation
- [ ] TypeScript compile-time validation of Schema.org properties
- [ ] All tests passing (OPM-17)
</success_criteria>

<output>
After completion, create `.planning/phases/92-on-page-seo-mastery/92-05-SUMMARY.md`
</output>
