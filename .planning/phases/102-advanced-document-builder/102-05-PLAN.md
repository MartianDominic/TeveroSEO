---
phase: 102-advanced-document-builder
plan: 05
type: execute
wave: 3
depends_on: [102-01, 102-02, 102-04]
files_modified:
  - apps/web/src/lib/document-builder/ab-testing-service.ts
  - apps/web/src/lib/document-builder/version-diff.ts
  - apps/web/src/components/document-builder/VariantCreator.tsx
  - apps/web/src/components/document-builder/VariantTabs.tsx
  - apps/web/src/components/document-builder/VersionDiff.tsx
  - apps/web/src/components/document-builder/BlockAnalyticsDashboard.tsx
autonomous: true
requirements: [REQ-06, REQ-08]

must_haves:
  truths:
    - "User can create A/B test variants on any block"
    - "Different prospects see different variants (deterministic hash per D-03)"
    - "Analytics show which variant performs better"
    - "User can compare two versions side-by-side with diff highlighting"
  artifacts:
    - path: "apps/web/src/lib/document-builder/ab-testing-service.ts"
      provides: "Variant assignment and statistical significance"
      exports: ["getVariantForProspect", "calculateSignificance", "ABTestResult"]
    - path: "apps/web/src/components/document-builder/VersionDiff.tsx"
      provides: "Side-by-side comparison with highlighting"
      min_lines: 80
    - path: "apps/web/src/components/document-builder/VariantCreator.tsx"
      provides: "Modal for creating new variants"
      min_lines: 60
  key_links:
    - from: "apps/web/src/lib/document-builder/ab-testing-service.ts"
      to: "apps/web/src/db/schema/document-builder.ts"
      via: "blockVariants table queries"
      pattern: "blockVariants"
---

<objective>
Implement A/B testing UI and side-by-side version diff for the document builder.

Purpose: Enable users to test block variations (REQ-06) and compare document versions (REQ-08), with deterministic variant assignment per D-03.

Output: Working variant creation, assignment, analytics display, and version diff view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-advanced-document-builder/102-CONTEXT.md (Section 3 - Variant Assignment)
@.planning/phases/102-advanced-document-builder/DOCUMENT-BUILDER-ARCHITECTURE.md (A/B Testing Architecture section)
@.planning/phases/102-advanced-document-builder/102-UI-SPEC.md (A/B Variant Tabs, Version Diff Styling)
@.planning/phases/102-advanced-document-builder/102-01-SUMMARY.md
@.planning/phases/102-advanced-document-builder/102-04-SUMMARY.md

<interfaces>
<!-- From Plan 01 - blockVariants table -->

From apps/web/src/db/schema/document-builder.ts:
```typescript
export const blockVariants = pgTable('block_variants', {
  id: text('id').primaryKey(),
  parentBlockId: text('parent_block_id').notNull().references(() => persuasionBlocks.id),
  variantName: text('variant_name').notNull(),
  content: jsonb('content').notNull(),
  weight: integer('weight').notNull().default(50),
  impressions: integer('impressions').default(0),
  conversions: integer('conversions').default(0),
  status: text('status').default('active'),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create A/B testing service with deterministic assignment</name>
  <files>apps/web/src/lib/document-builder/ab-testing-service.ts, apps/web/src/lib/document-builder/__tests__/ab-testing-service.test.ts</files>
  <read_first>
    - .planning/phases/102-advanced-document-builder/102-CONTEXT.md (Section 3 - Variant Assignment)
    - .planning/phases/102-advanced-document-builder/DOCUMENT-BUILDER-ARCHITECTURE.md (Variant Assignment, Statistical Significance sections)
    - apps/web/src/db/schema/document-builder.ts (blockVariants table)
  </read_first>
  <behavior>
    - Test 1: getVariantForProspect returns same variant for same prospect+block (deterministic)
    - Test 2: getVariantForProspect distributes evenly across variants based on weights
    - Test 3: calculateSignificance returns confidence level 0-100
    - Test 4: calculateSignificance marks isSignificant=true when confidence > 95
    - Test 5: calculateSignificance returns 'needs_more_data' when impressions < 100
  </behavior>
  <action>
Create ab-testing-service.ts with:

1. getVariantForProspect per D-03 (deterministic hash):
```typescript
function getVariantForProspect(
  prospectId: string,
  blockId: string,
  variants: BlockVariant[]
): BlockVariant {
  const hash = createHash('sha256')
    .update(`${prospectId}:${blockId}`)
    .digest();
  const hashNum = hash.readUInt32BE(0);
  const bucket = hashNum % 100;
  
  let cumulative = 0;
  for (const variant of variants) {
    cumulative += variant.weight;
    if (bucket < cumulative) return variant;
  }
  return variants[variants.length - 1];
}
```

2. calculateSignificance using z-test for proportions:
```typescript
interface ABTestResult {
  variantId: string;
  impressions: number;
  conversions: number;
  conversionRate: number;
  confidenceLevel: number;
  isSignificant: boolean;
  recommendation: 'winner' | 'loser' | 'needs_more_data';
}
```

3. createVariant(blockId: string, name: string, content: unknown): Creates new variant in DB

4. updateVariantWeights(blockId: string, weights: Record<string, number>): Adjusts traffic allocation

5. declareWinner(variantId: string): Sets status='winner', others='loser', stops test
  </action>
  <verify>
    <automated>cd apps/web && npm test -- --grep "ab-testing" --run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "sha256" apps/web/src/lib/document-builder/ab-testing-service.ts
    - grep -q "getVariantForProspect" apps/web/src/lib/document-builder/ab-testing-service.ts
    - grep -q "calculateSignificance" apps/web/src/lib/document-builder/ab-testing-service.ts
    - grep -q "needs_more_data\|winner\|loser" apps/web/src/lib/document-builder/ab-testing-service.ts
  </acceptance_criteria>
  <done>A/B testing service with deterministic assignment and significance calculation</done>
</task>

<task type="auto">
  <name>Task 2: Create VariantCreator and VariantTabs components</name>
  <files>apps/web/src/components/document-builder/VariantCreator.tsx, apps/web/src/components/document-builder/VariantTabs.tsx</files>
  <read_first>
    - .planning/phases/102-advanced-document-builder/102-UI-SPEC.md (A/B Variant Tabs section)
    - apps/web/src/lib/document-builder/ab-testing-service.ts (from Task 1)
    - packages/ui/src/components/dialog.tsx (Dialog pattern)
  </read_first>
  <action>
Create VariantCreator.tsx:
- Modal triggered from block actions "Create Variant"
- Form fields:
  - Variant name (default "Variant A", "Variant B", etc.)
  - Content (clone from control or start blank)
  - Traffic weight (slider 0-100, default 50)
- Calls createVariant on submit
- Shows success toast

Create VariantTabs.tsx:
- Renders in PersuasionBlock footer when variants exist
- Tab for each variant: [Control] [Variant A] [Variant B] [+ Variant]
- Visual states per UI-SPEC:
  - Inactive: --surface-2 background, --text-3
  - Active: --accent-soft background, --accent-ink text
  - Winner badge: --success-soft pill with checkmark
  - Loser badge: --error-soft pill with x
  - Needs data: --surface-3 pill, --text-4
- Shows impressions/conversions inline
- Click tab to preview that variant in editor
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit src/components/document-builder/VariantCreator.tsx 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "createVariant" apps/web/src/components/document-builder/VariantCreator.tsx
    - grep -q "Dialog\|Sheet" apps/web/src/components/document-builder/VariantCreator.tsx
    - grep -q "Control.*Variant" apps/web/src/components/document-builder/VariantTabs.tsx
    - grep -q "accent-soft\|success-soft\|error-soft" apps/web/src/components/document-builder/VariantTabs.tsx
  </acceptance_criteria>
  <done>Variant creation modal and tabs display with proper styling</done>
</task>

<task type="auto">
  <name>Task 3: Create VersionDiff component and version diff service</name>
  <files>apps/web/src/lib/document-builder/version-diff.ts, apps/web/src/components/document-builder/VersionDiff.tsx</files>
  <read_first>
    - .planning/phases/102-advanced-document-builder/102-UI-SPEC.md (Version Diff Styling section)
    - apps/web/src/components/proposals/VersionHistory.tsx (existing version list pattern)
    - .planning/phases/102-advanced-document-builder/102-CONTEXT.md (REQ-08)
  </read_first>
  <action>
Create version-diff.ts:
1. computeBlockDiff(blocks1: PersuasionBlock[], blocks2: PersuasionBlock[]):
   - Returns array of { block, status: 'added' | 'removed' | 'modified' | 'unchanged' }
   - Uses block ID matching
   - Detects content changes within same block

2. computeTextDiff(text1: string, text2: string):
   - Word-level diff (not character) per UI-SPEC
   - Returns array of { text, status: 'added' | 'removed' | 'unchanged' }
   - Uses simple LCS algorithm or diff library

Create VersionDiff.tsx:
- Side-by-side layout: Version A | Version B
- Each side renders blocks with diff highlighting per UI-SPEC:
  - Added block: background --success-soft, left border 3px --success
  - Removed block: background --error-soft, left border 3px --error
  - Modified block: background --warning-soft, left border 3px --warning
  - Unchanged: transparent
- Inline text diff within modified blocks:
  - Added text: background --success-soft, no strikethrough
  - Removed text: background --error-soft, strikethrough
- Version selector dropdowns at top
- "No changes between these versions" empty state per UI-SPEC
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit src/components/document-builder/VersionDiff.tsx 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "computeBlockDiff\|computeTextDiff" apps/web/src/lib/document-builder/version-diff.ts
    - grep -q "added.*removed.*modified" apps/web/src/lib/document-builder/version-diff.ts
    - grep -q "success-soft.*error-soft.*warning-soft" apps/web/src/components/document-builder/VersionDiff.tsx
    - grep -q "strikethrough\|line-through" apps/web/src/components/document-builder/VersionDiff.tsx
  </acceptance_criteria>
  <done>Side-by-side version diff with block and text-level highlighting</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Variant creation request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-08 | Tampering | variant weights | mitigate | CHECK constraint 0-100, sum validation |
| T-102-09 | Information Disclosure | A/B test results | accept | Workspace-scoped, no cross-tenant leak |
</threat_model>

<verification>
- [ ] Can create new variant from existing block
- [ ] Same prospect sees same variant across sessions
- [ ] Statistical significance displays when data sufficient
- [ ] Version diff shows added/removed/modified blocks
- [ ] Inline text diff highlights word-level changes
</verification>

<success_criteria>
- Create A/B test on guarantee block, see both variants
- After 100+ impressions, significance indicator shows
- Compare v1 to v2, see highlighted changes
</success_criteria>

<output>
After completion, create `.planning/phases/102-advanced-document-builder/102-05-SUMMARY.md`
</output>
