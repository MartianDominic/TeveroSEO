---
phase: 102-advanced-document-builder
plan: 03
type: execute
wave: 2
depends_on: [102-01, 102-02]
files_modified:
  - apps/web/src/lib/document-builder/ai-generator.ts
  - apps/web/src/lib/document-builder/template-service.ts
  - apps/web/src/components/document-builder/BlockEditor.tsx
  - apps/web/src/components/document-builder/FrameworkSelector.tsx
  - apps/web/src/app/api/document-builder/generate/route.ts
autonomous: true
requirements: [REQ-03, REQ-07]

must_haves:
  truths:
    - "User can select from 3 framework templates (Perfect Webinar, StoryBrand, PAS)"
    - "User can click 'Generate with AI' on any block"
    - "AI generates content using Gemini 3.1 Pro per D-05"
    - "Generated content appears in TipTap editor within block"
  artifacts:
    - path: "apps/web/src/lib/document-builder/ai-generator.ts"
      provides: "AI content generation service using Gemini"
      exports: ["generateBlockContent", "GenerationRequest"]
    - path: "apps/web/src/components/document-builder/FrameworkSelector.tsx"
      provides: "Modal for selecting framework templates"
      min_lines: 60
    - path: "apps/web/src/app/api/document-builder/generate/route.ts"
      provides: "POST endpoint for AI generation"
      exports: ["POST"]
  key_links:
    - from: "apps/web/src/components/document-builder/BlockEditor.tsx"
      to: "/api/document-builder/generate"
      via: "fetch POST for AI generation"
      pattern: "fetch.*document-builder.*generate"
---

<objective>
Implement AI content generation and framework templates for the document builder.

Purpose: Enable users to start from proven frameworks (REQ-03) and generate persuasive content with AI assistance (REQ-07) using Gemini 3.1 Pro per D-05.

Output: Working AI generation, 3 framework templates, and TipTap integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-advanced-document-builder/102-CONTEXT.md
@.planning/phases/102-advanced-document-builder/DOCUMENT-BUILDER-ARCHITECTURE.md (AI Generation Interface section)
@.planning/phases/102-advanced-document-builder/102-01-SUMMARY.md

<interfaces>
<!-- Existing AI/Gemini patterns -->

From apps/web/src/lib/seo-chat/proposal.ts (Gemini pattern):
```typescript
import { GoogleGenerativeAI } from '@google/generative-ai';

const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY!);
const model = genAI.getGenerativeModel({ model: 'gemini-1.5-pro' });
```

From apps/web/src/components/proposals/ProposalInlineEditor.tsx (TipTap pattern):
```typescript
import { useEditor, EditorContent } from '@tiptap/react';
import StarterKit from '@tiptap/starter-kit';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AI generation service with Gemini 3.1 Pro</name>
  <files>apps/web/src/lib/document-builder/ai-generator.ts, apps/web/src/lib/document-builder/__tests__/ai-generator.test.ts</files>
  <read_first>
    - apps/web/src/lib/seo-chat/proposal.ts (existing Gemini integration)
    - .planning/phases/102-advanced-document-builder/DOCUMENT-BUILDER-ARCHITECTURE.md (AI Generation Interface section)
    - apps/web/src/lib/document-builder/types.ts (GenerationRequest type)
  </read_first>
  <behavior>
    - Test 1: generateBlockContent accepts GenerationRequest and returns content string
    - Test 2: buildPrompt includes block type, prospect context, and style references
    - Test 3: Request includes language constraint (default 'lt' for Lithuanian)
    - Test 4: Error handling returns fallback message on API failure
  </behavior>
  <action>
Create ai-generator.ts with:

1. GenerationRequest interface per DOCUMENT-BUILDER-ARCHITECTURE.md:
```typescript
interface GenerationRequest {
  blockType: PersuasionBlockType;
  intent: 'create' | 'fill_variables' | 'regenerate' | 'improve';
  prospect: ProspectContext;
  styleReferences?: StyleReference[];
  existingContent?: string;
  customPrompt?: string;
  maxLength?: number;
  tone?: string;
  language: string; // 'lt', 'en'
  framework?: string;
  precedingBlocks?: string[];
}
```

2. generateBlockContent function:
   - Uses Gemini 3.1 Pro (gemini-1.5-pro) per D-05
   - Builds system prompt with block type purpose
   - Includes prospect context (company, industry, pain points)
   - Applies style reference tone/vocabulary if provided
   - Returns TipTap-compatible content

3. buildPrompt helper:
   - Block type purpose from PERSUASION_BLOCK_TYPES
   - Prospect personalization
   - Framework context if provided
   - Language enforcement

4. Cost: $1.25/1M tokens per D-05
  </action>
  <verify>
    <automated>cd apps/web && npm test -- --grep "ai-generator" --run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "gemini-1.5-pro\|gemini-3.1-pro" apps/web/src/lib/document-builder/ai-generator.ts
    - grep -q "GenerationRequest" apps/web/src/lib/document-builder/ai-generator.ts
    - grep -q "generateBlockContent" apps/web/src/lib/document-builder/ai-generator.ts
    - grep -q "blockType.*prospect.*language" apps/web/src/lib/document-builder/ai-generator.ts
  </acceptance_criteria>
  <done>AI generator service created with Gemini integration, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create framework selector and template service</name>
  <files>apps/web/src/lib/document-builder/template-service.ts, apps/web/src/components/document-builder/FrameworkSelector.tsx</files>
  <read_first>
    - apps/web/src/lib/document-builder/persuasion-blocks.ts (FRAMEWORK_TEMPLATES)
    - .planning/phases/102-advanced-document-builder/102-UI-SPEC.md (FrameworkSelector component)
    - packages/ui/src/components/dialog.tsx (existing Dialog pattern)
  </read_first>
  <action>
Create template-service.ts:
1. getFrameworkTemplate(frameworkId: string) - returns framework with required blocks
2. applyFrameworkToCanvas(frameworkId: string) - returns array of pre-configured blocks
3. validateFrameworkCompliance(blocks: PersuasionBlock[], frameworkId: string) - returns missing/extra blocks
4. Three frameworks per REQ-03:
   - russell_brunson: [pain_amplifier, villain_story, credibility, social_proof, process_reveal, offer_stack, risk_reversal, cta]
   - storybrand: [pain_amplifier, credibility, process_reveal, offer_stack, risk_reversal, cta, custom]
   - pas: [pain_amplifier, urgency, cta]

Create FrameworkSelector.tsx:
- Modal/Sheet component per UI-SPEC
- Shows 3 framework cards with:
  - Name, description
  - Block count
  - Win rate (if available)
- "No framework (freestyle)" option
- On select: calls store.setFramework + adds framework blocks to canvas
- Close button
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit src/components/document-builder/FrameworkSelector.tsx 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "russell_brunson.*storybrand.*pas" apps/web/src/lib/document-builder/template-service.ts
    - grep -q "validateFrameworkCompliance" apps/web/src/lib/document-builder/template-service.ts
    - grep -q "Dialog\|Sheet" apps/web/src/components/document-builder/FrameworkSelector.tsx
  </acceptance_criteria>
  <done>3 framework templates available, FrameworkSelector modal works</done>
</task>

<task type="auto">
  <name>Task 3: Create BlockEditor and generation API route</name>
  <files>apps/web/src/components/document-builder/BlockEditor.tsx, apps/web/src/app/api/document-builder/generate/route.ts</files>
  <read_first>
    - apps/web/src/components/proposals/ProposalInlineEditor.tsx (TipTap pattern)
    - apps/web/src/lib/document-builder/ai-generator.ts (from Task 1)
    - apps/web/src/app/api/seo-chat/route.ts (existing API route pattern)
  </read_first>
  <action>
Create POST /api/document-builder/generate route:
1. Accepts GenerationRequest body
2. Validates with Zod schema
3. Calls generateBlockContent
4. Returns { content: string, confidence: number }
5. Rate limit: 10 generations/hour/user (reuse existing rate-limit.ts pattern)
6. Error handling with proper status codes

Create BlockEditor.tsx:
1. Wraps TipTap editor (useEditor with StarterKit)
2. Receives blockId, initialContent props
3. "Generate with AI" button with sparkle icon
4. Loading state: skeleton shimmer per UI-SPEC
5. On generate:
   - Fetch POST /api/document-builder/generate
   - Update block content via store
   - Show success toast
6. Syncs content changes to store.updateBlockContent
7. Variable insertion support (reuse VariableExtension pattern from proposals)
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit src/app/api/document-builder/generate/route.ts 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "POST" apps/web/src/app/api/document-builder/generate/route.ts
    - grep -q "generateBlockContent" apps/web/src/app/api/document-builder/generate/route.ts
    - grep -q "useEditor\|EditorContent" apps/web/src/components/document-builder/BlockEditor.tsx
    - grep -q "Generate.*AI\|sparkle" apps/web/src/components/document-builder/BlockEditor.tsx
  </acceptance_criteria>
  <done>TipTap editor integrated in blocks, AI generation works end-to-end</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Generation request crosses trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-04 | Denial of Service | /api/document-builder/generate | mitigate | Rate limit 10/hour/user via rate-limit.ts |
| T-102-05 | Information Disclosure | AI prompt | accept | Prospect data owned by workspace, no cross-workspace leak |
</threat_model>

<verification>
- [ ] 3 framework templates selectable
- [ ] AI generation produces relevant content
- [ ] TipTap editor shows generated content
- [ ] Rate limiting prevents abuse
- [ ] Loading states display during generation
</verification>

<success_criteria>
- User can select "Russell Brunson" framework and see 8 blocks added
- User can click "Generate with AI" and receive content within 5 seconds
- Generated content appears in TipTap editor
</success_criteria>

<output>
After completion, create `.planning/phases/102-advanced-document-builder/102-03-SUMMARY.md`
</output>
