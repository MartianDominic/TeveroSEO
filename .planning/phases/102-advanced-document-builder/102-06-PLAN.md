---
phase: 102-advanced-document-builder
plan: 06
type: execute
wave: 3
depends_on: [102-01, 102-02, 102-03, 102-04, 102-05]
files_modified:
  - apps/web/src/lib/document-builder/ai-generator.ts
  - apps/web/src/lib/document-builder/analytics-service.ts
  - apps/web/src/components/document-builder/index.ts
  - apps/web/src/components/document-builder/BlockEditor.tsx
  - apps/web/src/components/document-builder/VariantCreator.tsx
  - apps/web/src/lib/document-builder/analytics-sync-worker.ts
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "AI prompt injection is mitigated with input sanitization"
    - "Redis KEYS command replaced with SCAN cursor iteration"
    - "Analytics sync worker persists Redis counters to Postgres every 5 minutes"
    - "All HIGH severity code quality issues from review are fixed"
  artifacts:
    - path: "apps/web/src/lib/document-builder/analytics-sync-worker.ts"
      provides: "BullMQ worker for Redis to Postgres sync"
      exports: ["analyticsSyncWorker", "syncAnalytics"]
    - path: "apps/web/src/lib/document-builder/input-sanitizer.ts"
      provides: "Input sanitization for AI prompts"
      exports: ["sanitizeForPrompt", "escapePromptInjection"]
  key_links:
    - from: "apps/web/src/lib/document-builder/analytics-sync-worker.ts"
      to: "apps/web/src/lib/document-builder/analytics-service.ts"
      via: "Redis counter keys"
      pattern: "block:id:variant:id:views"
---

<objective>
Fix all HIGH severity code quality issues identified in the 5-agent code review and create the missing analytics sync worker.

Purpose: Close architecture gaps and security issues before building Upload-First features. The analytics sync worker is CRITICAL - without it, Redis counters never persist to Postgres and A/B testing data is lost.

Output: All HIGH issues resolved, analytics pipeline complete, clean foundation for Wave 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-advanced-document-builder/102-CONTEXT.md
@.planning/phases/102-advanced-document-builder/102-GAPS.md

<interfaces>
HIGH severity issues to fix:
1. AI prompt injection risk (ai-generator.ts:107-203)
2. Redis KEYS command O(N) blocking (analytics-service.ts:391)
3. Missing exports in index.ts (BlockEditor, FrameworkSelector, HeatmapOverlay)
4. TipTap editor cleanup leak (BlockEditor.tsx)
5. Unstable callback reference (BlockEditor.tsx:132-141)
6. Stale form state (VariantCreator.tsx:90-94)

CRITICAL architecture gap:
- Analytics sync worker missing - Redis counters never persist to Postgres
- block_variants.impressions/conversions columns exist but never updated
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create input sanitizer for AI prompts</name>
  <files>apps/web/src/lib/document-builder/input-sanitizer.ts, apps/web/src/lib/document-builder/__tests__/input-sanitizer.test.ts</files>
  <read_first>
    - apps/web/src/lib/document-builder/ai-generator.ts (lines 107-203)
  </read_first>
  <behavior>
    - Test 1: sanitizeForPrompt strips system/assistant role markers
    - Test 2: sanitizeForPrompt escapes curly braces to prevent variable injection
    - Test 3: sanitizeForPrompt removes markdown code fences that could escape context
    - Test 4: escapePromptInjection handles multi-language injection attempts
    - Test 5: sanitizeForPrompt preserves legitimate content and formatting
  </behavior>
  <action>
Create input-sanitizer.ts with injection pattern detection and sanitization functions.
  </action>
  <verify>
    <automated>cd apps/web and npm test -- --grep "input-sanitizer" --run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "INJECTION_PATTERNS" apps/web/src/lib/document-builder/input-sanitizer.ts
    - grep -q "sanitizeForPrompt" apps/web/src/lib/document-builder/input-sanitizer.ts
  </acceptance_criteria>
  <done>Input sanitizer created with injection pattern detection</done>
</task>

<task type="auto">
  <name>Task 2: Fix AI generator prompt injection risk</name>
  <files>apps/web/src/lib/document-builder/ai-generator.ts</files>
  <read_first>
    - apps/web/src/lib/document-builder/ai-generator.ts
    - apps/web/src/lib/document-builder/input-sanitizer.ts (from Task 1)
  </read_first>
  <action>
Update ai-generator.ts:
1. Import sanitizeForPrompt from input-sanitizer.ts
2. Wrap ALL user inputs with sanitizeForPrompt before embedding in prompts
3. Add input validation at function entry points
4. Replace console.error with structured logger
  </action>
  <verify>
    <automated>cd apps/web and npx tsc --noEmit src/lib/document-builder/ai-generator.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "sanitizeForPrompt" apps/web/src/lib/document-builder/ai-generator.ts
    - grep -q "import.*input-sanitizer" apps/web/src/lib/document-builder/ai-generator.ts
  </acceptance_criteria>
  <done>AI generator sanitizes all user inputs before prompt embedding</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace Redis KEYS with SCAN</name>
  <files>apps/web/src/lib/document-builder/analytics-service.ts, apps/web/src/lib/document-builder/__tests__/analytics-service.test.ts</files>
  <read_first>
    - apps/web/src/lib/document-builder/analytics-service.ts (line 391)
  </read_first>
  <behavior>
    - Test 1: getAnalyticsKeys uses SCAN cursor iteration, not KEYS
    - Test 2: SCAN handles pagination correctly
    - Test 3: processBatchedEvents uses pipelined commands for atomicity
    - Test 4: Non-atomic Redis operations are pipelined
  </behavior>
  <action>
Update analytics-service.ts:
1. Replace redis.keys with SCAN cursor iteration pattern
2. Replace non-atomic operations with pipeline for INCR + ZADD
  </action>
  <verify>
    <automated>cd apps/web and npm test -- --grep "analytics-service" --run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "scan" apps/web/src/lib/document-builder/analytics-service.ts
    - grep -q "pipeline" apps/web/src/lib/document-builder/analytics-service.ts
  </acceptance_criteria>
  <done>Redis KEYS replaced with SCAN, operations pipelined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create analytics sync worker</name>
  <files>apps/web/src/lib/document-builder/analytics-sync-worker.ts, apps/web/src/lib/document-builder/__tests__/analytics-sync-worker.test.ts</files>
  <read_first>
    - apps/web/src/lib/document-builder/analytics-service.ts
    - apps/web/src/db/schema/document-builder.ts
    - .planning/phases/102-advanced-document-builder/102-CONTEXT.md (Section 4)
  </read_first>
  <behavior>
    - Test 1: syncAnalytics reads Redis counters and updates Postgres block_variants
    - Test 2: Uses GETSET pattern to atomically read and reset counters
    - Test 3: Handles missing variants gracefully
    - Test 4: BullMQ job processes every 5 minutes
    - Test 5: Batch updates to minimize DB round trips
  </behavior>
  <action>
Create analytics-sync-worker.ts with BullMQ worker that syncs Redis counters to Postgres every 5 minutes using GETSET pattern for atomic read-and-reset.
  </action>
  <verify>
    <automated>cd apps/web and npm test -- --grep "analytics-sync-worker" --run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "analyticsSyncWorker" apps/web/src/lib/document-builder/analytics-sync-worker.ts
    - grep -q "BullMQ\\|Worker" apps/web/src/lib/document-builder/analytics-sync-worker.ts
    - grep -q "getset" apps/web/src/lib/document-builder/analytics-sync-worker.ts
  </acceptance_criteria>
  <done>Analytics sync worker created, Redis to Postgres pipeline complete</done>
</task>

<task type="auto">
  <name>Task 5: Fix component quality issues</name>
  <files>apps/web/src/components/document-builder/index.ts, apps/web/src/components/document-builder/BlockEditor.tsx, apps/web/src/components/document-builder/VariantCreator.tsx</files>
  <read_first>
    - apps/web/src/components/document-builder/index.ts
    - apps/web/src/components/document-builder/BlockEditor.tsx
    - apps/web/src/components/document-builder/VariantCreator.tsx
  </read_first>
  <action>
1. index.ts - Add missing exports for BlockEditor, FrameworkSelector, HeatmapOverlay
2. BlockEditor.tsx - Fix TipTap cleanup leak with useEffect cleanup
3. BlockEditor.tsx - Fix unstable callback with useRef pattern
4. VariantCreator.tsx - Fix stale form state with useEffect reset on open
  </action>
  <verify>
    <automated>cd apps/web and npx tsc --noEmit src/components/document-builder/*.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export.*BlockEditor" apps/web/src/components/document-builder/index.ts
    - grep -q "export.*FrameworkSelector" apps/web/src/components/document-builder/index.ts
    - grep -q "editor.destroy\\|\\.destroy()" apps/web/src/components/document-builder/BlockEditor.tsx
  </acceptance_criteria>
  <done>Component quality issues fixed, exports added, memory leaks resolved</done>
</task>

<task type="auto">
  <name>Task 6: Remove unused import and add ARIA labels</name>
  <files>apps/web/src/lib/document-builder/template-service.ts, apps/web/src/components/document-builder/VersionDiff.tsx</files>
  <read_first>
    - apps/web/src/lib/document-builder/template-service.ts
    - apps/web/src/components/document-builder/VersionDiff.tsx
  </read_first>
  <action>
1. template-service.ts - Remove unused validateFromBlocks import
2. VersionDiff.tsx - Add ARIA labels with htmlFor associations
  </action>
  <verify>
    <automated>cd apps/web and npx tsc --noEmit src/lib/document-builder/template-service.ts src/components/document-builder/VersionDiff.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "aria-label\\|htmlFor" apps/web/src/components/document-builder/VersionDiff.tsx
  </acceptance_criteria>
  <done>Unused import removed, ARIA labels added for accessibility</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input to AI Prompt | Prompt injection risk |
| Redis to Postgres | Data loss if sync fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-102-06-01 | Tampering | AI prompts | mitigate | Input sanitization |
| T-102-06-02 | Denial of Service | Redis KEYS | mitigate | SCAN cursor iteration |
| T-102-06-03 | Information Disclosure | Analytics | mitigate | Sync worker with error handling |
</threat_model>

<verification>
- [ ] Input sanitizer strips injection patterns
- [ ] AI generator uses sanitizeForPrompt on all user inputs
- [ ] No Redis KEYS commands remain (use SCAN)
- [ ] Analytics sync worker runs every 5 minutes
- [ ] All component exports present in index.ts
- [ ] TipTap editor cleanup on unmount
- [ ] Form state resets on dialog open
</verification>

<success_criteria>
- npm test -- --grep "document-builder" passes
- grep -r "redis.keys" apps/web/src/lib/document-builder/ returns no results
- Analytics sync worker exists and is typed correctly
</success_criteria>

<output>
After completion, create .planning/phases/102-advanced-document-builder/102-06-SUMMARY.md
</output>
